---
alwaysApply: false
globs: Prompt_File/*.md
---

# Prompt_File 编辑规范

## 文件命名
`X-Y_Role_Field.md`：
- X = 角色层（0 = 聊天室级 / 1 = World Master / 2 = World State Keeper）
- Y = 字段序号（1 = Scene / 2 = Additional Personality Details / 3 = Extra Details）

## 章节格式
- 软标签章节头：`[章节名]`（如 `[Probability Check 块]`、`[关系规则]`）
- 引用章节时：`§[章节名]` 或 `§章节名`
- 章节名应稳定，改名时必须同步全库引用

## 跨文件引用格式
- 引用同角色其他字段：`World Master（本角色）的 Extra Details 字段 §[章节名]`
- 引用聊天室字段（仅用于一致性校验，不作为执行依赖）：复制关系记录在 PB §21.5，不在 prompt 文件中标注
- 引用对方角色输出：`World State Keeper 最新 [State Update] 实际输出中的 {字段名} 字段`
- 禁止使用模糊引用（如"见上节""如前所述"），必须写明文件和章节名
- **禁止执行依赖引用**：1-x / 2-x 中不得出现"按聊天室 XX 字段执行""见聊天室 XX 字段"类引用作为唯一来源；需要执行的规则必须内联为角色卡内的完整文本，复制关系记录在 PB §21.5，不在文件中标注

## 枚举一致性
修改任何枚举值时必须同步全库。主要枚举及其权威源：

| 枚举 | 权威源 | 取值 |
|------|--------|------|
| 知情范围 | 0-1 §跨位转移与知情范围协议 | hidden / party-known / local-only / publicly-known |
| 生存状态级 | 0-1 §环境生存压力系统 | stable / strained / weakened / critical / dying / dead |
| 天气 | 1-3 §轻量天气系统 | Clear / Overcast / Light Rain / Heavy Rain / Fog / Windy / Sleet / Snow / Storm |
| Temperature Band | 1-3 §温度分层与 5 轨压力联动 | Mild / Cool / Cold / Bitter Cold / Heat |
| Current Season | 1-3 §季节锚点 | Winter / Spring / Summer / Autumn |
| 记忆库存 Availability | 2-3 §[固定取值] | confirmed-intact / uncertain / likely-moved / likely-looted / likely-damaged / unreachable |
| Human Threat Stage | 2-3 §[固定取值] | none / signs / observed / followed / probed / blocked / robbed / violent / lethal |
| 弹药口径 | 2-3 §[弹药口径格式硬约束（按口径合并）] | 9mm / .45 ACP / .380 ACP / .22 LR / 霰弹 / 7.62 / 5.45 / 其他口径 |
| 据点分类 | 2-2 §[据点与庇护记录规则] | 主据点 / 物资点 / 安全屋 / 地图外据点 |
| 暴露响应等级 | 1-3 §[身份暴露风险] + 2-2 §[据点与庇护记录规则] | L0 未暴露 / L1 怀疑 / L2 暴露 / L3 追查 / L4 清算 |

## 数据表与机械规则：权威源 vs 执行副本
0-1 中的公共法是全库一致性校验的权威源（被动吸收有效）；0-2 已重写为叙事开场，不再承载规则。角色卡中需要执行的表格和机械规则在角色卡内自包含。
角色卡中需要执行的表格和机械规则必须内联为"执行副本"。

| 数据 / 规则 | 一致性权威源（0-1/0-2） | WM 执行副本 | WSK 执行副本 |
|------------|------------------------|------------|-------------|
| 弹药口径换算表 | 1-3 §[弹药口径换算表] | 1-3（交易场景） | 2-3（交易场景） |
| 可消耗物资渲染协议 | 0-1 §[可消耗物资渲染与记录协议] | 1-3（容器描述+约kg 格式） | 2-3（kg 记录格式） |
| 资源消耗基准 | 1-3 §[资源消耗基准] | 1-3（叙事估算） | 2-3（消耗投影） |
| 物资重量校准锚点 | 1-3 §[可消耗物资重量校准锚点] | 1-3（估算参考） | 2-3（估算参考） |
| 5 轨压力系统 | 0-1 §[环境生存压力系统] | 1-2/1-3（裁定+阈值表） | 2-2/2-3（记录+状态级） |
| 温度分层与季节 | 1-3 §[温度分层与 5 轨压力联动] / §[季节锚点] | 1-3（[主要状态] 输出） | 2-3 §[固定取值]（枚举） |
| 日期-温度对应表 | 1-3 §[温度分层与日期精细对应（D1 锚定）] | 1-3（[主要状态] 查表） | -- |
| 天气枚举与影响 | 1-3 §[轻量天气系统] | 1-3（叙事渲染） | 2-3 §[固定取值]（枚举） |
| 知情范围 | 0-1 §[跨位转移与知情范围协议] | 1-2（裁定） | 2-2（记录） |
| 跨位转移规则 | 0-1 §[跨位转移与知情范围协议] | 1-2（叙事转移） | 2-2（记账转移） |
| NPC 知识边界 | 0-1 §[NPC 反全知 / 反顾问化机制] | 1-2（NPC 发言控制） | -- |
| 移动与路由规则 | 1-3 §[跨区移动路由规则] | 1-3（叙事移动） | 2-2（移动验证） |
| 死亡/抢救规则 | 1-2 §[死亡/抢救/遗物规则] | 1-2（裁定） | -- |
| [主要状态] 格式 | 1-3 §[主要状态 格式模板] | 1-3（格式模板） | -- |
| 压力/风险栏规则 | 1-3 §[主要状态 格式模板] | 1-3（格式模板） | -- |

> **规则**：一致性权威源用于全库校验（修改时同步）；执行副本是角色卡中实际内联的规则文本，是 WM/WSK 运行时的唯一有效依据。执行副本与权威源取值必须一致，但格式可适配角色需求。复制关系记录在 PB §21.5 跨角色复制记录中，不在 prompt 文件中标注。

## 库存四类存储位
随身存储位 / 据点核心存储位 / 记忆存储位 / 载具存储位 -- 不得引入第五层（如"背包"独立层）。

## 输出角色区分
- WM 输出：`[主要状态]`（5 段固定结构，仅列非 stable 压力轨）
- WSK 输出：`[State Update]`（第一行 + 变化子段 + 完整视图 6 字段）
- 两者格式不同，不可混用
