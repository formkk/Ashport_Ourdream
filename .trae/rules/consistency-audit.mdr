---
alwaysApply: false
description: 当用户要求对提示词文件进行一致性检查、审计、对齐或全库检查时使用
---

# 一致性审计方法论

## 审计流程（按顺序执行）

### 第 1 步：交叉引用解析
- Grep 全库所有 `§[章节名]` 和 `§章节名` 引用
- 逐条验证：被引用的章节是否实际存在于目标文件中
- 重点关注：引用方写"Scene 字段"但实际数据在"Extra Details 字段"的错位
- 检查引用链是否形成循环或断链

### 第 2 步：枚举一致性
- 提取所有文件中的同类枚举取值
- 交叉比对：同一枚举在不同文件的取值列表是否完全一致
- 重点检查项：
  - 知情范围（4 档）
  - 生存状态级（6 档含 dead）
  - 天气（9 值）
  - Temperature Band（5 档）
  - Season（4 季）
  - 弹药口径（8 项）
  - Human Threat Stage（9 级）
  - 暴露响应等级（L0-L4）
  - 记忆库存 Availability（6 档）

### 第 3 步：权威源唯一性与内联副本一致性
- 同一数据表 / 枚举 / 规则在 0-1/0-2 中只有一个权威定义源
- 角色卡（1-x/2-x）中应有对应"执行副本"（内联完整文本），而非仅引用 0-1/0-2
- 交叉比对：执行副本取值与权威源是否一致
- 重点关注：多处独立定义同一数据导致取值分叉；角色卡中仅有 `§聊天室` 引用而无内联文本

### 第 4 步：输出结构完整性
- 对比同一角色的所有输出结构描述（输出白名单 / 使用原则 / 完整视图 / 正式提交顺序）
- 验证各描述中的步骤 / 字段 / 段落是否完全对齐
- 重点关注：某一段（如 Active Concerns）在定义处有位置说明，但在输出结构 / 提交顺序中遗漏

### 第 5 步：术语一致性（含回归检查）
- 同一概念在不同文件中是否使用相同名称
- 已知易混淆术语对照（均为历史已修复项，防止回退）：
  - "沉默协议" vs "静默协议"（已统一为"沉默协议"）
  - "Human Contact Status" vs "Human Threat Stage"（已统一为"Human Threat Stage"）
  - "据点核心库存" vs "据点库存" vs "据点储备"（已统一为"据点核心库存"）
  - 弹药口径短名（.45）vs 全名（.45 ACP）（已统一为全名）
  - "Inventory State 末尾" vs "Inventory State 之后"（记忆库存位置，已统一为"之后"）
  - "由 WSK 归档到 Pinned Memory" vs "由 WSK 产出素材、由用户手动复制"（已统一为后者）

### 第 6 步：流程描述一致性
- 同一流程（如 Pinned Memory 归档、跨位转移、死亡事件记录）在不同文件中的描述是否一致
- 重点关注：一处说"WSK 直接写入"另一处说"用户手动复制"的矛盾

### 第 7 步：内联完整性与引用失效审计
- 对照 `prompt-engineering.mdr` 的"WM/WSK 自包含要求"表，逐项检查角色卡中是否存在对应内联副本
- 扫描 1-x / 2-x 中所有 `§聊天室` / `聊天室 XX 字段` 引用，逐条判定：
  - 执行依赖（表格/机械规则/公共法）且无内联文本 -> **P1 重大遗漏**
  - 公共法背景（势力描述、地理概述等）-> 已在 Private Details 被动吸收，不需引用
- 检查内联规则是否与 0-1 公共法保持一致（复制关系记 PB §21.6，不在文件中标注）

## 严重度分级

| 级别 | 定义 | 示例 |
|------|------|------|
| **P0 严重矛盾** | 同一数据在不同文件取值不同；引用指向错误文件或不存在的段落 | 1-2 引用 Scene 字段为权威但 Scene 转发至 Extra Details |
| **P1 重大遗漏** | 输出结构描述缺少某步骤；权威源缺失应有内容；关键流程未定义 | Active Concerns 在定义处有位置说明但提交顺序中遗漏 |
| **P2 命名不一致** | 同一概念使用不同名称；格式示例不统一 | "沉默协议" vs "静默协议"；渲染格式 ≈kg vs 约 Xkg |
| **P3 缺口** | 内容缺失但不构成矛盾 | 某势力缺少详细描述；某区域不在事件表中 |

## 输出格式
每项发现包含：
1. 严重度标记（P0/P1/P2/P3）
2. 问题描述
3. 涉及文件和行号（带 `file:///` 可点击链接）
4. 建议修复方案
5. 修复优先级排序表（汇总）

## 审计范围
- 标准审计：`Prompt_File/` 下全部 9 个文件
- 全库审计：追加 `0-3_自定义指令.md` + `PROJECT_BLUEPRINT.md` + `.trae/rules/` 中的规则定义
- 跨角色审计：重点检查 WM（1-x）与 WSK（2-x）之间的引用和数据交接
- 内联审计：对照 `prompt-engineering.mdr` 自包含要求表，验证角色卡内联完整性
