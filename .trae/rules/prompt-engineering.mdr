---
alwaysApply: true
---

# Ash Harbor 提示词工程规范

## 项目概况
本项目是 Ash Harbor（灰港）LLM 聊天室 RPG 游戏的提示词工程仓库。
- 平台：OurDream.ai
- 架构：9 个提示词文件（`Prompt_File/0-1` 至 `2-3`）+ 2 个 AI 角色（World Master / World State Keeper）
- 权威文档：`PROJECT_BLUEPRINT.md`（SSOT），任何设计与决策以其为准

## 三层漏斗审查原则（最高优先级）
所有提示词文本在落盘前必须依次通过以下三层，顺序不可颠倒：

1. **字段身份层**：修改前先确认目标字段身份（聊天室级 / WM / WSK），内容须与字段分工匹配。决策逻辑写进 Scene（1-1/2-1）、输出模板写进 APD（1-2/2-2）、角色身份写进 Extra（1-3/2-3）均属错位。
2. **可见性层**：每句话必须能指名读者（某个执行角色的模型）。
   - 聊天室级字段（0-1/0-2）：为其他平台角色卡设立世界知识底座；WM/WSK 运行时不保证每次加载。
   - 角色卡字段（1-x / 2-x）：平台机制上角色隔离（WM 看不到 WSK 的字段，反之亦然），是各自角色的唯一可靠执行依据。
   - 不写开发者注释、审计备注或虚空说明。
3. **有效性层**：假设删掉这句，模型下一次输出会有任何不同吗？无差别即噪音。禁止以"描述内容是否正确"代替判定。

## 跨文件引用纪律
- 修改一处规则 / 枚举 / 章节名时，必须同步所有引用该规则的位置
- 引用方指向的段落必须实际存在于被引用文件中
- 同一数据的权威源唯一：枚举值在权威源定义一次，其他文件引用而非重复定义
- 修改前应先 Grep 全库搜索引用位置，确认影响范围
- **聊天室级字段引用失效**：角色卡中需要执行的表格、格式约束、机械规则和系统协议必须**内联到角色自己的字段**（1-x / 2-x），不能纯引用 0-1/0-2（见上方可见性层）。0-1/0-2 仅作为全库一致性校验的权威源

## 文件分工速查
| 层级 | 文件 | 承载内容 |
|------|------|----------|
| 聊天室（对玩家隐蔽） | `0-1_Private_Details.md` | 全局协议与系统铁律（供其他平台角色卡读取；一致性校验最高优先级；WM/WSK 运行时不保证加载） |
| 聊天室（对玩家公开） | `0-2_Scenario.md` | 世界共识与基础世界观（供其他平台角色卡读取；一致性校验权威源；WM/WSK 运行时不保证加载） |
| 聊天室（运行时） | `0-3_自定义指令.md` | 玩家动态调整指令（不适合承载全局静态指令） |
| WM | `1-1_WorldMaster_Scene.md` | WM 身份 + 运行锚点 + 输出约束 |
| WM | `1-2_WorldMaster_Additional_Personality_Details.md` | WM 裁定决策层（生存 / 战斗 / 交易 / 搜刮 / 关系 / 敌对） |
| WM | `1-3_WorldMaster_Extra_Details.md` | WM 模板与数据表（概率块 / Trust 表 / 搜刮表 / 势力表 / 据点基线） |
| WSK | `2-1_WorldStateKeeper_Scene.md` | WSK 身份 + 沉默协议 + 输出白名单 |
| WSK | `2-2_WorldStateKeeper_Additional_Personality_Details.md` | WSK 记账决策层（DO / REJECT / SILENT + 各状态记录规则） |
| WSK | `2-3_WorldStateKeeper_Extra_Details.md` | WSK 输出模板 + 提取细则 + 固定取值 |

## 权威源优先级
**一致性校验**：冲突时以 `0-1 Private Details` 为最高权威，其次 `0-2 Scenario`，再次各角色字段内 Scene（1-1/2-1）> APD（1-2/2-2）> Extra（1-3/2-3）。
**WM/WSK 执行**：以各自角色卡字段（1-x / 2-x）为唯一依据，不依赖 0-1/0-2。
`0-3 自定义指令` 为玩家运行时动态调整，不参与静态权威优先级排序。

## WM/WSK 自包含要求
WM/WSK 角色卡必须自包含执行所需的一切规则和数据。以下为必须内联清单：

### WM 必须内联的内容
| 内容 | 内联目标 | 0-1/0-2 一致性源（仅校验用） |
|------|----------|------------------------------|
| [主要状态] 格式与 5 段结构 | 1-1 | 0-1 §[主要状态栏硬约束] |
| 压力/风险栏规则与正反示例 | 1-1 | 0-1 §[压力/风险栏规则与正反示例] |
| 5 轨压力系统（轨道名/状态级/联动） | 1-2 / 1-3 | 0-2 §[环境生存压力系统] |
| 生存压力时间阈值表 | 1-3 | 1-3 §[生存压力时间阈值表]（已在角色卡内，无需迁移） |
| 移动与路由规则（九宫格/step/Route） | 1-3 | 0-2 §[地图调用规则] |
| 温度分层与季节锚点 | 1-3 | 0-2 §[温度分层与 5 轨道联动] / §[季节锚点] |
| 日期-温度精细对应表 | 1-3 | 0-2 §[温度分层与日期精细对应] |
| 天气枚举与影响 | 1-3 | 0-2 §[轻量天气系统] |
| 弹药口径换算表（交易场景） | 1-3 | 0-2 §[弹药口径换算表] |
| 可消耗物资渲染格式（容器描述+约kg） | 1-3 | 0-1 §[可消耗物资渲染与记录协议] |
| 物资重量校准锚点 | 1-3 | 0-2 §[可消耗物资重量校准锚点] |
| NPC 知识边界与反全知规则 | 1-2 | 0-2 §[NPC 反全知 / 反顾问化机制] |
| Knowledge Scope 枚举与传播规则 | 1-2 | 0-1 §[跨层转移与 Knowledge Scope 协议] |
| 跨层转移规则 | 1-2 | 0-1 §[跨层转移与 Knowledge Scope 协议] |
| 死亡/抢救/遗物规则 | 1-2 | 0-2 §[死亡 / 抢救 / 遗物规则] |
| 资源消耗基准（每人每日） | 1-3 | 0-2 §[资源消耗基准] |

### WSK 必须内联的内容
| 内容 | 内联目标 | 0-1/0-2 一致性源（仅校验用） |
|------|----------|------------------------------|
| Knowledge Scope 枚举与传播规则 | 2-2 | 0-1 §[跨层转移与 Knowledge Scope 协议] |
| 跨层转移规则 | 2-2 | 0-1 §[跨层转移与 Knowledge Scope 协议] |
| 5 轨压力状态级（stable->dead） | 2-2 / 2-3 | 0-2 §[环境生存压力系统] |
| 天气枚举 | 2-3 §[固定取值] | 0-2 §[轻量天气系统] |
| Temperature Band 枚举 | 2-3 §[固定取值] | 0-2 §[温度分层与 5 轨道联动] |
| Season / Month 枚举与推导 | 2-2 / 2-3 §[固定取值] | 0-2 §[季节锚点] / §[月份推进规则] |
| 资源消耗基准（消耗投影用） | 2-3 | 0-2 §[资源消耗基准] |
| 可消耗物资 kg 记录格式 | 2-3 | 0-1 §[可消耗物资渲染与记录协议] |
| 物资重量校准锚点 | 2-3 | 0-2 §[可消耗物资重量校准锚点] |
| 弹药口径换算表（交易场景） | 2-3 | 0-2 §[弹药口径换算表] |
| 移动与路由规则（移动验证用） | 2-2 | 0-2 §[地图调用规则] |

## 变更流程
1. 读取被修改文件 + Grep 全库搜索引用该文件/章节的其他位置
2. 三层漏斗审查修改内容
3. **内联检查**：若修改 0-1/0-2 中的执行依赖内容（表格/机械规则/系统协议），检查 1-x/2-x 中是否有对应内联副本需同步；若修改 1-x/2-x，检查 `§聊天室` 引用是否需要改为内联
4. 实施修改
5. 验证所有引用位置同步更新 + 内联副本与权威源取值一致
6. 提交时在 commit message 中记录变更类型和影响范围

### 变更流程检查清单

#### 步骤 1：读取与影响范围扫描
- [ ] 读取目标文件完整内容（不依赖记忆或缓存）
- [ ] Grep `§[章节名]` 搜索所有引用该文件/章节的位置
- [ ] Grep 口语化引用模式（`按 X 条` / `见 X 规则` / `按 X 律` / `按 X 机制` 等）
- [ ] Grep 被修改枚举值在全库的出现位置
- [ ] 记录影响范围清单（文件名 + 行号 + 引用类型）

#### 步骤 2：三层漏斗审查
- [ ] **字段身份层**：目标字段身份确认（聊天室级 / WM / WSK）；内容与字段分工匹配（决策逻辑在 Scene 1-1/2-1、输出模板在 APD 1-2/2-2、数据表在 Extra 1-3/2-3）
- [ ] **可见性层**：每句话能指名读者（某个执行角色的模型）；无开发者注释、审计备注或虚空说明
- [ ] **有效性层**：逐句判定"删掉后模型行为会改变吗"；无差别即删除；禁止以"描述内容是否正确"代替行为判定；分句级过检（分号/逗号连接的不同主张逐分句独立审查）

#### 步骤 3：内联检查
- [ ] **若修改 0-1/0-2**：对照"WM/WSK 自包含要求"表，逐项检查对应内联副本是否需要同步修改
- [ ] **若修改 1-x/2-x**：扫描文件内所有 `§聊天室` / `聊天室 XX 字段` 引用，逐条判定：
  - 目标为具体细节 / 表格 / 机械规则 / 系统协议 -> 标记"需内联"，不能仅靠引用
  - 目标为世界共识背景（势力定位、地理描述等）-> 可保留引用
- [ ] 检查已内联规则的 `（与 0-X §XX 一致）` 标注是否需要更新
- [ ] 若新增规则属于执行依赖，确认内联到角色卡字段而非 0-1/0-2

#### 步骤 4：实施修改
- [ ] 按三层漏斗通过的文本落盘
- [ ] 同步所有步骤 1 记录的引用位置
- [ ] 内联副本与权威源取值一致（枚举值、表格数据、格式约束）
- [ ] 新增 / 修改的章节名在全库引用中同步更新
- [ ] 新增 / 修改的枚举值在全库同步更新

#### 步骤 5：验证
- [ ] Grep 验证所有引用位置已同步（无断链）
- [ ] 枚举取值跨文件一致（对照 `prompt-file-conventions.mdr` 枚举一致性表）
- [ ] 内联副本与权威源取值一致（对照"权威源 vs 执行副本"表）
- [ ] 若修改了输出模板 / 提交顺序：验证输出结构完整性（输出白名单 / 使用原则 / 完整视图 / 正式提交顺序四处对齐）
- [ ] 若修改了 0-1/0-2 执行依赖内容：确认 1-x/2-x 内联副本已同步
- [ ] 确认无新增 `§聊天室` 执行依赖引用（步骤 3 标记的"需内联"项已处理）
- [ ] 确认术语一致性（同一概念无新引入的别名）

#### 步骤 6：提交
- [ ] commit message 包含变更类型（`fix` / `refactor` / `feature`）
- [ ] commit message 包含影响文件数和变更摘要
- [ ] commit message 包含内联同步状态（已同步 / 不涉及 / 待处理）
- [ ] 若涉及版本号变更，更新 `PROJECT_BLUEPRINT.md` 头部 `最后更新` 行（版本号 + 日期 + 变更摘要）
- [ ] 若涉及架构变更（角色增减 / 字段重新分配），更新 `审计历史记录.md`
