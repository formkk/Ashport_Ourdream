# 多据点容纳与全量输出 决议文档（已确认，已应用核心格式与边界）

状态：**已应用**（v1.68）。目标：让 WSK 完整视图在 ≥2 个正式据点时，库存分桶与设施结构清单有确定格式，全量输出不发散；贯通 WM→WSK 提取链的据点限定符；统一据点分类枚举。

已落地：2-3 字段 1/5 分桶格式 + Delta 权威格式限定符 + item 11 + 边界①② + 输出示例分桶式；2-2 2a 引用 + item 5 升级规则引用；1-3 消耗行 `据点({据点名})` + 枚举四分类；tools/sim_multibase_test.py（16 项，B8/B9 边界断言固化）。
未落地（择期）：落点 9 sim_base_decay_test.py 示例升级、落点 10 enum_registry.json 据点分类枚举组登记。

## 决议记录（已确认）

- **决策 1 = 方案 A：统一限定符。** 只要据点存在，Delta/消耗行/存储位一律写 `据点核心/{据点名}`，单据点也不例外。零分支、零歧义，正则与示例唯一；现有单据点示例（sim_base_decay_test 等）同步更新一次。
- **决策 2 = 方案 A：维持全量无上限。** 每轮全量、无上限、无变化逐字照抄；在自包含原则后补一句明确表述，其余不动；未来膨胀再升级分层。

---

## 修正 1：Inventory State 多据点分桶格式（核心交付）

**定义**（写入 2-3 完整视图字段 1 说明处）：

> Inventory State 随身段后，每个正式据点一段分桶行，段头格式：
> `据点核心/{据点名}（{Zone/Sub-zone/Location}，{主据点/安全屋/物资点/地图外据点}）: {功能门类}: {...}`
> 地图外据点锚点写 `（{Boundary Anchor}->{External Site}，地图外据点）`；段内门类结构与随身一致（额外含水储备/建材储备门类）。多据点按建立先后排列；无据点时不输出任何分桶行。

**样例（2 据点，含地图外）**：

```
1. Inventory State: 随身: 武器: 猎枪×1；弹药: 9mm×42发；食品: 大米×2.5kg, 干粮×1.5kg；饮水: 净水×1.5kg
据点核心/质检小楼（工业区/N/质检小楼，主据点）: 食品: 罐头×6, 大米×10kg；水储备: 净水×7kg；建材储备: 木材 充足, 金属件 具备
据点核心/河边安全屋（西区/S/泵房，安全屋）: 食品: 罐头×3；燃料动力: 煤炭木柴 具备
```

（地图外示例段头：`据点核心/林场木屋（北闸口->林场护林站，地图外据点）: ...`）

**方案 A 生效（已定案）**，单据点也用同一段头（`据点核心/质检小楼（...，主据点）: ...`），全库示例统一。

**边界 ①（组内合并的桶内限定）**：分桶不改变现有组内合并机制（三级门类、杂物归组、同义去重、kg 记录、"充足/具备"定性标记、"零项留空"）的适用性——它们全部作用于桶内物品层，逐桶独立执行。唯一需修正的文字漏洞：弹药口径合并条款（2-3 §[弹药口径格式硬约束] L152）现文"所有相同口径弹药合计为一个条目"须限定为"**同一存储位（桶）内**合计为一个条目"，防止多据点下 9mm 被全局合并、破坏分桶。

## 修正 2：Base Structure State 多据点块格式

**定义**（写入 2-3 完整视图字段 5 说明处，与 1-3 L200 模板对齐）：

> 多据点时字段内每据点一块：锚点行 `{据点名}（{锚点}，{分类}）` + 组件行 `` `{组件名}` = {状态描述}（{历史变更}） ``；块间空行分隔；据点间互不嵌套。单据点同样输出锚点行+组件行（不再是单行式）。无据点时维持 `5. Base Structure State: 无据点`。

**样例（2 据点）**：

```
5. Base Structure State:
质检小楼（工业区/N/质检小楼，主据点）
- `正门钢架` = 完好，可上闩（D6 新建）
- `二楼储物间` = 干燥，货架2排（D6 新建）
- `天台瞭望位` = 完好，视野开阔（D8 新建）

河边安全屋（西区/S/泵房，安全屋）
- `泵房铁门` = 完好，内侧加闩（D8 新建）
```

**无结构节点确认时**（据点已建立但 WM 未确认内部节点）：锚点行 + `- 结构节点未确认`。与 APD 2-1 L12（缺项标 `未确认`，不自动展开）一致。

## 修正 3：存储位限定符贯通（WM→WSK 提取链）

三处同步改，消除"3 个据点不知道转到哪个"的断裂：

| 位置 | 现状 | 改后 |
|------|------|------|
| Delta 条目格式（2-3 L73 权威） | `转移 罐头×2（随身->据点核心）` | `转移 罐头×2（随身->据点核心/质检小楼）`；获得/消耗同理 `（据点核心/质检小楼）` |
| WM [判定] 消耗行（1-3 L466/L470） | `据点 大米×1kg` | `据点(质检小楼) 大米×1kg`；`/` 分隔不同位置、`+` 分隔同位置多物品不变 |
| WSK 拆分细则（2-3 item 11） | `消耗 大米×1kg（据点核心）` | `消耗 大米×1kg（据点核心/质检小楼）` |

- 方案 A 生效（已定案）：单据点也写全限定符，三处规则无条件分支。
- 跨据点直转：`转移 木柴×5kg（据点核心/质检小楼->据点核心/河边安全屋）`（源->目标均为全限定符）。
- **边界 ②（定性标记升级规则）**：以"充足/具备"定性标记维护的组，一旦发生具体数量的获得/消耗/转移（跨位转移按 APD item 9 源位减、目标位加结算），该组升级为具体计数维护（按 §[可消耗物资重量校准锚点] 估初始量并标 `(估)`），不再回退定性标记。防"减无可减"：定性值无法参与数量结算。

## 修正 4：据点分类枚举统一

- 1-3 L200 块模板 `{主据点/安全屋/物资点}` → `{主据点/安全屋/物资点/地图外据点}`，与 2-2 L97 四分类对齐。
- 同步检查 enum_registry.json 是否需补"地图外据点"条目（现无该枚举组则新增，与 2-2 L97 同源）。

## 修正 5：容量策略（按决策 2 落地）

- 方案 A：仅把"每轮全量、无上限、无变化逐字照抄"在 2-3 完整视图自包含原则后补一句明确表述，其余不动。
- 方案 B/C：需额外定义活跃判据/折叠规则与 [主要状态] 提示，本草案不展开，选中后补二版。

---

## 落点明细（确认后逐条执行）

| # | 文件 | 改动 |
|---|------|------|
| 1 | [2-3 L25](Prompt_File/2-3_WorldStateKeeper_Extra_Details.md) 字段 1 | 补分桶段头定义 + 修正 1 样例 |
| 2 | [2-3 L29](Prompt_File/2-3_WorldStateKeeper_Extra_Details.md) 字段 5 | 补多据点块定义 + 修正 2 样例（含"结构节点未确认"） |
| 3 | [2-3 L73](Prompt_File/2-3_WorldStateKeeper_Extra_Details.md) Delta 权威格式 | 限定符改写（按决策 1） |
| 4 | [2-3 L51](Prompt_File/2-3_WorldStateKeeper_Extra_Details.md) item 11 | 拆分产物限定符 + 样例同步 |
| 5 | [2-3 L10-21](Prompt_File/2-3_WorldStateKeeper_Extra_Details.md) 输出示例 | 示例升级为分桶式（单据点，展示标准段头） |
| 5a | [2-3 L152](Prompt_File/2-3_WorldStateKeeper_Extra_Details.md) 弹药口径硬约束 | 边界 ①："所有相同口径弹药合计"限定为"同一存储位（桶）内合计" |
| 5b | [2-3 L75](Prompt_File/2-3_WorldStateKeeper_Extra_Details.md) 或 [2-2 L19](Prompt_File/2-2_WorldStateKeeper_Additional_Personality_Details.md) | 边界 ②：定性标记组发生具体数量变化后升级为具体计数维护，不回退定性标记 |
| 6 | [1-3 L466/L470](Prompt_File/1-3_WorldMaster_Extra_Details.md) | 消耗行加 `据点({据点名})` 写法；L470 说明同步 |
| 7 | [1-3 L200](Prompt_File/1-3_WorldMaster_Extra_Details.md) | 枚举补"地图外据点"；块模板与修正 2 对齐 |
| 8 | [2-2 L32](Prompt_File/2-2_WorldStateKeeper_Additional_Personality_Details.md) 2a | 追加"分桶行格式见 Extra Details §[完整视图] 字段 1"引用 |
| 9 | tools/sim_base_decay_test.py | 示例数据升级分桶式（含统一限定符）并跑回归 |
| 10 | tools/enum_registry.json | 据点分类枚举组（4 类）登记 |
| 11 | tools/e2e_test.py / validate_output.py | WSK 侧现仅查字段序，无格式正则，预计无改动；回归确认 |

## 验证计划（应用后）

1. `python tools/static_audit_test.py` — 60/60 无回归（关注 P2/P3 据点相关断言与跨文件枚举一致性）
2. `python tools/sim_base_decay_test.py` — 分桶示例升级后全过
3. `python tools/sim_multibase_test.py` — 多据点静态格式测试 16/16（边界 ① 断言 B8 = 两桶各自 9mm 计数不合并、无全局合计值；边界 ② 断言 B9 = 定性标记组在 Delta 出现具体数量后已升级计数；均已固化）
4. `python tools/e2e_test.py` + `python tools/run_audit.py` — 无新增发现
5. 人工核对：修正 1/2 样例与 1-3 L200 模板、2-2 2a/2b/2d 逐条对齐（锚点三要素、分类四枚举、组件名+Type）
