# 优化方案 Roadmap v1

## 文档元数据

| 项目 | 值 |
|------|-----|
| **版本** | v1.45 |
| **创建日期** | 2026-07-24 |
| **最后更新** | 2026-08-06 |
| **范围** | 本会话累积评估的待优化项汇总 |
| **适用范围** | Ash Harbor（灰港）LLM 聊天室 RPG 游戏项目 |
| **关联文档** | `PROJECT_ANALYSIS.md` / `OURDREAM_PLATFORM_REFERENCE.md` / `Prompt_File/*` |

---

## 一、背景与目标

### 1.1 背景

本会话期间，针对项目多个文件进行了评估与修复，识别出大量优化项。这些优化项可分为 3 类：

1. **已实施**：本会话内已完成的修复（已完成）
2. **待实施**：本会话识别但用户选择**暂不实施**的优化项（详见第三、四节）
3. **不实施**：经评估认为对本项目价值有限或不应实施（详见第五节）

### 1.2 目标

- 记录所有识别但暂未实施的优化项
- 提供可执行的实施方案（文件位置 / 修改内容 / 验收标准）
- 为后续 v4.3 / v5.0 版本迭代提供参考

### 1.3 优化原则

| 原则 | 说明 |
|------|------|
| **必要性优先** | 仅优化影响当前运营的规则 |
| **避免破坏** | 不引入新的越界风险或规则冲突 |
| **可回滚** | 每项优化可独立回滚 |
| **跨文件一致** | 修改一处必同步引用位置 |
| **不重复造轮** | 优先在现有结构内调整，不重建 |

---

## 二、本会话已实施的优化（存档）

### 2.1 WSK 规则四维合规性重构

| # | 文件 | 位置 | 优化内容 |
|---|------|------|---------|
| 1 | `Prompt_File\2-3_WorldStateKeeper_Extra_Details.md` | §压缩规则 + §最低归档标准 | Inventory Snapshot 按功能组压缩硬约束 + 弹药口径格式硬约束（**v1.5 修订为按口径合并**）+ Inventory Delta 与 Recent Changes 一致性硬约束 |
| 2 | `Prompt_File\2-2_WorldStateKeeper_Additional_Personality_Details.md` | §精简库存与盘点规则 §21 | 队伍成员库存维护硬规则（3 项进入条件 + 标注方式 + 严禁事项）|
| 3 | `Prompt_File\2-1_WorldStateKeeper_Scene.md` | §弹药口径合并（v1.5 修订）| 同步修订为按口径合并，消除与 2-3 的口径规则冲突 |

### 2.2 WER 输出模板两阶段优化

| # | 文件 | 位置 | 优化内容 |
|---|------|------|---------|
| 1 | `Prompt_File\3-3_WorldEventRecorder_Extra_Details.md` | §使用原则 + 模板示例 + §压缩规则 + §正式归档顺序 + §最低归档标准 | 档位 B 优化：必出段（`## 新增` + `## 不可压缩锚点`）+ 条件性段（`## 长期格局变化` / `## 未关闭事件链` / `## 结构变化`）+ 删除 `## 知识范围备注` 段 + 删除 `## 近五日重大事件` 段 |
| 2 | `Prompt_File\3-3` §最低归档标准 §9-11 | 新增 | 事件 vs 评估分离硬约束 + 静默原则 + 源头区分 |
| 3 | `Prompt_File\3-2` REJECT 表 | 新增 5 项 | 事件 vs 评估混淆 / 预测代替事实 / WSK 职责越界 / "无重大事件"反转描述 / 源头混淆 |
| 4 | `Prompt_File\3-1` L82-89 + L82 | 引用 + 删除 | 删除重复沉默协议 + 跨日规则改为引用 3-2 DO 表第 12 行 |
| 5 | `Prompt_File\0-1` + `3-2` | 引用 | WER 输出格式段名同步更新 |

### 2.3 D1 锚定日期生效（B 档）

| # | 文件 | 位置 | 优化内容 |
|---|------|------|---------|
| 1 | `Prompt_File\2-3` §State Update 模板 L19 | 元数据行 | State Update 第一行增加 `{Month} {Season}` 字段 + 跨月强制更新 + 缺失 REJECT |
| 2 | `Prompt_File\2-2` §环境与压力记录规则 L85-88 | 新增 §1d-1g | Current Month 默认取值 + 跨月判定与强制触发 + 缺失 REJECT + 月份-日期映射 |
| 3 | `Prompt_File\0-2_Scenario.md` §温度分层 | 新增段 | 温度分层与日期精细对应表（8 个时间段）|
| 4 | `Prompt_File\0-1_Private_Details.md` §状态栏硬约束 | 6 项 → 8 项 | [主要状态] 增加 Season / Temperature Band 字段 |
| 5 | `PROJECT_ANALYSIS.md` §十二 L684 | SSOT | D1 锚定日期规则在 SSOT 中已记录 |

### 2.4 OurDream Character Creation Guide 评估

| # | 文件 | 位置 | 评估内容 |
|---|------|------|---------|
| 1 | （无）| — | 用户选择 E：不修改，仅记录评估 → 已存档在本文档 |

### 2.5 11 文件全面评估（2026-07-24）

#### 2.5.1 跨文件一致性同步问题（从全面评估识别）

| # | 文件 | 位置 | 问题 / 优化 |
|---|------|------|-----------|
| 1 | `Prompt_File\1-1_WorldMaster_Scene.md` L24 | §状态栏硬约束 | 字段定义未同步：仍写“6 项固定字段（Day/Turn / 位置 / 时间/光照 / 天气 / 压力 / 当前风险）”；应同步为 0-1 v1.4 的“8 项固定字段”（Day/Turn / 位置 / 时间 / Season / 气温 / 天气 / 压力 / 风险）；字段名同步：时间/光照 → 时间，当前风险 → 风险 |
| 2 | `Prompt_File\1-1_WorldMaster_Scene.md` L12 | §首轮执行捷径 | 输出顺序未同步：仍写“-> 收尾”；应同步为 0-1 v1.4 严格出场次序（4 步，无“收尾”） |
| 3 | `Prompt_File\2-1 / 2-3` 弹药口径规则 | §弹药口径折算 / §弹药口径格式硬约束 | ✓ v1.5 已同步修订为按口径合并，消除两文件间矛盾 |

#### 2.5.2 LLM 实际输出未生效字段（从全面评估识别）

| # | 字段 | 问题 | 优化 |
|---|------|------|------|
| 1 | Season 必出 | LLM 未输出（玩家说“还没更新到游戏内”）| 0-1 L95 已强化字段必出；需玩家同步到 OurDream 平台验证 |
| 2 | 气温必出 | LLM 未输出 | 同上 |
| 3 | 风险字段名 | LLM 仍用“当前风险”未用“风险” | 同上 |
| 4 | 风险 1 项限制 | LLM 列 3 项并列 | 0-1 L95 已强化；需验证 |
| 5 | 压力无括号 | LLM 仍带括号说明 | 0-1 L111 已强化；需验证 |

### 2.6 治理评估问题点（待实施优化项汇总）

从 11 文件全面评估中识别出的待实施优化项，详见下文第四节 P2-1 / P3-1 / P3-2 / P3-3 / P3-4 / 第三节 P0 项。

### 2.7 v1.37-v1.40 实施（本次会话）

| # | 版本 | 优化项 | 文件 | 内容 |
|---|------|--------|------|------|
| 1 | v1.37 | [主要状态] 格式精简 | 0-1 / 1-1 | 8 字段+前缀 → 5 段结构（`D{Day}-T{Turn} {时间} \| {位置} \| {Season}-{天气}-{气温} \| {压力} \| {风险}`） |
| 2 | v1.38 | P1-1 NPC Trust 行为阈值映射 | 0-2 / 1-2 | 新增 Trust 阈值表（0-20 敌对/20-40 警惕/40-60 中立/60-80 友善/80-100 信任）+ 1-2 关系规则引用 |
| 3 | v1.38 | P1-2 5 轨压力全轨化 | 0-1 | [主要状态] 压力段强制 `{轨道名} {状态级}` 格式，仅列非 stable 轨道，与 WSK Party Condition 对齐 |
| 4 | v1.38 | P2-1 消耗估算 WM 驱动化 | 2-3 / 1-1 | WM 叙事估算优先（标注 WM判断），WSK 基准值兜底（标注 基准估算）；禁止"富余"等描述词 |
| 5 | v1.38 | P2-2 Active Concerns 分类约束 | 2-3 | 4 分类边界定义 + 禁止自创类别 + 判断示例 |
| 6 | v1.39 | [主要状态] 不输出根因修复 | 0-1 | 修复 L139 旧 8 字段格式与 L106 新 5 段格式矛盾（v1.37 漏改）；L139 改纯引用消除双重定义 |
| 7 | v1.40 | 字段可见性 SSOT 架构重构 | 0-1 / 2-1 / 2-3 | WSK 11字段/第一行格式以 2-3 为唯一权威；5轨压力状态级以 0-2 为权威（修复 0-1 漏 dead）；WSK kg 规则按字段角色拆分；所有引用经可见性验证无跨角色死链 |

---

## 三、待实施的优化项（按优先级）

### 优先级 P1（推荐实施）

#### ~~P1-1：NPC Trust 行为阈值映射~~ → 已实施（v1.38，见 §2.7）

#### ~~P1-2：5 轨压力追踪全轨化~~ → 已实施（v1.38，见 §2.7）

---

### 优先级 P2（可选实施）

#### ~~P2-1：消耗估算 WM 驱动化~~ → 已实施（v1.38，见 §2.7）

#### ~~P2-2：Active Concerns 分类约束强化~~ → 已实施（v1.38，见 §2.7）

---

#### P2-3：Anti-Progression Hard Lock 显式段

**来源**：OurDream Guide Phase 5 Part 3

**当前状态**：
- 已隐含：WM 不推进剧情 / WSK 不替 WM
- 未显式段

**优化目标**：
- 在 1-1 / 2-2 顶部增加显式 Anti-Progression 段

**修改文件清单**：

| # | 文件 | 位置 | 修改内容 |
|---|------|------|----------|
| 1 | `Prompt_File\1-1_WorldMaster_Scene.md` | 顶部 | 新增 `[Anti-Progression Hard Lock]` 段 |
| 2 | `Prompt_File\2-2_WorldStateKeeper_Additional_Personality_Details.md` | 顶部 | 同上 |

**预计工作量**：低（2 个文件各 1 段新增）

**验收标准**：
- 2 个文件均有显式 Anti-Progression 段
- 跨文件引用一致

**风险**：低（新增段，不修改既有规则）

**优先级理由**：
- ⭐⭐ 借鉴价值中
- 当前隐含执行已足够

---

#### P2-4：场景温度层显式化（叙事增强）

**来源**：WM 场景输出评估

**当前状态**：
- 场景隐含温度（"呼出白气" = Cool）
- [主要状态] 已显式 Temperature Band（已实施）

**优化目标**：
- 场景叙事中增加更明确的温度层提示

**修改文件清单**：

| # | 文件 | 位置 | 修改内容 |
|---|------|------|----------|
| 1 | `Prompt_File\1-2_WorldMaster_Additional_Personality_Details.md` | §环境描写 | 增加"温度层显式化"建议 |

**预计工作量**：低（单文件 1 处建议）

**风险**：低（仅建议性，非强制）

**优先级理由**：
- ⭐ 可选，当前隐含执行已足够

---

#### P2-5：[Resolution] / [Probability Check] 等展示块命名审计

**来源**：§13.2 展示块隔离原则

**当前状态**：
- 0-1 §结构块定位 已规定展示块用途
- 各展示块命名已固定

**优化目标**：
- 验证展示块未误用为通讯字段

**修改文件清单**：

| # | 文件 | 位置 | 修改内容 |
|---|------|------|----------|
| 1 | `Prompt_File\0-1_Private_Details.md` | §结构块定位 | 增加明确的"展示块命名清单"段 |

**预计工作量**：极低

**风险**：极低

**优先级理由**：
- ⭐ 审计性，当前命名已稳定

---

### 优先级 P3（远期/实验性）

#### P3-1：经济供需信号结构化

**来源**：v1.33 全面评估——经济系统

**当前状态**：
- 价格/供需变化散落在 Recent Changes 和 Faction 叙事中
- 无结构化的"当前市场价格/供需"参考
- 商会摊位费双倍、煤矿队运煤路线等经济信号无统一追踪

**优化目标**：
- 在 WSK 完整视图中新增轻量 `Economic Signal` 字段（Faction 字段之后）
- **WSK 仅提取 WM Scene 中已明确写出的经济事实**，不自行推算价格
- 不重建 Market State 机制（v1.22 已删除）

**具体实现**：
- 格式：`Economic Signal: {物资}: {状态}（{来源}）`
- 示例：
  ```
  Economic Signal:
  - 9mm弹药: 1盒25发=2罐头（稳定，0-2 §换算表）
  - 柴油: 稀缺↑（码头帮控制供给，冬季需求上升）
  - 煤炭: 充足（煤矿队 D29 运煤标记确认）
  - 商会费用: 摊位费/桥口费双倍（D29 碎瓷片留言确认）
  - 粮食: 商会囤积玉米（市政厅锅炉房，底层沤烂中上层可用）
  ```
- 无经济变化时省略整段
- 仅记录 WM Scene 中明确写出的经济信号，不推算、不预测

**修改文件清单**：

| # | 文件 | 位置 | 修改内容 |
|---|------|------|----------|
| 1 | `Prompt_File\2-1_WorldStateKeeper_Scene.md` | §输出 | 新增 Economic Signal 字段定义 |
| 2 | `Prompt_File\2-3_WorldStateKeeper_Extra_Details.md` | §完整视图 | 定义 Economic Signal 格式 + 提取规则 |
| 3 | `Prompt_File\0-1_Private_Details.md` | §后台提取规约 | 输出格式描述增加 Economic Signal |

**预计工作量**：低（3 个文件）

**风险**：中等（需避免重建已删除的 Market State；严格限定为"提取"而非"推算"）

**优先级理由**：
- ⭐⭐ 增强经济系统的可感知性
- 当前经济信号完全依赖叙事，WM 可能遗漏

---

## 四、不实施的优化（参考）

### 4.1 OurDream Guide 借鉴中**不实施**的模式

| 模式 | 不实施理由 |
|------|-----------|
| **Meter Display Rule**（数值追踪 0-100） | 本项目 WSK 用 State Update 软标签（文本快照），非数值追踪 |
| **Code-Block Method**（代码块格式规则） | 当前 DO/REJECT/SILENT 决策表用 markdown 表格稳定 |
| **Environmental Storytelling**（环境反映情绪） | 本项目是系统角色裁定者，无个体情感需要环境反映 |
| **User Commands ([HELP])** | OurDream 平台无 [HELP] 触发机制，仅支持角色卡点击 |
| **Anti-Objectification Rule**（亲密后扣分） | 系统角色，无浪漫化场景 |
| **Phased Relationship Progression** | 系统角色无关系进展需求 |

### 4.2 之前评估中**错误建议已撤回**的项

| 项 | 撤回理由 |
|----|---------|
| ❌ 删除 WSK Amber（伴侣）库存段 | **保留**（Amber 是 User 同行伴侣，非 NPC；WSK 应维护） |
| ❌ 弹药统一换算 9mm 单位 | **撤回**（弹药口径是武器填装信息，不可合并；仅在交易/经济场景临时折算） |
| ❌ WSK / WER 完全合并 | **不合并**（三权分立是 §13.2 审计定案）|
| ❌ 据点升级路径（旧P3-5） | **放弃**（据点升级已通过 Base Structure 组件新增/状态变化实现，无需额外等级系统） |
| ❌ 角色技能/熟练度系统（旧P4-2） | **放弃**（可能破坏"反爽文"硬核生存基调，角色能力不应有数值化成长） |
| ❌ 天气对搜刮影响规则（旧P4-3） | **放弃**（天气影响已由 WM 叙事判断覆盖，无需量化规则） |
| ❌ Hard Locks NEVER/ALWAYS（旧P2-1） | **放弃**（中文"严禁/绝不/必须"已具等价约束力，中英混排引入干扰风险；合规问题根因为结构矛盾非格式） |
| ✅ [主要状态] 字段命名（旧P2-2） | **已验证关闭**（8 字段命名已对齐平台机制，无需修改） |
| ❌ 记忆库存自动过期（旧P2-5） | **放弃**（后末日人口极少，隐藏物资被发现概率低，无需自动过期机制） |
| ❌ Pinned Memory JSON 化（旧P3-6） | **放弃**（当前自然语言模式运行良好，JSON 可能让 LLM 误以为是代码） |

### 4.3 Poor Man's Genius Model 借鉴中**不实施**的模式

| 模块 | 模式 | 不实施理由 |
|------|------|-----------|
| 模块 1 | **禁用词清单**（Banlist） | 项目未发现末日 RPG 套话问题；末日 RPG 题材本身抑制浪漫化套话 |
| 模块 2 | **第三人称 POV**（动作 `*...*` 用第三人称） | 本项目用第二人称（"你"），与官方冲突 |
| 模块 3 | **环境描写增强**（感官细节维度清单） | 项目 WM 场景描述已隐含环境描写（气味/声音/温度/触感），无需清单化 |
| 模块 4 | **AI 控制 NPC**（替 NPC 发言） | 本项目架构不适用（详 §常驻世界角色层） |
| 模块 5 | **角色自主性**（角色有独立意志） | WM 无个人意志（裁定者身份） |
| 模块 6 | **段落长度强制 150-300** | 末日 RPG 风格倾向于短密段落；强制会破坏叙事风格 |
| 模块 7 | **性化场景详细描写** | 多媒体不考虑（用户明确） |
| 模块 8 | **群聊多角色 AI 控制**（替多角色写对话） | 本项目架构不适用 |

**借鉴价值**（仅作参考）：
- 8 个模块均**不实施**；本项目架构与官方个人角色指导根本不同

---

## 五、实施优先级与时间表（建议）

### 时间表

| 优先级 | 项目 | 预计工作量 | 建议时间 |
|-------|------|-----------|----------|
| ~~**P1-1**~~ | NPC Trust 行为阈值映射 | — | ✅ v1.38 已实施 |
| ~~**P1-2**~~ | 5 轨压力全轨化 | — | ✅ v1.38 已实施 |
| ~~**P2-1**~~ | 消耗估算 WM 驱动化 | — | ✅ v1.38 已实施 |
| ~~**P2-2**~~ | Active Concerns 分类约束 | — | ✅ v1.38 已实施 |
| **P2-3** | Anti-Progression 显式段 | 低 | v5.0（1 月内） |
| **P2-4** | 场景温度层显式化 | 低 | v5.0 同步 |
| **P2-5** | 展示块命名审计 | 极低 | v5.0 同步 |
| **P3-1** | 经济供需信号结构化 | 低 | v6.0（远期） |

### 实施建议

1. **v4.3 批次已完成**：P1-1 / P1-2 / P2-1 / P2-2 已于 v1.38 实施（见 §2.7）
2. **P2-3~P2-5 按需实施**：属于"锦上添花"，无实际输出问题驱动
3. **分文件独立**：每项可独立实施 + 独立回滚
4. **跨文件同步**：修改时同步引用位置

---

## 六、风险评估

### 6.1 修改风险等级

| 修改类型 | 风险等级 | 缓解策略 |
|---------|---------|---------|
| **格式统一**（如 P2-1 NEVER/ALWAYS）| 中 | 保留中文含义 + 英文标记 + 短期观察 |
| **字段扩展**（如 P2-3 5 轨压力）| 中 | 状态稳定时不全显示，避免冗余 |
| **新增段**（如 P3-1 Anti-Progression）| 低 | 不修改既有规则，仅追加 |
| **可选模式**（如 P3-2 JSON）| 低 | 明确标注实验性，不强制 |

### 6.2 跨文件同步风险

| 关联文件 | 同步风险 |
|---------|---------|
| 1-1 / 1-2 / 1-3 | 高（WM 三个文件规则需同步）|
| 2-1 / 2-2 / 2-3 | 高（WSK 三个文件）|
| 0-1 / 0-2 | 中（全局硬规则）|
| PROJECT_ANALYSIS.md / OURDREAM_PLATFORM_REFERENCE.md | 低（SSOT 文档）|

**缓解**：每次修改前先 Grep 引用位置；修改后验证同步一致性。

---

## 七、验收清单

每项优化完成后，按以下清单验收：

| 验收项 | 通过标准 |
|-------|---------|
| **规则一致性** | 修改处 + 所有引用处一致 |
| **格式统一** | 关键硬约束格式全部统一 |
| **跨文件同步** | 涉及文件全部同步更新 |
| **不破坏既有规则** | 未修改的规则保持原样 |
| **LLM 测试** | 实际运行测试 5-10 次，无规则回滚 |
| **用户验收** | 用户实际操作 1 次确认无异常 |

---

## 八、字段身份审计结果（2026-08-05）

### 8.1 审计依据

依据 `PROJECT_ANALYSIS.md §0.1` **第一层 · 字段身份（前提）** 的字段承载分工，对 8 个落盘文件逐文件审查内容与字段身份的匹配度。字段身份定位如下：

| 字段 | 功能定位 |
|------|---------|
| 0-1 Private Details | **全局协议**：系统铁律、输出格式总则、通讯协议、角色协调原则 |
| 0-2 Scenario | **世界共识**：地理/势力/历史/社会规则/世界运转逻辑 |
| 1-1 WM Scene | **永久元身份 + 持续叙事框架**：身份声明 + 每轮输出契约（[主要状态]/[Resolution]/[Probability Check] 触发与格式） |
| 1-2 WM APD | **叙事人格与裁定哲学**：生存/战斗/交易/搜刮/关系/敌对/时间推进"怎么判" |
| 1-3 WM Extra | **机械引擎 + 详细可执行内容**：场外演化/势力活动表/据点结构基线/锚点协议 |
| 2-1 WSK Scene | **身份 + 输出/静默契约**：角色身份 + 触发契约 + 越界边界 |
| 2-2 WSK APD | **记账判断逻辑与记录纪律**：DO/REJECT/SILENT 决策表 + 各状态怎么记 + 提交前检查 |
| 2-3 WSK Extra | **输出模板 + 提取细则**：[State Update] 模板 + 完整视图 + 压缩规则 + 提取纪律 |

### 8.2 审计结论总表

| 文件 | 身份声明 | 内容匹配 | 风险等级 | 主要问题 |
|------|---------|---------|---------|---------|
| 0-1 Private Details | ✅ 清晰 | ✅ 匹配 | 无 | — |
| 0-2 Scenario | ✅ 清晰 | ✅ 匹配 | 无 | — |
| 1-1 WM Scene | ❌ 缺失 | ⚠️ 部分错位 | **P1** | 无身份声明；"世界调用总纲"含 Extra 级机制细节 |
| 1-2 WM APD | ⚠️ 模糊 | ✅ 匹配 | P3 | 声明为角色身份而非字段身份 |
| 1-3 WM Extra | ✅ 清晰 | ✅ 匹配 | 无 | — |
| 2-1 WSK Scene | ⚠️ 埋没 | ⚠️ 部分错位 | **P1** | 身份声明在 [身份] 段非文件顶部；[输出] 段含模板级细节 |
| 2-2 WSK APD | ⚠️ 模糊 | ✅ 匹配 | P3 | 声明为角色身份而非字段身份 |
| 2-3 WSK Extra | ❌ 缺失 | ✅ 匹配 | P3 | 无字段身份声明 |

### 8.3 详细发现

#### P1-12：1-1 Scene 缺少字段身份声明（新增）

**位置**：`Prompt_File\1-1_WorldMaster_Scene.md` 第 1 行

**当前状态**：文件以"这是一个以硬核后末日生存为核心的长期运行世界。"开头——这是世界基调叙事，不是字段身份声明。LLM 读到此句时无法获得"本字段 = Scene = 身份 + 输出契约"的语义定框。

**修复方向**：在文件顶部新增字段身份声明行，例如 `【Scene 平台锚点】本文件是 World Master 的永久元身份与叙事框架层；承接身份声明、输出契约与裁定入口。静态世界资料见聊天室 Scenario 字段，动态机制见 World Master（本角色）的 Extra Details 字段。`

**涉及文件**：1-1

**预计工作量**：极低（1 行）

---

#### P1-13：1-1 Scene "世界调用总纲"含 Extra 级机制细节（新增）

**位置**：`Prompt_File\1-1_WorldMaster_Scene.md` L94-L105

**当前状态**：`世界调用总纲` 段包含以下 Extra 级机制细节：
- **可消耗物资渲染硬约束**（L100）："容器描述 + ≈kg"双轨格式——这是 1-3 Extra 的机制执行细节
- **资源剩余天数估算指引**（L101）：WM 估算优先于 WSK 公式——这是 1-3 Extra 的机制执行细节
- **搜刮按 T1-T4 分层**（L102）：地点标签列表——这是 0-2 Scenario 的静态世界资料
- **高风险区域主类**（L103）：污染区/封闭地下设施/军事核心区——这是 0-2 Scenario 的静态世界资料
- **经济规则引用**（L104）：与 0-2 引用重复
- **长期关系追踪核心**（L105）：与 1-2 APD 的 §关系规则 重复

**根因**：`世界调用总纲` 设计为"快速参考摘要"，但将不同字段层的内容（Scene 契约 + Scenario 世界共识 + Extra 机制细节）混编在同一段。

**修复方向**：
- 将 L100-101（可消耗物资渲染 + 资源天数估算）迁移至 1-3 Extra，1-1 改为纯引用
- 将 L102-105（搜刮分层/高风险区域/经济/关系）迁移至 0-2 或 1-2 对应位置，或改为纯引用
- 保留 L96-99（地图调用 + 移动硬约束）作为 Scene 输出契约，因其约束的是"Scene 中怎么写移动"

**涉及文件**：1-1 / 1-3 / 0-2

**预计工作量**：低（3 个文件各 1-2 处改动）

---

#### P1-14：2-1 Scene 身份声明埋没在 [身份] 段（新增）

**位置**：`Prompt_File\2-1_WorldStateKeeper_Scene.md` 第 1-3 行

**当前状态**：文件以世界背景叙事开头（"你所在的聊天室承载一个硬核后末日长期生存世界。灰港灾变后第 3 年..."），身份声明（"你是 World State Keeper：硬状态管理员。"）在 L3-4 的 [身份] 段才出现。LLM 在前 2 行获得的语义定框是"聊天室世界背景"而非"WSK 身份契约"。

**修复方向**：文件顶部新增字段身份声明行，例如 `【Scene 平台锚点】本文件是 World State Keeper 的身份与输出契约层；承接身份声明、触发/静默契约与越界边界。详细模板见 World State Keeper（本角色）的 Extra Details 字段。`

**涉及文件**：2-1

**预计工作量**：极低（1 行）

---

#### P1-15：2-1 Scene [输出] 段含模板级格式细节（新增）

**位置**：`Prompt_File\2-1_WorldStateKeeper_Scene.md` L13-L27

**当前状态**：[输出] 段混合了输出契约（应该保留在 Scene）与模板级格式细节（应该只在 2-3 Extra）：
- L16-17 "第一行必备"：含具体格式示例 `D2-T5 / October / Autumn...`，与 2-3 Extra 模板重复
- L20 "移动字段合并"：具体格式字符串 `Travel Time: {值} ({备注}) / Steps: {值}`，与 2-3 Extra 模板重复
- L21 "库存格式"：具体格式示例 `武器: 霰弹枪×1(泵动式，空膛)...`，与 2-3 Extra 模板重复

**根因**：L14 已声明"本条仅给输出契约概要；第一行格式/变化子段/完整视图 9 字段的字段清单与顺序，统一以 Extra Details 字段为唯一权威定义"，但实际后续行仍嵌入了模板级细节。

**修复方向**：将 L16-17/L20/L21 的格式具体内容改为纯引用（"第一行格式以 2-3 Extra §使用原则「第一行必备」为准"），删除重复的格式字符串与示例。保留 L14/L15/L18/L19/L22-L26 的契约性内容。

**涉及文件**：2-1 / 2-3

**预计工作量**：低（2 个文件各 1-2 处改动）

---

#### P3-5：1-2 APD 角色身份声明非字段身份声明（新增）

**位置**：`Prompt_File\1-2_WorldMaster_Additional_Personality_Details.md` 第 1 行

**当前状态**："你是 World Master，也是当前世界唯一的场景裁定者；你的任务是维护一个长期运行的硬核后末日世界。"——这是对"WM 角色"的声明，不是对"本字段 = APD = 裁定决策逻辑"的声明。

**修复方向**：在该行之后补充字段身份标识，例如 `本文件是 World Master 的裁定补充层，承接裁定决策逻辑（生存/战斗/交易/搜刮/关系/敌对/时间推进"怎么判"）。`

**涉及文件**：1-2

**预计工作量**：极低（1 行）

---

#### P3-6：2-2 APD 角色身份声明非字段身份声明（新增）

**位置**：`Prompt_File\2-2_WorldStateKeeper_Additional_Personality_Details.md` 第 1-3 行

**当前状态**："你是 World State Keeper。你是这个多角色群聊中的硬状态管理员。你不负责扮演人物，不负责推进剧情，不负责裁定成败。"——角色身份声明，非字段身份声明。

**修复方向**：在第 3 行后补充字段身份标识，例如 `本文件是 World State Keeper 的记账决策层，承接 DO/REJECT/SILENT 决策表与各状态记录规则。`

**涉及文件**：2-2

**预计工作量**：极低（1 行）

---

#### P3-7：2-3 Extra 无字段身份声明（新增）

**位置**：`Prompt_File\2-3_WorldStateKeeper_Extra_Details.md` 第 1 行

**当前状态**：以"【平台锚点】你只生成 [State Update] / [Commit Rejected]..."开头——这是行为锚点，不是字段身份声明。LLM 无法获得"本字段 = Extra = 输出模板 + 提取细则"的语义定框。

**修复方向**：在平台锚点中补充字段身份标识，例如 【平台锚点】本文件是 World State Keeper 的模板与提取细则层，承载 [State Update] 输出格式、完整视图、压缩规则、提取纪律与最低提交标准。

**涉及文件**：2-3

**预计工作量**：极低（1 行）

---

### 8.4 修复优先级建议

| 阶段 | 任务 | 涉及问题 | 影响 |
|------|------|----------|------|
| **批次 4（P1）** | 1-1 新增字段身份声明 | P1-12 | 修正头部语义定框，减少 LLM 解读歧义 |
| **批次 4（P1）** | 1-1 "世界调用总纲"去 Extra 级细节 | P1-13 | 消除内容错位，降低 1-1 后段冗余 |
| **批次 4（P1）** | 2-1 新增字段身份声明 | P1-14 | 修正头部语义定框 |
| **批次 4（P1）** | 2-1 [输出] 段去模板级细节 | P1-15 | 消除与 2-3 的格式重复，SSOT 化 |
| **批次 5（P3）** | 1-2 / 2-2 / 2-3 补充字段身份声明 | P3-5/6/7 | 统一全文件头部语义定框 |

---

## 九、基于 §1.1 承载分工规范的整体优化可行性评估（2026-08-06）

### 9.1 评估依据

- **规范依据**：`PROJECT_ANALYSIS.md §1.1 提示词字段承载分工规范（优化首个规划）`——定义了 9 个落盘文件各自应承载的内容类型与边界
- **前置审计**：§8 字段身份审计结果（2026-08-05）——已覆盖"身份声明"层面的问题（P1-12 ~ P1-15, P3-5 ~ P3-7）
- **本次范围**：在 §8 基础上深入到"内容承载分工"层面——不仅看头部身份声明是否正确，更看文件实际承载的内容是否与 §1.1 定义的分工匹配

### 9.2 评估方法

逐文件提取节结构，对照 §1.1 的承载分工定义，识别三类偏差：

| 偏差类型 | 定义 | 严重性 |
|----------|------|--------|
| **内容错位** | 某文件承载了按 §1.1 应归属其他字段的内容类型 | P1（影响 LLM 语义定框与执行稳定性）|
| **边界模糊** | 某内容介于两个字段职责之间，需厘清归属 | P2（可运行但有维护风险）|
| **身份声明** | 文件头部缺少/埋没字段身份声明 | P3（已由 §8 覆盖，本次合并执行）|

### 9.3 评估结论总表

| 文件 | §1.1 定位 | 内容匹配 | 新发现问题 | 已有审计 | 风险 |
|------|----------|---------|-----------|----------|------|
| `0-1` Private Details | 全局协议 | ✅ 匹配 | — | — | 无 |
| `0-2` Scenario | 世界共识 | ⚠️ 部分错位 | P2-6 | — | P2 |
| `0-3` 自定义指令 | 输出风格约束 | ✅ 匹配（P1-17 已完成）| — | — | 无 |
| `1-1` WM Scene | 元身份 + 叙事框架 | ⚠️ 部分错位 | — | P1-12, P1-13 | **P1** |
| `1-2` WM APD | 风格与裁定哲学 | ✅ 匹配（P1-16, P2-8 已完成）| — | P3-5 | 无 |
| `1-3` WM Extra | 可执行机械系统 | ✅ 匹配（已接收迁移）| — | — | 无 |
| `2-1` WSK Scene | 元身份 + 输出/静默契约 | ⚠️ 部分错位（P1-18 已完成；P2-7 剩余）| P2-7 | P1-14, P1-15 | P2 |
| `2-2` WSK APD | 记账判断逻辑 | ✅ 匹配 | — | P3-6 | 无 |
| `2-3` WSK Extra | 输出机制 + 模板 | ✅ 匹配（待接收迁移）| — | P3-7 | 无 |

### 9.4 详细发现

#### P1-16：1-2 APD 含机械系统数据表与执行规则（新增）✅ 已完成（2026-08-06）

> **完成说明**：三段数据表（[生存压力时间阈值表] / [Turn / Day 推进规则] / [非移动动作时间基准]）已迁移至 1-3 Extra；1-2 保留 [压力裁定运行模型] 并将引用更新为指向 1-3 Extra（P2-8 一并完成）。

**位置**：`Prompt_File\1-2_WorldMaster_Additional_Personality_Details.md`

**当前状态**：1-2 按 §1.1 应承载"叙事声音、裁定哲学、情节推动、交互边界、氛围控制"。但以下节段属于机械系统数据表/执行规则，按 §1.1 应归 1-3 Extra（"详细结果裁定系统、状态管理与时间推进机制"）：

| 节段 | 行号 | 内容性质 | §1.1 归属 |
|------|------|----------|----------|
| [生存压力时间阈值表] | L36-78 | 脱水/饥饿/疲劳/体温/伤病的时间阈值数据表 | 1-3 Extra（详细结果裁定系统）|
| [Turn / Day 推进规则] | L85-93 | 时间推进的硬性执行规则（step 累加、跳号禁令、Day 判定条件）| 1-3 Extra（状态管理与时间推进机制）|
| [非移动动作时间基准] | L95-101 | 动作耗时折算数据表（5 级基准）| 1-3 Extra（可执行数据表）|

**保留在 1-2 的内容**：
- [压力裁定运行模型]（L80-83）——这是"怎么判"的裁定哲学（增量演化 + 周期校准），符合 §1.1 的"裁定哲学"定位，**保留在 1-2**
- [人类敌对风险机制]（L103+）——裁定哲学（4 维度判断），**保留在 1-2**
- [NPC Trust 行为阈值表]（L135+）——裁定哲学（Trust → 行为映射），**保留在 1-2**
- [搜刮过程机制]（L219+）——裁定哲学（如何裁定搜刮），**保留在 1-2**

**根因**：1-2 在迭代中逐步积累了时间推进规则和压力阈值数据表，这些内容以"裁定参考"名义混入 APD，但实质是 Extra 级机械系统。

**修复方向**：
1. 将 [生存压力时间阈值表]、[Turn / Day 推进规则]、[非移动动作时间基准] 三段迁移至 1-3 Extra
2. 1-2 对应位置改为纯引用（"时间推进规则见 WM Extra §Turn/Day 推进规则"）
3. 1-3 新增 [时间推进与压力阈值] 段承接迁移内容

**涉及文件**：1-2 / 1-3

**预计工作量**：中（3 段迁移 + 2 文件引用同步）

**风险**：中（时间推进规则是高频引用规则，迁移后需验证所有引用点）

---

#### P1-17：0-3 自定义指令含 WSK 库存归类规则（新增）✅ 已完成（2026-08-06）

> **完成说明**：[强化抑制机制] §工具组归类自检 已从 0-3 删除；大部分内容（功能组归类、超 10 项分组、据点库存精简、口径一致）已在 2-2 §精简库存与盘点规则 7a/7b 覆盖，去重不迁移；唯一新增内容（同义工具去重映射 + 高频/低频单件区分）合并为 2-2 新条目 7c。0-3 [强化抑制机制] 保留 3 个纯风格约束子段（感官词必查清单 / 动作短语去重 / 句式结构去重）。

**位置**：`Prompt_File\0-3_自定义指令.md` L14-19（[强化抑制机制] → 工具组归类自检）

**当前状态**：0-3 按 §1.1 应"仅约束 WM 的叙事输出风格，不承载世界设定或裁定规则"。但 [强化抑制机制] 段中的"工具组归类自检"（L14-19）是 WSK 库存压缩/归类规则：
- "工具/建材类按功能组归类（武器/弹药/医疗/工具/食物等大类）"
- "同义工具去重：刀/匕首/小刀归'刃具'"
- "据点库存只列关键物资（弹药/医疗/燃料），普通物资用'具备/充足'标记"
- "库存盘点口径与 World State Keeper 账本保持分组一致"

这些是 WSK 的记账执行规则，与 2-2 APD §精简库存与盘点规则（L177-223）内容重叠，不属于 WM 叙事输出风格约束。

**根因**：0-3 作为新增字段，在迭代中混入了本应归 WSK APD/Extra 的库存归类规则。

**修复方向**：
1. 将 L14-19（工具组归类自检）迁移至 2-2 APD §精简库存与盘点规则 段内（或 2-3 Extra 压缩规则段）
2. 0-3 对应位置删除该子段，保留 [强化抑制机制] 的其余内容（感官词必查清单、动作短语去重、句式结构去重——这些是真正的输出风格约束）
3. 验证 2-2 §精简库存与盘点规则 与迁移内容无冲突

**涉及文件**：0-3 / 2-2（或 2-3）

**预计工作量**：低（1 段迁移 + 1 文件删除 + 引用验证）

**风险**：低（库存归类规则已在 2-2 有对应段，迁移是合并而非新增）

---

#### P1-18：2-1 Scene 含 APD 级内容（新增）✅ 已完成（2026-08-06）

> **完成说明**：[常见误操作警示] 已迁移至 2-2 APD [提交前检查] 段之后，形成"检查 → 警示 → 优先顺序"完整防错链。2-1 完全删除该段（未添加纯引用，因 2-1 [决策依据索引] 已将 DO/REJECT/SILENT 规则路由到 2-2 APD，额外引用冗余）。实现与原计划第 2 步（"改为纯引用"）略有偏差，已确认偏差合理。[LLM 心理预设] 归属见 P2-7。

**位置**：`Prompt_File\2-1_WorldStateKeeper_Scene.md` L63-69（[常见误操作警示]）

**当前状态**：2-1 按 §1.1 应"只放最高优先级的运行契约，不放细则"。但以下节段属于 APD 级记账纪律：

| 节段 | 行号 | 内容性质 | §1.1 归属 | 处理 |
|------|------|----------|----------|------|
| [LLM 心理预设] | L41-45 | 压制 LLM 错误本能的决策心理 | 2-2 APD（记账判断逻辑）| P2-7（见下） |
| [常见误操作警示] | L63-69 | 6 条记账误操作的 DO/REJECT 判定 | 2-2 APD（记账纪律）| 迁移 |
| [你需要长期记录] | L60-61 | 已是纯引用（指向 APD） | ✅ 可保留 | 不动 |
| [弹药口径合并] | L77-78 | 已是纯引用（指向 Extra） | ✅ 可保留 | 不动 |

**修复方向**：
1. 将 [常见误操作警示]（L63-69）迁移至 2-2 APD（可作为 [提交前检查] 段的补充，或独立为 [常见误操作警示] 段）
2. 2-1 对应位置改为纯引用（"常见误操作警示见 APD 字段"）
3. [LLM 心理预设] 的归属见 P2-7

**涉及文件**：2-1 / 2-2

**预计工作量**：低（1 段迁移 + 1 文件引用同步）

**风险**：低（内容迁移至更匹配的字段，不修改规则本身）

---

#### P2-6：0-2 [可消耗物资渲染规范] 混合世界常识与输出格式协议（新增）

**位置**：`Prompt_File\0-2_Scenario.md` L561-576

**当前状态**：0-2 按 §1.1 应承载"世界共识"。[可消耗物资渲染规范] 包含两类内容：
- **输出格式协议**（L562-567, L574-576）：WM 渲染格式（"容器描述 + ≈kg"双轨制）、WSK 记录格式——这是跨角色输出协议，按 §1.1 应归 0-1 Private Details（"输出格式总则"）或 1-3 Extra（"输出格式的具体模块实现"）
- **世界常识校准锚点**（L568-573）：1L ≈ 1.0kg、标准编织袋 ≈ 25-50kg 等——这是世界常识参考数据，符合 0-2 Scenario 定位

**修复方向**：
- 方案 A（推荐）：拆分——格式协议部分迁移至 0-1 或 1-3，校准锚点保留在 0-2
- 方案 B：整体保留在 0-2，但在 0-1 中增加纯引用——风险较低但未消除错位

**涉及文件**：0-2 / 0-1（或 1-3）

**预计工作量**：低-中（取决于方案选择）

**风险**：低（格式协议已有跨文件引用，拆分不破坏执行）

---

#### P2-7：2-1 [LLM 心理预设] 归属边界模糊（新增）

**位置**：`Prompt_File\2-1_WorldStateKeeper_Scene.md` L41-45

**当前状态**：[LLM 心理预设] 压制 4 种 LLM 错误本能（凑齐字段 / 自动入账 / 过度展开 / 礼貌自介）。它兼具两种功能：
- **Scene 定框功能**：在文件头部为 LLM 建立正确的行为预期——这部分符合 Scene 的"运行契约"定位
- **APD 决策逻辑功能**：压制错误本能的判断逻辑——这部分符合 APD 的"记账判断逻辑"定位

**评估**：偏向保留在 2-1 Scene。理由：
1. 心理预设的本质是"身份定框"——告诉 LLM "你是审计者，不是叙事者"，这属于 Scene 的元身份契约
2. 它不包含具体的 DO/REJECT/SILENT 判定规则，只是行为预期
3. §1.1 Scene 职责包含"触发与静默契约"——心理预设是静默契约的心理层面

**建议**：保留在 2-1 Scene，不迁移。仅在 2-2 APD 增加交叉引用（"心理预设见 Scene 字段"）。

**涉及文件**：2-2（仅增加引用）

**预计工作量**：极低

---

#### P2-8：1-2 [压力裁定运行模型] 归属确认（新增）✅ 已完成（随 P1-16 一并处理）

> **完成说明**：[压力裁定运行模型] 保留在 1-2 APD（裁定哲学定位正确）；引用路径已更新为"World Master（本角色）的 Extra Details 字段 §生存压力时间阈值表"，指向 1-3 Extra 迁移后的位置。

**位置**：`Prompt_File\1-2_WorldMaster_Additional_Personality_Details.md` L80-83

**当前状态**：[压力裁定运行模型] 描述"增量演化 + 周期校准"的裁定方法。按 §1.1：
- 1-2 APD 职责包含"裁定哲学（如何判断成功/失败/部分成功）"——增量演化是"怎么判"的哲学
- 1-3 Extra 职责包含"详细结果裁定系统"——运行模型是裁定系统的执行流程

**评估**：保留在 1-2 APD。理由：
1. 它描述的是"裁定思路"（以增量演化为锚，不依赖重算），不是"执行流程"或"数据表"
2. §1.1 APD 职责明确包含"裁定哲学"——这正是裁定哲学
3. 它引用了 [生存压力时间阈值表]（P1-16 中将迁移至 1-3），迁移后改为跨字段引用即可

**建议**：保留在 1-2 APD。在 P1-16 迁移时间阈值表后，将 L82 的引用更新为"见 WM Extra 字段 §生存压力时间阈值表"。

**涉及文件**：1-2（仅更新引用路径）

**预计工作量**：极低（随 P1-16 一并处理）

---

### 9.5 优化方案

#### 批次 6（P1）：内容迁移 ✅ 全部完成（2026-08-06）

| 序号 | 任务 | 涉及问题 | 涉及文件 | 工作量 | 依赖 | 状态 |
|------|------|----------|----------|--------|------|------|
| 6-1 | 1-2 → 1-3 迁移三段机械系统数据表 | P1-16 | 1-2 / 1-3 | 中 | 无 | ✅ 已完成 |
| 6-2 | 0-3 → 2-2 迁移库存归类规则 | P1-17 | 0-3 / 2-2 | 低 | 无 | ✅ 已完成 |
| 6-3 | 2-1 → 2-2 迁移常见误操作警示 | P1-18 | 2-1 / 2-2 | 低 | 无 | ✅ 已完成 |

**执行顺序**：6-1 → 6-2 → 6-3（相互独立，可并行；6-1 优先因工作量最大）

**回滚策略**：每个子任务独立 commit，可单独回滚

**完成验证**：静态验证通过——Prompt_File 目录无悬空引用、无化石引用（ASSEMBLY_CHECKLIST / tools_prompt / WER）；diff 核对 5 文件 72 增 78 删（净减 6 行 = 去重收益）。

#### 批次 7（P2）：边界厘清

| 序号 | 任务 | 涉及问题 | 涉及文件 | 工作量 | 状态 |
|------|------|----------|----------|--------|------|
| 7-1 | 0-2 [可消耗物资渲染规范] 拆分 | P2-6 | 0-2 / 0-1（或 1-3）| 低-中 | 待实施 |
| 7-2 | 2-2 增加 [LLM 心理预设] 交叉引用 | P2-7 | 2-2 | 极低 | 待实施 |
| 7-3 | 1-2 [压力裁定运行模型] 引用路径更新 | P2-8 | 1-2 | 极低 | ✅ 已完成（随 6-1） |

**执行顺序**：7-3 随 6-1 一并处理（已完成）；7-1 / 7-2 独立

#### 批次 8（合并）：身份声明补全（与 §8 批次 4/5 合并执行）

| 序号 | 任务 | 涉及问题 | 来源 |
|------|------|----------|------|
| 8-1 | 1-1 新增字段身份声明 | P1-12 | §8 |
| 8-2 | 1-1 "世界调用总纲"去 Extra 级细节 | P1-13 | §8 |
| 8-3 | 2-1 新增字段身份声明 | P1-14 | §8 |
| 8-4 | 2-1 [输出] 段去模板级细节 | P1-15 | §8 |
| 8-5 | 1-2 / 2-2 / 2-3 补充字段身份声明 | P3-5/6/7 | §8 |

**执行建议**：批次 8 应在批次 6 完成后执行——先完成内容迁移，再补全身份声明，避免迁移后身份声明与实际内容不匹配。

### 9.6 可行性评估

#### 9.6.1 风险评估

| 风险项 | 等级 | 说明 | 缓解策略 |
|--------|------|------|----------|
| **迁移后引用断裂** | 中 | 时间推进规则被多文件引用，迁移后可能遗漏引用更新 | 迁移前 Grep 全量引用点；迁移后逐个验证 |
| **LLM 行为回退** | 中 | 内容迁移可能改变 LLM 的加载顺序和注意力分配 | 迁移后实际运行 5-10 轮验证 |
| **跨文件同步遗漏** | 低 | 每次迁移涉及 2 个文件，同步范围可控 | 逐文件验证引用一致性 |
| **库存归类规则冲突** | 低 | 0-3 → 2-2 迁移可能与 2-2 既有规则重复 | 迁移时与 2-2 §精简库存与盘点规则 做去重合并 |

#### 9.6.2 依赖关系

```
批次 6-1（1-2→1-3 迁移）──→ 批次 7-3（引用路径更新）
                      ────→ 批次 8-1/8-2（1-1 身份声明，因 1-1 引用 1-2 的内容）
批次 6-2（0-3→2-2 迁移）──→ 无依赖
批次 6-3（2-1→2-2 迁移）──→ 批次 8-3/8-4（2-1 身份声明，因迁移后 2-1 内容变化）
                      ────→ 批次 8-5（2-2 身份声明，因 2-2 接收迁移内容）
```

**关键路径**：批次 6 → 批次 7（7-3） → 批次 8

#### 9.6.3 工作量估算

| 批次 | 任务 | 预计工作量 | 涉及文件数 |
|------|------|-----------|-----------|
| 6-1 | 1-2 → 1-3 三段迁移 | 中（3 段 + 引用同步）| 2 |
| 6-2 | 0-3 → 2-2 库存规则迁移 | 低（1 段 + 去重）| 2 |
| 6-3 | 2-1 → 2-2 误操作迁移 | 低（1 段 + 引用）| 2 |
| 7-1 | 0-2 渲染规范拆分 | 低-中（拆分 + 迁移）| 2-3 |
| 7-2 | 2-2 增加交叉引用 | 极低 | 1 |
| 7-3 | 1-2 引用路径更新 | 极低 | 1 |
| 8-1~8-5 | 身份声明补全 | 低-中 | 5 |
| **合计** | | **中**（预计 1-2 个工作批次）| 7 个文件 |

#### 9.6.4 收益预期

| 收益项 | 说明 |
|--------|------|
| **语义定框准确** | 每个文件头部身份声明 + 实际内容与 §1.1 分工一致，LLM 获得正确的语义定框 |
| **SSOT 单一权威** | 消除跨文件内容重复（时间规则、库存归类、误操作警示），修改只需改一处 |
| **维护成本降低** | 机械系统数据表归 Extra、裁定哲学归 APD，后续修改时定位明确 |
| **认知适配** | 符合 §0.1"第一层·字段身份"原则——身份定框 → 可见性 → 有效性 |

### 9.7 实施建议

1. **先迁内容，后补声明**：批次 6（内容迁移）→ 批次 7（边界厘清）→ 批次 8（身份声明）。避免先补声明再迁移导致声明与内容不匹配。
2. **6-1 优先**：P1-16 工作量最大且引用最多，优先处理可暴露大部分同步风险。
3. **每步验证**：每个子任务完成后 Grep 引用点 + 实际运行 5-10 轮验证 LLM 行为。
4. **独立 commit**：每个子任务独立 commit，支持单独回滚。
5. **与 §8 合并**：批次 8 直接整合 §8 已有的 P1-12 ~ P1-15 和 P3-5 ~ P3-7，不重复审计。

### 9.8 §1.1 规范自身的改进建议

> **定位**：以下 4 项是 §1.1 承载分工规范的操作指南补充。§1.1 已在对应位置引用本节各子节（§9.8.1 分界标准、§9.8.2 依赖关系、§9.8.3 Pinned Memory、§9.8.4 子分类），本节提供完整的判定规则与操作细节。

#### 9.8.1 APD/Extra 分界判定标准

**问题**：§1.1 对 APD 的定义是"裁定哲学（如何判断）"，对 Extra 的定义是"详细结果裁定系统"。但"判断思路"与"执行系统"之间存在连续谱，缺乏可操作的判定标准。

**判定规则**：

| 判定维度 | → APD | → Extra |
|----------|-------|---------|
| **内容形态** | 原则/维度/思路的描述 | 数值表/格式模板/执行流程 |
| **可独立性** | 可脱离具体数值独立表述 | 依赖具体数值/格式才有意义 |
| **修改频率** | 低（裁定哲学稳定） | 中-高（数据表/模板随迭代调整） |
| **消费者** | LLM 的判断推理链 | LLM 的执行查表/格式套用 |
| **抽象层级** | "怎么判"（判断维度与思路） | "用什么判"（数据/格式/流程） |

**边界情况处理**：

| 情况 | 处理 | 示例 |
|------|------|------|
| 内容同时含判断思路 + 具体数值 | **拆分**：思路归 APD，数值归 Extra，APD 纯引用 Extra | [压力裁定运行模型]（思路→APD）+ [生存压力时间阈值表]（数值→Extra）|
| 内容是"阈值映射表"但被 APD 引用 | 表归 Extra，APD 保留引用 | NPC Trust 0-100→行为映射：映射逻辑归 APD，具体阈值表归 Extra |
| 内容描述"流程步骤"（先 A 再 B） | → Extra（执行流程） | [搜刮过程机制]的步骤流程→Extra；但"搜刮按 T1-T4 分层"的判断维度→APD |
| 内容是"维度清单"（4 维度判断） | → APD（判断维度） | [人类敌对风险机制]的 4 维度（暴露度/资源价值/敌意背景/动手成本）→APD |
| 内容是"正反例" | 跟随其支撑的规则归属 | 支撑 APD 原则的正反例→APD；支撑 Extra 格式的正反例→Extra |

**实操判定流程**：
```
问 1：内容是否包含具体数值/格式字符串/步骤流程？
  ├─ 是 → Extra
  └─ 否 → 问 2：内容是否描述"判断维度"或"原则性声明"？
           ├─ 是 → APD
           └─ 否 → 问 3：内容是否被 APD 的裁定哲学引用为数据支撑？
                    ├─ 是 → Extra（APD 保留引用）
                    └─ 否 → 按内容性质判断，默认归 Extra（Extra 是"兜底"字段）
```

---

#### 9.8.2 字段间依赖关系图

**问题**：§1.1 定义了每个字段"装什么"并在开头引用了本节，但字段间的具体依赖关系需要完整图表支撑，修改时仍容易遗漏同步点。

**依赖关系总表**：

| 修改源字段 | 需同步检查的目标字段 | 同步原因 |
|-----------|-------------------|----------|
| **0-1** Private Details | 所有文件（1-1/1-2/1-3/2-1/2-2/2-3） | 全局协议被所有角色引用；输出格式总则被 WM/WSK 引用 |
| **0-2** Scenario | 1-2 / 1-3 / 2-2 / 2-3 | 世界共识被裁定哲学引用（1-2）、势力表引用（1-3）、记录规则引用（2-2）、模板字段引用（2-3） |
| **0-3** 自定义指令（运行时纠偏） | 1-1 / 1-2 | 运行时纠偏指令被 Scene 输出契约（1-1）和叙事风格（1-2）引用 |
| **1-1** WM Scene | 1-2 / 1-3 / 2-1 | Scene 契约被 APD 裁定入口引用（1-2）、Extra 执行引用（1-3）；WSK Scene 需对齐输出协议（2-1） |
| **1-2** WM APD | 1-3 | APD 引用 Extra 的数据表；裁定哲学变化需同步数据支撑 |
| **2-1** WSK Scene | 2-2 / 2-3 | Scene 输出契约被 APD 决策逻辑引用（2-2）、Extra 模板引用（2-3） |
| **2-2** WSK APD | 2-3 | APD 记账规则被 Extra 模板的字段定义引用 |

**依赖关系图**：

```
聊天室级（共享层）
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  0-1 协议    │────→│  0-2 共识    │     │  0-3 纠偏    │
│  (全局铁律)  │     │  (世界知识)  │     │  (运行时)    │
└──────┬──────┘     └──────┬──────┘     └──────┬──────┘
       │ 全局引用            │ 世界共识引用       │ 纠偏引用
       ▼                    ▼                    ▼
角色卡级（WM）                    角色卡级（WSK）
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  1-1 Scene  │────→│  1-2 APD    │────→│  1-3 Extra  │     │  2-1 Scene  │
│  (元身份)   │     │  (裁定哲学)  │     │  (机械系统)  │     │  (元身份)   │
└─────────────┘     └─────────────┘     └─────────────┘     └──────┬──────┘
                                                                       │
                    ┌─────────────┐     ┌─────────────┐               │
                    │  2-2 APD    │←────│  2-3 Extra  │←──────────────┘
                    │  (记账逻辑)  │     │  (输出模板)  │
                    └─────────────┘     └─────────────┘

跨角色依赖：
  0-1 ──→ 2-1（输出协议对齐）     0-2 ──→ 2-2（世界状态记录规则）
  1-1 ──→ 2-1（WM→WSK 提取源）    1-3 ──→ 2-3（WM 机制→WSK 记录字段）
```

**图例**：`──→` 表示"修改源字段时需同步检查目标字段"。聊天室级字段向下流向角色卡级；角色卡内 Scene→APD→Extra 自上而下。

**使用方法**：修改任一字段时，查表确认需同步检查的目标字段，逐个 Grep 引用并更新。

---

#### 9.8.3 Pinned Memory 承载分工

**问题**：§1.1 已在 §1.1.1 末尾引用了 Pinned Memory 的简要描述并指向本节，但完整的承载分工定义需要在此展开。Pinned Memory 是记忆模型 Layer 5（见 PROJECT_ANALYSIS §9.2），承载历史账本。

**Pinned Memory 定位**：

| 属性 | 定义 |
|------|------|
| **字段性质** | 非平台字段；用户手动维护的持久化文本块 |
| **维护方式** | 用户从 WSK [State Update] 的 `## 新增` 段复制到 Pinned Memory |
| **生命周期** | 跨会话持久（不随聊天上下文窗口重置而丢失） |
| **消费者** | 用户（人工查阅）+ LLM（如果用户粘贴到对话中） |

**承载内容分工**：

| 内容类型 | 承载内容 | 来源 | 保留策略 |
|----------|----------|------|----------|
| **不可压缩锚点** | 死亡 / 失踪 / 永久残疾 / 据点失守等不可逆事件 | WSK [State Update] `## 新增` 段 | 永久保留 |
| **已关闭事件链** | 已解决的报复链、已完成的交易、已撤离的据点 | WSK [State Update] `## 新增` 段 | 永久保留 |
| **长期格局变化** | 势力兴衰、区域封锁/开放、常驻 NPC 状态变化 | WSK [State Update] `## 新增` 段 | 永久保留 |
| **结构变化** | 据点组件增减、阵营关系矩阵变化 | WSK [State Update] `## 新增` 段 | 永久保留 |

**与 2-3 Extra 的关系**：

```
2-3 Extra [State Update]        Pinned Memory
┌──────────────────┐           ┌──────────────────┐
│ ## 新增（当轮变化）│──用户复制──→│ 历史账本（累积）  │
│ - 不可压缩锚点    │           │ - 不可压缩锚点    │
│ - 长期格局变化    │           │ - 已关闭事件链    │
│ - 结构变化        │           │ - 长期格局变化    │
└──────────────────┘           │ - 结构变化        │
                               └──────────────────┘
```

**Pinned Memory 不承载的内容**：
- ❌ 实时状态（库存/压力/位置等）→ 由 WSK [State Update] 维护
- ❌ 世界共识（地理/势力/规则）→ 由 0-2 Scenario 维护
- ❌ 全局协议 → 由 0-1 Private Details 维护
- ❌ 裁定规则 → 由 1-2/1-3 维护

**维护纪律**：
- Pinned Memory 的内容**只能从 WSK [State Update] 的 `## 新增` 段复制**，不可手动编写
- 复制后 WSK 的 `## 新增` 段仍保留当轮输出（不删除）；Pinned Memory 是沉淀副本
- Pinned Memory 不受压缩规则约束（用户可自行组织格式，但建议按日期/类型分组）

---

#### 9.8.4 1-3 WM Extra 子分类建议

**问题**：§1.1 将 1-3 Extra 定位为"全部可执行机械系统 + 可执行细节"并在 WM Extra 定义中引用了本节的四子分类，但子分类的详细定义、当前内容归类和组织建议需要在此展开。

**子分类定义**：

| 子分类 | 职责 | 内容特征 | 当前已有内容 |
|--------|------|----------|-------------|
| **A. 世界模拟机制** | 驱动世界运转的规则 | 描述"世界在没有玩家干预时怎么变化" | [回合后场外演化规则]、[世界事件遭遇机制]、[区域-势力-事件-概率统一表]、[势力事件池]、[§3.0-3.7 势力首领/据点/关系矩阵] |
| **B. 时间推进系统**（P1-16 迁移后新增） | 时间与压力状态管理 | 时间推进规则 + 压力阈值数据表 | [Turn/Day 推进规则]、[非移动动作时间基准]、[生存压力时间阈值表]（P1-16 迁移后） |
| **C. 裁定执行系统** | 支撑 WM 裁定的执行规则与数据 | 描述"WM 在裁定特定行为时按什么流程/数据执行" | [搜刮入账职责]、[§13 据点结构基线]、[身份暴露风险]、[关键锚点] |
| **D. 参考数据表** | 静态参考数据 | 纯数据表/清单，不包含执行逻辑 | [锚点非冻结硬规则]、[Role Anchor 模板]、[死亡事件传播标注] |

**子分类在 1-3 文件内的组织建议**：

```
1-3 WM Extra Details
├── 【平台锚点】（字段身份声明）
├── [文件定位与使用方式]
│
├── ── A. 世界模拟机制 ──
│   ├── [回合后场外演化规则]
│   ├── [世界事件遭遇机制]
│   ├── [区域-势力-事件-概率统一表]
│   ├── [势力事件池与关系参考]
│   └── [§3.0-3.7 势力首领/据点/关系矩阵]
│
├── ── B. 时间推进系统（P1-16 迁移后）──
│   ├── [Turn / Day 推进规则]
│   ├── [非移动动作时间基准]
│   └── [生存压力时间阈值表]
│
├── ── C. 裁定执行系统 ──
│   ├── [搜刮入账职责]
│   ├── [§13 据点结构基线]
│   ├── [身份暴露风险]
│   └── [关键锚点]
│
└── ── D. 参考数据表 ──
    ├── [群聊 AI 角色动态锚点协议]
    ├── [Role Anchor 模板]
    └── [死亡事件传播标注]
```

**新增内容判定规则**：
- 问"这条规则描述的是世界自己怎么变化吗？"→ 是→A. 世界模拟机制
- 问"这条规则描述的是时间/压力状态怎么推进吗？"→ 是→B. 时间推进系统
- 问"这条规则描述的是 WM 裁定某行为时怎么执行吗？"→ 是→C. 裁定执行系统
- 问"这条内容是纯数据/清单/模板吗？"→ 是→D. 参考数据表

**实施建议**：子分类标记可使用注释行（如 `<!-- A. 世界模拟机制 -->`）或空行分隔 + 小标题，不改变既有内容，仅做组织标注。可作为批次 6-1（P1-16 迁移）的附加工作一并实施。

---

**历史版本**：完整变更历史见 Git 提交记录（v1.0 - v1.23 全部在 git log 中可查）。

**v1.20 实验性分支**：近五日主要事件机制优化（方案 C）
- 术语变更："重大事件" → "主要事件"
- 提取方式：LLM 语义提取该日的主要事件（参考类别，包括但不限于）
- 参考类别：人员变动 / 据点变动 / 阵营变动 / 关系变动 / 区域变动
- 关系变动优化：建立 / 加深 / 特殊关系 / 变淡 / 破裂
- 放弃经济变动（价格暴涨暴跌 / 黑市形成）
- 放弃路线变动（封锁 / 开通）
- 事件摘要限 150-500 字符/条（原 300 字符）
- 按 D 升序排列（从最早到最近）
- 涉及文件：0-1 / 2-1 / 2-2 / 2-3 / ROADMAP

**v1.21 实验性分支**：时间机制重构（WM 为唯一时间源）
- 问题：日内时间由 WM 在主要状态栏随意输出 + 多次日期漂移
- 方案：WM 作为唯一时间源，每轮由 WM 判断并推进时间
- WSK 不再验证时间字段（Day/Turn/时间/Month/Season）
- 删除 Official Day 冲突 REJECT 规则
- WSK 仅记录 WM 决定的时间，不推算、不判定冲突
- 涉及文件：0-1 / 1-1 / 2-1 / 2-2 / 2-3 / ROADMAP

**v1.22 实验性分支**：时间推进规则完善 + Season 强制查表 + 0-2 矛盾修复
- 新增 Turn 推进规则：WM 根据本轮内容判断耗时（移动/行动/对话）
- 新增 Day 推进规则：WM 根据上下文判断（用户明确/睡眠后/超时/语义兜底）
- 新增 Season/气温强制查表：WM 必须按 Day 查 0-2 对应表
- 修复 0-2 三处矛盾：删除"由 WSK 落盘/校验"措辞
- 涉及文件：0-1 / 0-2 / ROADMAP

**v1.23 实验性分支**：移动时间压缩问题修复
- 问题：WM 把跨子区域移动压缩为单 Turn，时间戳只推进 30-40 分钟（违反 1 step = 30 分钟基准）
- 修复 #1：0-1 §Turn 推进规则强化——Turn 编号每轮 +1（与时间独立），时间戳必须按累计 step × 30 分钟累加，不得压缩
- 修复 #2：1-1 §首轮执行捷径 新增移动硬约束——跨子区域移动必须显式写 Route + 总 step + 总耗时；提供正反例（T178/10:36 → T179/12:06 正确 vs T179/11:12 违规）
- 删除修复 #3（Turn 与时间绑定）和 #4（WSK 校验时间）：前者混淆 Turn 编号与物理量；后者违反 WM 唯一时间源原则
- 涉及文件：0-1 / 1-1 / ROADMAP

**v1.24 实验性分支**：[主要状态]必出规则冗余精简（变体 A）
- 问题：同一规则在 4 个文件重复，LLM 对重复强调产生注意力疲劳
- 删除 1-2 L26 §自动通讯规则 0c. 整段（与 0-1 / 1-1 完全重复）
- 保留 0-1（主权威定义）+ 1-1（WM Scene 必读）
- 2-1 保留（数据源引用，非规则重复）
- LLM 加载顺序：1-1 Scene → 0-1 Private Details → 仍可获得完整规则
- 涉及文件：1-2 / ROADMAP

**v1.25 实验性分支**：移动路由地理准确性硬约束
- 问题：WM 跨子区域移动时凭印象虚构 Route，跳过相邻区（如跳过东南区直达东区 E）
- 新增 1-1 §移动路由地理准确性硬约束：跨子区域移动必须严格按 `0-2 §分区邻接` 拓扑书写
- 必须经过目标点所在子区域（如码头调度楼在 东南区/C，Route 必须经过东南区 N → C）
- 提供正反例（以 D18-T176 输出为案例）
- 涉及文件：1-1 / ROADMAP

**v1.26 实验性分支**：Turn 编号规则补强
- 问题：Turn 编号规则定义模糊（仅"每轮 +1"），缺少每日重置、跳号限制等细节
- 0-1 §Turn 推进规则 补充 3 条硬约束：
  - 每日从 T1 开始计数（Day 推进时 Turn 重置为 T1）
  - 每轮 +1 不可跳号（不可从 T5 直接到 T7）
  - 单轮只 +1 Turn（无论本轮内容多复杂）
- 涉及文件：0-1 / ROADMAP

**v1.27 实验性分支**：WSK 输出设计（变化子段 + 完整视图）
- 设计：用户每次点击 WSK，输出 = 第一行 + 变化子段（Inventory Delta / Recent Changes / 近五日主要事件等）+ 完整视图（Inventory Snapshot / Party Condition / Relationship / Base Structure / 近五日主要事件 / Equipment / Faction / Contamination / Base Registry / Trade Obligation）
- 无需区分默认视图/完整视图模式，简化心智
- 完整视图字段集：10 个（覆盖全部世界状态）
- 涉及文件：0-1 / 2-1 / 2-3 / ROADMAP

**v1.28 实验性分支**：WSK 输出结构去重（13 字段全集版）
- 问题：v1.27 仍有 4 处字段重复（Inventory Snapshot / 近五日主要事件 / Base Structure 等在变化子段 + 完整视图都出现），并遗漏 3 个字段（Human Contact Status / Scavenging Status Snapshot / Survival Anchor Snapshot）
- 修复：变化子段仅保留 2 个（Inventory Delta / Recent Changes）；完整视图扩展为 13 字段全集（新增 3 个遗漏 + 完整 Base Structure Snapshot）
- 优势：完全消除重复，覆盖全部世界状态
- 涉及文件：0-1 / 2-1 / 2-3 / ROADMAP

**v1.29 实验性分支**：WSK 完整视图字段游戏性优化
- 新增 Map Knowledge（已探索子区域 + 已知资源点）—— 避免玩家/WSK 重复探索
- 调整字段顺序：近五日主要事件排到最后（便于 WM 优先读取状态后回看历史）
- 不引入 Active Quest / Player Knowledge（用户决定不添加）
- 完整视图字段：13 个（新增 Map Knowledge 后仍为 13 个，因为单一替换近五日主要事件位置）
- 涉及文件：0-1 / 2-1 / 2-3 / ROADMAP

**v1.30 实验性分支**：WSK 完整视图字段游戏性最终版
- 删除 Contamination 字段（前期低频，玩家可用 Recent Changes 描述污染事件）
- 保留 11 字段：Inventory Snapshot / Party Condition / Relationship / Faction / Human Contact Status / Map Knowledge / Trade Obligation / Survival Anchor Snapshot / Base Structure Snapshot / Scavenging Status Snapshot / 近五日主要事件
- 涉及文件：0-1 / 2-1 / 2-2 / 2-3 / ROADMAP

**v1.31 实验性分支**：Inventory Snapshot 输出策略调整（分类 + 明细）
- 旧规则（v1.5+）：超过 10 项必压缩，普通物资用"具备×1组"标记 → 精度损失
- 新规则：按功能组归类 + 保留每件物品明细（口径/数量/状态）
- 超过 10 项时按功能组归类，但同组内仍列出具体物品
- 唯一仍可压缩：普通物资（绳索、容器等）以"具备×1组"标记
- 涉及文件：2-3 / ROADMAP

**v1.32 实验性分支**：可消耗物资双轨制计量规范
- 问题：可消耗物资（食物/饮水/燃料/药品）计量单位不统一，WM 用"一袋""半桶"等容器描述渲染，WSK 无法精确记录
- 方案：WM 渲染采用"容器描述 + 估算重量（≈kg）"双轨格式；WSK 提取净重值，按 kg 统一记录
- LLM 实时估算优先：不依赖预设换算表；提供校准锚点仅作常识参考（非强制）
- 新增规则：0-2 §可消耗物资渲染规范 v1.32（双轨制定义 + 校准锚点 + 混合/非标准单位处理）
- 新增硬约束：1-1 WM 渲染硬约束（不得仅写容器单位或仅写重量）
- 新增硬约束：2-1 WSK 记录硬约束（必须 kg、不得记录容器类型、不得使用非 kg 单位）
- 更新库存规则：2-3 可消耗物资 kg 记录规则（同物资合并 kg、来源明细仅存 Delta）
- 涉及文件：0-2 / 1-1 / 2-1 / 2-3 / ROADMAP

**v1.33 实验性分支**：WSK 输出增强 — 消耗投影 + Active Concerns
- 问题：WM 读取 State Update 后需自行计算资源剩余天数、扫描多个字段拼凑威胁全貌
- 消耗投影：Inventory Snapshot 末尾新增资源剩余天数估算（食物/饮水/燃料，kg→天数自动换算），供 WM 快速判断生存紧迫度
- Active Concerns：新增紧急关注点摘要（变化子段之后、完整视图之前），WSK 自动汇总 3-5 项 `[生存]/[人际]/[环境]/[据点]` 分类的关注点
- 新增规则：2-3 §消耗投影 v1.33（估算格式/基准/冬季加倍）
- 新增字段：Active Concerns（v1.33，桥接变化子段与完整视图）
- 更新输出结构：0-1 / 2-1 / 2-3 三文件同步
- 涉及文件：0-1 / 2-1 / 2-3 / ROADMAP

---

## 九、关联文档

- `PROJECT_ANALYSIS.md`：SSOT 项目设计文档（含 §13 审计结果）
- `OURDREAM_PLATFORM_REFERENCE.md`：OurDream 平台机制参考
- `The Unnecessarily In-Depth OurDream Character Creation Guide.md`：评估来源文档
- `Prompt_File\0-1 / 0-2`：全局硬规则 + 场景字段
- `Prompt_File\1-1 / 1-2 / 1-3`：WM 角色卡
- `Prompt_File\2-1 / 2-2 / 2-3`：WSK 角色卡

---

## 关键决策（当前 v1.31）

- **WEK 一致性原则**：DO/REJECT/SILENT 三表 + 跨文件引用保持一致
- **机制裁剪原则**：仅当机制实际无效时才删除（如不可压缩锚点、压缩机制）
- **Pinned Memory 持久化**：由用户手动复制 `## 新增` 段
- **历史版本**：完整变更历史见 Git 提交记录

**完整决策记录**（v1.0 - v1.31）见 git log + commit message。如需追溯特定决策，可用 `git log --grep="<关键词>"`。