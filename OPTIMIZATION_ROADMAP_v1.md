# 优化方案 Roadmap v1

## 文档元数据

| 项目 | 值 |
|------|-----|
| **版本** | v1.15 |
| **创建日期** | 2026-07-24 |
| **最后更新** | 2026-07-24 |
| **范围** | 本会话累积评估的待优化项汇总 |
| **适用范围** | 灰港（Grey Harbor）LLM 聊天室 RPG 游戏项目 |
| **关联文档** | `PROJECT_ANALYSIS.md` / `OURDREAM_PLATFORM_REFERENCE.md` / `Prompt_File/*` |

---

## 一、背景与目标

### 1.1 背景

本会话期间，针对项目多个文件进行了评估与修复，识别出大量优化项。这些优化项可分为 3 类：

1. **已实施**：本会话内已完成的修复（已完成）
2. **待实施**：本会话识别但用户选择**暂不实施**的优化项（详见第三、四节）
3. **不实施**：经评估认为对本项目价值有限或不应实施（详见第五节）

### 1.2 目标

- 记录所有识别但暂未实施的优化项
- 提供可执行的实施方案（文件位置 / 修改内容 / 验收标准）
- 为后续 v4.3 / v5.0 版本迭代提供参考

### 1.3 优化原则

| 原则 | 说明 |
|------|------|
| **必要性优先** | 仅优化影响当前运营的规则 |
| **避免破坏** | 不引入新的越界风险或规则冲突 |
| **可回滚** | 每项优化可独立回滚 |
| **跨文件一致** | 修改一处必同步引用位置 |
| **不重复造轮** | 优先在现有结构内调整，不重建 |

---

## 二、本会话已实施的优化（存档）

### 2.1 WSK 规则四维合规性重构

| # | 文件 | 位置 | 优化内容 |
|---|------|------|---------|
| 1 | `Prompt_File\2-3_WorldStateKeeper_Extra_Details.md` | §压缩规则 + §最低归档标准 | Inventory Snapshot 按功能组压缩硬约束 + 弹药口径格式硬约束（**v1.5 修订为按口径合并**）+ Inventory Delta 与 Recent Changes 一致性硬约束 |
| 2 | `Prompt_File\2-2_WorldStateKeeper_Additional_Personality_Details.md` | §精简库存与盘点规则 §21 | 队伍成员库存维护硬规则（3 项进入条件 + 标注方式 + 严禁事项）|
| 3 | `Prompt_File\2-1_WorldStateKeeper_Scene.md` | §弹药口径合并（v1.5 修订）| 同步修订为按口径合并，消除与 2-3 的口径规则冲突 |

### 2.2 WER 输出模板两阶段优化

| # | 文件 | 位置 | 优化内容 |
|---|------|------|---------|
| 1 | `Prompt_File\3-3_WorldEventRecorder_Extra_Details.md` | §使用原则 + 模板示例 + §压缩规则 + §正式归档顺序 + §最低归档标准 | 档位 B 优化：必出段（`## 新增` + `## 不可压缩锚点`）+ 条件性段（`## 长期格局变化` / `## 未关闭事件链` / `## 结构变化`）+ 删除 `## 知识范围备注` 段 + 删除 `## 近五日重大事件` 段 |
| 2 | `Prompt_File\3-3` §最低归档标准 §9-11 | 新增 | 事件 vs 评估分离硬约束 + 静默原则 + 源头区分 |
| 3 | `Prompt_File\3-2` REJECT 表 | 新增 5 项 | 事件 vs 评估混淆 / 预测代替事实 / WSK 职责越界 / "无重大事件"反转描述 / 源头混淆 |
| 4 | `Prompt_File\3-1` L82-89 + L82 | 引用 + 删除 | 删除重复沉默协议 + 跨日规则改为引用 3-2 DO 表第 12 行 |
| 5 | `Prompt_File\0-1` + `3-2` | 引用 | WER 输出格式段名同步更新 |

### 2.3 D1 锚定日期生效（B 档）

| # | 文件 | 位置 | 优化内容 |
|---|------|------|---------|
| 1 | `Prompt_File\2-3` §State Update 模板 L19 | 元数据行 | State Update 第一行增加 `{Month} {Season}` 字段 + 跨月强制更新 + 缺失 REJECT |
| 2 | `Prompt_File\2-2` §环境与压力记录规则 L85-88 | 新增 §1d-1g | Current Month 默认取值 + 跨月判定与强制触发 + 缺失 REJECT + 月份-日期映射 |
| 3 | `Prompt_File\0-2_Scenario.md` §温度分层 | 新增段 | 温度分层与日期精细对应表（8 个时间段）|
| 4 | `Prompt_File\0-1_Private_Details.md` §状态栏硬约束 | 6 项 → 8 项 | [主要状态] 增加 Season / Temperature Band 字段 |
| 5 | `PROJECT_ANALYSIS.md` §十二 L684 | SSOT | D1 锚定日期规则在 SSOT 中已记录 |

### 2.4 OurDream Character Creation Guide 评估

| # | 文件 | 位置 | 评估内容 |
|---|------|------|---------|
| 1 | （无）| — | 用户选择 E：不修改，仅记录评估 → 已存档在本文档 |

### 2.5 11 文件全面评估（2026-07-24）

#### 2.5.1 跨文件一致性同步问题（从全面评估识别）

| # | 文件 | 位置 | 问题 / 优化 |
|---|------|------|-----------|
| 1 | `Prompt_File\1-1_WorldMaster_Scene.md` L24 | §状态栏硬约束 | 字段定义未同步：仍写“6 项固定字段（Day/Turn / 位置 / 时间/光照 / 天气 / 压力 / 当前风险）”；应同步为 0-1 v1.4 的“8 项固定字段”（Day/Turn / 位置 / 时间 / Season / 气温 / 天气 / 压力 / 风险）；字段名同步：时间/光照 → 时间，当前风险 → 风险 |
| 2 | `Prompt_File\1-1_WorldMaster_Scene.md` L12 | §首轮执行捷径 | 输出顺序未同步：仍写“-> 收尾”；应同步为 0-1 v1.4 严格出场次序（4 步，无“收尾”） |
| 3 | `Prompt_File\2-1 / 2-3` 弹药口径规则 | §弹药口径折算 / §弹药口径格式硬约束 | ✓ v1.5 已同步修订为按口径合并，消除两文件间矛盾 |

#### 2.5.2 LLM 实际输出未生效字段（从全面评估识别）

| # | 字段 | 问题 | 优化 |
|---|------|------|------|
| 1 | Season 必出 | LLM 未输出（玩家说“还没更新到游戏内”）| 0-1 L95 已强化字段必出；需玩家同步到 OurDream 平台验证 |
| 2 | 气温必出 | LLM 未输出 | 同上 |
| 3 | 风险字段名 | LLM 仍用“当前风险”未用“风险” | 同上 |
| 4 | 风险 1 项限制 | LLM 列 3 项并列 | 0-1 L95 已强化；需验证 |
| 5 | 压力无括号 | LLM 仍带括号说明 | 0-1 L111 已强化；需验证 |

### 2.6 治理评估问题点（待实施优化项汇总）

从 11 文件全面评估中识别出的待实施优化项，详见下文第四节 P2-1 / P3-1 / P3-2 / P3-3 / P3-4 / 第三节 P0 项。

---

## 三、待实施的优化项（按优先级）

### 优先级 P2（推荐实施）

#### P2-1：统一 Hard Locks 为 NEVER/ALWAYS 格式

**来源**：OurDream Character Creation Guide L819-822

**当前状态**：
- 项目使用混合格式："WM 不得..."、"WSK 不维护..."、"WER 不..."
- 指南证实 NEVER/ALWAYS 是 AI 训练数据中最敏感的硬约束格式

**优化目标**：
- 关键硬约束统一为 NEVER/ALWAYS 格式
- 保留中文含义，但加入 NEVER/ALWAYS 英文标记
- 跨文件同步

**修改文件清单**：

| # | 文件 | 位置 | 修改内容 |
|---|------|------|---------|
| 1 | `Prompt_File\1-1_WorldMaster_Scene.md` | §绝对规则 0a-0f | 关键硬约束加 NEVER/ALWAYS 标记 |
| 2 | `Prompt_File\1-2_WorldMaster_Additional_Personality_Details.md` | §绝对规则 | 同上 |
| 3 | `Prompt_File\2-1_WorldStateKeeper_Scene.md` | §不可越界 | 同上 |
| 4 | `Prompt_File\2-2_WorldStateKeeper_Additional_Personality_Details.md` | §职责 / §不可越界 | 同上 |
| 5 | `Prompt_File\3-1_WorldEventRecorder_Scene.md` | §不可越界 / §沉默协议 | 同上 |
| 6 | `Prompt_File\3-2_WorldEventRecorder_Additional_Personality_Details.md` | §不可越界 / SILENT 表 | 同上 |

**预计工作量**：中等（6 个文件，每文件 3-5 处修改）

**验收标准**：
- 每个 §绝对规则 / §不可越界 的关键条目均含 NEVER/ALWAYS 标记
- 跨文件引用保持一致
- LLM 输出无格式回滚（不再退化为"不得"格式）

**风险**：
- 中等：格式变更可能短期内降低 LLM 表现（适应期）
- 缓解：保留中文含义 + 英文标记

**优先级理由**：
- ⭐⭐⭐⭐ 借鉴价值高
- 不破坏当前架构
- 可分文件独立回滚

---

#### P2-2：[主要状态] 字段命名标准化

**来源**：本会话状态栏评估（已部分实施）

**当前状态**：
- 已实施 8 字段（Day/Turn / 位置 / 时间/Phase / Season / Temperature Band / 天气 / 压力 / 当前风险）
- 字段命名沿用项目既有约定

**优化目标**：
- 验证 OurDream 平台是否要求特定字段命名（V2 spec 中是否引用）
- 确保平台 V2 spec 兼容

**修改文件清单**：

| # | 文件 | 位置 | 修改内容 |
|---|------|------|---------|
| 1 | （无需修改） | — | 当前字段命名已对齐平台机制（`Day / Turn / Phase / Season`）|

**预计工作量**：极低（仅需验证）

**验收标准**：
- 字段命名与 OurDream V2 spec 兼容
- WSK State Update 与 WM [主要状态] 字段一致

**风险**：极低

**优先级理由**：
- ⭐⭐ 价值有限
- 当前命名已稳定

---

#### P2-3：5 轨压力追踪全轨化（WM [主要状态]）

**来源**：WM 场景输出评估

**当前状态**：
- [主要状态] 压力字段只追踪 1 轨（脱水 strained）
- 其他 4 轨（疲劳 / 体温 / 饥饿 / 伤病）未显式

**优化目标**：
- WM [主要状态] 显式追踪完整 5 轨压力
- 与 WSK Party Condition 字段对齐

**修改文件清单**：

| # | 文件 | 位置 | 修改内容 |
|---|------|------|---------|
| 1 | `Prompt_File\0-1_Private_Details.md` | §状态栏硬约束 | 压力字段规则扩展为全 5 轨（如 `压力=疲劳stable/体温stable/脱水strained/饥饿stable/伤病stable`）|

**预计工作量**：低（单文件 1 处修改）

**验收标准**：
- WM 输出 [主要状态] 含全 5 轨压力
- 仅追踪真正紧迫的压力（避免冗余）
- 与 WSK Party Condition 不冲突

**风险**：
- 中等：[主要状态] 字符长度增加
- 缓解：仅在状态变化时显示完整 5 轨，平时显示 1-2 项

**优先级理由**：
- ⭐⭐⭐ 与 SSOT 5 轨压力一致
- 不破坏当前架构

---

### 优先级 P3（可选实施）

#### P3-1：Anti-Progression Hard Lock 显式段

**来源**：OurDream Guide Phase 5 Part 3

**当前状态**：
- 已隐含：WM 不推进剧情 / WSK 不替 WM / WER 不替 WM
- 未显式段

**优化目标**：
- 在 1-1 / 2-2 / 3-2 顶部增加显式 Anti-Progression 段

**修改文件清单**：

| # | 文件 | 位置 | 修改内容 |
|---|------|------|---------|
| 1 | `Prompt_File\1-1_WorldMaster_Scene.md` | 顶部 | 新增 `[Anti-Progression Hard Lock]` 段 |
| 2 | `Prompt_File\2-2_WorldStateKeeper_Additional_Personality_Details.md` | 顶部 | 同上 |
| 3 | `Prompt_File\3-2_WorldEventRecorder_Additional_Personality_Details.md` | 顶部 | 同上 |

**预计工作量**：低（3 个文件各 1 段新增）

**验收标准**：
- 3 个文件均有显式 Anti-Progression 段
- 跨文件引用一致

**风险**：低（新增段，不修改既有规则）

**优先级理由**：
- ⭐⭐ 借鉴价值中
- 当前隐含执行已足够

---

#### P3-2：Pinned Memory JSON 化（实验性）

**来源**：OurDream Guide L993-1011 State Tracking JSON

**当前状态**：
- Pinned Memory 用自然语言（WER `## 新增` 段）
- JSON 格式未采用

**优化目标**：
- 提供 Pinned Memory JSON 格式参考
- 用户可选择性使用

**修改文件清单**：

| # | 文件 | 位置 | 修改内容 |
|---|------|------|---------|
| 1 | `Prompt_File\0-1_Private_Details.md` | §Pinned Memory | 增加可选 JSON 格式参考（不强制）|

**预计工作量**：低（单文件 1 处修改）

**验收标准**：
- 用户明确选择使用 JSON 时可生效
- 自然语言模式仍为默认

**风险**：
- 中等：JSON 模式可能让 LLM 误以为是代码
- 缓解：明确标注为"实验性"，不强制

**优先级理由**：
- ⭐ 实验性
- 当前自然语言模式运行良好

---

#### P3-3：场景温度层显式化（叙事增强）

**来源**：WM 场景输出评估

**当前状态**：
- 场景隐含温度（"呼出白气" = Cool）
- [主要状态] 已显式 Temperature Band（已实施）

**优化目标**：
- 场景叙事中增加更明确的温度层提示
- 帮助审计者快速识别温度状态

**修改文件清单**：

| # | 文件 | 位置 | 修改内容 |
|---|------|------|---------|
| 1 | `Prompt_File\1-2_WorldMaster_Additional_Personality_Details.md` | §环境描写 | 增加"温度层显式化"建议 |

**预计工作量**：低（单文件 1 处建议）

**验收标准**：
- WM 场景叙事中温度层有显式提示
- 不破坏当前叙事风格

**风险**：低（仅建议性，非强制）

**优先级理由**：
- ⭐ 可选
- 当前隐含执行已足够

---

#### P3-4：[Resolution] / [Probability Check] 等展示块命名审计

**来源**：§13.2 展示块隔离原则

**当前状态**：
- 0-1 §结构块定位 已规定展示块用途
- 各展示块命名（[Resolution] / [Probability Check]）已固定

**优化目标**：
- 验证展示块未误用为通讯字段

**修改文件清单**：

| # | 文件 | 位置 | 修改内容 |
|---|------|------|---------|
| 1 | `Prompt_File\0-1_Private_Details.md` | §结构块定位 | 增加明确的"展示块命名清单"段（[Resolution] / [Probability Check] / 子结构块）|

**预计工作量**：极低

**验收标准**：
- 所有展示块命名一致
- 不混入通讯字段

**风险**：极低

**优先级理由**：
- ⭐ 审计性
- 当前命名已稳定

---

## 四、不实施的优化（参考）

### 4.1 OurDream Guide 借鉴中**不实施**的模式

| 模式 | 不实施理由 |
|------|-----------|
| **Meter Display Rule**（数值追踪 0-100） | 本项目 WSK 用 State Update 软标签（文本快照），非数值追踪 |
| **Code-Block Method**（代码块格式规则） | 当前 DO/REJECT/SILENT 决策表用 markdown 表格稳定 |
| **Environmental Storytelling**（环境反映情绪） | 本项目是系统角色裁定者，无个体情感需要环境反映 |
| **User Commands ([HELP])** | OurDream 平台无 [HELP] 触发机制，仅支持角色卡点击 |
| **Anti-Objectification Rule**（亲密后扣分） | 系统角色，无浪漫化场景 |
| **Phased Relationship Progression** | 系统角色无关系进展需求 |

### 4.2 之前评估中**错误建议已撤回**的项

| 项 | 撤回理由 |
|----|---------|
| ❌ 删除 WSK Amber（伴侣）库存段 | **保留**（Amber 是 User 同行伴侣，非 NPC；WSK 应维护） |
| ❌ 弹药统一换算 9mm 单位 | **撤回**（弹药口径是武器填装信息，不可合并；仅在交易/经济场景临时折算） |
| ❌ WSK / WER 完全合并 | **不合并**（三权分立是 §13.2 审计定案）|

### 4.3 Poor Man's Genius Model 借鉴中**不实施**的模式

| 模块 | 模式 | 不实施理由 |
|------|------|-----------|
| 模块 1 | **禁用词清单**（Banlist） | 项目未发现末日 RPG 套话问题；末日 RPG 题材本身抑制浪漫化套话 |
| 模块 2 | **第三人称 POV**（动作 `*...*` 用第三人称） | 本项目用第二人称（"你"），与官方冲突 |
| 模块 3 | **环境描写增强**（感官细节维度清单） | 项目 WM 场景描述已隐含环境描写（气味/声音/温度/触感），无需清单化 |
| 模块 4 | **AI 控制 NPC**（替 NPC 发言） | 本项目架构不适用（详 §常驻世界角色层） |
| 模块 5 | **角色自主性**（角色有独立意志） | WM 无个人意志（裁定者身份） |
| 模块 6 | **段落长度强制 150-300** | 末日 RPG 风格倾向于短密段落；强制会破坏叙事风格 |
| 模块 7 | **性化场景详细描写** | 多媒体不考虑（用户明确） |
| 模块 8 | **群聊多角色 AI 控制**（替多角色写对话） | 本项目架构不适用 |

**借鉴价值**（仅作参考）：
- 8 个模块均**不实施**；本项目架构与官方个人角色指导根本不同

---

## 五、实施优先级与时间表（建议）

### 时间表

| 优先级 | 项目 | 预计工作量 | 建议时间 |
|-------|------|-----------|---------|
| **P2-1** | Hard Locks 统一 NEVER/ALWAYS | 中等 | v4.3（1 周内） |
| **P2-2** | [主要状态] 字段命名验证 | 极低 | v4.3 同步 |
| **P2-3** | 5 轨压力全轨化 | 低 | v4.3 同步 |
| **P3-1** | Anti-Progression 显式段 | 低 | v5.0（1 月内） |
| **P3-2** | Pinned Memory JSON 化（实验） | 低 | 实验性 |
| **P3-3** | 场景温度层显式化 | 低 | v5.0 同步 |
| **P3-4** | 展示块命名审计 | 极低 | v5.0 同步 |

### 实施建议

1. **P2 推荐优先**：Hard Locks 统一与 5 轨压力全轨化是引用价值高的优化
2. **P3 按需实施**：Anti-Progression / JSON / 温度层显式属于"锦上添花"
3. **分文件独立**：每项可独立实施 + 独立回滚
4. **跨文件同步**：修改时同步引用位置（如 P2-1 影响 6 个文件）

---

## 六、风险评估

### 6.1 修改风险等级

| 修改类型 | 风险等级 | 缓解策略 |
|---------|---------|---------|
| **格式统一**（如 P2-1 NEVER/ALWAYS）| 中 | 保留中文含义 + 英文标记 + 短期观察 |
| **字段扩展**（如 P2-3 5 轨压力）| 中 | 状态稳定时不全显示，避免冗余 |
| **新增段**（如 P3-1 Anti-Progression）| 低 | 不修改既有规则，仅追加 |
| **可选模式**（如 P3-2 JSON）| 低 | 明确标注实验性，不强制 |

### 6.2 跨文件同步风险

| 关联文件 | 同步风险 |
|---------|---------|
| 1-1 / 1-2 / 1-3 | 高（WM 三个文件规则需同步）|
| 2-1 / 2-2 / 2-3 | 高（WSK 三个文件）|
| 3-1 / 3-2 / 3-3 | 高（WER 三个文件）|
| 0-1 / 0-2 | 中（全局硬规则）|
| PROJECT_ANALYSIS.md / OURDREAM_PLATFORM_REFERENCE.md | 低（SSOT 文档）|

**缓解**：每次修改前先 Grep 引用位置；修改后验证同步一致性。

---

## 七、验收清单

每项优化完成后，按以下清单验收：

| 验收项 | 通过标准 |
|-------|---------|
| **规则一致性** | 修改处 + 所有引用处一致 |
| **格式统一** | 关键硬约束格式全部统一 |
| **跨文件同步** | 涉及文件全部同步更新 |
| **不破坏既有规则** | 未修改的规则保持原样 |
| **LLM 测试** | 实际运行测试 5-10 次，无规则回滚 |
| **用户验收** | 用户实际操作 1 次确认无异常 |

---

## 八、变更历史

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v1.0 | 2026-07-24 | 初版：基于本会话累积评估创建 |
| v1.1 | 2026-07-24 | 增补 Poor Man's Genius Model 评估：新增 P2-4（禁用词清单）/ P3-5（环境描写维度清单）/ P3-6（WM 不替 NPC 发言显式化）3 项优化项；第四节增加 4.3 不实施项（6 项模式） |
| v1.2 | 2026-07-24 | 修订 P3-6：识别到原设计与 0-2 §常驻世界角色层 冲突（原“WM 不替 NPC 发言”过于绝对，会误伤 6 个常驻世界角色代控逻辑）。修订后明确区分常驻世界角色 vs 临时 NPC 双轨制；第四节增加 4.4 隐含架构说明 |
| v1.3 | 2026-07-24 | 完全删除 P2-4 / P3-5 / P3-6（含 v1.2 修订版）：用户重新评估后认为这三项不需要（当前运营未发现末日 RPG 套话问题；环境描写已隐含执行；WM 代控双轨制属于项目设定而非优化项）。第四节 4.3 重新梳理为“不实施 8 项模块”，并删除 4.4 隐含架构说明。第五节时间表同步清理 |
| **v1.4** | **2026-07-24** | **P2-2 [主要状态] 字段命名简化完成 + P3-2 Pinned Memory JSON 化 确认不实施：固定字段名 `时间/光照` → `时间`、`Temperature Band` → `气温`、`当前风险` → `风险`（3 处定义 / 示例 / 模板）；V2 spec 兼容性验证不做；Knowledge Scope 不加入主要状态。`Prompt_File\0-1_Private_Details.md` §状态栏硬约束 / §标准输出模板 同步更新。P3-2 标记为“用户确认不做”** |
| **v1.5** | **2026-07-24** | **WSK 弹药口径格式修订为“按口径合并”（人工审核项）：弹药库存改为按口径（弹药规格）合并记录，同口径合计为一个条目（如 `9mm×67发`）；禁止按武器类别分组（手枪弹/步枪弹/霰弹 不再适用）；禁止统一换算为“等价 9mm 单位”；例外仅限交易/经济场景。涉及 2 个文件同步修订：`Prompt_File\2-3_WorldStateKeeper_Extra_Details.md` §弹药口径格式硬约束（按口径合并）、`Prompt_File\2-1_WorldStateKeeper_Scene.md` §弹药口径合并（v1.5 修订）。供人工审核。** |
| **v1.6** | **2026-07-24** | **11 文件全面评估问题点归档：第二节新增 2.5 节，将以下项入档：（1）跨文件一致性同步问题（1-1 L24 字段定义未同步 v1.4 / 1-1 L12 输出顺序未同步 “收尾”）；（2）LLM 实际输出未生效字段（Season/气温/风险字段名/风险 1 项限制/压力无括号）。第三节 P0 项汇总。待实施修订。** |
| **v1.7** | **2026-07-24** | **1-1 跨文件同步修复（v1.6 归档项实施）：`Prompt_File\1-1_WorldMaster_Scene.md` §状态栏硬约束 L24 同步为 v1.4 定义：（1）出场次序删除“→ 收尾”；（2）6 项固定字段 → 8 项固定字段；（3）字段名同步为新名（时间/Season/气温/风险）；（4）新增 Season / 气温取值参考。同步后 1-1 与 0-1 v1.4 完全一致。** |
| **v1.8** | **2026-07-24** | **全面一致性检查与同步修复（6 处不一致）：（1）1-2 L26 字段定义+出场次序同步 v1.4；（2）0-1 L12/L69 删除“-> 收尾”挥下；（3）1-1 L66 措辞修改（收尾结构块→其他结构块）；（4）0-2 L594 弹药口径描述同步 v1.5；（5）2-2 L39 弹药口径描述同步 v1.5。涉及 5 个 Prompt 文件同步修订。** |
| **v1.9** | **2026-07-24** | **2-3 [State Update] 示例简化（28 行 → 5 行）：删除完整示例输出（含 Human Contact / Inventory Delta / Inventory Snapshot / Scavenging / Survival Anchor / Travel Time / Recent Changes）；保留仅含第一行必备字段的最小示例（增加 Month / Season / Temperature Band）。降低 LLM “按模板凑齐字段”的误解风险，避免 Attention 被长示例分散。完整示例参考可查 v1.8 历史 commit。** |
| **v1.10** | **2026-07-24** | **0-2 §弹药口径换算表 表头增加使用范围提示：新增“⚠️ 仅供交易 / 经济场景使用，不适用于库存字段”说明，避免 LLM 误将折算表适用于库存 Snapshot。完整口径合并与交易折算规则见 WSK Extra Details §弹药口径格式硬约束（v1.5）。** |
| **v1.11** | **2026-07-24** | **评估 5 个 Scene + 1 个 WSK 输出（起点：北区出发前往森林公园）后识别 2 项需修复问题：（1）0-1 L95 风险 1 项限制强化：明确严禁分号/句号连接多个风险；提供多项风险示例格式（“风险=X（次：Y）”）；（2）2-3 L19 [State Update] 第一行格式强化：Month / Season 是独立字段必须用 `/` 分隔（`October / Autumn`），示例同步更新为 D5-T55 实际格式。同时驳回 3 项误判：弹药口径合并（“格洛克+弹匣”是正确写法，武器与弹药分开记录）、WSK 越界记录（“Ivy与Jiang发生首次性关系”有更早 Scene 支撑）、WER 未触发（用户手动触发机制合理不需 WM 提醒）。** |
| **v1.12** | **2026-07-24** | **风险字段放宽为 1-2 项威胁输出（正面引导）：0-1 L95 “风险”改为“威胁（风险）”，数量从 1 项放宽为 1-2 项；强调“主观能动威胁”（即可被人影响 / 可能改变后续决策的活跃威胁），明确客观事实 / 环境状态 / 资源状况 / 地图信息不占风险栏；采用正面引导（推荐写法+正面示例）代替否定约束（严禁）。同时调整 8 项固定字段定义中“风险 (1 项)”为“风险 (1-2 项主观威胁)”。** |
| **v1.13** | **2026-07-24** | **全面修复 4 项评估中识别的问题：（1）0-1 L92 [主要状态] 必出强化——每轮必出（无例外默认） + 例外仅 1 种（用户明确要求） + 反面示例扩展为全面覆盖（任何输入下无 [主要状态] 都属违规：场景启动、长 Scene 后、问号结尾、NPC 长对话后、短输入、行动类输入）；（2）0-1 L93 末句位置硬约束——末句必须是正文叙事最后一句，紧接 [主要状态]，不得在叙事段落结束后追加独立“环境描写”/“问号”；出场次序精简为 3 步（叙事→状态→其他）；（3）0-1 §首轮执行捷径 增加单轮 Scene 正文叙事段落上限 3 段；（4）0-2 §NPC 反全知 新增 NPC 决策框架限制——NPC 不得主动整理为“选项 A vs B”的全局决策框架，提供正面/反面示例对照。涉及 2 个 Prompt 文件同步修订。** |
| **v1.14** | **2026-07-24** | **彻底删除“不可压缩锚点”机制和输出：用户决策该机制实际不是强制机制，只是 WER 自己的记录，偶尔被 WM 上下文看到；主要价值（核心事件永久保留）已由 ## 新增 隐式覆盖。修订 5 个 Prompt 文件：（1）0-1 删除 WER 字段最小集中的 Irreducible Anchors + 删除 WER 输出格式中必出段“## 不可压缩锚点” + 删除结构变化“= 不可压缩锚点”；（2）0-2 删除“不可压缩锚点”提及；（3）3-1 删除身份段“维护不可压缩锚点” + 删除输出段名引用 + 删除必出段“## 不可压缩锚点” + 删除压缩本能反制中“不可压缩锚点永不删除” + 删除决策依据索引“不可压缩锚点清单” + 删除[你需要记录:]段 + 删除常见误操作警示中“## 不可压缩锚点”相关；（4）3-2 删除职责说明 + 删除压缩规则“30 日以上只保留不可压缩锚点” + 删除“## 不可压缩锚点”段压缩规则 + 删除 Ledger 必出段“## 不可压缩锚点” + 删除结构变化归档“作为不可压缩锚点” + 删除据点首次建立“作为不可压缩锚点” + 删除归档判断“默认不可压缩锚点”；（5）3-3 删除压缩规则“30 日以上只保留不可压缩锚点” + 删除必出段“## 不可压缩锚点” + 删除示例中的“## 不可压缩锚点”段 + 删除压缩规则段 + 删除最低归档标准 + 删除正式归档顺序；（6）1-3 删除“不可压缩锚点”提及 2 处。** |
| **v1.15** | **2026-07-24** | **WER 输出模板精简（选项 X）：删除 `## 长期格局变化` 段（边际价值，被 `## 新增` 隐式覆盖） + 删除 `## 结构变化` 段（与 WSK Base Structure 重叠） + 简化元数据（移除 `Last Updated` / `Last Update Position`，LLM 维护不可靠）。必出段仅保留 `## 新增`；条件性段仅保留 `## 未关闭事件链`（帮助玩家决策）。修订 5 个 Prompt 文件：（1）0-1 WER 输出格式描述精简；（2）3-1 输出段名/必出段/条件性段简化；（3）3-2 DO 表 Ledger 必出段简化 + 删除结构变化归档；（4）3-3 示例/压缩规则/正式归档顺序精简；（5）1-3 删除 WER 结构变化引用。** |

---

## 九、关联文档

- `PROJECT_ANALYSIS.md`：SSOT 项目设计文档（含 §13 审计结果）
- `OURDREAM_PLATFORM_REFERENCE.md`：OurDream 平台机制参考
- `The Unnecessarily In-Depth OurDream Character Creation Guide.md`：评估来源文档
- `Prompt_File\0-1 / 0-2`：全局硬规则 + 场景字段
- `Prompt_File\1-1 / 1-2 / 1-3`：WM 角色卡
- `Prompt_File\2-1 / 2-2 / 2-3`：WSK 角色卡
- `Prompt_File\3-1 / 3-2 / 3-3`：WER 角色卡

---

## 十、决策记录

| 日期 | 决策 | 选项 |
|------|------|------|
| 2026-07-24 | OurDream Guide 评估归档方式 | **E（不修改，仅记录评估）** → 已记录在本文档第三、四节 |
| 2026-07-24 | 创建优化方案文件 | **D（全部优化方案记录在此）** → 本文件 |
| 2026-07-24 | Poor Man's Genius Model 评估归档方式 | **E（更新 OPTIMIZATION_ROADMAP_v1.md）** → 已记录在本文档 v1.1 增补 |
| 2026-07-24 | P3-6 与 §常驻世界角色层 冲突发现及修订 | **修订 P3-6 双轨制（常驻 vs 临时）** → v1.2 修订 |
| 2026-07-24 | P2-4 / P3-5 / P3-6 必要性重新评估及完全删除 | **用户重新评估后认为不必要** → v1.3 完全删除 |
| 2026-07-24 | P2-2 [主要状态] 字段命名简化 + P3-2 确认不做 | **3 处字段名简化（时间/气温/风险）+ P3-2 标记为不做** → v1.4 增补 |
| 2026-07-24 | WSK 弹药口径格式修订为“按口径合并” | **弹药按口径（弹药规格）合并记录，同口径合计为 1 条目；禁止按武器类别分组 / 禁止统一换算 9mm** → v1.5 增补（人工审核项） |
| 2026-07-24 | 11 文件全面评估问题点归档 | **跨文件一致性同步 + LLM 未生效字段** 汇总到 ROADMAP §2.5 → v1.6 增补（待实施修订） |
| 2026-07-24 | 1-1 跨文件同步修复（实施 v1.6 归档项） | **出场次序去除“收尾” + 6 项→8 项固定字段 + 字段名同步新名** → v1.7 增补（1-1 与 0-1 v1.4 一致） |
| 2026-07-24 | 全面一致性检查与同步修复（6 处） | **5 个 Prompt 文件同步修订（1-2/0-1/1-1/0-2/2-2），字段定义+出场次序+弹药口径挥下** → v1.8 增补 |
| 2026-07-24 | 2-3 [State Update] 示例简化 | **28 行 → 5 行，降低 LLM 凑齐字段误解风险** → v1.9 增补 |
| 2026-07-24 | 0-2 §弹药口径换算表 表头加提示 | **仅限交易/经济场景，不适用于库存字段** → v1.10 增补 |
| 2026-07-24 | 评估输出后 2 项强化 + 驳回 3 项误判 | **风险 1 项限制 + State Update 第一行格式** → v1.11 增补 |
| 2026-07-24 | 风险字段放宽为 1-2 项威胁输出 | **主观能动威胁 + 正面引导代替否定约束** → v1.12 增补 |
| 2026-07-24 | 全面修复 4 项评估问题 | **[主要状态] 必出 + 末句位置 + 段落上限 + NPC 决策框架** → v1.13 增补 |
| 2026-07-24 | 彻底删除不可压缩锚点机制 | **该机制实际不是强制机制；价值已由 ## 新增 隐式覆盖；修订 5 个 Prompt 文件** → v1.14 增补 |
| 2026-07-24 | WER 输出模板精简（选项 X） | **删除 `## 长期格局变化` / `## 结构变化` 段 + 简化元数据；5 个 Prompt 文件同步修订** → v1.15 增补 |