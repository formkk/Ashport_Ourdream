# v2 讨论文档

> **日期**：2026-08-09
> **来源**：v1.55 + v1.56 执行过程中确认的 v2 排期项 + 本轮新增讨论项
> **状态**：待用户讨论

---

## 一、v2.01 遗留任务

### T1：据点消耗机制（消耗扣减 + 触发 + 优先级）

**问题**：WSK 消耗投影记录了食物/饮水/燃料剩余天数，但库存实际 kg 值不会自动扣减。当玩家说"睡觉"/"第二天"跳过时间时，消耗未被记录，库存与实际脱节。

**决议**：WM 负责消耗裁定与叙事，WSK 只提取记录。主机制 + 增强覆盖所有场景。

#### 1. 主机制：Day 推进 -> [Resolution] 消耗行（硬规则）

当 Day 推进时（无论玩家输入"睡觉"/"第二天"/"过夜"还是其他原因），WM 必须在 [Resolution] 中写一行消耗摘要：
- 格式：`消耗（D{prev}->D{curr}）: {物品×数量} / {物品×数量}` 或 `消耗: 无`
- 据点过夜：写据点消耗（食物 + 燃料）
- 野外过夜：写随身消耗（干粮/饮水）
- 两者并存：`消耗（D5->D6）: 随身 干粮×0.5kg / 据点 大米×1kg + 木柴×2kg（留守）`
- 这是硬规则（类似 [主要状态] 必含 Day），不是隐式判断

#### 2. 增强：做饭/取暖场景 -> 参照 WSK 库存 + 优先级（软指引）

当 WM 在 Scene 叙事中涉及做饭/取暖时，应参照上一轮 WSK [State Update] 中的 Inventory State（食物/燃料种类与存量），按优先级选择消耗物，在叙事中写出具体消耗。此为软指引 -- 若漏了不影响主机制兜底。

**食物优先级**（据点）：
1. **尽量用主食+肉类+蔬菜的组合**（营养均衡优先）
2. 在组合内优先消耗易腐食物（新鲜肉类/蔬菜）
3. 其次消耗腌肉/熏制
4. 再次消耗主食（大米/面粉）
5. 最后消耗罐头（耐放，留作储备）

**燃料优先级**（据点）：
1. 木柴（低价值，易获取）
2. 煤
3. 木炭
4. 煤油（高价值，留作照明/应急）

**野外远征**：吃带的什么就吃什么（远征通常携带干粮/罐头/压缩饼干，不存在优先级问题）。

**其他进食场景**（如玩家说"进食"/"吃东西"）：WSK 现有提取机制覆盖 -- WM 自然叙事"你吃了点干粮"，WSK 直接提取，不需要专门触发器。

#### 3. [Resolution] 改为每轮必出

- [Resolution] 从条件输出改为每轮必出
- 无事件时输出 `[Resolution]\n- 无`
- Day 推进时必含消耗行
- WSK 每轮都有结构化提取源

#### 4. 断粮场景

- 随身/据点食物耗尽 -> WM 写 `消耗: 无（食物已耗尽）` + 饥饿轨压力上升
- WSK 提取到"无消耗" + 消耗投影归零 + Active Concerns [生存] 标注

#### 5. 角色边界

| 角色 | 职责 |
|------|------|
| WM | 裁定消耗种类与数量；Day 推进时 [Resolution] 必含消耗行；做饭/取暖时参照 WSK 库存 + 优先级 |
| WSK | 从 WM 输出中提取消耗；更新 Inventory + 消耗投影；不做计算、不自行扣减 |

#### 6. 改动范围

| 文件 | 改动 |
|------|------|
| 1-1 Scene | [Resolution] 改为每轮必出（无事件时输出"无"）；Day 推进时必含消耗行 |
| 1-2 APD | 新增 [据点消耗规则]：主机制（Day 推进 -> Resolution 消耗行）+ 增强（做饭/取暖参照 WSK 库存 + 优先级） |
| WSK | 不改动（现有"从 WM 发言内容强语义提取"覆盖 Scene 叙事 + [Resolution]） |

**状态**：✅ 已执行（1-1 Resolution 每轮必出 + 1-3 Resolution 格式更新 + 1-2 据点消耗规则）

---

### T2：Insulation 从 WSK 账本侧移除

**问题**：v1.55 保留了 Insulation 在 WSK，但 WSK 定位 = "记会变化的状态"，Insulation 是衣物静态属性（一件冬衣保暖值不变），不属 WSK 账本。

**决议**（用户已确认）：从 WSK 移除。

**改动**：
- 2-2 L48：删除 `装备 Condition / Insulation 作为物品条目属性记录在 Inventory 内`
- 2-2 [物资状态规则]：删除 `衣物和装备应尽量记录 Insulation` + 删除 Condition 枚举（Pristine/Worn/Damaged/Badly Damaged/Ruined）
- 全库 Insulation 清除

**状态**：✅ 已执行（Insulation + Condition 全库清除）

---

### T3：库存规则梳理

**结论**：不大重构。当前结构已通过三轮验证，运行时合规。大规模条目移动风险高，收益是结构整洁而非运行时改善。

**状态**：✅ 已结题

---

### T4：Condition 全库移除 + Component 生命周期简化

**背景**：Condition 枚举（Pristine/Worn/Damaged/Badly Damaged/Ruined）原用于物品状态和据点结构节点状态。评估发现：物品 Condition 降级联动难以落地（用户确认放弃）；据点结构 Condition 使用物品枚举套用于结构不合适，且 Component 生命周期（前 Condition -> 后 Condition 跨轮追踪）LLM 不可靠执行。

**决议**：全库删除 Condition，简化 Component 生命周期。

**改动**：
- 2-2：删除物品 Condition 枚举 + 简化 Component 生命周期为"组件名 + Type + Role + 状态描述（自然语言）"+ 删除完整基线/简表双模式
- 2-3：删除结构 Condition 引用 + 简化最低提交标准
- 1-3：锚点表定义删除 Condition + Delta 格式改为"组件名 + Type + Role + 状态描述"
- 组件锚点表实际条目（"大门 = 完好+机械加固"）已是自然语言，无需改动

**状态**：✅ 已执行（Condition/Pristine/Worn/Damaged/Badly Damaged/Ruined 全库 0 残留）

---

## 二、v2.02+ 机制设计讨论项

### D1：保暖机制设计（原 Insulation）

**背景**：Insulation 已从 WSK 账本移除。现改为中文 [保暖] 机制，由 WM 负责推进与裁定。属 WM 侧机制。

**决议方向**：保暖不是独立的趋势计算系统，而是对现有 Temperature Band 的**修正**--把实际温度层上调或下调，然后照常使用现有的 [温度分层与 5 轨压力联动] 映射。

#### 1. 保暖值定义

5 档定性值，WM 根据玩家当时身上的衣服以语义裁定：

| 保暖值 | 语义 | 典型穿着 |
|--------|------|----------|
| 优 | 冬季极地装备，几乎无视寒冷 | 完整冬装（防风外层+保暖外套+毛衣或抓绒衣+保暖裤+鞋帽） |
| 良 | 正常冬衣，缓解寒冷 | 冬季外套+毛衣 |
| 中 | 普通衣物，按实际温度层 | 春秋外套+普通衣物 |
| 弱 | 衣物不足，寒冷加剧 | 普通衣物，缺厚外套 |
| 差 | 几乎裸露或湿透，极度危险 | 单衣/湿普通衣物/严重破损的普通衣物 |

#### 2. 保暖修正表

保暖值修正**有效温度层**（非数值计算，WM 语义查表）：

| 保暖 | 效果 |
|------|------|
| 优 | 有效温度层 +2 档 |
| 良 | 有效温度层 +1 档 |
| 中 | 无修正 |
| 弱 | 有效温度层 -1 档 |
| 差 | 有效温度层 -2 档 |

档位序列：`Heat > Mild > Cool > Cold > Bitter Cold`（+2 档 = 向 Heat 方向移 2 步，封顶 Heat；-2 档 = 向 Bitter Cold 方向移 2 步，封顶 Bitter Cold）

#### 3. 环境修正（与保暖叠加）

| 因素 | 效果 |
|------|------|
| 室内 | +1 档 |
| 取暖源（火/暖气） | +2 档 |
| 风/雨/雪 | -1 档 |
| 海边/山区暴露 | -1 档 |

多因素叠加时，WM 语义综合判断，不做整数加减。修正后得到有效温度层，照常查 [温度分层与 5 轨压力联动] 表。

#### 4. 示例

保暖=良，Clear，Cool，室外：
1. 实际温度层 = Cool
2. 保暖=良 -> +1 档 -> 有效 = Mild
3. 查表：Mild = "5 轨压力均不受温度推动；适合长途行动"

结论：穿正常冬衣在室外凉爽天气下行动 -> 无体温压力，适合长途。符合常识。

#### 5. 时间推进（基于 [主要状态] Time）

不做"每 30 分钟降一级"的机械计时（违反跨轮计数禁令）。改为 WM 根据 [主要状态] 中的 Time 推进来裁定压力升降：
- WM 每轮在 [主要状态] 输出当前 Time（如 10:00 -> 12:00）
- WM 对比上轮 Time 与当前 Time，得出暴露时长（如 2 小时）
- WM 结合有效温度层 + 暴露时长，语义裁定体温压力推进（如"Cold + 暴露 2 小时 + 保暖=弱 -> 体温压力升一级"）
- 这不是跨轮计数（WM 在单轮内对比两个已知时间值），而是 WM 利用已建立的时间数据进行裁定

#### 6. 游戏体验落地

WM 在 Scene 叙事中体现，不显示数字：

> 你裹紧冬衣推门出去，北风立刻灌进领口。气温大概五六度，好在衣服够厚，暂时不觉得冷，但手指已经开始发僵。

这段叙事背后：保暖=良，Cold -> 修正为 Cool，Cool 压力 = "轻微体温压力，长时间暴露后疲劳上升"。玩家感受到的是叙事，不是数字。

#### 7. 承载位置

- **保暖值定义 + 修正表**：1-3 [温度分层与 5 轨压力联动] 节内新增子节 [保暖修正]
- **WM 裁定逻辑**：1-2 相关规则（"WM 根据玩家穿着 + 天气 + 环境裁定保暖档位"）
- **WSK**：不记录保暖值（实时评估，非持久状态）；体温仍由 WSK 在 Party Condition 中记录定性状态，保暖机制驱动体温变化由 WM 裁定

#### 8. 为什么不用数值计算方案

| 维度 | 数值方案（基值+修饰符加减） | 修正方案（档位平移查表） |
|------|--------------------------|------------------------|
| 计算 | 整数加减（LLM 不可靠） | 语义查表（LLM 可执行） |
| 时间 | 每 30 分钟计数（跨轮禁令） | WM 叙事时间驱动 |
| 与现有系统 | 叠加新层（趋势+旧映射） | 修正输入（保暖改变有效温度层 -> 旧映射照常） |
| 浸入感 | 数值化（+1, -1） | 叙事化（玩家体验冷但不看数字） |

**状态**：✅ 已执行（1-3 新增 [保暖修正] 子节 + 1-2 新增 [保暖裁定] 节 + 静态测试通过）

**注**：Condition 降级联动已放弃（Condition 全库移除，见 T4）。保暖值由 WM 根据穿着语义裁定，不依赖 Condition。

---

### D2：据点损耗机制（原"据点降级状态与游戏机制联动"）

**背景**：据点和庇护所是核心生存资源，长期使用有代价，必须不断维护。损耗机制让玩家体验"世界在动"--不在据点时据点也在老化，不关注的区域也在发生变化。

**决议方向**：WM 负责损耗裁定与叙事，WSK 只提取记录。组件状态用自然语言描述追踪全生命周期，不需要枚举或数值。

#### 1. 组件状态生命周期

```
完好 -> 损耗（故障）-> 修补（恢复）-> 加固（升级）-> 更耐损耗
```

全程用自然语言状态描述，现有组件锚点表已支持：
```
大门 = 完好+机械加固（D3 初加固/D8 二次加固/D12 机械加固） | main-entry
```

#### 2. 多因素触发

Day 推进时，WM 综合考量以下因素（全部从 WSK 上一轮 State Update 读取）：

| 因素 | 来源 | 影响 |
|------|------|------|
| 天气 | [主要状态] Weather | Storm/Heavy Rain/Snow -> 物理损坏 |
| 暴露 | Base/Shelter State Security/Exposure | 高暴露 -> 闯入尝试 -> 门窗损坏 |
| 仇恨关系 | Relationship & Threat | 仇恨级关系 -> 针对性破坏 |
| 据点人数 | Base/Shelter State Occupancy | 高人数 -> 使用损耗加速 |
| 维护压力 | Base/Shelter State Maintenance Pressure | 持续高 -> 老化失效 |
| 加固状态 | 组件状态描述 | 已加固 -> 更耐损耗（WM 语义判断） |

WM 语义综合判断，不逐项打分。例如：读到"Security/Exposure = 差" + "劫掠者兄弟会 Relationship = 仇恨" + "Day 推进" -> 判断"很可能有人来搞破坏"。

#### 3. 故障产生与叙事

WM 在 Scene 叙事中写出故障（不是 [Resolution] 结构化行，是自然叙事）：

> 你回到据点，发现后门的锁被撬过了--门框上有新的凿痕，锁芯虽然没被弄开，但已经变形。天台集水器的接口也在昨天的暴风里松了，水顺着外墙渗了下来。

#### 4. WSK 记录

WSK 从叙事提取，更新组件状态描述 + 降级字段：
- `后门锁 = 锁芯变形（D15 被撬，未突破）`
- `天台集水器 = 接口松脱（D15 暴风）`
- Maintenance Pressure：恶化
- Security/Exposure：恶化（被撬 = 暴露上升）

#### 5. 修补与升级

玩家说"修补集水器，给后门加根门闩"：
1. WM 叙事修复过程 + 结果
2. WM 在 [Resolution] 写材料消耗（走 T1 消耗机制）：`消耗: 铁丝×0.2kg + 铁门闩×1`
3. WSK 更新状态：
   - `天台集水器 = 已修复+铁丝加固（D16）`
   - `后门锁 = 锁芯变形+铁门闩加固（D16）`
   - Maintenance Pressure：改善
   - Security/Exposure：改善

#### 6. 加固 = 更耐损耗

加固后的组件状态描述本身就是"防御力"。下次 WM 判断损耗时，读到"后门锁 = 锁芯变形+铁门闩加固" -> 语义理解：门闩加固了，再被撬时能扛住 -> 判断"未突破"或"无损坏"。

不需要数值防御力 -- WM 从自然语言状态描述中语义理解组件的当前防御能力。

#### 7. 升级类型

| 类型 | 状态描述变化 | 示例 |
|------|-------------|------|
| 修补 | 损坏 -> 已修复 | `玻璃破碎` -> `已更换玻璃` |
| 加固 | 已修复 -> 加固 | `已更换玻璃` -> `更换玻璃+木板封钉` |
| 改装 | 原功能 -> 新功能 | `杂物间` -> `杂物间->食物储藏间（D22 改造）` |
| 扩建 | 新增组件 | 新增 `后院柴垛 = 已建（D25）` |

#### 8. 与现有机制的关系

| 机制 | 关系 |
|------|------|
| T1 消耗 | 共用 Day 推进触发点；修复/加固的材料消耗走 T1 机制 |
| [Resolution] | 修复/加固的材料消耗写入 [Resolution] 消耗行 |
| Base Structure State | 组件状态描述承载损耗/修复/升级的全生命周期 |
| Maintenance Pressure | 损耗 -> 恶化；修复 -> 改善 |
| Active Concerns [据点] | 损耗事件触发关注（"集水器损坏，饮水来源受影响"） |

#### 9. 可行性评估

| 维度 | 评估 | 说明 |
|------|------|------|
| WM 触发可靠性 | **中** | Day 推进时考量损耗（与 T1 共用触发点）；但损耗非每日必发，WM 可能漏判 |
| 多因素语义综合 | **高** | WM 擅长语义综合判断（读 WSK 状态 -> 判断"很可能被搞破坏"） |
| 自然语言状态追踪 | **高** | 已有组件锚点表使用自然语言状态，无需新字段 |
| 加固防御力语义理解 | **中高** | WM 从"加固"描述推断耐久度，可行但依赖 WM 语义理解一致性 |
| 修补/升级材料消耗 | **高** | 走 T1 机制（[Resolution] 消耗行），已有基础设施 |
| WSK 提取可靠性 | **高** | 现有"从 WM 叙事强语义提取"覆盖 |

#### 10. 风险评估

| 风险 | 等级 | 缓解 |
|------|------|------|
| WM 漏判损耗（不产生故障事件） | **中** | Maintenance Pressure 持续高时 WSK Active Concerns [据点] 已提醒；WM 看到"维护压力高"应考量损耗 |
| WM 损耗叙事过度（每轮都坏东西） | **低** | 损耗考量框架有前置条件（恶劣天气/高暴露/仇恨关系等），非每轮触发 |
| WM 加固效果判断不一致（同样"加固"有时扛住有时扛不住） | **中** | 可接受--现实中也存在不确定性；WM 叙事时可描述加固效果（"门闩扛住了"或"门闩也弯了"） |
| 组件状态描述过长（多次修补后历史堆积） | **低** | WSK 压缩规则可裁剪旧历史（保留最近状态 + 关键变化日） |
| 多文件联动风险（T1 + D2 + Base Structure State 交互） | **中** | T1 已落地验证；D2 复用 T1 触发点和消耗机制，不新增独立结构 |

#### 11. 改动范围

| 文件 | 改动 |
|------|------|
| 1-2 APD | [据点损耗机制]：天气损耗（Probability Check）+ 人为破坏（Probability Check）+ 使用损耗（WM 语义）+ 节奏控制 + 组件故障后果映射 |
| WSK | 不改动（现有提取机制 + 自然语言状态描述覆盖；MP 为累积指示器非触发器） |

**状态**：✅ 已执行（1-2 落地，简化为 Probability Check + WM 语义，MP 降为指示器）

---

### D3：Relationship 7 档升降规则细化

**背景**：v1.56 将 Trust 0-100 + Hostility 0-100 替换为 Relationship 7 档定性值。WSK 根据 WM Scene 叙事中的互动语义判断升降档，不做数值记录。

**待讨论问题**：
1. **升降档触发示例**：哪些叙事事件对应哪些档位变化？（如"首次交易成功" -> 中立 -> 好感？"背叛" -> 直接降到仇恨？）
2. **跨档跳跃**：重大事件（如救命之恩、杀人夺财）是否允许跨多档升降，还是必须逐档？
3. **不对称关系**：A 对 B 的 Relationship 与 B 对 A 的不同如何记录？（2-2 已声明"A 对 B ≠ B 对 A"，但需确认格式）
4. **势力 vs 个体**：势力的 Relationship 变化是否比个体更迟钝？（如"码头帮 Relationship=好感"需要多次个人互动累积，还是一次势力级事件即可？）

**建议方向**：不在 WSK 侧设计触发规则（WSK 只记录 WM 裁定的结果），触发规则属 WM 裁定逻辑，排入 1-2 或 1-3 讨论。

---

### D4：伪装暴露机制后续设计

**背景**：v1.56 移除了 L0-L4 暴露追踪框架，1-3 §[身份暴露风险] 改为"Probability Check 判定 -> Relationship 降为敌意/仇恨 + 自然后果"。

**待讨论问题**：
1. **渐进式暴露 vs 二元判定**：当前是 Probability Check 一次判定（暴露/未暴露）。是否需要中间态（如"被怀疑但未确认"）？如果需要，用 Relationship 降档（好感 -> 冷漠）表达即可，还是需要额外机制？
2. **恢复路径**：暴露后 Relationship 降到敌意/仇恨，是否有恢复路径？（如"证明自己不是间谍"、"势力更迭"）
3. **跨势力传染**：劫掠者兄弟会识别后，是否会通报其他势力？如何用 Relationship 表达？

**建议方向**：伪装暴露的中间态用 Relationship 降档表达（好感 -> 冷漠 -> 敌意），不需要额外机制。跨势力传染通过 WM 叙事 + Relationship 联动处理。

---

### D5：库存规则大重构

**背景**：v2.01-T4 评估结论为"不重构"。但如未来规则膨胀到影响运行时，可重新评估。

**状态**：暂不讨论，待规则膨胀时再议。

---

## 三、PAM v1.2 工具化

**背景**：v1.56 将 PAM 从 v1.1 升级到 v1.2，新增：
- Phase 1 执行证据约束（`[机]` 检查必须输出 grep 命令 + 结果计数）
- 1.6 跨文件枚举一致性检查
- 形态黑名单 2.10（SSOT 元注释）
- 陷阱 #21（机械阶段空转）/ #22（枚举跨文件不一致）

**待讨论问题**：
1. **脚本化**：Phase 1 的 1.1-1.6 是否值得写自动化脚本？（grep + 集合比对）
2. **审计模板**：是否为每次审计生成标准化的执行证据模板？

**建议方向**：优先脚本化 1.1（引用活性）和 1.6（跨文件枚举一致性），这两项纯客观、可完全自动化。

---

## 四、优先级建议

| 项 | 类型 | 建议 |
|----|------|------|
| T1 据点消耗机制 | 机制设计 | ✅ 已执行（1-1+1-2+1-3 落地） |
| T2 Insulation 移除 | 纯文本改动 | ✅ 已执行 |
| T3 库存规则梳理 | 评估 | ✅ 已结题（不重构） |
| T4 Condition 移除 + Component 简化 | 纯文本改动 | ✅ 已执行 |
| D1 保暖机制设计 | 机制设计 | ✅ 已执行（1-3+1-2 落地，静态测试通过） |
| D2 据点损耗机制 | 机制设计 | ✅ 已执行（1-2 落地，一致性检查通过） |
| D3 Relationship 升降规则 | 机制设计 | 需讨论，但属 WM 裁定逻辑 |
| D4 伪装暴露后续 | 机制设计 | 需讨论，但当前简化方案可用 |
| D5 库存大重构 | 结构重构 | 暂不讨论 |
| PAM 工具化 | 工具开发 | 可并行，低优先 |
