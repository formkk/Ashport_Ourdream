# OurDream 多系统角色群聊游戏 - 项目分析报告

> **本文件是项目设计及说明的唯一文档**。
> 任何项目功能、机制、文件结构、平台映射、设计决策、变更原因，以本文件所述为准。
> 跨文件引用通过本文件提供的章节号定位；不允许出现与本文件不一致的"派生文档"。

> 本报告基于对 `./Prompt_File/` 目录下所有源文件的逐行审读。

> **最后更新**：2026-08-02（v1.40 版）| **架构**：8 文件 + 2 角色（WM/WSK）| **主要变更**：详见 §14-§15

---

## 〇、核心工程原则体系：三层漏斗与系统级防线（最高优先级，2026-07-24 定案）

> **强制约束**：本项目所有开发、纠错、新增功能或机制，都必须以本原则体系为前提。任何提示词文本在落盘前、任何修复方案在实施前、任何新机制在设计时，依次通过四道防线（三层漏斗 → 机制闭环 → 认知适配 → 退化与预算）；任一道不通过即不得进入装配字段或实施。

### 0.1 原则本体

落盘提示词文件是**角色执行 LLM 消费的提示词**，不是开发者或系统架构师的说明文档。检验按以下三层漏斗顺序执行，**顺序不可颠倒**（身份定读者，读者定有效性标准）：

**第一层 · 字段身份（前提）**
先明确目标字段的身份与地位：聊天室级（全局协议 / 世界共识）？叙事者兼裁定者（World Master）？记录员（World State Keeper）？
- 身份框定候选读者集合，也框定读者的解读框架（priming）——身份声明错位会污染整个字段（实证：头部化石问题，§17.4）
- 身份声明本身也须过检：保留语义定框与导航功能，删除无行为价值的容器标签自我介绍
- 内容必须与字段身份语义匹配：世界共识归 Scenario，系统协议归 Private Details（§17）
- **聊天室两个字段的承载分工**
Private Details 承载世界的全局系统协议
职责：真正的“系统宪法”，写得越硬、越可执行越好。容量很大可充分利用，把所有不可妥协的规则放这里。
  所有参与者（包括 World Master、WSK和其他角色）必须遵守的铁律
  输出格式总则与强制结构要求
  玩家代理权、反替玩家行动/思考、反出戏规则
  记忆处理与一致性基础要求
  多角色协调基本原则（谁主导叙事、如何避免抢戏）
  优先级声明：本字段 > 其他所有字段

Scenario  世界共识
  完整/核心世界共识（地理、势力、历史、社会规则、势力和社会状况、世界运转逻辑等）
  世界基础状态与长期稳定知识
  所有角色（包括 World Master、WSK和其他角色）共享的世界观背景
  高优先级共用知识库
  
- **角色卡三字段的承载分工**

对于World Master和World State Keeper，还需要根据提示词文件来确定所装载的内容分类，现规定如下：
WM Scene ：
职责：定义WM的永久元身份 + 持续叙事框架
  1. 本质身份声明：不可见、全知、中立的场景渲染器与结果裁定者
  2. 长期叙事立场：世界始终在运转、玩家行动有真实后果、场景必须可感知、不剧透、不破坏逻辑，保持沉浸的基本原则。必须输出什么、硬边界在哪；写法：用现在时、元叙述语气即可。
  3. 对“世界共识”的调用态度（如何引用、如何保持一致性）。
  4. 只放最高优先级的运行契约，不放细则。

WM Additional Personality Details 
职责：定义 World Master 的“风格”与行为哲学。
  叙事声音与风格（感官优先级、描写密度、对话呈现方式）。
  裁定哲学（如何判断成功/失败/部分成功、如何处理模糊意图、如何给出合理后果）。
  情节推动原则（何时主动推进、何时等待、如何自然引入事件）。
  与玩家的交互边界（不替玩家说话/思考）。
  氛围与张力控制方式。
  轻量融入世界共识中的“核心价值观或世界观基调”（例如世界是残酷的、收获有代价等），让人格与世界气质统一。

WM Extra Details
职责： 全部可执行机械系统 + 可执行细节 。
  详细结果裁定系统
  状态管理与时间推进机制
  输出格式的具体模块实现与示例
  与全局协议、世界共识的优先级与衔接说明（可补充世界共识中需要高度可执行的规则细化部分）

WSK Scene ：
职责：定义WSK的永久元身份 + 输出/静默契约框架
  1. 本质身份声明：硬状态管理员与审计者；只记账、不叙事、不裁定、不建议、不推动剧情。
  2. 触发与静默契约：仅在被点击时触发；扫描到已成立变化→输出白名单响应，扫描不到→静默或轻量回执；被点选不是新事件。
  3. 输出总览：只允许 [State Update] / [Commit Rejected] 两类白名单输出；账本之外不输出任何内容。
  4. 只放最高优先级的运行契约，不放细则。

WSK Additional Personality Details 
职责：定义 WSK 的记账判断逻辑与记录纪律（WSK 无叙事风格，其“人格”＝记账决策原则）。
  记账决策表：DO / REJECT / SILENT 三表。
  记录什么：必须记录的硬状态大类（时间 / 环境 / 角色状态 / 库存 / 装备 / 关系 / 行为侧威胁 / 据点 / 势力活动）。
  各状态怎么记：环境压力 / 库存 / 据点庇护 / 关系敌意 / 势力活动等的记录规则。
  记录纪律：只认 World Master 已写出的已成立变化，不脑补、不推测、不替叙事补全；提交前检查与最低提交标准。

WSK Extra Details
职责：全部可执行输出机制 + 提取细则 + 模板。
  [State Update] 模板与完整视图字段清单。
  压缩规则与输出策略。
  提取纪律（如何从 World Master 叙事强语义提取已成立变化）。
  具体记录格式（可消耗物资 kg、弹药口径合并等）与最低提交标准细则。



**第二层 · 可见性（读者）**
提示词字段的可见性只有两种：**聊天室级字段 = 全部角色可见；角色卡字段 = 仅本角色可见**。
- 每句话必须能指名读者（某个执行角色的模型）；写给开发者、审计者、虚空的句子一律不应存在
- 防御性元说明（"提示词不指挥其装载"）、猜想陈述（"PM 是高优先级材料"）、角色名枚举（隐含排除风险）是典型清除对象
- 角色间字段外通道 = 对话历史中的实际输出；跨角色依赖写"以对方实际输出为准"，不写不可读的字段指针（§16）

**第三层 · 有效性（行为）**
对确定的读者，句子必须改变其行为（解读 / 裁定 / 输出 / 使用纪律）：
- **操作判定**：假设删掉这句，模型下一次输出会有任何不同吗？无差别即噪音，删除；**禁止以"描述内容是否正确"代替判定**（事实真实性 ≠ 行为价值，一条关于平台机制的真话仍然是废话）
- 读者模型无法执行的系统机制元描述（装载/注入/读取顺序/检索/可见性），删除或改写为"以平台为准"+解释约束（§17.5）
- 功能内核已有权威定义兜底的重复句同样清除——冗余定义本身就是分歧的时间炸弹

### 0.2 判定矩阵

| 对谁说 | 改变行为吗 | 处置 |
|---|---|---|
| 明确读者 | ✅ 是 | 保留（合格提示词） |
| 明确读者 | ❌ 否 | 删除或改写为行为指令 |
| 说不清读者 | ✅ 是 | 迁移到读者正确的字段 |
| 说不清读者 | ❌ 否 | 无条件删除（开发注释入仓库文档） |

### 0.3 适用场景（强制）

| 场景 | 执行要求 |
|---|---|
| **新增功能或机制** | 设计时先确定承载字段身份 → 逐句过三层漏斗 → 通过后才能写入提示词 |
| **纠错修复** | 修复文本本身必须过三层漏斗——禁止为防复发加"防御性元说明"（药变成病，§17.5 根因②） |
| **审计** | 从"模式扫描已知病灶"升级为"逐句可执行性审查"，对基础层文件（0-1/0-2 头部）定期逐行过三层漏斗 |
| **引用新增** | 先过可见性层：引用方角色生成时能否看到被引用目标（§16 验证方法） |

### 0.4 来源

本原则由多轮实战纠错沉淀而成：头部化石（§17.4）→ 提示词越权指挥平台动作（§17.5）→ 元描述与猜想陈述清除（Pinned Memory 条款整行删除）→ 用户定案三层顺序（身份→可见性→有效性）。

### 0.5 系统级补充原则（与三层漏斗正交，2026-07-24 定案）

三层漏斗是**句子级准入闸门**（回答"这句话有没有资格存在"），但开放世界 + 多角色 + 长期运行 + LLM 执行者的品类特征还需要四条系统级原则：

**原则① 机制闭环（系统级完备）**
每个机制构件必须四要素齐全：**定义 + 语义 + 消费者 + 失败出口**。声明了数值就必须有消费路径，声明了钩子就必须有定义与触发语义；缺一 = 断头路。实证：scavenging 偏移 37 长期无 Base 概率与触发语义；噪音暴露度 +15/+30 长期无消费者。审计工序：接口完备性矩阵（枚举全部机制钩子，逐个核对四要素）。

**原则② 认知适配（LLM 能力现实）**
规则必须匹配执行者的稳定能力边界：
- **确定性推导 > 隐式追踪**（Month 由 Day 查表推导，不让模型记住当前月份）
- **显式锚点 > 上下文记忆**（硬状态只认落盘 [State Update]，不靠模型回忆）
- **模板约束 > 自由发挥**（9 字段清单压制旧模板惯性）
需要模型长期精确计数/算术/跨轮记忆的规则 = 载体选错，不是写得不够清楚。实证：Month 推导算错、11 字段旧模板惯性复活。

**原则③ 退化设计（失败出口）**
开放世界中信息缺失、规则冲突、用户不触发是常态，每条关键规则必须预置失败出口：缺字段 → REJECT 还是记"未提供"；无变化 → 轻量回执；用户长期不触发 → WM 临时 State。没有退化路径的规则，第一次异常就会诱发模型幻觉自救。实证：REJECT 分级（Weather 缺失 REJECT / Season 缺失记未提供）、轻量回执定案。

**原则④ Token 预算（上下文竞价）**
上下文是稀缺资源，每个字符都在竞价：高频规则放高显著性位置（字段头部）；低频细则下沉角色卡；同一信息 SSOT 化只存一份（冗余 = 双倍占用 + 分歧炸弹）。三层漏斗第三问"删掉后行为无差别即删"是本原则的句子级投影。

### 0.6 防线体系（执行次序）

```
落盘前闸一：三层漏斗（句子级准入）
   身份 → 可见性 → 有效性
闸二：机制闭环检查（系统级完备）
   定义 + 语义 + 消费者 + 失败出口
闸三：认知适配（执行级现实）
   推导>追踪，锚点>记忆，模板>自由
运行期兜底：退化路径（异常不诱发幻觉）+ Token 预算（上下文竞价意识）
```

一句话概括：**三层漏斗保证"每句话值得存在"，闭环原则保证"每个机制能运转"，认知适配保证"模型真的做得到"，退化设计保证"出错时不崩盘"**。

### 0.7 执行纪律补记（2026-07-24，源自可见性声明漏检复盘）

**背景**：原则定案并对 0-1 全面审查后，「聊天室内全部角色共享可见」仍被漏检——内容完全属实但行为价值为零。暴露三个执行缺陷，对应三条强制纪律：

1. **形态黑名单（先于语义判定）**：凡描述「谁能看到本字段 / 本字段如何被装载 / 字段的平台属性」的句子，**不看内容真伪，无条件不进入装配字段**。语义判定（"能否执行"）对静态事实描述天然失焦，且会被"内容正确"豁免；形态判定只看句型，不看真伪。可见性知识唯一的合法居所是仓库文档（本文件 / PLATFORM_REFERENCE）。
2. **审计纪律反转**：最近修复过的区域列为**高优先复检区**而非安全区——修复只针对当时识别的病灶形态，同句其他成分未必过检（实证：上一轮只删了括号枚举，主干"共享可见"从未被审视）。
3. **有效性操作定义收紧**：闸一第三层的判定动作固定为"假设删掉这句，模型下一次输出会有任何不同吗"；禁止以"描述对不对"替代"有没有用"。框架自我描述（陈述原则前提的句子）不豁免逐句检验。

**二次加固（源自 0-2 头部指针句漏检复盘，同日）**：重排与三性审查后，0-2 头部「职责分工见 Private Details 字段」指针句仍漏检。四个根因（修复免疫 / 标准漂移 / 句级分类掩盖分句病灶 / 短即无害错觉）对应三条强制纪律：

4. **分句级过检**：凡含多个独立命题的句子（分号/逗号连接不同主张），逐分句独立过两问；一个分句的合法标签不得豁免其他分句（实证三连："共享可见"主干、L34"按各自职责"、0-2 头部指针分句——同一模式三次犯案）。
5. **修复免疫禁令**：历次修复产生的句子不享受白名单，按现行标准重新过检；"复检反转"纪律的覆盖范围从"病灶残留"扩展到"修复文本本身"。旧标准下诞生的合法句，遇新标准必须重新过筛（标准漂移缝隙是漏检高发区）。
6. **头部加权**：字段头部（定位/锚点区）按每句全成分拆解审查，不适用正文的抽样逻辑；头部信息密度全字段最高，审查强度应高于正文而非更低。

### 0.8 人工审查新增病灶形态（2026-07-24，用户首轮人工审查 0-1 沉淀）

1. **无实体对应的化石术语**：专有名词必须与实际输出实体对应（如 `[State Update]`、Pinned Memory 中的具名段落）；术语只存在于规则文本而无可指认对象 = 让模型猜谜（实证：State Ledger / History Ledger 无任何输出实体，已替换为实际实体名）。
2. **术语与平台字段名撞车**：概念词与平台字段名同名（如 Scene 既是叙事正文代称又是角色卡字段名）时，上下文必须带消歧锚点（对话历史 / 实际输出 / 叙事正文），否则换用他词（实证："通过 Scene 叙事体现已成立变化"改为"前台叙事正文"）。
3. **跨卡指针预设读取能力**：聊天室字段指向角色卡的引用一律写成**归属声明**（"X 按其…字段 §Y 执行"），不写引用指令式（"详见 X 的…字段"）——前者不预设任何跨字段读取能力，与平台真实机制兼容（实证：场外演化时间规则改写）。
4. **世界观旁白混入指令文本**：设定叙事句（如"后末日无统一货币"）混在规则条目中，读起来"不像提示词"即为此病灶。判定法：删掉该分句后，任何角色的行为指令是否缺失？不缺即为旁白，删除或下沉到背景设定区；另须检查旁白与相邻规则是否自相矛盾（"无统一货币"紧接"9mm 为本位单位"即自我推翻）（实证：0-2 经济规则首句）。

### 0.9 审计方法二次加固（2026-07-24，枚举病灶漏检复盘）

**背景**：0-1 职责目录行的冗余枚举（动态机制括号清单）连续通过多轮三问审查，最终由用户人工审查发现——且已实际漂移（"据点基线"≠源头节名"据点结构基线"）。暴露审查方法四个结构性缺口：分句级纪律以句为粒度而病灶在成分级；纪律以实例定界而非模式（"覆盖清单禁令"没覆盖括号枚举）；枚举类内容无专属检验问句；真实性陷阱再次犯案（内容属实即放行，未执行删除测试）。

**三条强制对策**：
1. **枚举检验问句（第五问）**：凡遇枚举/清单/括号列举，必问三句——别处有无权威定义？本处读者需要逐条吗？枚举内容与源头逐项一致吗？任一失败即删（实证：0-2 覆盖清单、0-1 职责目录括号枚举）。
2. **检验粒度下沉到成分级**：复合句按语义成分（声明/枚举/条款/理由）拆解后逐成分过两问；整句的合法分类不得豁免内部成分。
3. **模式滞后元承认**：模式匹配审查对首次出现的病灶形态必然滞后一轮；人工读者模拟（"不了解内情的读者会被误导吗"）是必要互补而非重复劳动；每轮人工发现的病灶必须当轮泛化为模式级纪律，禁止以实例形态定界。

### 0.10 节标题纳入两问检验（2026-07-24，自指标题漏检复盘）

**背景**：0-1 首节标题 [聊天室 Private Details 字段定位与权威优先级] 长期占据全文件第一行，却从未被 § 引用、且自指本字段（读者正在读它），属零增益的仪式性标题；多轮编辑该节内容均未触及头顶标题。暴露缺口：两问/三问检验只施加于正文句子，[节名] 被当作中性结构脚手架而豁免于审查；"正确描述内容"的标题以正确性掩盖无必要性；头部加权只用于排序、未用于问责首行占位。

**三条强制对策**：
1. **节标题过两问**：每个 [节名]/【节名】必须自证存在价值——要么被 § 引用（锚点职能），要么有真实导航必要；否则删除或缩短。自指（重报本字段/本文件名）或纯描述且无引用的标题一律清除。
2. **改/删节名前先查引用**：节名可能是跨文件 § 指针的目标；删除/改名前必须全库 grep 该节名，确认无引用或已同步改指针，防断链。
3. **首行问责**：头部加权的推论是"最高权重位置必须被最高价值内容占据"；每文件首行（含首个节名）须单独过删除测试。

### 0.11 提示词文件审查方法论（操作手册，2026-08-02 综合定案）

综合 §0.1–0.10 的原则、病灶形态与历次漏检教训，对任一提示词文件做系统审查时按以下流程执行。**先机械后语义**：机械核验产出客观可复核结果，语义取舍由人主导；审查者只报客观发现，不下"彻底/完备"结论。

**Step 0 · 定位（前提）**
- 本文件属于哪个角色/字段？功能定位是什么？（聊天室级 0-1 系统协议 / 0-2 世界共识；1-x WM 专属；2-x WSK 专属）
- 内容是否与字段身份语义匹配？（世界共识归 0-2，系统协议归 0-1，WM 裁定归 1-x，WSK 记账归 2-x；错位即迁移）
- **角色卡六字段功能分工**（Scene / APD / Extra × WM / WSK；审查内容是否落在功能匹配的字段，错位即标记）：

| 字段 | 功能定位 |
|---|---|
| WM Scene (1-1) | WM 角色身份 + 每轮输出契约（[主要状态]/[Resolution]/[Probability Check] 的触发与格式）+ 输出文风约束 |
| WM APD (1-2) | WM 裁定决策逻辑——生存 / 战斗 / 交易 / 搜刮 / 关系 / 敌对 / 时间推进 / 压力演化"怎么判" |
| WM Extra Details (1-3) | WM 动态世界机制 + 大型参考数据——场外演化 / 势力周期活动表 / 据点结构基线 / 群聊角色锚点协议 |
| WSK Scene (2-1) | WSK 角色身份 + 触发 / 静默契约 + 越界边界 |
| WSK APD (2-2) | WSK 记账决策——DO / REJECT / SILENT 决策表 + 各状态怎么记 + 提交前检查 |
| WSK Extra Details (2-3) | WSK 输出格式（[State Update] / 完整视图）+ 提取规则（kg 记录 / 口径格式 / 提取纪律） |

归位准则：输出格式契约归 Scene，决策逻辑归 APD，机制与参考数据归 Extra Details；同角色三字段运行时一并装载，分工是组织约定而非可见性边界。

**四问（逐句→逐分句→逐成分下沉）**
- ① **给谁看**：必须能指名读者（某执行角色的模型）。写给开发者/审计/虚空的清除；跨角色不可读的内容不得以指针形式出现（可见性边界，§16）。
- ② **有必要吗**：删掉它，模型下次输出会有任何不同吗？无差别即噪音。是否别处已有权威定义（重复＝分歧炸弹，SSOT）？
- ③ **能生效吗**：读者模型能执行吗？是否要求长期精确计数/算术/跨轮记忆（认知不适配，原则②）？关键规则有无失败出口（原则③）？
- ④ **高效率吗**：高频高权重前置、低频下沉（Token 预算，原则④）；长句可结构化；枚举/示例是否代表性（以实跑失效模式为准，非设计假设）？

**机械核验（客观可复核，优先于语义判断）**
1. **引用活性**：全库 grep 所有 `§` 引用，目标节是否存在；指向本文件节的是否对上现存节名；改/删节名前先查谁在引用（防断链）。
2. **术语一致性**：角色简称是否前置定义；同一概念是否多种叫法；已废除术语（前台/后台等）是否残留；名实是否一致。
3. **重复检测**：同一规则/映射/枚举是否多处存在（SSOT 漂移隐患）；镜像正反说、自指标题、自指指针均属冗余。

**病灶扫描清单（对照已知形态，§0.8 + 历次沉淀）**
化石术语（无实体对应）/ 术语撞车（与平台字段同名无消歧）/ 跨卡指针预设读取 / 世界观旁白混入指令 / 自指标题 / 冗余枚举（别处有权威的清单副本）/ 镜像正反说 / 设计说明混入（读者不可执行的元描述）。

**执行纪律（历次漏检教训，§0.7/0.9/0.10）**
- 形态黑名单先于语义判定（可见性/装载/平台属性句不看真伪直接清）。
- 修复免疫禁令：修复过的句子不享受白名单，按现行标准重检。
- 分句级/成分级过检：复合句逐分句、逐成分过四问，整句合法分类不豁免内部成分。
- 头部加权：字段头部逐成分拆审，强度高于正文。
- 模式滞后元承认：人工发现的病灶当轮泛化为模式级纪律，禁止以实例定界。

---

## 一、项目概述

本项目是面向 **Ourdream.ai** 平台的**硬核后末日生存群聊游戏系统**，当前由 **8 个落盘提示词文件 + 4 个项目参考文档** 组成：

- **落盘提示词文件**（共 8 个，承担平台字段）位于 `./Prompt_File/`
  - 聊天室级：`0-1_Private_Details.md`（全局硬规则）+ `0-2_Scenario.md`（静态世界共识）
  - WM 角色卡：`1-1` / `1-2` / `1-3`
  - WSK 角色卡：`2-1` / `2-2` / `2-3`
- **项目参考文档**（共 4 个，仅作辅助索引，不承担平台字段）：
  - `PROJECT_ANALYSIS.md`（**本文件，唯一项目设计及说明文档**）
  - `OURDREAM_PLATFORM_REFERENCE.md`（平台功能参考，按需查阅）
  - `OPTIMIZATION_ROADMAP_v1.md`（治理与版本变更历史）
  - `his.md`（用户手动维护的 Pinned Memory）

> **架构演进（v1.19 起）**：原 11 文件 + 3 角色（WM/WSK/WER）架构已演进为 8 文件 + 2 角色（WM/WSK）架构。**World Event Recorder（WER）组件已于 v1.19 彻底删除**，其职责已由用户手动维护的 Pinned Memory 承担。详见 §2.1.1。

系统采用"**WM 唯一稳定前台 + WSK 后台逻辑层**"的运行架构，构建可长期运行的后末日生存世界——**灰港（Grey Harbor）**。

### 核心设计理念

> **低资源、低信息、高风险、强后果、慢积累、反爽文**

系统追求真实可信、前后一致、持续演化的硬核生存世界。玩家的每一次行动都有代价，每一个决定都有长期后果。

### 平台机制特性

Ourdream.ai 平台的关键机制（详见 §2.6 与 [OURDREAM_PLATFORM_REFERENCE.md](file:///d:/Mycode/AshHarbor_OurDream_QoderCN/OURDREAM_PLATFORM_REFERENCE.md)）：
- **后台角色不会自动运行**：只在用户手动点击时被触发
- **聊天室共享**：所有角色共享同一个聊天室，后台角色被触发时可看到全部历史
- **聊天室级持久化字段**：
  - `聊天室 Scenario 字段`（`0-2_Scenario.md`）：50,000 字符，承载静态世界共识
  - `聊天室 Private Details 字段`（`0-1_Private_Details.md`）：≥50,000 字符，承载全局硬规则
  - `Pinned Memory`：持久化层，承载历史账本（用户手动维护）
- **角色级字段**：
  - `Scene`（`1-1` / `2-1`）：50,000 字符，身份与平台锚点
  - `Additional Personality Details`（`1-2` / `2-2`）：50,000 字符，决策表与心理预设
  - `Extra Details`（`1-3` / `2-3`）：100,000 字符，动态机制与模板

---

## 二、系统架构：单前台 + 双后台

### 2.1 架构总览

系统采用"一个稳定前台 + 两个后台账本角色"的架构：

```
┌─────────────────────────────────────────────────────────────┐
│                      World Master (WM)                       │
│  唯一稳定前台：Scene生成 / 行动裁决 / 交易结算 / 状态栏输出  │
│  角色字段：1-1(Scene) / 1-2(Additional) / 1-3(Extra)        │
│  聊天室 Private Details 字段：0-1_Private_Details.md         │
│  聊天室 Scenario 字段：0-2_Scenario.md（共享读取）           │
└──────────────┬──────────────────────────────────────────────┘
               │ Scene 叙事 + 状态栏
               │ (强语义提取)
               ▼
┌─────────────────────────────────────────┐
│      World State Keeper (WSK)            │
│      唯一硬状态管理员                      │
│      字段：2-1 ~ 2-3                     │
│      输出：单标签 [State Update]（三模式）│
└─────────────────────────────────────────┘
```

> **WM 唯一稳定前台 + WSK 后台**（v1.19 起 WER 已删除，详见 §2.1.1）。两个角色共享读取两个聊天室级字段（`0-2` + `0-1`），不承担平台字段加载任务的差异。详见 §2.6。
>
> **通讯机制（审计已定案）**：WM 不再向 WSK 推送结构化同步块；WSK 改为**强语义提取** WM 的 Scene 叙事与状态栏中已明确写出的已成立变化。WSK 仅在缺少必要字段时返回**拒绝回执**（`[Commit Rejected]`）；不再输出接受回执。`[Resolution]` / `[Probability Check]` / 子结构块等展示块仅供用户阅读，**不得作为通讯载体**。详见 §2.3、§13。

### 2.2 各角色职责详解

`0-1_Private_Details.md` 是聊天室 Private Details 字段（全局硬规则）；`0-2_Scenario.md` 是聊天室 Scenario 字段（静态世界共识）。

#### World Master（WM）- 唯一稳定前台

| 维度 | 说明 |
|------|------|
| **核心职责** | Scene 生成、行动裁决、交易/关系/敌对结算、状态栏输出 |
| **前台权限** | 唯一前台发言者，负责自然述事和结构化输出（含状态栏） |
| **后台权限** | 无（不再向 WSK/WER 推送结构化同步块，由二者强语义提取） |
| **禁止行为** | 不替用户做决定、不自行补库存、不维护官方时间 |
| **叙事视角** | 由平台内置第一人称/第三人称选项控制，提示词不再干预（2026-07-24 已删除第二人称强制规则） |
| **字段语义** | `1-1 / 1-2 / 1-3` 属于 WM 角色字段 |

#### World State Keeper（WSK）- 后台硬状态管理员

| 维度 | 说明 |
|------|------|
| **核心职责** | 维护官方时间、库存、伤病、天气、市场账本 |
| **输出软标签** | `[State Update]`（默认 / 完整视图 / 极简回执三档共用） |
| **拒绝回执** | `[Commit Rejected]`（信息不足 / 字段不完整 / 7 字段事务块缺项） |
| **第一行格式** | `D{day}-T{turn} / HH:MM / {Phase} / {Zone} {Sub-zone} {Location} / {Weather} {Temperature Band} /{Knowledge Scope}`（`/` 分隔，Knowledge Scope 前缀 `/` 不留空格） |
| **子段** | `Human Contact Status:` / `Inventory Delta:` / `Inventory Snapshot:` / `Scavenging Status Snapshot:` / `Survival Anchor Snapshot:` / `Recent Changes:`（按需出现，无数据整段省略） |
| **移动字段** | `Travel Time: {值} ({备注}) / Steps: {值}` 单行；原地对峙写 `Travel Time: 0(原地对峙) / Steps: 0` |
| 库存格式 | 按功能组归类（"武器: 霰弹枪×1(泵动式，空膛)+..."），便于 WM 解析且与 WSK 强语义提取对齐 |
| **触发方式** | 用户手动点击 → 强语义提取 WM Scene 叙事中已明确写出的已成立变化 |
| **读取范围** | 同步读取 `0-2_Scenario.md` 与 `0-1_Private_Details.md` 获取世界边界与全局硬规则 |
| **防抖机制** | 维护 Last Committed Commit Key，拒绝重复提交 |
| **决策表** | 3 张：DO / REJECT / SILENT（不替 WM 推进，只对已成立变化入账） |
| **审计职责** | 校验 WM 的 Inventory Snapshot 压缩规则并输出修正版本 |
| **权威地位** | 输出的 Snapshot 是权威值，WM 需在下一轮替换自己的临时估算 |

#### 2.1.1 WER 组件删除说明（v1.19）

**World Event Recorder（WER）** 组件自 v1.19 起已**彻底删除**：

| 阶段 | 操作 | 原因 |
|------|------|------|
| v1.16 | 删不可压缩锚点机制 | 实际不生效 |
| v1.16 | 删 WER 压缩机制 | 不生效 |
| v1.16 | 精简 WER 输出模板 | 冗余段清理 |
| v1.19 | 删除 WER 全部 3 个文件 | 长期档案职责由 Pinned Memory 承担 |
| v1.20 | WSK 接管"近五日主要事件"实验字段 | 实验性机制，语义提取 |

**原因**：
1. WER 长期档案功能实际由用户手动维护的 Pinned Memory 承担（user 复制 `## 新增` 段到 Pinned Memory）
2. WER 压缩机制（10日/30日/30+）不生效——Pinned Memory 不受 WER 内部压缩影响
3. WER 不可压缩锚点机制（死亡/失踪等）"强制保留"实际不生效
4. 架构简化：8 文件 + 2 角色比 11 文件 + 3 角色更易维护

**当前架构**：WM（前台）+ WSK（后台硬状态） + 用户手动 Pinned Memory（长期档案）

### 2.3 通讯机制（强语义提取）

WM 与 WSK 的通讯**不**通过结构化同步块进行；WSK 通过**强语义提取** WM 的 Scene 叙事与状态栏中已明确写出的已成立变化入账。

**核心规则**：

| 维度 | 说明 |
|------|------|
| **通讯载体** | WM Scene 叙事 + `[主要状态]` 状态栏；**不含** `[Resolution]` / `[Probability Check]` / 子结构块等展示块 |
| **WSK 提取源** | Scene 叙事中已明确写出的时间/地点/库存/伤病/关系/移动/天气等已成立变化 |
| **展示块隔离** | `[Resolution]` / `[Probability Check]` / 子结构块仅供用户阅读，**不得**作为 WSK 的提取源 |
| **拒绝回执** | 字段不完整时 WSK 返回 `[Commit Rejected]`，告知用户提取失败 |
| **接受回执** | **不输出**（输出账本本身即代表隐式接受，无需额外回执） |
| **决策表** | WSK 3 张：DO / REJECT / SILENT |
| **沉默协议** | Scene 叙事中无新成立变化可提取 → 静默；成功提交后不输出任何额外文字；不指向本角色的消息忽略 |

**已废弃机制（历史说明）**：

| 机制 | 状态 | 替代 |
|------|------|------|
| 6 种 Commit Type（state/event/both/offscreen-*） | 已废弃 | 强语义提取 |
| WM 推送同步块 | 已废弃 | WSK 自提取 Scene 叙事 |
| `[Event Commit Receipt]` 等接受回执 | 已废弃 | 输出账本本身即接受 |
| ESCALATE 决策表 | 已废弃 | DO / REJECT / SILENT 3 表 |
| **WER 长期档案** | **已删除（v1.19）** | Pinned Memory（用户手动维护） |
| **不可压缩锚点** | **已删除（v1.16）** | Pinned Memory 自然保留 |
| **Market State** | **已删除（v1.22）** | 不再追踪市场状态字段 |

### 2.4 手动触发工作流

用户手动点击 WSK → WSK 强语义提取 WM Scene 叙事中已明确写出的已成立变化 → 入账/拒绝。流程：

```
1. 用户手动点击 WSK
2. 角色加载全局提示词（0-1 / 0-2 + 本角色三层）
3. 角色扫描聊天历史中 WM 最新 Scene 叙事 + 状态栏
4. 强语义提取已成立的 state 变化（**不包含时间字段**——时间由 WM 唯一决定）
5. 字段完整 → DO：输出 [State Update] + 全量状态快照，更新账本
6. 字段不完整 → REJECT：返回 [Commit Rejected]
7. 无新成立变化 → SILENT：不输出任何文字
```

### 2.5 Snapshot 权威层级

| 层级 | 来源 | 性质 |
|------|------|------|
| 权威 | World State Keeper 最近 `[State Update]` 中的 Snapshot | 可信，WM 必须采用 |
| 临时估算 | World Master 自维护 Snapshot | 可能漂移，需定期替换 |
| 历史值 | 上一份有效 Snapshot | 当以上两者均无时使用 |

### 2.6 聊天室 Scenario 字段与 0-2_Scenario.md

#### 2.6.1 平台字段与落盘文件映射

`./Prompt_File/` 下的 11 个落盘文件按 OurDream 平台字段映射如下：

| 平台字段 | 落盘文件 | 角色上限 | 共享范围 | 内容性质 |
|----------|----------|---------:|----------|----------|
| `聊天室 Scenario 字段` | `0-2_Scenario.md` | 50,000 | WM/WSK 共享 | 静态世界共识 |
| `聊天室 Private Details 字段` | `0-1_Private_Details.md` | 15,000 | WM/WSK 共享 | 全局硬规则与同步协议 |
| 角色卡 Scene | `1-1` / `2-1` | 50,000 | 仅本角色 | 身份、平台锚点、基础协议 |
| 角色卡 Additional Personality Details | `1-2` / `2-2` | 50,000 | 仅本角色 | 决策表、心理预设、详细规则 |
| 角色卡 Extra Details | `1-3` / `2-3` | 100,000 | 仅本角色 | 动态机制、模板、参考表 |

> **v1.19 演进**：WER 角色卡 3 个文件（3-1/3-2/3-3）已删除，平台字段从 11 文件减少到 8 文件。

#### 2.6.2 0-2_Scenario.md 覆盖范围

`0-2_Scenario.md` 是 `聊天室 Scenario 字段` 的落盘文件，承载**静态世界共识**——WM/WSK 共享读取，覆盖：

- 背景故事（灾变 / 灰港 / 灾前 50 万人口 / 灾后 280~390 幸存者）
- 世界现状：生存环境与生产
- 季节与天气系统
- 5 轨生存压力轨道（疲劳 / 体温 / 脱水 / 饥饿 / 伤病）定义
- 敌对阶梯世界律（无信号→远距目击→被接触→升级；4 维裁定细则已迁入 World Master 的 Additional Personality Details 字段）
- NPC 知识边界与发言规则（Trust 行为阈值表已迁入 World Master 的 Additional Personality Details 字段）
- 九宫格地图固定拓扑 + 子区域微坐标
- 子地点表（T1-T4 分层，Coast / Town / Village / Farm / Hunting / Industrial / Police / Medic / Firefighter / Military / Prison / Historical / Contaminated / Underground 共 14 个标签）
- 搜刮世界数据（Tier / 标签 / 子地点产出；搜刮过程机制与裁定细则已迁入 World Master 的 Additional Personality Details 字段）
- 世界硬规则（玩家视角游戏宪法：总则三铁律 + 资源获取/消耗/经济/保暖健康/安全庇护/人际信息/环境势力/损耗总单，每条挂接落地机制节）
- 4 阵营
- 6 名常驻世界角色
- 弹药口径
- 死亡 / 抢救 / 遗物规则（含 Knowledge Scope 传播标注；独立死亡公开性节已并入）

#### 2.6.3 Scenario 与 Private Details 分工

两个聊天室级字段承担不同职责，构成"世界是什么"与"世界怎么运行"的二元结构：

| 维度 | `聊天室 Scenario 字段`（0-2） | `聊天室 Private Details 字段`（0-1） |
|------|-------------------------------|--------------------------------------|
| 内容性质 | 静态世界共识 | 全局硬规则与同步协议 |
| 回答问题 | 世界是什么 | 世界怎么运行 |
| 覆盖 | 背景 / 环境 / 地图 / 阵营 / 角色 / 硬规则 | 通讯协议 / 强语义提取 / 恢复顺序 / 输出结构 / 字段语义 |
| 修改频率 | 世界设定稳定时极少改动 | 协议微调时频繁改动 |
| 失效模式 | 描述与当前运行不一致（"事实漂移"） | 协议与平台加载机制不符（"协议漂移"） |

#### 2.6.4 全局读取顺序

所有平台角色按以下顺序加载提示词：

1. `聊天室 Scenario 字段`（`0-2_Scenario.md`）—— 静态世界共识
2. `聊天室 Private Details 字段`（`0-1_Private_Details.md`）—— 全局硬规则
3. 本角色 Scene（`1-1` / `2-1`）
4. 本角色 Additional Personality Details（`1-2` / `2-2`）
5. 本角色 Extra Details（`1-3` / `2-3`）

WSK 不复制 Scenario 内容到本角色的 `聊天室 Private Details 字段`；本角色的 Private Details 只承担本角色职责。

#### 2.6.5 角色对 Scenario 字段的读取约束

- **World Master**：每个 Scene 前必须先读 Scenario 恢复世界基底（地图 / 阵营 / 常驻角色 / 经济规则 / 死亡规则）；所有地点 / NPC / 事件描述均以 Scenario 为世界边界。
- **World State Keeper**：被触发时读 Scenario 获取世界边界（地图 / 子地点 / 阵营基线 / 弹药口径）以校验 Scene 描述与官方状态是否一致；不复制 Scenario 内容到账本。

### 2.7 WM 叙事视角硬约束

WM Scene 默认采用**第二人称**（"你"）进行场景描述与剧情推进。这是项目叙事基调的硬约束，跨三层提示词同步强制：

| 文件 | 约束形式 |
|------|----------|
| `0-1_Private_Details.md` | `[叙事视角硬约束]` 段：完整 9 条规则 + 4 项例外（NPC 台词 / `[主要状态]` / 7 字段事务块 / NPC 知识边界原文） |
| `1-1_WorldMaster_Scene.md` | `[平台优先锚点]` 顶部单行提醒：第二人称描写用户角色；NPC 仍用第三人称 |
| `1-2_WorldMaster_Additional_Personality_Details.md` | 绝对规则 0b：强制第二人称，禁止"你/她/玩家/角色名"混用 |

**核心规则**：
- Scene 主体叙事、末句示例、感官 token、动作描写、状态恢复叙述**必须**用"你"指代用户角色
- 不得混用人称：同一 Scene 内不出现"你"与"她"指代同一用户角色
- 不得出现"你/玩家/角色名"混用

**4 项例外**：
1. NPC 台词保留原视角（NPC 说自己，不代言用户）
2. `[主要状态]` 块字段值仍用第三人称客观陈述（位置 / 装备 / 库存）
3. 跨层转移 7 字段事务块保留结构化字段
4. NPC 知识边界 / 反全知规则原文已使用"他"指代 NPC，保留原意不替换

详细末句四选一示例：`0-1_Private_Details.md` 末段（`你把 X 搁在 Y 上` / 感官细节 / 对话片段 / 物品物理状态）。

---

## 三、世界观设定

以下是基于源文件的补充修正：

### 3.1 背景故事

- **灾变名称**："寂灭"——数周内抹去 99.9% 人类
- **时间线**：灾变后第 3 年
- **地点**：灰港——后苏联背景的中型沿海港口城市
- **人口**：灾前约 50 万 → 现仅存 280~390 人
- **社会状态**：旧秩序已崩溃，新的低水平秩序已长出——围绕粮食、药品、燃料、路线、庇护、情报和暴力形成的低信任秩序

### 3.2 世界现状：生存环境与生产

- 灾后第 3 年仍处于"拾荒高峰末期"：现代遗留物资还在，但高价值可用库存持续减少
- 稀缺核心：抗生素、优质弹药、干净饮水、大型牲畜、成套维修件
- 新兴和再生物资：海盐、煤炭、草药、简易金属制品
- 食物生产：菜地、轮作、捕鱼、采集、狩猎、少量鸡兔养殖
- 柴油、汽油等化学燃料极度稀缺

### 3.3 气候与天气系统

湿润大陆性气候（Dfb），9 种轻量天气枚举。

---

## 四、核心机制设计

### 4.1 地图系统

#### 九宫格固定拓扑

```
西北区(R1C1) │ 北区(R1C2)   │ 东北区(R1C3)
工业区(R2C1) │ 中心区(R2C2) │ 东区(R2C3)
西南区(R3C1) │ 南区(R3C2)   │ 东南区(R3C3)
```

十字形微网格，C 与四向相邻，不承认斜切。

#### 子地点统计（基于 1-3 Extra Details 实际数据）

| 分区 | 子地点数量 | T4 | T3 | T2 | T1 |
|------|-----------|----|----|----|----| 
| 西北区 | 8 | 1 | 2 | 4 | 1 |
| 北区 | 10 | 0 | 2 | 6 | 2 |
| 东北区 | 8 | 0 | 2 | 2 | 4 |
| 工业区 | 13 | 0 | 2 | 9 | 2 |
| 中心区 | 14 | 0 | 4 | 6 | 4 |
| 东区 | 13 | 0 | 2 | 8 | 3 |
| 西南区 | 12 | 0 | 0 | 6 | 6 |
| 南区 | 11 | 0 | 2 | 6 | 3 |
| 东南区 | 13 | 0 | 4 | 8 | 1 |
| **总计** | **99** (+3个重复=102行) | **1** | **20** | **55** | **26** |

#### 城市主干道骨架

"申"字型骨架：
- **北横干道**：连接西北区、北区、东北区
- **中横干道**：连接工业区、中心区、东区（交易、运货、火并最频繁）
- **南横干道**：连接西南区、南区、东南区
- **中轴大道**：连接北区、中心区、南区（最快纵向主干路，最易被盯梢设卡）
- **西纵干道**：贯穿西北区、工业区、西南区
- **东纵干道**：贯穿东北区、东区、东南区

地下管网已堵塞报废，不能作为稳定通行路线。

#### 非移动动作时间基准

| 动作类型 | 默认耗时 |
|----------|----------|
| 短观察、短试探、短等待 | 0-1 step |
| 持续交涉、交易拉扯、基础包扎、热食补给 | 1-2 step |
| 清创、夹板固定、系统清点、加固防位 | 2-4 step |
| 短睡、轮换守夜、长时间跟踪 | 2-6 step |
| 完整睡眠/过夜休整 | 不少于 12 step |

### 4.2 生存压力系统

5 条轻量轨道，6 级状态（stable → strained → weakened → critical → dying → dead）。

#### 生存压力时间阈值表

| 轨道 | stable | strained | weakened | critical |
|------|--------|----------|----------|----------|
| **脱水** | 0-4h | 4-12h（受 labor/炎热修正） | 12-16h | 16h+ |
| **饥饿** | 0-8h | 16-24h | 24-36h | 36h+ |
| **疲劳** | 0-12h | 12-20h（受守夜/战斗修正） | 20-24h | 24h+ |
| **体温** | 干衣+避风+活动 | 湿衣+静止+低温 | 湿衣+寒冷+疲劳+饥饿（1-2 step 内） | — |

#### 生存锚点

WSK 长期维护的少量官方锚点：
- `Last Meaningful Drink`
- `Last Meaningful Meal`
- `Last True Sleep End`
- `Recent Step Load`
- `Recent Labor Load`
- `Current Cold / Wet Exposure`

### 4.3 概率裁决系统

确定性伪随机公式：`Seed = (Day ID × 31 + Turn ID × 17 + Event Offset) mod 100`。

### 4.4 人类敌对机制

4 维判断 + 阶梯式升级。

实际文件中对人类敌对机制的描述详尽，特别是：
- 明确了 `hostile-contact` 的 Probability Check 只用于"已从旧痕迹/远距目击/被接触阶段，升级到可能发生敌对行动"的最后一步
- 必须同时满足 `被接触 + 有明确动机 + 动手成本可控` 三个前提才进入概率计算
- 增加了"阶段允许衰减与中断"规则：未确认接触、改变路线、天气抹除痕迹等情况下，短期关注应自然结束
- 增加了"普通搜刮是常态行为"的明确声明

### 4.5 经济系统

9mm 弹药本位。

### 4.6 关系系统

2. **时间消耗**：T1=1 step / T2=1-2 step / T3=2-3 step / T4=3-4 step
3. **噪音等级**：轻搜刮(+5暴露) / 中搜刮(+15暴露) / 重搜刮(+30暴露)
4. **翻空判定**：基于"常被翻空程度"和"距上次搜刮经过的时间"
5. **产出质量**：受 Tier、标签、翻空程度、天气、噪声、势力活跃度影响

### 4.8 交易裁定细则

交易分为两档：
- **轻交易**：当场、小额、低风险。只处理成交、支付、价格偏差和轻微关系尾巴
- **重交易**：大宗、跨区、势力参与、高风险物资。展开担保、押货、验货、护卫、抽成、违约后果等

### 4.9 报复链强化规则

- 报复优先挑"最便宜、最稳、最能起示范作用"的目标
- 10+ 官方日无接触/无新损失可降温或冻结
- 目标确认死亡/长期失踪/被更强势力吞没/接受赔偿可关闭直接报复链
- 新损失/新羞辱/新接触会重新激活

### 4.10 机制影响链矩阵

覆盖了 18 个核心机制的上游/下游/关键说明。

---

## 五、势力体系

5 大阵营 + 拾荒者阶层。

实际文件中对每个阵营的描述详尽，每个阵营都包含：
- 定位
- 核心收入/收益
- 组织结构（多条业务线）
- 生存/扩张动机
- 常见执行动作
- 对外关系逻辑（逐一分析与其他阵营的关系）

如水源商会的"据点费"（2 合 9mm/月）、"拍卖人口抵债"等细节。

---

## 六、常驻世界角色

6 名常驻角色。

实际文件增加了详细的调用原则：
- 5 步调用前判断
- 速记调用表
- 组合调用表（如谢尔盖+格奥尔基 = 武力执行+交易秩序）
- 回合内调用顺序
- 常见误用列表

---

## 七、防幻觉机制

多层防护：

### 7.1 信息流控制

| 规则 | 说明 |
|------|------|
| **无同步块机制** | 不存在 WM 推送的同步块；WSK/WER 通过强语义提取 WM Scene 叙事中的已成立变化 |
| **库存不可脑补** | 只能从最近官方 State Update 读取 + 本轮 Scene 叙事中明确写出的增减 |
| **场景隔离** | 后台可知全局，但角色只能知道亲见亲闻/可信转述/无线电 |
| **Knowledge Scope** | 固定取值：world-only / local-only / party-known / publicly-known |
| **Snapshot 权威层级** | WM 自维护为临时估算，WSK 输出为权威 |

### 7.2 NPC 反全知机制

NPC 反全知机制详尽，包含：
- NPC 信息只允许来自 4 层：亲眼所见、刚收到且可信的消息、符合职业经验的推断、无法确认的不确定猜测
- 职业只提供"可说范围"，不提供全知能力
- 天气与生存压力必须压缩 NPC 表达质量
- critical/dying 状态下的 NPC 不应输出条理完整的分析
- 更优先的 NPC 输出类型：观察、偏好、条件、拒绝、催促、警告、讨价还价

### 7.3 主角光环禁止规则

### 7.4 抗跑偏硬约束

但实际文件中条目更多更细（8 条主要约束 + 多条子约束）。

---

## 八、动态锚点系统

实际文件增加了：
- 锚点非冻结硬规则（锚点协议不是把角色冻结成背景板）
- 同行平台角色规划边界
- Anchor 刷新判定清单（必须刷新 / 可沿用旧锚点 / 常见误刷）
- 长时间未出场角色的低成本维持模板（`[Role Anchor - Keep]`）
- 多角色批量维护模板（`[Role Anchor Batch]`）

---

## 九、文件结构分析

### 9.1 实际文件结构

```
├── Prompt_File/
│   ├── 0-1_Private_Details.md                    # 聊天室 Private Details 字段（全局硬规则层）
│   ├── 0-2_Scenario.md                           # 聊天室 Scenario 字段（静态世界共识层）
│   ├── 1-1_WorldMaster_Scene.md                  # WM 场景与概率入口
│   ├── 1-2_WorldMaster_Additional_Personality_Details.md  # WM 裁定补充层
│   ├── 1-3_WorldMaster_Extra_Details.md          # WM 世界扩展层
│   ├── 2-1_WorldStateKeeper_Scene.md             # WSK 场景锚点
│   ├── 2-2_WorldStateKeeper_Additional_Personality_Details.md  # WSK 详细规则层
│   ├── 2-3_WorldStateKeeper_Extra_Details.md     # WSK 模板与回执层
│   ├── 3-1_WorldEventRecorder_Scene.md           # WER 场景锚点
│   ├── 3-2_WorldEventRecorder_Additional_Personality_Details.md  # WER 详细规则层
│   └── 3-3_WorldEventRecorder_Extra_Details.md   # WER 模板与回执层
├── ASSEMBLY_CHECKLIST.md                         # 运行前装配清单
├── PROJECT_ANALYSIS.md                            # 项目分析报告（本文件）
├── OURDREAM_PLATFORM_REFERENCE.md                # OurDream.ai 平台参考知识
├── tools_prompt.md                               # 工具/调试提示词
└── .gitignore
```

### 9.3 现行五层记忆模型

1. **Layer 0：World Canon Layer** → `0-2_Scenario.md`（聊天室 Scenario 字段，静态世界共识）
2. **Layer 1：Global Protocol Layer** → `0-1_Private_Details.md`（聊天室 Private Details 字段，全局硬规则）
3. **Layer 2：WM Execution Layer** → `1-1 / 1-2`
4. **Layer 3：World Mechanism Layer** → `1-3`
5. **Layer 4：Runtime Ledger Layer** → WSK / WER 的运行账本

### 9.4 当前字符占用（基于 ASSEMBLY_CHECKLIST.md）

| 文件 | 字段 | 字符数 | 平台限制 |
|------|------|-------:|----------|
| `0-2` | `聊天室 Scenario 字段` | — | 50,000 |
| `0-1` | `聊天室 Private Details 字段` | — | 15,000（12,000 内更优）|
| `1-1` | WM 角色卡 Scene | — | 50,000 |
| `1-2` | WM 角色卡 Additional | — | 50,000 |
| `1-3` | WM 角色卡 Extra | — | 100,000 |
| `2-1` | WSK 角色卡 Scene | — | 50,000 |
| `2-2` | WSK 角色卡 Additional | — | 50,000 |
| `2-3` | WSK 角色卡 Extra | — | 100,000 |
| `3-1` | WER 角色卡 Scene | — | 50,000 |
| `3-2` | WER 角色卡 Additional | — | 50,000 |
| `3-3` | WER 角色卡 Extra | — | 100,000 |

---

## 十、系统质量评估

### 10.1 架构设计评分

| 维度 | 评分 | 说明 |
|------|------|------|
| **职责分离** | ★★★★★ | WM 裁定权、WSK 记账权、WER 归档权三权分立，边界清晰 |
| **防幻觉设计** | ★★★★★ | 多层防护：强语义提取边界、库存不可脑补、场景隔离、Knowledge Scope、防重复提交 |
| **长期一致性** | ★★★★★ | WSK 硬状态管理 + WER 历史压缩 + 不可压缩锚点，支持 100+ 日运行 |
| **平台适配** | ★★★★☆ | 充分利用手动触发、字段分层、Pinned Memory；但受限于平台上下文窗口 |
| **可扩展性** | ★★★★☆ | 九宫格地图、搜刮分层、阵营关系都预留了扩展空间 |
| **规则完备性** | ★★★★★ | 覆盖了移动、搜刮、交易、战斗、报复、伤病、死亡、场外演化等全场景 |
| **可维护性** | ★★★☆☆ | 规则重复度高，跨文件引用复杂，修改一处可能需要联动多处 |

### 10.2 关键风险与问题

#### 风险 1：规则重复度高（中风险）

多个核心规则在多个文件中反复出现，例如：
- "平台优先锚点"在 1-1、1-2、1-3、2-1、2-2、2-3、3-1、3-2、3-3 中都有
- "手动触发机制"描述在 WSK 和 WER 的所有三层字段中重复
- "Knowledge Scope 不可自动升级"规则在 0-1、2-2、2-3、3-2、3-3 中反复出现

**影响**：修改一条规则时需要同步更新多个文件，否则可能产生规则分叉。

**建议**：考虑将高频重复的"平台机制"声明统一为最短引用形式，详细内容只在 0-1 中维护一份。

**审计状态（2026-07-22）**：⚠ **部分缓解**。v4 已建立 echo 链（0-1 权威 / 1-1~3-3 最短引用），但跨角色文件的"平台优先锚点"仍存在重叠表述；后续批次需进一步压缩。

#### 风险 2：WM 提示词总量过大（中风险）

WM 的三个角色字段 + Custom Instructions 合计约 **75,282 字符 / 约 1,170 行**。其中 1-3 单独就有 49,019 字符 / 697 行。

**影响**：大模型在处理超长提示词时可能出现"后段遗忘"，导致 1-3 后半段的阵营详解、常驻角色、场外演化等规则被忽略。

**建议**：考虑将 1-3 中的地点列表做进一步分区化索引，或在 Scene 层按需调用而非全量加载。

**审计状态（2026-07-22）**：⚠ **仍存在**。v4.4 迁出 0-1 至 Private Details（≥50K 限额解除）后，WM 总提示词压力部分缓解；但 1-3 仍为 33,144 字节（约 92% 旧 Custom Instructions 限额），未拆分至独立参考文件；分区化索引列入 P1 仍未评估。

#### 风险 3：Custom Instructions 接近上限（低风险）

0-1 当前 11,720 字符，平台上限 15,000。ASSEMBLY_CHECKLIST 已指出应控制在 12,000 内。

**影响**：后续若继续添加全局协议，可能突破上限或进入表现下降区间。

**审计状态（2026-07-22）**：✓ **已解决**。v4.4 2026-07-20 迁入 Private Details 字段后限额解除（≥50,000 限额 ≈ 31.65% 占用）；本风险不再适用。

#### 风险 4：WSK 模板复杂度极高（中风险）

2-3 的 WSK 输出模板包含 **双视图（最小/完整）+ 7 种 Snapshot + 12 条最低提交标准 + 大量条件补充规则**，总计 432 行。

**影响**：大模型在执行 WSK 角色时可能难以同时满足所有格式要求，特别是 Snapshot 分层输出和压缩规则校验。

**审计状态（2026-07-22）**：⚠ **部分缓解**。决策表已统一为 DO/REJECT/SILENT 3 表（commit `e583e13`），降低了模板多变性；但 2-3 模板本身未缩减；简化版快速参考未提供。

### 10.3 亮点评价

#### 亮点 1：确定性伪随机

概率裁决使用公开公式 `Seed = (Day ID × 31 + Turn ID × 17 + Event Offset) mod 100`，保证可复现、可追溯、多事件可分离。这在 AI 游戏系统中是罕见的设计。

#### 亮点 2：库存三层 + Snapshot 审计

WM 输出临时 Snapshot → WSK 审计并输出权威版本 → WM 下一轮替换。这种"前台估算 + 后台审计"的双层校验机制有效防止了库存漂移。

#### 亮点 3：Knowledge Scope 信息流控制

4 级可见性枚举 + 不可自动升级规则 + 传播事件依赖，构建了一套完整的信息流控制系统。这是防止 AI "全知叙事"的关键机制。

#### 亮点 4：报复链的动态生命周期

报复链不是简单的"有/无"状态，而是有完整的生命周期：形成 → 活跃 → 降温 → 冻结 → 关闭/重新激活。这让世界的人际关系更加真实可信。

#### 亮点 5：场外演化的轻量设计

通过时间门槛（白天段切换 / 12 小时累计）触发轻量检查，只同步"已成立变化"，不制造虚假事件。这让世界在玩家不活跃时也能缓慢演化，同时避免了过度生成。

#### 亮点 6：NPC 反全知的系统性设计

从 5 问到 4 层信息源，从职业可说范围到压力压缩表达质量，构建了一套系统性的 NPC 认知边界控制机制。这是保证世界可信度的核心支柱之一。

---

## 十一、项目规模统计

| 指标 | 实际数值 | 说明 |
|------|----------|------|
| 落盘文件总数 | 11 | 0-1 / 0-2 / 1-1~1-3 / 2-1~2-3 / 3-1~3-3 |
| 项目参考文档 | 2 | PROJECT_ANALYSIS.md（本文件） + OURDREAM_PLATFORM_REFERENCE.md |
| 辅助文档 | 2 | ASSEMBLY_CHECKLIST.md（运行前装配清单） + tools_prompt.md（调试提示词） |
| 聊天室级字段 | 2 | 聊天室 Scenario 字段（0-2）+ 聊天室 Private Details 字段（0-1） |
| 角色卡字段 | 9 | WM 3 + WSK 3 + WER 3 |
| 平台字段映射 | 5 类 | Scenario / Private Details / Scene / Additional / Extra |
| `0-2_Scenario.md` 字节数 | 46,434 | 承载静态世界共识（50,000 限额内） |
| `0-1_Private_Details.md` 字节数 | 17,763 | 承载全局硬规则（≥50,000 实测限额） |
| 1-1 字节数 | 12,771 | WM Scene（50,000 限额内） |
| 1-2 字节数 | 18,421 | WM Additional（50,000 限额内） |
| 1-3 字节数 | 33,144 | WM Extra（100,000 限额内） |
| 2-1 字节数 | 7,202 | WSK Scene |
| 2-2 字节数 | 28,513 | WSK Additional |
| 2-3 字节数 | 14,134 | WSK Extra |
| 3-1 字节数 | 8,978 | WER Scene |
| 3-2 字节数 | 7,758 | WER Additional |
| 3-3 字节数 | 8,496 | WER Extra |
| 提示词总字节数 | ~203,614 | 11 个落盘文件合计 |
| 分区数量 | 9 | 九宫格固定拓扑 |
| 子地点数量 | 99 | 1-3 Extra Details |
| 常驻角色 | 6 | 阵营内外长期出没 |
| 主要阵营 | 5 + 拾荒者 | — |
| 天气状态 | 9 | 湿润大陆性 Dfb |
| 生存压力轨道 | 5 | 疲劳/体温/脱水/饥饿/伤病 |
| 关系追踪维度 | 3 核心轴 + 按需次级 | Trust/Hostility/Revenge Drive |
| 搜刮分层 | T1-T4 | 按地点标签 + tier |
| 库存层级 | 3 | 随身/据点核心/记忆库存 |
| 决策表 | 3 × 2 | WSK / WER 各 3 张：DO / REJECT / SILENT |

---

## 十二、总结

### 本项目的核心价值

这是一个在**提示词工程领域具有参考价值的复杂系统设计**，其核心创新包括：

1. **三权分立架构**：裁定权（WM）、记账权（WSK）、归档权（WER）分离，通过强语义提取协同
2. **Snapshot 审计机制**：前台估算 + 后台审计的双层校验，防止库存漂移
3. **确定性伪随机**：公开公式保证可复现、可追溯
4. **Knowledge Scope 信息流控制**：4 级可见性 + 不可自动升级 + 传播事件依赖
5. **报复链动态生命周期**：形成 → 活跃 → 降温 → 冻结 → 关闭/重新激活
6. **NPC 反全知系统**：5 问 + 4 层信息源 + 职业可说范围 + 压力压缩表达
7. **场外演化轻量设计**：时间门槛触发 + 只同步已成立变化
8. **历史压缩策略**：分层压缩 + 不可压缩锚点，支持 100+ 日运行

### 改进建议优先级

| 优先级 | 建议 | 原因 | 状态（2026-07-22 审计） |
|--------|------|------|-------------------------|
| **P0** | 修复 §13 中的 P0 引用源错误（2-1 L80 / L87 引用 WM Extra Details，实际数据在 0-2） | 提示词可能引用死链，造成行为失准 | ✓ 已完成（commit `045e57d`，§13.11） |
| **P1** | 修复 §13 中的 P1 编号 / 数字 / 术语冲突 | 跨文件一致性 | ✓ 已完成（commits `e583e13` / `6240226` / `2abafe5`，§13.11） |
| **P1** | 减少跨文件规则重复，核心规则只在 0-1 维护一份 | 降低维护成本和规则分叉风险 | ⚠ 部分缓解（v4 echo 链已建；仍存在重叠表述） |
| **P1** | 考虑将 1-3 的地点数据做分区化索引或拆分到独立参考文件，1-3 只保留索引 | 降低 WM 提示词总量，缓解后段遗忘 | ⚠ 仍存在（1-3 = 33,144 字节，未拆分） |
| **P2** | 为 WSK 模板提供简化版快速参考，降低执行复杂度 | 提高后台角色执行质量 | ⚠ 部分缓解（决策表已统一；模板仍复杂） |
| **P2** | 修复 1-3 中南区的重复子地点条目 | 数据一致性 | ⏳ 未评估 |
| **P2** | 修复 §13 中的 P2 段名引用与术语细节 | 长期可维护性 | ✓ 部分完成（commit `1522e57`，§13.11） |
| **P3** | 更新 ASSEMBLY_CHECKLIST 中的行数/字符数数据 | 保持文档准确性 | ⏳ 未评估 |

### 近期重大变更

| 变更 | 影响 |
|------|------|
| 文件重命名 | `0-1_CustomInstructions.md` → `0-1_Private_Details.md`；`PROJECT_ANALYSIS_v2.md` → `PROJECT_ANALYSIS.md` |
| 新增文件 | `0-2_Scenario.md`（聊天室 Scenario 字段）；`OURDREAM_PLATFORM_REFERENCE.md`（平台功能参考） |
| 移除文件 | `History_Ledger_Template.md`（并入 WER 输出模板）；`PROJECT_ANALYSIS.md`（v1 旧版） |
| 字段引用统一 | 全平台字段引用统一为「聊天室 Scenario 字段」/「聊天室 Private Details 字段」 |
| WM 叙事视角 | 第二人称（"你"）三层强制：0-1 完整规则 + 1-1 顶部提醒 + 1-2 绝对规则 0b |
| 记忆模型 | 四层 → 五层（新增 Layer 0 = World Canon Layer = 0-2） |
| WSK 输出格式 | 软标签统一为 `[State Update]`（单行 header + 子段格式）；旧版 `[WSK 账本]` / `[State Commit Receipt]` 全清；27 处替换跨 9 个文件 |
| WM 状态栏硬约束 | `[主要状态]` 三层强制：0-1 `[状态栏硬约束]` 独立段（6 项固定字段 + 出场次序 + 正反对照） + 1-1 独立段 + 1-2 绝对规则 0c；与 [叙事视角硬约束] 平行 |
| 跨文件引用 | 唯一权威引用协议：本文件 §X.Y；不允许"派生文档" |
| **架构审计合并** | **取消"同步块 (Sync Blocks)"通讯机制 → 强语义提取；WSK / WER 决策表统一为 DO / REJECT / SILENT（3 张）；移除接受回执 `[Event Commit Receipt]` 等；保留拒绝回执 `[Commit Rejected]` / `[Event Commit Rejected]`；`[Resolution]` / `[Probability Check]` 等展示块仅供用户阅读。详见 §13** |
| **D1 锚定日期** | **`D1 = 10月1日`**（灾变后第 3 年开局日）；开局季节 = 秋季；开局温度层 = Cool；开局典型天气 = Overcast / Fog / Light Rain / Windy；月份推进规则：D1-D31=10月 / D32-D61=11月 / D62-D92=12月（冬开始）/ D93-D120=1月 / D121-D151=2月 / D152-D181=3月（春开始）；WSK 在 State Update 中正式落盘月份变更。详见 `0-2_Scenario.md` §季节锚点 |

---

## 十三、架构审计结果（2026-07-22）

### 13.1 审计范围

- 11 个落盘文件（`Prompt_File/` 下 0-1 / 0-2 / 1-1~1-3 / 2-1~2-3 / 3-1~3-3）
- 2 个聊天室级字段（`聊天室 Scenario 字段` + `聊天室 Private Details 字段`）
- 3 个系统角色（WM / WSK / WER）的职责分工与功能能效性

### 13.2 核心审计结论

| 维度 | 审计结论 | 状态 |
|------|----------|------|
| 通讯机制 | WM 不推送同步块；WSK/WER 强语义提取 Scene 叙事中已明确写出的已成立变化 | **已定案** |
| 接受回执 | 不输出（账本/Ledger 本身即隐式接受） | **已移除** |
| 拒绝回执 | WSK `[Commit Rejected]` / WER `[Event Commit Rejected]` 字段不完整时返回 | **保留** |
| 展示块隔离 | `[Resolution]` / `[Probability Check]` / 子结构块仅供用户阅读 | **已隔离** |
| 决策表 | WSK / WER 各 3 张：DO / REJECT / SILENT | **已统一** |
| 沉默协议 | 无新成立变化可提取时静默；成功提交后无额外文字 | **已统一** |
| Pinned Memory | 持久化层由人类管理员维护（不属 AI 定义范围） | **不** |
| 平台字段映射 | 5 类（Scenario / Private Details / Scene / Additional / Extra） | **无变化** |

### 13.3 顶层架构图

```
                        ┌──────────────────────────────────┐
                        │  聊天室 Private Details 字段      │
                        │  (0-1_Private_Details.md)         │
                        │  全局硬规则 + 通讯协议 + 输出格式 │
                        └──────────────────────────────────┘
                                       ▲ 共享读取
                        ┌──────────────────────────────────┐
                        │  聊天室 Scenario 字段             │
                        │  (0-2_Scenario.md)                │
                        │  静态世界共识（背景/地图/阵营/硬规则）│
                        └──────────────────────────────────┘
                                       ▲ 共享读取
   ┌──────────────────┐    ┌─────────────────────┐    ┌──────────────────┐
   │   WM 角色卡      │    │   WSK 角色卡        │    │   WER 角色卡     │
   │   1-1 Scene      │    │   2-1 Scene         │    │   3-1 Scene      │
   │   1-2 Additional │    │   2-2 Additional    │    │   3-2 Additional │
   │   1-3 Extra      │    │   2-3 Extra         │    │   3-3 Extra      │
   └────────┬─────────┘    └──────────┬──────────┘    └─────────┬────────┘
            │                         │                          │
            │ 唯一前台                  │ 后台账本                 │ 后台档案
            │ Scene + 状态栏            │ [State Update]          │ [WER 历史]
            │                         │                          │
            │      强语义提取          │ 强语义提取                │
            └─────────────────────────┴──────────────────────────┘
                                          ▲
                                          │
                                    Pinned Memory
                                  (人类管理员持久化)
```

### 13.4 三角色职责矩阵

| 维度 | WM | WSK | WER |
|------|----|----|----|
| **前台/后台** | 唯一前台 | 后台 | 后台 |
| **核心职责** | Scene + 状态栏 | 硬状态账本 | 长期历史 |
| **触发方式** | 持续运行 | 用户手动点击 | 用户手动点击 |
| **写入场景** | 每个用户回合 | 提取到变化时 | 提取到事件时 |
| **输出标签** | 无（Scene） | `[State Update]` | `[WER 历史]` |
| **拒绝回执** | 不适用 | `[Commit Rejected]` | `[Event Commit Rejected]` |
| **接受回执** | 不适用 | 不输出 | 不输出 |
| **决策表** | 内部规则 | DO/REJECT/SILENT | DO/REJECT/SILENT |
| **展示块处理** | 生成 | **不解析** | **不解析** |
| **持久化** | 平台上下文 | WSK 自维护账本 | 用户拷贝到 Pinned Memory |
| **读取 Scenario** | ✓（每 Scene） | ✓（边界校验） | ✓（重大事件判定） |
| **读取 Private Details** | ✓ | ✓ | ✓ |
| **5 层记忆位置** | Layer 2-3 | Layer 4（账本） | Layer 4（Ledger） |

### 13.5 聊天室字段分工

| 维度 | 聊天室 Scenario 字段（0-2） | 聊天室 Private Details 字段（0-1） |
|------|----------------------------|----------------------------------|
| 字符限额 | 50,000 | ≥50,000（实测） |
| 内容性质 | 静态世界共识 | 全局硬规则 + 通讯协议 |
| 回答问题 | 世界是什么 | 世界怎么运行 |
| 共享范围 | WM/WSK/WER + 玩家 + NPC | WM/WSK/WER |
| 修改频率 | 世界设定稳定时极少改动 | 协议微调时频繁改动 |
| 主要覆盖 | 背景 / 环境 / 季节 / 天气 / 压力 / 敌对 / NPC / 地图 / 子地点 / 搜刮 / 硬规则 / 4 阵营 / 弹药口径 / 死亡公开性 | 通讯机制 / 输出格式 / 决策表 / 字段语义 / 状态栏硬约束 / 视角硬约束 / 不可越界清单 |
| 失效模式 | 描述与当前运行不一致 | 协议与平台加载不符 |

### 13.6 角色卡文件分工（9 个）

| 角色 | Scene (50K) | Additional (50K) | Extra (100K) |
|------|-------------|------------------|--------------|
| **WM (1-x)** | 平台锚点 + 视角硬约束 + 状态栏 + 概率块位置 | 决策心理预设 + 5 轨压力时间阈值 + 敌对 4 维 + 关系 3 轴 + 报复链 + 伤病恶化 + 交易 2 档 + 搜刮细则 | 动态机制（势力周期活动表 / 锚点协议 / 据点结构基线 / 角色维持 / 死亡公开性） |
| **WSK (2-x)** | 平台锚点 + 输出格式（`[State Update]` 三模式） + 决策表 + 不可越界 + 7 字段事务块 | 详细规则（10 大类必记 + 势力活动追踪 + 环境压力记录 + 库存 3 层 + 据点 4 分类 + 8 条结构 + 提交前检查） | 输出模板（默认视图 / 完整视图 / 极简回执 / 拒绝回执 / 压缩规则 / Availability 6 档 / External Location / Inventory Transaction） |
| **WER (3-x)** | 平台锚点 + 沉默协议 + 3 决策表 + 输出段名（必出 2 段 + 条件性 3 段）+ 触发频率 | 决策心理 + 重大事件清单 + 压缩策略 + 不可压缩锚点 + 结构变化 5 字段 + 据点首次建立 5 字段 | 压缩规则 + 输出模板（必出段 + 条件性段）+ 死亡公开性处理 |

### 13.7 功能 / 机制能效性总览

| 机制 | 设计文件 | 触发条件 | 生效情况 |
|------|----------|----------|----------|
| Scene 强语义提取 | 0-1 + 1-1 + 2-1 + 3-1 | 用户手动点击 WSK/WER | ✓ 生效 |
| 状态栏（6 字段） | 0-1 + 1-1 + 1-2 | 每个 Scene 必出 | ✓ 生效 |
| `[State Update]` 三模式 | 2-1 + 2-2 + 2-3 | WSK 输出 | ✓ 生效 |
| `[WER 历史]` 必出 2 段 + 条件性 3 段 | 3-1 + 3-2 + 3-3 | WER 输出 | ✓ 生效 |
| 拒绝回执 | 2-1 / 3-1 | 字段不完整 | ✓ 生效 |
| DO/REJECT/SILENT 决策表 | 2-2 + 3-2 | 每次点击 | ✓ 生效 |
| 7 字段事务块 | 0-1 + 1-1 + 2-1 | 跨层转移 | ✓ 生效 |
| 5 轨压力轨道 | 0-2 + 1-2 + 2-2 | 每次结算 | ✓ 生效 |
| 6 势力周期活动 | 1-3 + 2-2 | 白天段切换 / 12h 累计 | ✓ 生效 |
| Faction Exposure Tracker | 1-3 + 2-2 | 劫掠者伪装 | ✓ 生效 |
| 据点结构 8 条 | 1-3 + 2-2 + 2-3 | 首次建 / Delta | ✓ 生效 |
| 库存 3 层 | 2-2 + 2-3 | 每次入账 | ✓ 生效 |
| Knowledge Scope 4 档 | 0-1 + 各角色 | 每次入账 | ✓ 生效 |
| Death Publicity 4 档 | 0-2 + 1-3 + 2-2 | 死亡事件 | ✓ 生效 |
| NPC 反全知 | 0-2 + 1-1 | NPC 每次发言 | ✓ 生效 |
| NPC 锚点协议 | 1-3 | 长时间未出场 | ✓ 生效 |
| 第二人称叙事 | 0-1 + 1-1 + 1-2 | 每个 Scene | ✓ 生效 |
| Pinned Memory | 平台外（人类） | 用户手动 | ✓ 生效（不属 AI） |

### 13.8 关键问题清单（22 个；第一轮 14 个 + 第二轮 8 个）

#### P0 阻塞（2 个）

| # | 文件 | 行 | 问题 | 修复方向 |
|---|------|----|----- |----------|
| P0-1 | `2-1_WorldStateKeeper_Scene.md` | L80 | "不复制 World Master 的 Extra Details 表格"（弹药口径实际在 0-2） | 改为"不复制聊天室 Scenario 字段 §弹药口径换算表" |
| P0-2 | `2-1_WorldStateKeeper_Scene.md` | L87 | "见 World Master 的 Extra Details"（Death Publicity 实际在 0-2） | 改为"见聊天室 Scenario 字段 §死亡公开性 4 档枚举" |

#### P1 重要（11 个；第一轮 5 + 第二轮 6）

| # | 文件 | 行 | 问题 | 修复方向 |
|---|------|----|----- |----------|
| P1-1 | `1-3_WorldMaster_Extra_Details.md` | L176 | "每周期（白天段 6h）"与 L34 定义 14h 矛盾 | 改为"每周期（白天段 14h）" |
| P1-2 | `1-3_WorldMaster_Extra_Details.md` | L5 / L196 | §十二 身份暴露风险段名编号 | 统一为 `[身份暴露风险]`（去掉 § 前缀） |
| P1-3 | `1-1_WorldMaster_Scene.md` | L119 | 引用 `§Inventory Snapshot 压缩规则` 段名不存在 | 改为 `WSK 的 Additional Personality Details §精简库存与盘点规则` |
| P1-4 | `1-3_WorldMaster_Extra_Details.md` | L17-18 | "平台角色和世界角色都会因脱水...死亡"（术语错） | 删除"平台角色和"或统一为"系统角色" |
| P1-5 | `0-2_Scenario.md` L3 + 1-3 + 2-2 | 多处 | "4 阵营" vs "6 势力" 冲突 | 统一为"6 势力"（4 阵营 + 拾荒者 + 劫掠者兄弟会） |
| **P1-6** | `1-3_WorldMaster_Extra_Details.md` L168 + `2-3_WorldStateKeeper_Extra_Details.md` L29 | — | "Base Structure Snapshot 每次必须输出完整基线" 与 2-3 L29 例子 "物资不变(见上一轮完整账本)" 矛盾 | 1-3 改为"首次建账 + Base Structure Delta 时输出完整基线；日常用 Snapshot 简表" |
| **P1-7** | `0-2_Scenario.md` L60 + `2-2_WorldStateKeeper_Additional_Personality_Details.md` L83 | — | 温度层术语不一致：0-2 = `Mild / Cool / Cold / Bitter Cold / Heat`；2-2 = `Bitter Cold / Cold / Cool / Mild / Warm` | 统一为 `Mild / Cool / Cold / Bitter Cold / Heat`（2-2 移除 Warm） |
| **P1-8** | `0-1_Private_Details.md` L121 + `2-3_WorldStateKeeper_Extra_Details.md` L20 | — | WSK 提取最小集含 `Base Structure`，但 2-3 L20 子段定义无 `Base Structure Snapshot` | 2-3 L20 子段补 `Base Structure Snapshot:` |
| **P1-9** | `3-3_WorldEventRecorder_Extra_Details.md` L99-101 + `3-1_WorldEventRecorder_Scene.md` L114 | — | WER 死亡公开性读取源矛盾：3-3 说"从 WM Scene 读"；3-1 说"可读 WSK State" | 统一为"WER 优先从 WSK State Update 读，缺失时按 hidden 处理" |
| **P1-10** | `2-1_WorldStateKeeper_Scene.md` | 缺 | WSK 缺"上次发言位置"维护规则（WER 3-1 L34-39 有明确维护） | 在 2-1 沉默协议段补"上次发言位置 = 我最后一次 `[State Update]` 输出位置" |
| **P1-11** | `0-1_Private_Details.md` L21 + `1-1_WorldMaster_Scene.md` L27 | — | "WM 发给 WSK 的同步除 `Location Change` 外…" 是过时的"同步块"残留术语 | 改为"WM 在 Scene 叙事中体现移动时…" |

#### P2 一般（9 个；第一轮 7 + 第二轮 2）

| # | 文件 | 行 | 问题 | 修复方向 |
|---|------|----|----- |----------|
| P2-1 | `2-2_WorldStateKeeper_Additional_Personality_Details.md` | L115 | "§十二 身份暴露风险 5 级响应"段名不存在 | 改引用名为 `[身份暴露风险]`（与 P1-2 同步） |
| P2-2 | `1-1_WorldMaster_Scene.md` | L66 | 概率块输出位置 | 确认是否仍需保留（无同步块通讯后） |
| P2-3 | `1-1_WorldMaster_Scene.md` | L82-103 | 反煽情元规则 + 5 轮内高频词禁用 | 需 L1 检查法去污染 |
| P2-4 | `2-3_WorldStateKeeper_Extra_Details.md` | L82-85 | `[State Update]` 通用压缩规则 | 确认段名与本文件 §2.2 一致 |
| P2-5 | `0-1_Private_Details.md` | L132 | 重大事件清单 | 与 WER 3-2 重大事件清单去重 |
| P2-6 | `0-1_Private_Details.md` | L134 | 5 字段结构变化 | 与据点首次建立 5 字段统一表述 |
| P2-7 | `0-1_Private_Details.md` | L91-103 | 状态栏 6 字段固定 | 与 WM 状态栏 1-1 引用统一 |
| **P2-8** | `1-1_WorldMaster_Scene.md` L106-110 | — | Scene ≤800 字符 + ≤3 段 与 0-1 L69 "2-3 段自然述事" 紧贴边界 | 维持现状；触发率受 LLM 输出影响 |
| **P2-9** | `0-1_Private_Details.md` L43 | — | LLM 上下文窗口限制下长 Session 后 WSK 输出可能不在上下文 | 不可控；标记为已知风险 |

### 13.9 修复优先级建议

| 阶段 | 任务 | 涉及问题 | 影响 |
|------|------|----------|------|
| **批次 1（P0）** | 修复 2-1 L80 / L87 引用源 | P0-1, P0-2 | 立即可执行；防止 WSK 因引用死链而误判 |
| **批次 2A（P1 术语统一）** | 平台角色/系统角色 + 4 阵营/6 势力 + 温度层 Heat/Warm + 旧同步块术语 | P1-4, P1-5, P1-7, P1-11 | 跨文件术语一致；防止 LLM 解释分裂 |
| **批次 2B（P1 数字/段名修正）** | 白天段 6h/14h + §十二 段名 + 引用段名 | P1-1, P1-2, P1-3 | 段名 / 数字一致；防止引用死链 |
| **批次 2C（P1 字段/语义补全）** | 据点结构输出策略 + WSK 子段补全 + WER 读取源 + 上次发言位置 | P1-6, P1-8, P1-9, P1-10 | 字段与语义对齐；防止行为分裂 |
| **批次 3（P2）** | 段名细节 + 风险标注 | P2-1 ~ P2-9 | 长期可维护性 |

### 13.10 审计原则声明

本次审计遵循以下原则：

1. **冲突以审计结果为准**：本节内容与项目其它文档冲突时，以本节为准
2. **L1 检查法**：每行必须为可执行指令或定义；无元数据污染；无审计者口吻
3. **SSOT 原则**：本文件是项目设计及说明的唯一文档；跨文件引用通过本文件 §X.Y 定位
4. **机制优先于文档**：若文档描述与已确立机制冲突，机制优先
5. **已废弃机制保留历史记录**：所有已废弃机制在 §2.3 列出，便于回溯

### 13.11 修复执行记录（2026-07-22 完成）

| 批次 | 内容 | commit | 项数 | 文件 | 风险 | 状态 |
|------|------|--------|------|------|------|------|
| **1** | P0 引用源（2-1 L80/L87） | `045e57d` | 2 | 1 | 无 | ✓ 已完成 |
| **2A** | P1 术语统一 | `e583e13` | 4 | 4 | 低 | ✓ 已完成 |
| **2B** | P1 数字/段名 | `6240226` | 3 | 2 | 低 | ✓ 已完成 |
| **2C** | P1 字段/语义 | `2abafe5` | 4 | 4 | 中 | ✓ 已完成 |
| **3** | P2 段名清理 | `1522e57` | 1（实质）+ 8 个非问题 / 风险标注 | 1 | 低 | ✓ 已完成 |

**总计**：**14 项实质修改 / 13 个文件改动 / 5 个 commit**，全部已推送至 `main`。
**P2 非问题清单**（执行验证后发现无需修改）：
- P2-2（1-1 L66 概率块位置）：保留，[Probability Check] 仍由 WM 输出供用户阅读
- P2-3（1-1 L82-103 反煽情元规则）：保留，元规则层不需 L1 重构
- P2-4（2-3 L82-85 压缩规则）：保留，与 §2.2 一致
- P2-5（0-1 L132 重大事件清单）：无重复，3-2 L70 仅引用
- P2-6（0-1 L134 结构变化 5 字段）：已显式标注"与据点首次建立 5 字段不同"，无须统一
- P2-7（0-1 L91-103 状态栏 6 字段）：0-1 为权威源，1-1/1-2 已正确引用
- P2-8（1-1 L106-110 Scene ≤800 字符）：硬约束已正确，受 LLM 实际输出影响
- P2-9（0-1 L43 上下文窗口）：平台机制，提示词层不可控

#### 修复后跨文件一致性复核

| 维度 | 一致性 |
|------|--------|
| 弹药口径引用 | 2-1 L80 → 0-2（一致） |
| 死亡公开性引用 | 2-1 L87 → 0-2（一致） |
| 6 势力 vs 4 阵营 | 0-2 / 0-1 / 1-3 / 1-2（一致为 6 势力） |
| 温度层 5 档 | 0-2 / 2-2（一致为 Mild/Cool/Cold/Bitter Cold/Heat） |
| §十二 段名 | 1-3 L5 / 2-2 L115（一致为 [身份暴露风险]） |
| 白天段时间 | 1-3 L34 (14h) / 1-3 L176 (14h)（一致） |
| 系统角色术语 | 1-3 L17-18（一致，无平台角色残留） |
| WSK 旧同步块术语 | 0-1 L21（一致，Scene 叙事提取） |
| WSK 子段 Base Structure | 0-1 L121 / 2-3 L20（一致，补全） |
| WER 死亡公开性读取源 | 3-3 L99-101（WSK 优先；WM Scene 回退）（一致） |
| WSK 上次发言位置 | 2-1 L37（与 WER 3-1 L34-39 对齐） |

**审计结论**：22 个问题中 14 个已实质修复，8 个为非问题或已知风险。**全部机制现已生效（✓）**。

#### 不在本次修复范围

- `output.md`（untracked 模板文件）
- `OURDREAM_PLATFORM_REFERENCE.md`（平台参考）
- `ASSEMBLY_CHECKLIST.md`（装配清单）
- `tools_prompt.md`（调试）

#### 暂不修复（标记 P2-9）

- LLM 上下文窗口限制：平台机制，不在提示词层可控
- WM Scene ≤800 字符：硬约束已正确，影响仅在 LLM 实际输出

---

## 十四、近期重要版本演进（v1.21-v1.26）

本节记录 v1.21-v1.26 期间的关键机制变更。详细变更历史见 `OPTIMIZATION_ROADMAP_v1.md`。

### 14.1 时间机制重构（v1.21）

**变更**：确立 WM 为唯一时间源。

| 维度 | 原状态 | v1.21+ |
|------|--------|--------|
| 时间字段决定者 | WSK 强语义提取 + REJECT 冲突 | WM 唯一决定 |
| WSK 时间验证 | WSK 验证 + REJECT | WSK 不验证 / 不推算 / 不判定冲突 |
| Official Day 冲突 | REJECT 回执 | 删除机制，信任 WM |
| 月份推算 | WSK 自动推算 | WM 决定，WSK 记录 |
| Current Season/Temperature | WSK 推算 | WM 决定，WSK 信任 |

**影响**：消除 WSK 验证与 WM 决策的冲突，避免日期漂移。

### 14.2 Day 推进 5 种触发（v1.22）

1. 用户明确指令（如"第二天"）
2. 夜间/日落后睡眠醒来
3. WM 自然叙事跨日（日落→日出）
4. 连续行动超 16-18 小时提示疲劳
5. WM 语义判断构成跨日

### 14.3 Season/气温强制查表（v1.22）

WM 输出 [主要状态] 时必须按当前 Day 查 `0-2 §温度分层与日期精细对应` 表确定 Season/气温，允许 ±1 档浮动（需叙事依据）。

### 14.4 WSK 全量状态快照（v1.22）

WSK 每次被点击时，输出变化子段后**必须继续输出当前所记录的全部状态和库存**（Inventory Snapshot / Party Condition / Relationship / Base Structure / 近五日主要事件），供 WM 读取。确保 WM 每轮可独立获取完整世界状态。

### 14.5 Market State 机制删除（v1.22）

WSK 不再追踪/记录/输出市场状态字段。保留：地名（老市场/批发市场）、传播机制（市场传闻）、交易规则（等价 9mm）。

### 14.6 移动硬约束（v1.23 + v1.25 强化）

**v1.23 时间准确性**：
- 1 step = 30 分钟基准，不得压缩
- 跨子区域移动必须显式写 Route + 总 step + 总耗时
- 反例：T178/10:36 跨 3 子区域 → 11:12（+36）违规；正确 12:06（+90）

**v1.25 地理准确性**：
- 跨子区域移动必须严格按 `0-2 §分区邻接` 拓扑书写
- 不得凭印象虚构路径或跳过相邻区
- 涉及据点/势力/资源点时必须经过该点所在子区域
- 例：去码头调度楼（东南区/C）必须经过东南区 N → C

### 14.7 [主要状态]必出规则冗余精简（v1.24）

同一规则原本在 4 个文件重复，导致 LLM 注意力疲劳。v1.24 变体 A：保留 0-1（主权威定义）+ 1-1（WM Scene 必读），删除 1-2 L26 0c. 段。从 4 文件 → 2 文件。

### 14.8 Turn 编号规则补强（v1.26）

在 v1.22 "Turn 编号每轮 +1（与时间独立）" 基础上补强 3 条硬约束：

- 每日从 T1 开始计数（Day 推进时 Turn 重置为 T1）
- 每轮 +1 不可跳号（不可从 T5 直接到 T7）
- 单轮只 +1 Turn（无论本轮内容多复杂）

### 14.9 实验性字段：近五日主要事件（v1.20）

WSK 新增子段：`近五日主要事件:`（按 D 升序，1500 字符总量，150-500 字符/条）。LLM 语义提取该日主要事件（人员/据点/阵营/关系/区域 5 类）。天气/经济变动/路线变动已放弃。

### 14.10 WSK 弹药口径合并（v1.5）

弹药库存按口径（弹药规格）合并记录（如 `9mm×67发`），禁止按武器类别分组。例外：交易/经济场景可临时折算为"等价 9mm 单位"。

---

## 十五、近期版本演进（v1.27-v1.32）

本节记录 v1.27-v1.32 期间的关键机制变更。v1.21-v1.26 变更见 §14，完整历史见 `OPTIMIZATION_ROADMAP_v1.md`。

### 15.1 WSK 输出视图重组（v1.27）

**变更**：废弃 SNAPSHOT 模式概念，改为每次点击输出 = 变化子段（仅 Inventory Delta / Recent Changes）+ 完整视图。无需区分默认/完整视图模式，简化心智模型。

### 15.2 WSK 输出结构去重（v1.28）

**变更**：v1.27 存在 4 处字段重复（Inventory Snapshot / 近五日主要事件 / Base Structure 等），遗漏 3 个关键字段（Human Contact Status / Scavenging Status Snapshot / Survival Anchor Snapshot）。v1.28：变化子段仅保留 2 个（Inventory Delta / Recent Changes），完整视图扩展为 13 字段全集，消除全部重复与遗漏。

### 15.3 WSK 新增 Map Knowledge（v1.29）

**变更**：完整视图新增 Map Knowledge 字段——记录玩家已探索子区域清单 + 已知资源点。近五日主要事件调整至完整视图最末（便于 WM 优先读状态后回看历史）。

### 15.4 删除 Contamination 字段（v1.30）

**变更**：完整视图从 13 字段精简至 11 字段。删除 Contamination（污染）字段——前期低频，污染事件可通过 Recent Changes 描述。涉及文件：0-1 / 2-1 / 2-2 / 2-3。

### 15.5 Inventory Snapshot 输出策略调整（v1.31）

**变更**：从旧规则"超过 10 项必压缩为 ×1组"转为"分类 + 明细"——按功能组归类（武器/弹药/医疗/工具/食物等），但保留每件物品具体信息（口径/数量/状态）。仅普通物资（绳索、容器等）仍可压缩为"具备×1组"。

### 15.6 可消耗物资双轨制计量规范（v1.32）

**变更**：
- **WM 渲染**：采用"容器描述 + 估算重量（≈kg）"双轨格式（如"一小袋大米，约 5kg"）
- **WSK 记录**：提取净重值，**按 kg 统一记录**（"大米×5kg"），剥离容器描述
- **LLM 实时估算优先**：不依赖预设换算表；校准锚点仅作常识参考（非强制）
- **涉及文件**：0-2（主规范 + 校准锚点）/ 1-1（WM 渲染硬约束）/ 2-1（WSK 记录硬约束）/ 2-3（kg 合并规则）

### 15.7 WSK 输出增强：消耗投影 + Active Concerns（v1.33）

**变更**：
- **消耗投影**：Inventory Snapshot 末尾新增资源剩余天数估算（`消耗估算（N人）：食物≈X天 / 饮水≈Y天 / 燃料≈Z天`），WSK 根据队伍人数 + kg 库存自动估算，供 WM 快速判断生存紧迫度
- **Active Concerns**：变化子段之后新增 3-5 项紧急关注点摘要（`[生存]/[人际]/[环境]/[据点]` 四分类），WSK 扫描全部字段自动汇总当前最需 WM 优先处理的事项
- **涉及文件**：0-1（输出格式描述）/ 2-1（输出结构）/ 2-3（模板 + 消耗投影规则 + Active Concerns 定义）

---

### 15.8 [主要状态] 格式精简为 5 段结构（v1.37）

**变更**：
- [主要状态] 由原 8 字段+字段名前缀格式精简为 5 段结构：`D{Day}-T{Turn} {时间} | {位置} | {Season}-{天气}-{气温} | {压力} | {风险}`
- 去除字段名前缀（不写 `Day=` / `位置=` 等），8 项信息按段序隐含；信息完整性不变
- **涉及文件**：0-1（状态栏硬约束 + 标准输出模板）/ 1-1（状态栏硬约束精简版）

### 15.9 v4.3 批次四项机制优化（v1.38）

**变更**：
- **NPC Trust 行为阈值映射**：0-2 新增 Trust 阈值表（0-20 敌对 / 20-40 警惕 / 40-60 中立 / 60-80 友善 / 80-100 信任），定义交易/信息/帮助基调；1-2 关系规则引用。解决 Trust 数值"死数据"问题
- **5 轨压力格式化**：[主要状态] 压力段强制 `{轨道名} {状态级}` 格式，仅列非 stable 轨道，与 WSK Party Condition 对齐
- **消耗估算 WM 驱动化**：消耗投影改为 WM 叙事估算优先（标注 WM判断），WSK 基准值兜底（标注 基准估算）；禁止"富余"等描述词。解决 WSK 公式估算脱离生活经验问题
- **Active Concerns 分类约束**：4 分类边界定义 + 禁止自创类别（如 `[威胁]`）+ 判断示例
- **涉及文件**：0-2 / 1-2 / 0-1 / 2-3 / 1-1

### 15.10 [主要状态] 不输出根因修复（v1.39）

**问题**：v1.37 后 WM 频繁不输出 [主要状态]。

**根因**：v1.37 精简格式时只更新了 0-1 `[状态栏硬约束]`（L106），漏改同文件 `[标准输出模板]`（L139），造成**文件内双重定义矛盾**（新 5 段格式 vs 旧 8 字段格式）。LLM 面对冲突格式产生不确定性，进而回避输出。

**为何全面检查未捕获**（三叠加因素）：
1. 检查锚定修改者心智模型——编辑时漏掉的位置，检查时同样漏掉
2. 双重定义藏在不同名章节（"状态栏硬约束" vs "标准输出模板"），按概念名扫描时"模板"章节被跳过
3. 旧格式用全合法字段名（无禁用旧术语），术语级 grep 全绿——问题在结构而非术语

**修复**：L139 改为纯引用（"以 [状态栏硬约束] 为唯一权威定义，本段不复述格式字符串"），消除双重定义。

### 15.11 基于字段可见性的 SSOT 架构重构（v1.40）

**核心原则（二维判定）**：
- **维度一·可见性**：WM 看不到 WSK 角色卡（2-x），WSK 看不到 WM 角色卡（1-x）；聊天室级字段（0-1/0-2）所有角色共享。跨角色共用概念的权威源**必须**放聊天室级，否则引用是死链。
- **维度二·字段角色**：行为规则 → Scene/Additional；格式模板/表格 → Extra Details；决策表 → Additional。

**权威源布局**：

| 概念 | 权威源 | 依据 |
|------|--------|------|
| [主要状态] 格式 | 0-1（聊天室） | WM 产出 + WSK 提取，跨角色 |
| 5 轨压力 | 0-2（聊天室） | WM/WSK 共用世界机制 |
| WSK 完整视图 11 字段 + 第一行格式 | 2-3（WSK Extra） | WSK 专用格式模板 |
| WSK kg 记录格式 | 2-3（WSK Extra） | WSK 专用格式 |
| DO/REJECT/SILENT 决策表 | 2-2（WSK Additional） | WSK 决策逻辑 |

**实施的 SSOT 化**：
- WSK 11 字段列表：2-3 `[完整视图]` 为唯一权威，2-3 内 L19/L21/L122 + 2-1 改引用
- 5 轨压力状态级：0-2 为权威，0-1/2-3 引用（顺带修复 0-1 漏 `dead` 状态级漂移）
- WSK kg 规则：格式权威归 2-3，提取纪律归 2-1
- 0-1 L149（WM 对 WSK 输出知情）：去精确字段名留结构概览（WM 读实际输出即可，不引用 2-3）

**经验沉淀**：冗余定义本身就是风险源；SSOT 治本优于"保持一致"；改格式前后用字面字符串 grep 枚举所有出现位置（对抗锚定盲区）。全面位置审计确认：项目整体布局健康，无共享概念被困单角色字段。

> **结论修正（2026-07-24）**：本节"无跨角色死链引用"的结论已过期——后续审计发现 6 处跨卡冗余指针（详见 §16.2），其内容均已就地内联故未造成功能失效，但属于会误导 LLM 的死指针。教训：引用审计必须按"引用方角色生成时能否看到被引用内容"逐条验证，而非仅看布局。

---

## 十六、引用有效性审计与引用写法统一（2026-07-24）

### 16.1 背景：装配视角下的引用失效问题

提示词开发时以文件编号（0-1/0-2/1-x/2-x）组织，但装配到平台后：
- **平台没有这些文件名**——聊天室只有 Private Details / Scenario 字段，角色卡只有 Scene / Additional Personality Details / Extra Details 字段；写 `0-2 §月份推进规则` 的引用在运行时无法被解析
- **平台只认角色全称**（World Master / World State Keeper），引用串里的 WM/WSK 简称无法被识别
- **角色卡互不可见**：World Master 生成时看不到 World State Keeper 的字段，反之亦然；跨角色共享只能靠对话历史中的实际输出

### 16.2 审计发现（全库 8 文件逐条扫描）

**① 编号/简称引用 21+ 处**（全部改写）：

| 旧写法 | 新写法 | 分布 |
|---|---|---|
| `0-2 §月份推进规则 / 环境生存压力系统 / 季节锚点 / 温度分层与日期精细对应 / 分区邻接 / 可消耗物资渲染规范 / 世界硬规则 / NPC Trust 行为阈值表 / 非移动动作时间基准` | `聊天室 Scenario 字段 §…` | 0-1 / 1-1 / 2-1 / 2-2 / 2-3 |
| `1-1 §Probability Check` / `1-2 §生存压力时间阈值表` / `1-3 §13 据点结构基线` | `World Master 的 Scene / Additional Personality Details / Extra Details 字段 §…` | 0-2 |
| `WSK Extra Details §弹药口径格式硬约束 / 压缩规则` / `WM Extra Details §演化触发流程` | `World State Keeper / World Master 的 Extra Details 字段 §…` | 0-1 / 0-2 |

**② 跨卡死指针 6 处**（引用方运行时不可读，全部消除）：

| 位置 | 原引用 | 处理 |
|---|---|---|
| 1-1 工具组归类自检 | 「参考 WSK 的 APD §精简库存与盘点规则」（唯一指令式跨卡引用，风险最高） | 改为"与最新 [State Update] 实际输出保持分组一致" |
| 2-2 Faction Name | 「官方名以 1-3 §势力周期活动表 为准」 | 本地已枚举 → "以上 6 个官方名即权威枚举" |
| 2-2 Suspicion Level | 「升级判定以 WM Extra Details [身份暴露风险] 为权威」 | 标准内联（单一异常→L1 / 多指标→L2 / 持续3+周期→L3） |
| 2-2 硬下限追踪 | 「（1-3 §硬下限）」 | 删指针（执行规则本就内联） |
| 2-3 场外演化 ×2 | 「按 WM Extra Details §势力活动 Scene 显式叙事规约」 | "以 World Master 已在 Scene 叙事中显式写出为准" |
| 1-3 周期检查数据源 | 「WSK 的 APD 中 Faction Activity Calendar 字段」 | "World State Keeper 最新 [State Update] 实际输出中的…字段" |

**③ 角色内引用说法混乱 28 处**（统一为单一格式）：`本角色 Scene §X` / `本文件 Extra Details` / 裸 `Scene §X` / `以 Extra Details 为准` 等变体 → 统一为 `{角色全称}（本角色）的 {字段名} 字段 §章节`。

### 16.3 引用写法规范（定案）

| 引用类型 | 统一格式 |
|---|---|
| 指向聊天室级字段 | `聊天室 {Private Details / Scenario} 字段 §章节名` |
| 角色内跨字段 | `{角色全称}（本角色）的 {Scene / Additional Personality Details / Extra Details} 字段 §章节名` |
| 跨文件指向某角色卡 | `{角色全称} 的 {字段名} 字段 §章节名`（不带"（本角色）"） |
| 跨角色内容依赖 | 禁止引用对方角色卡；改指"对话历史中的实际输出" |

完整规范与审计方法见 `OURDREAM_PLATFORM_REFERENCE.md §2.1a`。

### 16.4 经验与教训

1. **引用目标必须是"平台实体"**：开发编号与简称只是仓库管理工具，一旦写进提示词引用串就是运行时无法解析的死引用；引用只有三种合法形态——聊天室字段名 / 本角色字段名 / 角色全称+字段名。
2. **跨卡引用必然失效，但失效方式分两种**：内容未内联 = 功能断裂（规则丢失）；内容已内联 = 冗余指针（LLM 可能尝试调用不可见内容并幻觉补全，浪费 token 且引入风险）。指令式引用（"参考 X"）比说明式引用（"权威在 X"）危险得多。
3. **对话历史是跨角色唯一活通道**：任何"A 角色需要 B 角色的规则/数据"的需求，正确解法要么把规则内联到 A 可见处，要么指向 B 的实际输出（[State Update] / [主要状态]）。
4. **引用修复会滚雪球**：每次 SearchReplace 的 diff 都会暴露同模式的漏网引用（本轮 3 次补漏），修复后必须用模式级 grep 终扫（编号模式 + 简称进引用串模式 + 旧变体模式）。
5. **审计结论有保质期**：v1.40 的"无跨角色死链"结论在后续多轮编辑后失效（新引入的引用未过可见性检查）。引用审计应作为每次大批量编辑后的固定工序，而非一次性结论。

---

## 十七、聊天室基础字段层审计：语义分工与放置错位（2026-07-24）

### 17.1 背景：两个聊天室字段的语义分工

0-1（Private Details）与 0-2（Scenario）**可见性完全相同**（全角色共享），区别在语义语境——角色按什么框架理解读到的内容：

| | 0-2 Scenario | 0-1 Private Details |
|---|---|---|
| 定位 | **世界共识**："这个世界是什么" | **系统协议**："这套机器怎么运转" |
| 承载 | 背景/气候/地图/势力/压力/敌对/经济/死亡 | 读取顺序/出场序列/后台通讯/提取规约/输出格式 |
| 判定问句 | 这是世界本身的事实/规律吗？ | 这是角色分工/输出/通讯的规定吗？ |

注意："压力系统""敌对阶梯"也是机制，但属**世界规律**（玩家也需感知）归 0-2；"谁记账、规则在哪、不得越界"是**系统分工**归 0-1。

### 17.2 审计发现与修复（7 项放置错位 + 1 项示例瑕疵）

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| 1 | 0-2 文件头三行 | 系统分工目录（动态机制→WM ED / 决策规则→WSK APD / 心理预设→Scene）混在世界共识文件；其中"角色决策规则（经济/关系/库存）见 WSK APD"与 WM 每轮亲裁的领域重叠，可能诱发 WM 越权谦让或幻觉补全 | 整块迁入 0-1 §平台优先锚点；WSK 决策规则条目加**显式排他声明**（仅该角色适用且仅其可读，其他角色无需关注、不得引用或复现） |
| 2 | 0-2 [Scene 温度与天气硬约束] | "WSK 在 State Update 第一行记录 Weather…"——WSK 记账行为规格放在世界文件，且 0-1/2-3 已有权威定义（三重冗余） | 删除 |
| 3 | 0-1 [地图逻辑] | 九宫格坐标/邻接/step/Route 与 0-2 [地图调用规则] 双重定义（v1.39 同类病灶） | SSOT 化为纯引用；保留 0-2 没有的补点/地图外字段规则 |
| 4 | 0-1 [NPC 发言边界] | 与 0-2 [NPC 反全知/反顾问化机制] 重复摘要 | 改纯引用 |
| 5 | 0-1 [经济规则] | 世界共识（实物计价/硬通货）放进系统协议文件 | 迁入 0-2 §世界硬规则 |
| 6 | 0-1 L4 | "WSK 自己的聊天室 Private Details 字段"——Private Details 是聊天室级共享字段，不属于 WSK，概念混淆 | 改写为"WSK 的角色卡字段（其他角色不可见）" |
| 7 | 0-1 §后台提取规约 | "每日一次 WSK 累计前一日收获"——WSK 是被动触发，"每日一次"非事实 | 改为"触发时累计扫描窗口内未入账收获" |
| 8 | 2-1 第一行示例 | `/party-known` 缺空格，与格式定义不符 | 补空格 |

**WM/WSK 机制有效性结论**：除上述放置问题外，两个基础字段内的机制链路全部闭环——WM 每轮必出链（读账本→叙事→[主要状态]→结构块）、WSK 稀疏触发兼容（扫描窗口覆盖多日缺口 + carry-forward + 轻量回执 + 长期未触发兜底）均验证通过。

### 17.3 经验与教训

1. **可见性相同 ≠ 语义语境相同**：聊天室字段虽全角色共享，内容仍须按"世界共识 vs 系统协议"分家。把 A 角色的职责规则放进共享字段，等于让主叙事者 WM 每轮暴露在其他角色的规则噪音下——尤其当指针主题（经济/关系/库存）与 WM 自身裁定领域重叠时，会诱发越权谦让或幻觉补全。
2. **对自卡内容的导航指针价值≈0，代价真实**：WSK 不需要 0-2 里的指针来找自己角色卡里已有的规则（自卡永远在其上下文中）；这类指针收益近零，却每轮暴露给 WM。跨角色指向的目录行应默认怀疑，除非能证明目标消费者真实受益。
3. **摘要式重复 = 双重定义时间炸弹**：0-1 对 0-2 内容的"简要复述"（地图拓扑、NPC 边界）与 v1.39 [主要状态] 事故同病灶——改一处漏一处。修法统一为 SSOT 化：保留独有部分 + 重复部分改纯引用。
4. **聊天室字段内容三步判定法**（本次审计的操作方法）：逐段问——这段在描述"世界是什么"（留 0-2）/"系统如何运转"（留 0-1）/"某角色如何行为"（应在该角色卡）？第三类出现在聊天室字段即为放置错位。
5. **干扰类问题的修复不是简单删除，而是"迁移 + 显式排他"**：信息本身可能有保留价值（人类读者/目标角色需要），正确做法是移到语义正确的家，并在原位留下排除声明（"其他角色无需关注、不得尝试引用或复现"），主动压制 LLM 对不可见内容的脑补冲动。

### 17.4 文件头化石问题（2026-07-24 补记）

**发现**：0-1 开头 `[World Master 规则分级与索引]` 及"World Master 读取顺序"——聊天室级共享字段被框定为 WM 专属，且 5 处文件引用的"§全局读取协议"章节实际不存在（软死链）。

**根因**：
1. **头部化石**：项目从 WM 单角色卡起家，多轮架构演进中正文不断搬迁重组，文件头从未重写——头部是全库修改频率最低、过时风险最高的部位
2. **审计缝隙**：头部既非引用也非正文规则，是"元描述"，落在引用审计与放置错位审计两种维度的夹缝中
3. **WM 中心惯性**：早期架构以 World Master 为唯一主角，旧框架语言残留

**修复**：标题改 `[聊天室 Private Details 字段定位与全局读取协议]`；读取顺序改角色通用版（"任一系统角色生成时…"）；5 处引用从此有真实权威定义。

**新增审计项——文件头自我描述审计**（纳入开盘前检查清单）：
- 每个文件的标题/定位声明必须与其实际装配位置（聊天室级 vs 角色卡）与读者范围一致
- 文件头中声称的章节/概念必须实际存在于该文件（防软死链）
- 同一文件头内不得出现互相矛盾的自我描述

**提示词本体论补充**：字段装配到平台后，字段内不存在"仅供开发者看的标注"——每一个字符都是 LLM 消费的提示词。括号标注（如"静态世界资料""全局硬规则"）是有弱功能的提示词（为模型提供语义框架与导航），不是开发注释；开发性注释只能留在仓库文档（如本文件），不得进入装配字段。

### 17.5 提示词越权指挥平台动作问题（2026-07-24）

**发现**：多处提示词以"先读 X 再读 Y""必须先读取 Pinned Memory"等句式指挥平台机制。逐条剖析：

| 句式 | 问题定性 |
|---|---|
| 「任一系统角色生成时，先读 Scenario 再读本字段再读本角色卡三段」 | **虚构时序**：平台在角色生成前已把聊天室字段与角色卡全部注入上下文，模型是一次看到全部，不存在"先读后读"动作；且角色卡装载是平台机制，不需提示词设定 |
| 「WM 每次进入新 Scene 前必须先读取 Pinned Memory」 | **指挥无权动作**：Pinned Memory 的注入/检索机制归平台管（仅知优先级高），模型无法"主动去读"；此句真正有效的内核是后半句的反幻觉约束（事件记录只定义"过去"，不得反推硬状态） |
| 「每轮先读最新官方状态，再读最近正式记录」 | **伪时序包裹的真内核**：真实意图是证据权威优先级（State Update = 现在 > 历史段 = 过去），应改写为优先级声明 |

**确立新原则——提示词不得指挥平台动作**：装载、注入、读取顺序、检索机制都是平台职责；提示词只能定义**解释方式、权威优先级、使用纪律**。凡"先读/再读/必须读取"式句子，若主语动作实际由平台完成，即为对抗性虚构，应改写或删除。判定边界：若"读"的对象是对话历史中的可见内容（模型确实能选择性处理），如 1-3 世界角色锚点解读优先级，则属合法的解释纪律，保留。

**修复**：0-1 全局读取协议改写为"信息源清单 + 权威优先级"并显式声明"无需也不存在主动读取动作"；Pinned Memory 条款改为"机制以平台为准，提示词不指挥其装载"+ 保留反幻觉内核；全库 6 处"读取顺序"措辞统一改为"信息源与权威优先级"（0-1/1-1/1-2/1-3/2-1/2-2）；0-1 Layer 4 恢复条款改为优先级表述。

**经验**：提示词写"谁在什么时候做什么"时，先问一句"这个动作的执行权在模型还是在平台？"——执行权在平台的，只能写"以平台为准"+ 解释约束；执行权在模型的（解读、裁定、输出），才能写行为指令。

---

## 附录 A：tools_prompt.md 分析

`tools_prompt.md` 是一个简短的调试/工具提示词文件（13 行），包含：
1. 通用角色调用模板（限制 NPC 全知、禁止替 User 做决定）
2. 角色卡重置命令（让角色根据自身提示词独立发言）

> **v1.19 更新**：原 WER 事件查询命令模板已删除（WER 组件删除）

这是运维辅助文件，不是核心提示词架构的一部分。

## 附录 B：ASSEMBLY_CHECKLIST.md 分析

装配清单（193 行）是上线前的最终核对表，覆盖：
- 平台字段映射（哪个文件放哪个字段）
- 字符占用检查
- 三后台运行配置核对
- 上线前必须核对项（字段放置、角色理解、主协议一致性、恢复顺序、后台闭环）
- 当前红旗项（CI 回涨控制、1-3 体量控制）
- 装配顺序建议

**补充说明**：装配清单中引用的配套文档（IMPROVEMENT_ANALYSIS.md / MEMORY_MODEL.md / SMOKE_TEST_*）已整合到本文件 §2-§9 与 §13（架构审计结果），不再单独维护。后续如需此类文档，从本文件对应章节提取即可。
