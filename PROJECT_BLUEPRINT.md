# Ash Harbor - 项目蓝图（PROJECT_BLUEPRINT，简称 PB）

> **本文件是项目设计、说明、开发与审计的唯一基准文档（SSOT）**。
> 任何项目功能、机制、文件结构、平台映射、设计决策、开发方法论、审计原则、变更原因，以本文件所述为准。
> 跨文件引用通过本文件提供的章节号定位；不允许出现与本文件不一致的"派生文档"。
> 历史审计记录（原 §十三-§十七）已迁入 `审计历史记录.md`，仅供追溯，不作为当前规范依据。

> 本蓝图基于对 `./Prompt_File/` 目录下所有源文件的逐行审读，并整合开发设计方法论（原 `开发设计方法论.md`，已并入 §十八-§二十）。

> **最后更新**：2026-08-13（v1.64 版 + WSK 转移机制修复 + Delta/State 解耦）| **架构**：9 文件 + 2 角色（WM/WSK）| **主要变更**：v1.64 WSK 库存转移机制修复 4 项（T-1 分仓约束过严/T-2 Delta 缺转移示例/T-3 "同轮或相邻轮"改"同一批次内"/T-4 Delta 与 State 职责边界声明--方案 C 解耦，Delta=物品级变化摘要供 WM 阅读，State=组级当前存量供 WM 使用，各自独立推导不要求算术链）+ 转移抵消规则 + 半量单位校准锚点 + [Resolution] 改名 [判定] + Scavenging->获得 + ≈->约。v1.63 全库功能性审计修复 22 项（P0-P3）--详见 §20.2。v1.62 既有--WM 三字段结构重组。v1.55 基础上增加--WSK 反应模型精简为 2 反应（输出完整视图 / 输出空白标记 `-`，去除 [Commit Rejected] 机制与全部 REJECT 理由；决策表 3->2 张 DO/SILENT，SILENT 改为输出 `-`）；完整视图字段 8->6（旧 3 关系 + 4 阵营 + 5 人为威胁阶段 合并为"Relationship & Threat"，Map Knowledge / Base Structure / 近五日主要事件 顺移为 4/5/6）；Trust/Hostility 双轴替换为 Relationship 7 档定性值（依恋/完全信任 / 亲密/信任 / 好感 / 中立 / 冷漠 / 敌意 / 仇恨，WSK 做语义升降档、不记数值）；变化子段精简为仅 Inventory Delta:（去除 Recent Changes:）；2-2 删除 [势力暴露追踪规则]（Suspicion Level L0-L4 / Last Exposed Day / Last Trigger Type 全部移除）。D1 保暖机制落地（1-3 [保暖修正] + 1-2 [保暖裁定]，保暖值 5 档定性优/良/中/弱/差修正有效温度层，WM 每轮实时裁定不持久化）；Condition 全库移除 + Component 生命周期简化（组件名 + Type + Role + 状态描述，替代旧 Condition + Last Confirmed）。T1 据点消耗机制落地（1-1 [判定] 每轮必出 + Day 推进必含消耗行 + 1-2 [据点消耗规则] 主机制+增强+优先级 + 1-3 [判定 格式模板] 格式更新）。D2 据点损耗机制最终版（三级状态完好/损坏/毁坏 + 统一Day推进触发 + 天气掷骰 + 人为OR关系 + 二级后果映射 + MP降为指示器）。D3 Relationship升降档指引（升降档触发表+跨档跳跃+势力vs个体+初始档冷漠+单向记录）。D4 据点暴露机制（Security/Exposure三档hidden/local-only/publicly-known+暴露来源+后果联动D2人为破坏Base）。v1.55 既有：全面去除 Month（Day 直接查表定 Season）；WSK 记账层精简（删 Elapsed、装备属性 5->2、据点降级改非二元否定、持续代价字段合并）；enum_registry 同步清理；PAM v1.2（Phase 1 执行证据约束 + 跨文件枚举一致性检查 + 形态黑名单 2.10）

---

## 〇、核心工程原则体系：三层漏斗与系统级防线（最高优先级，2026-07-24 定案）

> **强制约束**：本项目所有开发、纠错、新增功能或机制，都必须以本原则体系为前提。任何提示词文本在落盘前、任何修复方案在实施前、任何新机制在设计时，依次通过四道防线（三层漏斗 → 机制闭环 → 认知适配 → 退化与预算）；任一道不通过即不得进入装配字段或实施。

### 0.1 原则本体

落盘提示词文件是**角色执行 LLM 消费的提示词**，不是开发者或系统架构师的说明文档。检验按以下三层漏斗顺序执行，**顺序不可颠倒**（身份定读者，读者定有效性标准）：

**第一层 · 字段身份（前提）**
先明确目标字段的身份与地位：聊天室级（公共法 / 叙事开场）？叙事者兼裁定者（World Master）？记录员（World State Keeper）？
- **字段承载分工（谁装什么）**：各字段有明确的承载定位，内容须与所在字段分工匹配（完整分工表见 §1.1 提示词字段承载分工规范）——聊天室 Private Details = 公共法（所有角色共需的总则和基座，被动吸收有效）；聊天室 Scenario = 叙事开场（世界底座的叙事呈现，玩家入口）。World Master Scene = 永久元身份 + 持续叙事框架；Additional Personality Details = 叙事人格与裁定哲学；Extra Details = 机械引擎 + 详细可执行内容（含详细地图等）。World State Keeper Scene = 身份 + 输出/静默契约；Additional Personality Details = 记账判断逻辑与记录纪律；Extra Details = 输出模板 + 提取细则。判定：决策逻辑写进 Scene、输出模板写进 APD、角色身份写进 Extra，均属错位。
- 身份框定候选读者集合，也框定读者的解读框架（priming）——身份声明错位会污染整个字段（实证：头部化石问题，见 审计历史记录.md §17.4）
- 身份声明本身也须过检：保留语义定框与导航功能，删除无行为价值的容器标签自我介绍
- 内容必须与字段身份语义匹配：叙事开场归 Scenario，公共法归 Private Details（见 审计历史记录.md §十七）

**第二层 · 可见性（读者）**
提示词字段的可见性只有两种：**聊天室级字段 = 全部角色可见；角色卡字段 = 仅本角色可见**。
- 每句话必须能指名读者（某个执行角色的模型）；写给开发者、审计者、虚空的句子一律不应存在
- 防御性元说明（"提示词不指挥其装载"）、猜想陈述（"PM 是高优先级材料"）、角色名枚举（隐含排除风险）是典型清除对象
- 角色间字段外通道 = 对话历史中的实际输出；跨角色依赖写"以对方实际输出为准"，不写不可读的字段指针（见 审计历史记录.md §十六）

**第三层 · 有效性（行为）**
对确定的读者，句子必须改变其行为（解读 / 裁定 / 输出 / 使用纪律）：
- **操作判定**：假设删掉这句，模型下一次输出会有任何不同吗？无差别即噪音，删除；**禁止以"描述内容是否正确"代替判定**（事实真实性 ≠ 行为价值，一条关于平台机制的真话仍然是废话）
- 读者模型无法执行的系统机制元描述（装载/注入/读取顺序/检索/可见性），删除或改写为"以平台为准"+解释约束（见 审计历史记录.md §17.5）
- 功能内核已有权威定义兜底的重复句同样清除——冗余定义本身就是分歧的时间炸弹

### 0.2 判定矩阵

| 对谁说 | 改变行为吗 | 处置 |
|---|---|---|
| 明确读者 | ✅ 是 | 保留（合格提示词） |
| 明确读者 | ❌ 否 | 删除或改写为行为指令 |
| 说不清读者 | ✅ 是 | 迁移到读者正确的字段 |
| 说不清读者 | ❌ 否 | 无条件删除（开发注释入仓库文档） |

### 0.3 适用场景（强制）

| 场景 | 执行要求 |
|---|---|
| **新增功能或机制** | 设计时先确定承载字段身份 → 逐句过三层漏斗 → 通过后才能写入提示词 |
| **纠错修复** | 修复文本本身必须过三层漏斗——禁止为防复发加"防御性元说明"（药变成病，见 审计历史记录.md §17.5 根因②） |
| **审计** | 从"模式扫描已知病灶"升级为"逐句可执行性审查"，对基础层文件（0-1/0-2 头部）定期逐行过三层漏斗 |
| **引用新增** | 先过可见性层：引用方角色生成时能否看到被引用目标（见 审计历史记录.md §十六 验证方法） |

### 0.4 来源

本原则由多轮实战纠错沉淀而成：头部化石（审计历史记录.md §17.4）→ 提示词越权指挥平台动作（审计历史记录.md §17.5）→ 元描述与猜想陈述清除（Pinned Memory 条款整行删除）→ 用户定案三层顺序（身份→可见性→有效性）。

### 0.5 系统级补充原则（与三层漏斗正交，2026-07-24 定案）

三层漏斗是**句子级准入闸门**（回答"这句话有没有资格存在"），但开放世界 + 多角色 + 长期运行 + LLM 执行者的品类特征还需要五条系统级原则：

**原则① 字段内组织与内容质量**
字段确定后，字段内部的组织与内容质量决定执行稳定性：
- **字段内顺序很重要**：模型大致按从上到下阅读（尤其 Extra Details）；最重要的硬锁、核心规则放最顶部。
- **职责分离 > 谁覆盖谁**：把“人格”与“系统规则”分开放，比混在一个字段里，模型执行更稳定。
- **内容清晰度 > 字段位置**：写得越结构化、越可执行，模型越容易遵守；内容质量比字段位置影响更大。

**原则② 机制闭环（系统级完备）**
每个机制构件必须四要素齐全：**定义 + 语义 + 消费者 + 失败出口**。声明了数值就必须有消费路径，声明了钩子就必须有定义与触发语义；缺一 = 断头路。实证：获得偏移 37 长期无 Base 概率与触发语义；噪音暴露度 +15/+30 长期无消费者。审计工序：接口完备性矩阵（枚举全部机制钩子，逐个核对四要素）。

**原则③ 认知适配（LLM 能力现实）**
规则必须匹配执行者的稳定能力边界：
- **确定性推导 > 隐式追踪**（Season 由 Day 查 [温度分层与日期精细对应] 表推导，不让模型记住当前季节）
- **显式锚点 > 上下文记忆**（硬状态只认落盘 [State Update]，不靠模型回忆）
- **模板约束 > 自由发挥**（9 字段清单压制旧模板惯性）
需要模型长期精确计数/算术/跨轮记忆的规则 = 载体选错，不是写得不够清楚。实证：Month 推导算错（v1.55 已去除 Month，Season 改由 Day 直接查表确定）、11 字段旧模板惯性复活。

> **跨轮计数禁令（2026-08-06 全库对齐审计实证）**：任何要求 LLM 跨轮精确计数的规则（如"是否被搜过""2 次互动""10+ 日无接触""16-18 小时清醒""最近 2 天冲突"）都应改为"以 WM 叙事中的已成立事实为准"。LLM 不做计数器，只做叙事依据判断--若叙事中已体现触发条件，则规则触发；若叙事依据不足，规则不触发（SILENT 等效）。这是 LLM 系统与传统游戏引擎的根本差异之一。

> **LLM 可读性原则（2026-08-06 人工发现）**：规则的每个引用和指针必须让 LLM 能直接定位目标，不需要推断。原则③关注"LLM 能不能执行"（认知适配），本条关注"LLM 能不能读懂"（可读性）：
> - **引用必须显式**：用 `§[section名]` 指向目标，不用口语化简称（如"经济条""人类敌对律"）。LLM 不推断"这个简称指的是哪条规则"。
> - **前向引用必须可解析**：引用同 section 后续条目时，LLM 可能还未读到目标条目。如果相隔很近（同 section 内 <20 行），直接内联关键信息，不做引用；如果相隔远或跨 section，用 `§` 指针。
> - **同一句中引用格式必须一致**：如果同句中有 2 个以上引用，必须用相同格式（全部 `§` 或全部内联），不能混用。LLM 对格式一致性敏感，混用会导致部分引用被忽略。
> - **概述层与数据表/世界事实层级分离**：LLM 一次性处理整个文件，不需要"跳转"去读某个 section。概述层（如 [世界硬规则]）的 § 引用应指向**规则机制**（驱动逻辑/判定规则），不应指向**纯数据表**（换算系数/枚举列表）或**世界事实**（环境描述/势力档案）--这些内容有明确 section 标题，LLM 处理相关场景时能通过标题自行激活。概述层引用数据表或世界事实是层级混淆（陷阱 #16/#17）。
> - **后向引用冗余**：LLM 顺序处理 token 序列，已读过的内容在上下文中。后向引用（目标在前面）且相隔 <20 行时，LLM 已有目标内容，引用是冗余的。应内联关键信息，不做引用（陷阱 #17）。
> - **装配可见性**：聊天室级字段（0-1/0-2/0-3）都会被同时装配到 WM/WSK 上下文中。它们之间的跨文件引用等同于同上下文引用--LLM 已有目标内容，引用不必要。只有角色卡之间的横向引用才是真正的"读者看不到目标"（陷阱 #18）。
> - 审计检查：Phase 1.1 扫描口语化引用模式（陷阱 #14）；Phase 5.2 判定引用必要性（陷阱 #15）+ 概述层目标类型检查（陷阱 #16）+ 后向引用距离检查（陷阱 #17）+ 跨文件装配可见性检查（陷阱 #18）；Phase 5.5 检查同句引用格式一致性。

> **聊天室级字段引用失效原则（2026-08-07 冒烟测试 + 实际运行实证）**：聊天室级字段（0-1/0-2）的作用是给加载的平台角色卡设立世界知识底座；而对于系统角色 WM 和 WSK，在具体运行时不保证每次加载。因此 WM/WSK 不可依赖 0-1/0-2 中的任何内容（包括具体细节、表格、机械规则和公共法）作为执行依据。角色卡中的 `§聊天室` 引用对执行无效。实证（3 轮冒烟测试 + 实际运行）：
> - [主要状态] 压力段格式（0-1 L19"不带括号说明"）-- WM 不读，压力段带括号说明（8 处违规）
> - 路由拓扑规则（原 0-2 [地图调用规则]）-- WM 不查邻接表，凭九宫格视觉位置跳过中间分区（3 次路由错误）
> - 物资渲染双轨格式（0-1 L53-57）-- WM 部分不读，有些物资不写重量
> - NPC 知识边界（0-2 [NPC 反全知]）-- WM 引用但不在自己字段中执行
> - 弹药口径换算表 / 资源消耗基准 / 可消耗物资渲染协议（0-1/0-2 中的表格与格式细节）-- 角色卡引用但执行时不查表
>
> 对策：WM/WSK 必须自包含--执行所需的具体细节、表格、机械规则和公共法必须**内联到角色自己的字段**（1-x / 2-x）中，不能依赖聊天室级字段。聊天室级字段服务于其他平台角色卡（世界知识底座），角色卡字段是 WM/WSK 的唯一可靠执行依据。0-1/0-2 仍作为全库一致性校验的权威源。审计检查：Phase 5.2 对每个角色卡字段中的 `§聊天室` 引用，判定目标是否为具体细节/表格/机械规则/系统协议；若是，标记为需内联。

**原则④ 退化设计（失败出口）**
开放世界中信息缺失、规则冲突、用户不触发是常态，每条关键规则必须预置失败出口：缺字段 → REJECT 还是记"未提供"；无变化 → 轻量回执；用户长期不触发 → WM 临时 State。没有退化路径的规则，第一次异常就会诱发模型幻觉自救。实证：REJECT 分级（Weather 缺失 REJECT / Season 缺失记未提供）、轻量回执定案。

**原则⑤ Token 预算（上下文竞价）**
上下文是稀缺资源，每个字符都在竞价：高频规则放高显著性位置（字段头部）；低频细则下沉角色卡；同一信息 SSOT 化只存一份（冗余 = 双倍占用 + 分歧炸弹）。三层漏斗第三问"删掉后行为无差别即删"是本原则的句子级投影。



### 0.6 防线体系（执行次序）

```
落盘前闸一：三层漏斗（句子级准入）
   身份 → 可见性 → 有效性
闸二：机制闭环检查（系统级完备）
   定义 + 语义 + 消费者 + 失败出口
闸三：认知适配（执行级现实）
   推导>追踪，锚点>记忆，模板>自由
运行期兜底：退化路径（异常不诱发幻觉）+ Token 预算（上下文竞价意识）
```

一句话概括：**三层漏斗保证"每句话值得存在"，闭环原则保证"每个机制能运转"，认知适配保证"模型真的做得到"，退化设计保证"出错时不崩盘"**。

### 0.7 执行纪律补记（2026-07-24，源自可见性声明漏检复盘）

**背景**：原则定案并对 0-1 全面审查后，「聊天室内全部角色共享可见」仍被漏检——内容完全属实但行为价值为零。暴露三个执行缺陷，对应三条强制纪律：

1. **形态黑名单（先于语义判定）**：凡描述「谁能看到本字段 / 本字段如何被装载 / 字段的平台属性」的句子，**不看内容真伪，无条件不进入装配字段**。语义判定（"能否执行"）对静态事实描述天然失焦，且会被"内容正确"豁免；形态判定只看句型，不看真伪。可见性知识唯一的合法居所是仓库文档（本文件 / PLATFORM_REFERENCE）。
2. **审计纪律反转**：最近修复过的区域列为**高优先复检区**而非安全区——修复只针对当时识别的病灶形态，同句其他成分未必过检（实证：上一轮只删了括号枚举，主干"共享可见"从未被审视）。
3. **有效性操作定义收紧**：闸一第三层的判定动作固定为"假设删掉这句，模型下一次输出会有任何不同吗"；禁止以"描述对不对"替代"有没有用"。框架自我描述（陈述原则前提的句子）不豁免逐句检验。

**二次加固（源自 0-2 头部指针句漏检复盘，同日）**：重排与三性审查后，0-2 头部「职责分工见 Private Details 字段」指针句仍漏检。四个根因（修复免疫 / 标准漂移 / 句级分类掩盖分句病灶 / 短即无害错觉）对应三条强制纪律：

4. **分句级过检**：凡含多个独立命题的句子（分号/逗号连接不同主张），逐分句独立过两问；一个分句的合法标签不得豁免其他分句（实证三连："共享可见"主干、L34"按各自职责"、0-2 头部指针分句——同一模式三次犯案）。
5. **修复免疫禁令**：历次修复产生的句子不享受白名单，按现行标准重新过检；"复检反转"纪律的覆盖范围从"病灶残留"扩展到"修复文本本身"。旧标准下诞生的合法句，遇新标准必须重新过筛（标准漂移缝隙是漏检高发区）。
6. **头部加权**：字段头部（定位/锚点区）按每句全成分拆解审查，不适用正文的抽样逻辑；头部信息密度全字段最高，审查强度应高于正文而非更低。

### 0.8 人工审查新增病灶形态（2026-07-24，用户首轮人工审查 0-1 沉淀）

1. **无实体对应的化石术语**：专有名词必须与实际输出实体对应（如 `[State Update]`、Pinned Memory 中的具名段落）；术语只存在于规则文本而无可指认对象 = 让模型猜谜（实证：State Ledger / History Ledger 无任何输出实体，已替换为实际实体名）。
2. **术语与平台字段名撞车**：概念词与平台字段名同名（如 Scene 既是叙事正文代称又是角色卡字段名）时，上下文必须带消歧锚点（对话历史 / 实际输出 / 叙事正文），否则换用他词（实证："通过 Scene 叙事体现已成立变化"改为"前台叙事正文"）。
3. **跨卡指针预设读取能力**：聊天室字段指向角色卡的引用一律写成**归属声明**（"X 按其…字段 §Y 执行"），不写引用指令式（"详见 X 的…字段"）——前者不预设任何跨字段读取能力，与平台真实机制兼容（实证：场外演化时间规则改写）。
4. **世界观旁白混入指令文本**：设定叙事句（如"后末日无统一货币"）混在规则条目中，读起来"不像提示词"即为此病灶。判定法：删掉该分句后，任何角色的行为指令是否缺失？不缺即为旁白，删除或下沉到背景设定区；另须检查旁白与相邻规则是否自相矛盾（"无统一货币"紧接"9mm 为本位单位"即自我推翻）（实证：0-2 经济规则首句）。

### 0.9 审计方法二次加固（2026-07-24，枚举病灶漏检复盘）

**背景**：0-1 职责目录行的冗余枚举（动态机制括号清单）连续通过多轮三问审查，最终由用户人工审查发现——且已实际漂移（"据点基线"≠源头节名"据点结构基线"）。暴露审查方法四个结构性缺口：分句级纪律以句为粒度而病灶在成分级；纪律以实例定界而非模式（"覆盖清单禁令"没覆盖括号枚举）；枚举类内容无专属检验问句；真实性陷阱再次犯案（内容属实即放行，未执行删除测试）。

**三条强制对策**：
1. **枚举检验问句（第五问）**：凡遇枚举/清单/括号列举，必问三句——别处有无权威定义？本处读者需要逐条吗？枚举内容与源头逐项一致吗？任一失败即删（实证：0-2 覆盖清单、0-1 职责目录括号枚举）。
2. **检验粒度下沉到成分级**：复合句按语义成分（声明/枚举/条款/理由）拆解后逐成分过两问；整句的合法分类不得豁免内部成分。
3. **模式滞后元承认**：模式匹配审查对首次出现的病灶形态必然滞后一轮；人工读者模拟（"不了解内情的读者会被误导吗"）是必要互补而非重复劳动；每轮人工发现的病灶必须当轮泛化为模式级纪律，禁止以实例形态定界。

### 0.10 节标题纳入两问检验（2026-07-24，自指标题漏检复盘）

**背景**：0-1 首节标题 [聊天室 Private Details 字段定位与权威优先级] 长期占据全文件第一行，却从未被 § 引用、且自指本字段（读者正在读它），属零增益的仪式性标题；多轮编辑该节内容均未触及头顶标题。暴露缺口：两问/三问检验只施加于正文句子，[节名] 被当作中性结构脚手架而豁免于审查；"正确描述内容"的标题以正确性掩盖无必要性；头部加权只用于排序、未用于问责首行占位。

**三条强制对策**：
1. **节标题过两问**：每个 [节名]/【节名】必须自证存在价值——要么被 § 引用（锚点职能），要么有真实导航必要；否则删除或缩短。自指（重报本字段/本文件名）或纯描述且无引用的标题一律清除。
2. **改/删节名前先查引用**：节名可能是跨文件 § 指针的目标；删除/改名前必须全库 grep 该节名，确认无引用或已同步改指针，防断链。
3. **首行问责**：头部加权的推论是"最高权重位置必须被最高价值内容占据"；每文件首行（含首个节名）须单独过删除测试。

### 0.11 提示词文件审计方法论 PAM v1.2（2026-08-09 改版；v1.1 拆分节级/行级两遍审查；v1.2 增加 Phase 1 执行证据约束 + 1.6 跨文件枚举一致性检查 + 形态黑名单 2.10 + 陷阱 #21/#22；2026-08-10 2.10 增加角色卡 SSOT 例外 + 1.3 重复检测增加角色卡内联例外）

综合 §0.1–0.10 的原则、病灶形态与历次漏检教训，将 §0.6 防线体系（闸一→闸二→闸三→运行期兜底）从"原则声明"重组为"可执行工序"。五条执行法则：**先机械后语义**（客观可复核优先，语义由人主导）；**先形态后内容**（形态黑名单先于语义判定，不看真伪）；**先粗后细**（节级三层漏斗先于行级成分审计，二者为独立工序，节级通过不豁免行级）；**逐层下沉**（文件→节→句→分句→成分；头部加权，强度高于正文）；**规则与数据分治**（规则类内容用行为有效性测试——删掉后行为有不同吗；数据类内容用功能+防幻觉测试——被规则引用吗，删掉后模型会虚构吗）。审查者只报客观发现，不下"彻底/完备"结论。

**角色卡六字段功能分工**（Scene / APD / Extra × WM / WSK；审查内容是否落在功能匹配的字段，错位即标记）：

| 字段 | 功能定位 |
|---|---|
| WM Scene (1-1) | WM 角色身份与职责 + 输出契约（每轮必出什么、出场次序、触发条件）+ 运行锚点与必查表 + 行为禁区；输出契约分 [输出结构]（次序与终止）与 [输出内容]（逐块必出/空标记/格式指针）两节 |
| WM APD (1-2) | WM 叙事审美（语气/节奏/质感）+ 裁定决策逻辑——生存 / 战斗 / 交易 / 搜刮 / 关系 / 敌对 / 时间推进 / 压力演化"怎么判" |
| WM Extra Details (1-3) | WM 执行协议与资料库——格式模板（[主要状态]/[判定]/[掷骰]/[移动] 长什么样）+ 数据表 + 参考表 + 地图 + 据点结构基线 |
| WSK Scene (2-1) | WSK 角色身份 + 触发 / 静默契约 + 越界边界 |
| WSK APD (2-2) | WSK 记账决策--DO 决策表 + 各状态怎么记 + 记入前检查 |
| WSK Extra Details (2-3) | WSK 输出格式（[State Update] / 完整视图）+ 提取规则（kg 记录 / 口径格式 / 提取纪律） |

归位准则：
- **输出契约**（每轮必出什么、出场次序、触发条件）归 Scene；**格式模板**（具体长什么样）归 Extra Details
- **叙事审美**（语气/节奏/质感）归 APD；**裁定决策逻辑**（怎么判）归 APD；**数据表/参考表/资料库**归 Extra Details
- 同角色三字段运行时一并装载，分工是组织约定而非可见性边界

**WM 三字段承载边界（基于优化方案 L171-228）**：

| 字段 | 适合承载 | 不适合承载 |
|------|---------|-----------|
| WM Scene (1-1) | 身份与职责声明、行为禁区、输出契约（必出什么/出场次序/触发条件，分 [输出结构] 与 [输出内容] 两节）、运行锚点与必查表 | 格式模板（长什么样）、数据表、地图、裁定决策逻辑 |
| WM APD (1-2) | 叙事审美（冷峻/克制/重质感/少说教、压力用环境与身体写、先结果后线索）、裁定决策原则（怎么判）、执行顺序、各域裁定细则 | 格式模板、数据表、状态机代码式条文 |
| WM ED (1-3) | 格式模板（[主要状态]/[判定]/[掷骰]/[移动]）、数据表、参考表、地图、据点结构基线、搜刮数据、温度/天气系统 | 公共法（NPC 读不到）、叙事审美指引 |

#### 七阶段执行流程

每阶段标注执行主体（`[机]` 可脚本化 / `[人]` 人工判断）与对应防线。先机械后语义，先形态后内容，先粗后细，规则与数据分治。

**Phase 0 · 定位与依赖映射** `[人]`

对应 §0.1 第一层（字段身份）。

> **字段归属优先原则（2026-08-06 复核修正）**：Phase 0 的 0.2（内容-身份匹配）必须先于 Phase 1.1（引用活性）执行。若内容归属错误（角色私有规则出现在聊天室级字段），直接判为字段归属错位，不进入可见性检查流程。判定依据是内容性质（角色专用 vs 共享世界知识，见 §1.1），不是对方角色能否解析指针。

| # | 检查项 | 操作 | 通过标准 |
|---|--------|------|----------|
| 0.1 | 字段身份 | 确认文件角色/字段归属（0-1 公共法 / 0-2 叙事开场 / 1-x WM / 2-x WSK） | 身份明确 |
| 0.2 | 内容-身份匹配 | 逐节核对内容是否落在功能匹配的字段（上方六字段分工表） | 无错位 |
| 0.3 | 引用出向 | 提取所有 `§` / `聊天室 X 字段` 引用，记录指向哪些文件/节 | 依赖清单完整 |
| 0.4 | 引用入向 | 全库 grep 本文件每个 [section] 名，记录谁在引用本文件 | 被引用清单完整 |

输出：文件定位卡 + 依赖图（供 Phase 1.1 和 6.4 使用）。

**Phase 1 · 机械核验** `[机+人]`

全客观，可脚本化。对应 §0.12 格式规范。

| # | 检查项 | 工具 | 违规示例 |
|---|--------|------|----------|
| 1.1 | 引用活性 | grep/rg | `§世界事件遭遇机制` 无对应 [section]；口语化引用（"按 X 条""按 X 律"）未用 `§` 前缀（陷阱 #14） |
| 1.2 | 术语一致性 | 术语表+grep | 角色简称未前置定义；化石术语（无实体对应）残留 |
| 1.3 | 重复检测（SSOT） | diff/相似度 | 同一规则/映射/枚举多处存在。**例外**：角色卡字段（1-x/2-x）从聊天室级字段（0-x）内联的数据节不计为重复--因 WM 运行时不反查聊天室字段，角色卡必须内联机械规则与数据（陷阱 #20 对策）。仅聊天室级字段之间的重复判定为违规 |
| 1.4 | 格式合规（粗筛） | regex lint | `[主要状态]` 行内引用缺 backticks；子标题非 [section] 格式 |
| 1.5 | 节名引用链 | grep | 改/删节名前查谁在引用（防断链） |
| 1.6 | 跨文件枚举一致性 | grep | 本文件中的枚举/列表（如 Role 取值、天气枚举、口径列表）在别处有权威定义时，grep 权威源逐项比对值集合，标记缺失/多余/不一致项（陷阱 #22） |

输出：违规清单（文件:行号 + 类别 + 原文）+ **执行证据**（每项 `[机]` 检查的 grep 命令与结果计数）。注意：1.4 为粗筛，行级精检在 Phase 5.3。

> **精确统计要求（2026-08-06 审计实证）**：Phase 1 的 1.3（重复检测）和 1.4（格式合规）必须用 grep 统计精确数量，不靠估算。实证：bold 标记预估 80+ 处，实际 159 处；估算会导致修复批次规划失准。

> **执行证据约束（2026-08-09 审计实证）**：Phase 1 所有 `[机]` 检查项的输出必须包含实际执行的 grep 命令与结果计数（如 `rg -c "pattern" file` -> N 处），不接受"已检查，无违规"的无证据结论。`[机+人]` 标注是能力声明，不是执行保证--方法论写"工具：grep/rg"不等于审计者真的跑了 grep。实证：2-3 审计中 Role 枚举不一致（本文件 7 值 vs 权威源 20 值）此前多轮未发现，根因是 Phase 1.3 只标注"diff/相似度"但未要求跨文件 grep 比对，机械阶段空转（陷阱 #21）。

**Phase 2 · 形态黑名单扫描** `[机+人]`

对应 §0.7 纪律1（形态黑名单先于语义判定）+ §0.8 病灶形态。只看句型模式，不看内容真伪。

| # | 病灶形态 | 检测特征 | 来源 |
|---|----------|----------|------|
| 2.1 | 可见性/装载/平台属性句 | "本字段被全部角色可见" / "字段如何被装载" / "平台属性" | §0.7 |
| 2.2 | 防御性元说明 | "提示词不指挥其装载" 类自证句 | §0.1 |
| 2.3 | 猜想陈述 | "PM 是高优先级材料" 类未验证断言 | §0.1 |
| 2.4 | 跨卡指针预设读取 | "详见 X 的…字段" 引用指令式（应改归属声明） | §0.8 |
| 2.5 | 世界观旁白混入指令 | 设定叙事句混在规则条目中（删掉后行为指令无缺失） | §0.8 |
| 2.6 | 自指标题/自指指针 | 节名重报本字段名；指针指向自身 | §0.10 |
| 2.7 | 冗余枚举 | 括号列举别处已有权威定义的清单副本 | §0.9 |
| 2.8 | 强调性格式化石 | `**bold**` / `【】` / emoji（⚠️等）/ section 名含开发编号（`[§3.x]`） | §0.12 + 2026-08-06 审计 |
| 2.9 | 版本化石 | `(v1.xx)` 版本标注 / "字段已删除"类指向不存在内容的描述 | 2026-08-06 审计 |
| 2.10 | SSOT 元注释/开发者指针 | `（与 0-X §[...] 一致）``（见 X 字段 §[...]）`类开发者维护用 SSOT 指针--数据已内联但保留指向源头的注释，执行 LLM 无法验证、不改变行为。**v1.60 起：所有 prompt 文件中禁止 `（与 0-X §XX 一致）` 标注**，复制关系仅记录在 PB §21.6 跨角色复制记录中 | 2026-08-09 审计；2026-08-12 v1.60 全面禁止 |

输出：嫌疑句清单（行号 + 病灶形态 + 原文）。

**Phase 3 · 节级三层漏斗** `[人]`

对应 §0.6 闸一（三层漏斗）节级执行。以**节**为最小审查单元，识别结构性问题（错位 / SSOT 跨节违规 / 节级可见性违规 / 整节无效）。执行顺序：头部加权 → 正文逐节。此阶段为粗筛，**节级通过不豁免行级审计（Phase 5）**。

- **3.0 头部加权审查**：首行 + 首节名 + 定位区，强度高于正文（§0.7 纪律6）；首行单独过删除测试（§0.10 纪律3）。
- **3.1 第一层 · 身份（给谁看）**：本节内容能否指名读者？写给开发者/审计/虚空的标记删除；跨角色不可读内容以指针形式出现的标记迁移。
- **3.2 第二层 · 可见性（读者能读到吗）**：本节是否有跨角色不可读的依赖？聊天室级 = 全部角色可见；角色卡级 = 仅本角色可见。跨角色依赖写"以对方实际输出为准"，不写不可读的字段指针。
- **3.3 第三层 · 有效性（整节删掉后行为有不同吗）**：本节作为整体是否改变行为？别处已有权威定义的整节标记删除（SSOT）。**数据类节**（地图列表/势力档案/物品表/枚举表等）有效性测试替换为两个问句--功能性（被规则引用/消费？）+ 防幻觉性（删掉后模型会虚构替代数据？）。

> **跨文件语义等价检测（陷阱 #19，2026-08-06 人工发现）**：对叙述性内容块（非规则/非数据/非引用的描述性段落，如"运行流程""角色分工""系统机制"），检查其他文件中是否有语义等价的内容。特别关注：0-1 已定义的公共法（角色分工/通讯机制/运行流程）不应在其他文件中重述。Phase 1.3 SSOT 重复检测关注逐字重复，检测不到语义等价但文字不同的重述--此检查需人工判断。
- **3.4 枚举检验（节级）**：本节内的枚举/清单是否别处已有权威定义？本处读者需要逐条吗？

输出：节级发现清单 + 嫌疑节标记（供 Phase 5 重点深挖）。**注意：节级无发现的节仍须全量过 Phase 5 行级审计。**

**Phase 4 · 机制闭环审查** `[人]`

对应 §0.6 闸二（机制闭环）+ 闸三（认知适配）+ 运行期兜底（退化路径）。原 §0.11 把这三条压进"四问③"一句话，此处拆为独立工序。

- **4.1 接口完备性矩阵（闸二）**：对文件中声明的每个机制构件（数值/钩子/规则），逐个核对四要素——定义 + 语义 + 消费者 + 失败出口；缺任一 = 断头路。模板：

| 构件 | 定义 | 语义 | 消费者 | 失败出口 | 判定 |
|------|------|------|--------|----------|------|
| _例：噪音暴露度+15_ | 有 | 有 | 无 | — | 断头路 |

- **4.2 认知适配检查（闸三）**：是否要求长期精确计数/算术/跨轮记忆？载体选错不是写得不够清楚。三条优选序：确定性推导 > 隐式追踪 / 显式锚点 > 上下文记忆 / 模板约束 > 自由发挥。

> **跨轮计数检测（2026-08-06 审计实证）**：扫描所有包含数字阈值 + 跨轮时间窗口的规则（如"N 轮内""N 次互动""N 日无接触""N 小时清醒""最近 N 天"）。凡要求 LLM 跨轮精确计数的，标记为认知不适配，改为"以 WM 叙事中已体现…为准"（见 §0.5 原则③跨轮计数禁令）。
- **4.3 退化路径检查（运行期兜底）**：每条关键规则预置失败出口——缺字段 REJECT 还是记"未提供"？无变化轻量回执？用户长期不触发兜底状态？没有退化路径的规则第一次异常就会诱发模型幻觉自救。
- **4.4 示例一致性检查（2026-08-15 WSK 审计实证）**：同一输出格式的多处示例（文件头部示例块 vs 规则内嵌示例）必须逐字一致——LLM 把示例当输出规范逐字模仿，双示例格式冲突 = 输出格式随机，直接破坏下一轮自解析（实证：2-3 顶部 Delta 示例 `随身->据点核心` 箭头式 vs [提取纪律] 8 示例 `（随身）` 括号式并存，纯文本审查两周未发现，静态模拟暴露）。
- **4.5 判定依据充分性检查（2026-08-15 WSK 审计实证）**：引用结构完整（§ 回指闭合）≠ 判定依据充分——凡规则要求角色"根据叙事语义判断 / 升降档"的枚举，必须配该角色可从自身输入直接读到的判定锚点（叙事信号→档位），仅有档位标签 = run 间漂移可 ≥2 档（实证：Relationship 7 档 / HTS 9 档此前无锚点）。跨角色锚点表红线：只写判定角色可直接读到的输入信号，不写行为预测（行为预测属前台角色阈值表职责）。
- **4.6 静态模拟（执行层验证，2026-08-15 新增）**：文本审计全绿后，构造 1 组复合场景输入（跨位转移 + Day 推进 + 关系变化 + 1 项异常输入）人工推演理想输出路径——失败出口缺失、双示例矛盾、格式随机等执行层缺陷只能由此暴露（实证：Step 1-7 全绿背景下静态模拟仍发现 6 处失败出口缺口；方法论沉淀于 .trae/skills/prompt-consistency-audit SKILL Step 8）。

输出：机制构件清单（含消费者/失败出口）。**此清单供 Phase 5 行级审计引用——行级审查引用机制名的分句时，须对照本清单确认构件完备。**

**Phase 5 · 行级成分审计** `[人]`

对应 §0.6 闸一（三层漏斗）行级执行 + §0.7 纪律4（分句级下沉）。以**行**为最小审查单元，将每行的复合句拆分为独立分句/成分，逐个过三层漏斗。此阶段在 Phase 4 之后执行——行级判断常需引用机制闭环矩阵（如分句引用某机制名时，须确认该机制有定义、有消费者、有失败出口）。**节级（Phase 3）无发现的节仍须全量过此阶段。**

- **5.1 分句拆解**：含分号/逗号/括号连接多个独立命题的复合句，拆分为独立分句列表；整句的合法分类不得豁免内部成分（§0.7 纪律4）。
- **5.2 逐成分三层漏斗**：每个分句独立过检--身份（给谁看）/ 可见性（读者能读到吗）/ 有效性（删掉后行为有不同吗）/ 引用必要性（引用该不该有）；无差别即噪音，标记删除；**禁止以"描述内容是否正确"代替判定**（§0.7 纪律3）。**数据类行**（地点条目/物品枚举/数值表等）有效性测试同 3.3 适配规则（功能性 + 防幻觉性）；格式合规（5.3）仍全量适用。

> **引用必要性判定（陷阱 #15/#16/#17，2026-08-06 人工发现）**：对每个含引用的分句，判定引用是否必要：
> - **同 section 内引用**（如 L21 引用 L23）：默认冗余--WM 逐行阅读时自然读到，不需要指针。除非相隔超过 20 行或跨子 section。概述层应内联关键信息，不引用同 section 后续条目。
> - **跨 section 引用**：需判定方向和距离：
>   - **前向引用**（目标在后面）：一般必要--LLM 还未读到目标，需要 `§` 指针建立关联。但概述层引用的目标应为**规则机制**（驱动逻辑/判定规则），不应引用**纯数据表**或**世界事实**（换算系数/枚举列表/环境描述/势力档案）--这些内容有明确 section 标题，LLM 处理相关场景时能自行激活（陷阱 #16/#17）。
>   - **后向引用**（目标在前面）：默认冗余--LLM 已经读过目标内容，已在上下文中。除非相隔超过 20 行（注意力可能衰减）或目标 section 内容复杂需要强调关联。近距后向引用（<20 行）应内联关键信息，不做引用（陷阱 #17）。
> - **跨文件引用**：需按装配可见性判定（陷阱 #18）：
>   - **聊天室级 -> 聊天室级**（如 0-2 -> 0-1）：非必要--两者都在 WM/WSK 上下文中，等同于同上下文引用。目标有明确 section 标题可自行激活。
>   - **角色卡 -> 聊天室级**（如 1-1 -> 0-2）：非必要但有微弱导航价值--两者都在角色上下文中。可保留但不强制。
>   - **角色卡 -> 角色卡（横向）**（如 1-2 -> 2-3）：禁止--互不可见（§18.3 可见性矩阵）。改为"以对方实际输出为准"。
>   - **聊天室级 -> 角色卡**（如 0-2 -> 1-x）：禁止--字段归属错位（§1.1）。
- **5.3 格式合规（行级精检）**：逐行检查 backticks / `[section]` / 字面量标记——同一句中已加 backticks 的字面量别处同类漏加也违规（如 `strained` 有 backticks 但 `stable` 漏了）。
- **5.4 枚举检验（行级）**：凡遇括号列举/内联清单，必问四句--别处有无权威定义？本处读者需要逐条吗？枚举内容与源头逐项一致吗？**跨文件枚举值集合一致吗**（陷阱 #22）？任一失败标记删除（§0.9 对策1）。
- **5.5 跨句引用一致性**：引用机制名/术语的分句，对照 Phase 4 构件清单确认完备；引用 `§` 的分句确认目标节存在且语义匹配；跨角色术语（如 WM 文件中引用 WSK 侧概念）确认可见性。

输出：行级发现清单（行号 + 分句编号 + 类别 + 原文）。

**Phase 6 · 组织与预算审查** `[人]`

对应 §0.5 原则①（字段内组织）+ 原则⑤（Token 预算）+ §0.10（节标题问责）。从字段整体视角做最终检查。

| # | 检查项 | 通过标准 |
|---|--------|----------|
| 6.1 | 字段内顺序 | 最重要的硬锁/核心规则在最顶部；高频高权重前置 |
| 6.2 | 职责分离 | 人格与系统规则分离，未混在同一节 |
| 6.3 | Token 预算 | 同一信息 SSOT 化只存一份；冗余 = 双倍占用 + 分歧炸弹 |
| 6.4 | 节标题必要性 | 每个 [section] 过两问：被 § 引用 or 有导航必要？否则删除 |

**Phase 7 · 汇总与处置** `[人]`

发现记录格式：`[L#] [类别] [P0/P1/P2] 原文 / 违规原则 / 处置（删除·迁移·改写·保留）`。

严重度分级：P0 = 错位/越权/断头路（影响运行正确性）；P1 = 冗余/化石/旁白（污染认知或浪费 token）；P2 = 格式/顺序（不影响正确性但影响一致性）。

处置决策矩阵（复用 §0.2）：明确读者+改变行为=保留；明确读者+不改变=删除或改写；说不清读者+改变行为=迁移；说不清读者+不改变=无条件删除。

> **审计质量指标（2026-08-06 审计实证）**：
> - **严重度校准率**：P0 判定中实际降级的比例。本次 25 项 P0 中 4 项降级（16%），说明 P0 判定偏严。后续审计应在 Phase 7 增加"P0 校准复查"：逐项问"此项是否真的影响运行正确性？"
> - **审计误判率**：误判项数 / 总项数。本次 231 项中 1 项误判（0.4%），可接受。误判须记录原因并撤回，不留痕迹会导致后续审计沿错误方向重复。
> - **格式数量预估偏差**：预估数量 / 实际数量。本次 bold 预估 80+ / 实际 159（偏差 50%），说明 Phase 1 必须用 grep 精确统计。

#### 审计陷阱清单（强制对照，历次漏检教训）

| # | 陷阱 | 对策 | 来源 |
|---|------|------|------|
| 1 | 修复免疫 | 修复过的句子不享受白名单，按现行标准重检 | §0.7 纪律2/5 |
| 2 | 标准漂移 | 旧标准下合法的句遇新标准必须重新过筛 | §0.7 纪律5 |
| 3 | 短即无害错觉 | 短句也可能是无价值指针，照样过检 | §0.7 二次加固 |
| 4 | 句级掩盖 | 整句合法分类不豁免内部成分 | §0.7 纪律4 |
| 5 | 真实性陷阱 | 内容属实 ≠ 行为价值；禁止以"对不对"代替"有没有用" | §0.7 纪律3 |
| 6 | 模式滞后 | 首次病灶形态必然漏一轮；人工模拟"不了解内情的读者会被误导吗"是必要互补 | §0.9 对策3 |
| 7 | 节标题豁免 | [section] 不是中性脚手架，须自证存在价值 | §0.10 |
| 8 | 头部欠审 | 头部信息密度全字段最高，审查强度应高于正文 | §0.7 纪律6 |
| 9 | 节级免疫 | 节级审查通过不豁免行级审计；0-1 L19 节级无发现但行级有 3 个（meta 注释 / `stable` 缺 backticks / 跨角色术语可见性） | 0-1 审计实证 |
| 10 | 可见性矩阵误用 | 将"字段归属错位"误判为"可见性死链"--聊天室级字段两个角色都能看到，问题不是"对方看不到指针"而是"角色私有逻辑不应出现在公共法字段"。对策：Phase 0 字段归属判定必须先于 Phase 1 可见性检查 | 2026-08-06 全库对齐审计 #1-#3 |
| 11 | 严重度高估 | 问题真实存在但严重度被高估（如冗余指针判为 P0、参数过大判为断头路）。对策：P0 判定须问"此项是否真的影响运行正确性？"；仅冗余/参数问题降级 P1 | 2026-08-06 全库对齐审计 #3/#14/#19/#20 降级 |
| 12 | 审计误判 | 审计结论本身可能错误（如将同步触发条件引用误判为经济规则权威源矛盾）。对策：审计结论须验证原文上下文；误判须记录并撤回，不留痕迹会导致后续审计沿错误方向重复 | 2026-08-06 全库对齐审计 P1-C 经济规则误判 |
| 13 | 格式数量低估 | bold/emoji/编号等格式问题数量靠估算会严重低估（预估 80+ 处，实际 159 处）。对策：Phase 1 机械核验阶段用 grep 统计精确数量，不靠估算 | 2026-08-06 全库对齐审计 P2 bold 清理 |
| 14 | 不确定性引用 | 引用未用 `§` 前缀，用口语化简称（如"经济条""人类敌对律"）指向规则，LLM 需推断目标。对策：Phase 1.1 引用活性检查不能只扫 `§`，还要扫描"按 X 条""按 X 律""见 X 规则"等口语化引用模式；Phase 5.5 跨句引用一致性检查同一句中是否有 `§` 引用和非 `§` 引用并存 | 2026-08-06 人工发现 0-2 L21 |
| 15 | 不必要引用 | 同 section 内的条目互相引用（如 L21 引用 L23，相隔 2 行），WM 逐行阅读时自然读到，引用是冗余的前向引用。对策：Phase 5.2 行级审计增加"引用必要性"判定--同 section 内的引用默认冗余（除非相隔超过 20 行或跨子 section）；跨 section 引用才需要 `§` 指针。概述层应内联关键信息而非引用同 section 后续条目 | 2026-08-06 人工发现 0-2 L21 |
| 16 | 概述层引用数据表 | 概述层（如 [世界硬规则]）引用纯数据表（如 [弹药口径换算表]）是层级混淆：概述层应引用规则机制（驱动逻辑/判定规则），不应引用数据表（换算系数/枚举列表）。LLM 一次性处理整个文件，数据表有明确 section 标题可自行激活，不需要概述层预先建立关联。同 section 其他条目的引用模式可作为判定基准--如果其他条目都引用规则机制，唯独某条引用数据表，即为异常。对策：Phase 5.2 引用必要性判定增加"目标类型"检查--概述层 § 引用的目标应为规则机制，非纯数据表 | 2026-08-06 人工发现 0-2 L21 |
| 17 | 近距后向引用冗余 | 跨 section 但相距很近（<20 行）的后向引用（目标在前面）是冗余的：LLM 已经读过目标内容，已在上下文中。陷阱 #15 只覆盖同 section 内引用，陷阱 #16 只覆盖数据表，未覆盖"跨 section 近距后向引用世界事实"的情况。对策：Phase 5.2 跨 section 引用判定增加方向和距离检查--后向引用（目标在前）且相隔 <20 行时，内联关键信息不做引用；陷阱 #16 的目标类型扩展为"纯数据表或世界事实" | 2026-08-06 人工发现 0-2 L21 §世界现状 |
| 18 | 跨文件引用装配可见性盲区 | "跨文件引用 = 必要"是无条件短路规则，未考虑装配可见性。聊天室级字段（0-1/0-2/0-3）都会被同时装配到 WM/WSK 上下文中，LLM 已有目标内容。但"已有内容"≠"能定位内容"：指向整个字段（如"见 `聊天室 Scenario 字段`"）时，LLM 无法在大量内容中精确定位；指向具体 section（如"见 `聊天室 Scenario 字段 §地图调用规则`"）时，LLM 能通过 section 标题定位，但也能通过标题自行激活。对策：Phase 5.2 跨文件引用按精度和装配可见性判定：(1) 指向整个字段 -> 删除或改为指向具体 section；(2) 指向具体 section 但无独有信息 -> 可删除（LLM 能通过标题自行激活）；(3) 指向具体 section 且有独有信息（如职责边界）-> 保留独有信息，删除引用；(4) 角色卡->角色卡（横向）= 禁止 | 2026-08-06 人工发现 0-2 L26 + 1-3 评估修正 |
| 19 | 跨文件内容重述冗余 | 两个文件中存在语义等价的叙述性内容块（如"运行流程""角色分工"），但没有引用关系。陷阱 #14-#18 解决"引用该不该有"，但未覆盖"内容本身是否冗余"。Phase 1.3 SSOT 重复检测关注规则/映射/枚举的逐字重复，检测不到语义等价但文字不同的重述。对策：Phase 3.3 有效性检查增加"跨文件语义等价"判定--对叙述性内容块（非规则/非数据/非引用的描述性段落），检查其他文件中是否有语义等价的内容；特别关注"运行流程""角色分工""系统机制"等描述性内容，这些通常在 0-1 中已有定义，其他文件中不应重述 | 2026-08-06 人工发现 1-1 L63-L66 |
| 20 | 机械规则跨字段依赖 | 角色字段中的机械规则（路由计算、格式约束、物资渲染、NPC 行为等"怎么做"的具体规则）纯引用聊天室级字段（0-1/0-2），但角色执行时不仔细读聊天室级字段的格式细节，导致规则不执行。陷阱 #14-#19 解决"引用该不该有"和"内容是否冗余"，但未覆盖"角色能不能读到并执行"的问题。实证：WM [主要状态] 压力段格式依赖 0-1 -> 8 处带括号说明违规；WM 路由拓扑依赖原 0-2 [地图调用规则] -> 3 次跳过中间分区；WM 物资渲染格式依赖 0-1 -> 部分物资不写重量；WM NPC 知识边界依赖 0-2 -> 不在自己字段中执行；WSK 消耗投影基准值引用 0-2 §[世界硬规则] 据点持续成本 -> 失效引用（1-3 已内联同条规则）。对策：角色卡（WM/WSK）必须执行的机械规则与数据必须内联到自己的字段（1-x / 2-x）中，不能纯引用聊天室级字段。Phase 5.2 对每个角色卡字段中的 `§聊天室` 引用，判定目标是否为机械规则/数据/公共法；若是，标记为需内联 | 2026-08-07 冒烟测试 + 2026-08-09 2-3 审计实证 |
| 21 | 机械阶段空转 | Phase 1 标注 `[机+人]` 但审计者未实际执行工具命令，仅靠阅读推理完成"机械验证"。方法论写"工具：grep/rg"是能力声明而非执行保证；不跑 grep 就不会发现跨文件枚举不一致（如 7 值 vs 20 值）、精确计数偏差（预估 4 处 vs 实际 8 处）等只能靠工具捕获的客观事实。对策：Phase 1 每项 `[机]` 检查必须输出实际 grep 命令 + 结果计数作为执行证据；无证据的"已检查"结论不接受 | 2026-08-09 2-3 审计实证 |
| 22 | 枚举跨文件不一致 | 同一概念（如 Role 取值、天气枚举）在不同文件中的枚举值集合不匹配，但各文件均未声明自身为非权威源。此前陷阱 #7（节标题豁免）#14-#18（引用类）#19（内容重述）均未覆盖"枚举值集合本身不一致"的情况。对策：Phase 1 增加 1.6 跨文件枚举一致性检查--本文件含枚举/列表时，grep 全库找权威定义源，逐项比对值集合；Phase 5.4 枚举检验增加"跨文件枚举值集合一致吗"作为第四问 | 2026-08-09 2-3 审计实证（Role 枚举 7 值 vs 20 值） |

#### 可自动化清单（建议工具化）

| 步骤 | 可脚本化 | 建议 |
|------|----------|------|
| 1.1 引用活性 | 是 | grep 脚本，全库 § 引用 → 目标节存在性 |
| 1.2 术语一致性 | 部分 | 术语表 + grep 匹配 |
| 1.3 重复检测 | 是 | 相似度/diff 脚本 |
| 1.4 格式合规（粗筛） | 是 | regex lint（backticks / [section] / 交叉引用格式） |
| 1.6 跨文件枚举一致性 | 是 | grep + 集合比对脚本（提取枚举值 -> grep 权威源 -> 差集） |
| 2.1–2.7 形态黑名单 | 部分 | 句型模式匹配脚本 |
| 3.x 节级漏斗 | 否 | 纯人工判断 |
| 4.1 接口完备性 | 部分 | 机制构件提取 + 矩阵模板 |
| 5.x 行级审计 | 否 | 纯人工判断 |
| 6.3 重复 token | 是 | 重复段检测脚本 |

### 0.12 提示词格式规范清单（2026-08-06 定案）

> **定案背景**：经 LLM 功能性验证，提示词中的符号分为"结构性"（LLM 文档解析依赖）与"强调性"（对 LLM 无实质功能）两类。本节明确必须保留与可以删除的符号类型，指导后续所有提示词文件的编写与维护。

#### 必须保留（结构性 — LLM 文档解析依赖）

| 符号 | 用途 | 示例 |
|------|------|------|
| `[section]` | 段落边界 + 交叉引用锚点 | `[系统角色分工与通讯]` |
| `-` 列表项 | 并列项结构化 | `- World Master 是唯一场景裁定者` |
| `` `backticks` `` | 标记字面量/技术术语/输出标签 | `` `[State Update]` ``、`` `World Master` `` |
| `()` 括号 | 标准标点，缩写/澄清 | `（WM）`、`（WSK）` |

#### 可以删除（强调性 — 对 LLM 无实质功能）

| 符号 | 说明 | 处理方式 |
|------|------|----------|
| `【】` 中文括号标题 | 元声明标记，不改变 LLM 理解 | 删除整行，保留身份声明纯文本 |
| `**bold**` 加粗 | 对 LLM 注意力影响极小 | 现有内容应逐步清理，新内容禁止使用 |

#### 身份声明格式规范
- 用纯文本声明，不加 `【】` 括号标题
- 正确示例（0-1 格式）：`本字段承载全局硬规则；规则冲突时以本字段为准。`
- 错误示例（已弃用）：`【Scenario 平台锚点】` + bullet 声明

#### 交叉引用格式规范
- 段级引用：`§[section名]`
- 子段引用：`§[section名]「子部分名」`（如 `§[使用原则]「第一行必备」`）
- 跨文件引用：`聊天室 X 字段 §[section名]` 或 `World Master（本角色）的 X 字段 §[section名]`
- **禁止**用 `§子标题名` 直接引用非 `[section]` 格式的内容（如 bullet point 或纯文本标题）

### 0.13 提示词工程设计原则（2026-08-11 定案）

> 本节是 prompt 内容编写的设计原则，与 §0.5 系统级原则（架构层）正交。§0.5 关注"字段怎么组织、机制怎么闭环"（架构层），本节关注"prompt 内容怎么写"（编写层）。两节共同构成开发者在编写 prompt 时的设计原则基座，与 §十九 开发方法论（流程层）、§0.11 PAM（审计层）形成完整体系。
>
> 来源：从库存系统重构（D5）的实战经验中提炼，系统化散落在 project_memory 和子系统设计文档中的 prompt 工程经验。原则编号续接 §0.5 原则①-⑤。

#### A. 术语与定义

原则⑥ 术语 SSOT
每个术语只有一个权威定义。标签、字段名、枚举值在全局唯一。
- 同一概念只用一个术语（禁止"据点库存"与"据点核心库存"并存）
- 同一术语只有一个含义（禁止同一术语在不同场景有不同含义）
- 新增术语时必须在本文件或子系统设计文档中定义
- 标签/字段名/枚举值在所有文件中保持一致
- 术语动词/名词区分：描述系统角色（WM/WSK）对数据和事件的处理行为时，使用"更新"、"维护"、"提交"等词，不使用"记录"、"保存"、"持久化"、"入账"等暗示存储能力的词。理由：系统角色由 LLM 驱动，没有持久化存储能力，"记录/保存"暗示系统有存储功能，与实际机制不符。同一词语作动词时替换，作名词时保留（如"记录"作动词改为"更新/维护"，作名词"近五日主要事件记录"保留；"账本"作为角色定位/输出隐喻名词保留，"入账"作动词改为"提交"）
- 引用必要性：同文件内 section 引用默认不加（LLM 加载整文件时已在上下文）；仅跨文件 SSOT 规则加引用
- 审计检查：跨文件术语扫描，识别同义异名和异义同名；Phase 1.6 跨文件枚举一致性检查的扩展；术语动词用法扫描（"记录/保存/持久化/入账"作动词描述系统角色行为时需替换）

原则⑦ 单一职责
每个字段/规则只承担一个职责，避免字段含义重叠。
- 一个字段只描述一类信息（如 Inventory Delta 只记变化量，不记存量）
- 字段之间不重叠（如 Base Structure State 只记结构节点状态，不记库存）
- 设计文档与 prompt 文件的职责分工：设计文档定义"为什么"（设计意图/原则），prompt 文件定义"怎么做"（机械规则）。设计文档不重复 prompt 文件的机械规则，只引用
- 审计检查：字段语义重叠扫描——检查两个字段是否描述同一类信息；检查设计文档是否重复了 prompt 文件的机械规则（审计项列表、格式规则、枚举值列表、输出示例等）

#### B. 规则编写

原则⑧ 目标性指令 vs 机械规则
目标性指令（"尽量节省空间"）LLM 会误用为省略许可；机械规则（"标签后不换行"）LLM 可执行。
- 设计意图通过机械规则落地，不直接作为 prompt 指令
- 目标性指令与硬规则冲突时，LLM 会拿目标性指令当例外许可
- 审计检查：扫描"尽量""尽可能""优先"等目标性措辞，评估是否可转化为机械规则

原则⑨ 防反例设计
避免省略成为反例。"标签必出，无变化时内容留空"——不得省略，省略会成为反例。
- 标签必出，无变化时内容留空（不写"无变化时不输出标签"）
- 固定字段每次必出（不写"无内容时可省略字段"）
- 审计检查：扫描"省略""不写""可不输出"等措辞，评估是否会产生省略反例

原则⑩ 负面指令最小化
优先用正面指令（"做 Y"），最小化负面指令（"不要做 X"）。负面指令的注意力锚点是被禁止的行为本身，LLM 可能模仿被禁止的行为。
- 非安全关键场景的负面指令应转化为正面指令
- 安全关键场景保留负面指令（如"不得脑补""不得自行扣减"）
- 同一条规则中同时包含正面和负面表述时，正面表述在前、负面表述在后（LLM 注意力优先激活前置内容，正面在前先建立正确行为锚点）
- 当正面表述已足够约束行为时，不附加负面表述（正面表述在逻辑上已排除负面行为时，负面表述是冗余的，且负面注意力锚点有害）
- 审计检查：负面指令扫描——评估每条负面指令是否可转化为正面指令；保留的安全关键负面指令需有充分理由；检查同一条规则中正面表述是否在前；检查正面表述已足够约束时是否仍附加了冗余负面表述
- 反例（库存系统重构 D5 实战教训）：
  - 错误写法："根据 WM 裁定更新存量：WSK 根据 WM 在 [判定] 中裁定的消耗量更新库存存量，不根据消耗速率自行计算扣减量"
  - 正确写法："根据 WM 裁定更新存量：WSK 根据 WM 在 [判定] 中裁定的消耗量更新库存存量"
  - 错误原因：正面表述"根据 WM 裁定"在逻辑上已排除"自行计算"，负面表述冗余且"消耗速率""自行计算""扣减"等概念激活反效果

原则⑪ 边界明确化
每个规则明确"做什么"和"不做什么"。DO/SILENT 决策表是此原则的标准实现。
- 每个角色用 DO/SILENT 决策表明确职责边界
- 规则应明确适用范围和例外条件
- 审计检查：检查规则是否只有"做什么"而无"不做什么"（或反之），评估边界是否完整

#### C. 结构与注意力

原则⑫ 注意力锚点
LLM 整体全看（并行 attention）非层层查阅，注意力分布受位置、重复、结构标记、指令强度影响。
- 每次输出都用到的规则放 Scene（靠前+强指令）
- 按需触发的判定/边界/流程规则放 Additional（中段）
- 枚举/表格/格式模板放 Extra（靠后+长表格）
- SSOT 声明（"以 X 字段为权威"）对 LLM 无效，不写入 prompt
- 元信息（关于信息的信息，如"此原则不直接作为 prompt 内容"）应放在内容前，让 LLM 先建立认知框架再读内容
- 审计检查：检查关键规则是否在正确位置；扫描 SSOT 声明是否残留；检查元信息是否放在内容前

原则⑬ 输出结构分离
顺序声明与细节约束分离。箭头只声明出场次序，约束独立成条。
- 输出结构用箭头序列声明出场次序（如 `A -> B -> C`）
- 细节约束用独立条目说明（不在箭头序列中嵌入括号说明）
- 审计检查：检查输出结构是否将顺序声明与细节约束混在一起

原则⑭ 角色定框
身份声明压制 LLM 错误本能。"你是 X，不是 Y"明确角色边界。
- 每个角色卡用 [身份] 声明压制错误本能（凑齐字段/自动提交/过度展开/礼貌自介/叙事漂移）
- LLM 心理预设是 Scene 层的身份定框，指导 DO/SILENT 决策表的执行心理
- 审计检查：检查角色卡是否有 [身份] 和 [LLM 心理预设]；检查预设是否对应 DO/SILENT 决策表

#### D. 示例与验证

原则⑮ 正反示例
每条输出格式规则配正反示例。反例特别重要——LLM 容易从反例中学习错误模式。
- 正例展示正确格式
- 反例展示常见错误（标注错误原因）
- 反例不得使用可能被 LLM 模仿的完整错误输出——用文字描述错误类型，或用 ✅/❌ 标注正反
- 审计检查：检查输出格式规则是否有正反示例；检查反例是否可能被模仿

原则⑯ 输出对齐
提示词中"必须输出"的列举顺序 = 权威输出模板的字段顺序。
- 角色字段中"必须维护和输出的内容"列表顺序必须与权威输出模板（如 [完整视图]）的字段编号顺序一致
- 不得增删字段（如权威模板有 6 字段，列举不得少列也不得多列）
- 审计检查：对比角色字段"必须输出"列表与权威输出模板，检查字段数量和顺序是否一致

原则⑰ DO/SILENT 职责分工
DO = 触发时必须执行的核心动作；SILENT = DO 无法覆盖的反面边界。
- DO 表只写核心动作，不写格式约束（格式约束放输出模板或独立条目）
- SILENT 表只写 DO 无法覆盖的边界，不写 DO 的反面回声（如"不输出叙事文本"是 Scene 层职责，不属于 WSK 的 SILENT）
- 审计检查：检查 DO 表是否混入格式约束；检查 SILENT 表是否重复了其他角色的职责

原则⑱ 跨角色复制标注
跨角色需要同一术语/规则时复制定义，复制关系记录在 PB §21 术语表中（不在 prompt 文件中标注），便于未来一方修改时 grep 找到另一方。
- 跨角色复制时不在 prompt 文件中放任何注释或标记（注释会进入 LLM 上下文，影响输出）
- 复制关系仅记录在 PB §21 术语表的"复制来源"列中
- 审计检查：扫描 prompt 文件中是否有 `# dup`、`<!-- dup -->` 等维护性注释；检查 PB §21 术语表是否记录了所有跨角色复制关系

#### E. 字段承载与 LLM 注意力机制

原则⑲ LLM 上下文注意力分层
LLM 将聊天室字段和角色卡字段拼装为一个大表；聊天室字段在前部，角色卡在后部。前部内容被动吸收有效（塑造行为基调），但精确反查不可靠（注意力稀释 + 距离衰减）。后部内容靠近生成端，精确检索可靠。
- 总则和基座放聊天室字段（被动吸收足以塑造行为基调）
- 具体规则、表、机械流程放角色卡（需精确检索，必须就近放置）
- 审计检查：检查"总则和基座"是否在聊天室字段；检查"具体规则/表/机械"是否在角色卡

原则⑳ Opening Messages 权重衰减
Opening Messages 主要影响开场；进入中后期后，权重被对话历史和记忆稀释。不能当长期规则容器。
- Opening Messages 只放一次性开场内容（台词/钩子/氛围）
- 需要持续生效的规则不得放 Opening Messages
- 审计检查：扫描 Opening Messages 中是否有规则性内容

原则㉑ 公共法原则
聊天室字段服务所有角色（WM/WSK/NPC 卡），只放公共法；WM 独有的执行协议、输出格式、裁定流程、数据表放 WM 卡。
- 聊天室字段中不得出现 WM 专属内容（防止越权污染其他角色）
- 公共法 = 所有角色共需的总则和基座（世界硬规则/NPC 铁律/知情范围/阵营描述性介绍）
- 审计检查：扫描聊天室字段中是否有 WM 专属执行规则、输出格式、裁定流程

原则㉒ 阵营内容拆分
阵营和势力的描述性介绍（起源、活动区域、组织架构、主要人员、利益诉求）放聊天室 Private Details 字段（所有角色需知势力边界）；阵营活动的具体规则（首领基线、活动表、调用规则）放 WM 卡。
- 描述性介绍让所有角色能与世界基座互动
- 活动规则只需 WM 精确检索
- 审计检查：检查阵营描述是否在 Private Details；检查阵营活动规则是否在 WM 卡

> **与 §0.5 的关系**：§0.5 原则①-⑤是架构层原则（字段组织/机制闭环/认知适配/退化设计/Token 预算），本节原则⑥-㉒是编写层原则（术语/职责/规则编写/结构/示例/输出对齐/职责分工/复制标注/字段承载与注意力）。两节正交：§0.5 回答"这个内容放哪里、机制是否闭环"，§0.13 回答"这个内容怎么写、怎么让 LLM 可靠执行"。
>
> **与 §十九 的关系**：§十九 是开发流程（按什么步骤开发），§0.13 是开发原则（开发时遵循什么设计原则）。§十九 Step 3（机制设计）和 Step 4（编写）应对照 §0.13 检查。
>
> **与 §0.11 PAM 的关系**：§0.13 的每条原则都可作为审计检查项，补充到 PAM 的 Phase 2 形态扫描或 Phase 3 节级漏斗中。

---

## 一、项目概述

本项目是面向 **Ourdream.ai** 平台的**硬核后末日生存群聊游戏系统**，项目名称为 **Ash Harbor（灰港）**。当前由 **9 个落盘提示词文件 + 4 个项目参考文档** 组成：

- **落盘提示词文件**（共 9 个，承担平台字段）位于 `./Prompt_File/`
  - 聊天室装配字段（供所有平台角色卡读取的世界知识底座）：`0-1_Private_Details.md`（对玩家隐蔽，承载公共法——所有角色共需的总则和基座）+ `0-2_Scenario.md`（对玩家公开，承载叙事开场——世界底座的叙事呈现）
  - 运行时调整字段：`0-3_自定义指令.md`（玩家在游戏过程中给出的动态调整指令；不适合承载全局静态指令）
  - WM 角色卡：`1-1` / `1-2` / `1-3`
  - WSK 角色卡：`2-1` / `2-2` / `2-3`
- **项目参考文档**（共 5 个，仅作辅助索引，不承担平台字段）：
  - `PROJECT_BLUEPRINT.md`（**本文件，简称 PB，本项目唯一基准文档（SSOT）**）：承载项目设计说明、架构定义、机制闭环、工程原则体系、开发方法论及审计原则。任何项目功能、机制、文件结构、平台映射、设计决策、开发方法论、审计原则、变更原因，以本文件所述为准。跨文件引用通过本文件提供的章节号定位。
  - `OURDREAM_PLATFORM_REFERENCE.md`（**平台机制及相关知识参考文档**）：记录 OurDream.ai 平台字段体系、可见性边界、触发机制、字符限额、持久化层等平台层基线知识。供提示词优化、字段裁剪、规则校对、漂移审计、新人上手时查阅。
  - `OPTIMIZATION_ROADMAP_v1.md`（**原工程优化记录**）：记录本会话累积评估的待优化项、已实施优化、已放弃方案及实施优先级建议。作为版本迭代与治理决策的历史追溯依据。
  - `审计历史记录.md`（**历史审计归档**）：归档 PROJECT_BLUEPRINT.md 的历史审计记录（原 §十三-§十七），仅供历史追溯，不作为当前规范依据。
  - `The Unnecessarily In-Depth OurDream Character Creation Guide.md`（**平台字段官方指南**）：OurDream.ai 平台角色创建官方指南的完整存档，供理解平台字段设计意图时参考。

> **架构演进（v1.19 起）**：原 11 文件 + 3 角色（WM/WSK/WER）架构已演进为 8 文件 + 2 角色（WM/WSK）架构。**World Event Recorder（WER）组件已于 v1.19 彻底删除**，其职责已由用户手动维护的 Pinned Memory 承担。详见 §2.1.1。后续新增 `0-3_自定义指令.md`，当前共 9 个落盘文件。

系统采用"**WM 唯一稳定前台 + WSK 后台逻辑层**"的运行架构，构建可长期运行的后末日生存世界——**灰港（Ash Harbor）**。

### 核心设计理念

> **低资源、低信息、高风险、强后果、慢积累、反爽文**

系统追求真实可信、前后一致、持续演化的硬核生存世界。玩家的每一次行动都有代价，每一个决定都有长期后果。

### 平台机制特性

Ourdream.ai 平台的关键机制（详见 §2.6 与 [OURDREAM_PLATFORM_REFERENCE.md](file:///d:/Mycode/AshHarbor_OD_Trae/Ashport_Ourdream/OURDREAM_PLATFORM_REFERENCE.md)）：
- **后台角色不会自动运行**：只在用户手动点击时被触发
- **聊天室共享**：所有角色共享同一个聊天室，后台角色被触发时可看到全部历史
- **聊天室级持久化字段**：
  - `聊天室 Scenario 字段`（`0-2_Scenario.md`）：50,000 字符，对玩家公开，承载叙事开场——世界底座的叙事呈现，供所有平台角色卡读取
  - `聊天室 Private Details 字段`（`0-1_Private_Details.md`）：≥50,000 字符，对玩家隐蔽，承载公共法——所有角色共需的总则和基座，供所有平台角色卡读取
  - `自定义指令字段`（`0-3_自定义指令.md`）：玩家在游戏过程中给出的动态调整指令（不适合承载全局静态指令）
  - `Pinned Memory`：持久化层，承载历史账本（用户手动维护）
- **角色级字段**：
  - `Scene`（`1-1` / `2-1`）：50,000 字符，身份与平台锚点
  - `Additional Personality Details`（`1-2` / `2-2`）：50,000 字符，决策表与心理预设
  - `Extra Details`（`1-3` / `2-3`）：100,000 字符，动态机制与模板

### 1.1 提示词字段承载分工规范（优化首个规划）

> **定位**：本节是对各提示词字段所承载内容的**规范化重新设计**，是后续逐字段优化的基础框架与首要依据。任何字段内容的增删、迁移、重写都必须先对照本节确认目标字段的承载分工是否匹配。
>
> 字段内组织遵循原则①：最重要的硬锁与核心规则放字段顶部；人格与系统规则分离；内容结构化、可执行优先于字段位置。
>
> **关联规范**：修改某字段时需同步检查哪些字段，见优化文档 §9.8.2 字段间依赖关系图。

#### 1.1.1 聊天室装配字段与运行时调整字段

**装配层（聊天室创建时设定，全局持久；供所有平台角色卡读取的世界知识底座）**：

- **Private Details = 公共法（对玩家隐蔽）**：所有角色共需的总则和基座。对玩家在平台界面上隐蔽，但所有平台角色卡均可读取（被动吸收有效，精确反查不可靠）。
  - 世界概况（背景、生存环境、生产方式）
  - 世界硬规则（资源有限且有代价、获取/消耗/经济/保暖/安全/人际/环境总则）
  - 环境生存压力系统概念（5 轨名称、状态级、step 基准、影响因素、恢复原则）
  - NPC 反全知 / 反顾问化机制（所有 NPC 角色共需的发言铁律）
  - 跨位转移与知情范围协议（传播规则与枚举）
  - 可消耗物资渲染与记录协议（双轨制原则，不含角色专属格式）
  - 阵营描述性介绍（起源、活动区域、组织架构、主要人员、利益诉求）
  - 常驻NPC层（6 名默认人物清单与场景隔离原则）
  - 优先级声明：本字段 > 其他所有字段
  - 不放：WM 专属输出格式、裁定流程、数据表、调用规则（防止越权污染其他角色）
- **Scenario = 叙事开场（对玩家公开）**：玩家入口，世界底座的叙事呈现。对玩家在平台界面上公开可见。
  - 场景（地点/时间/天气体感）
  - 世界与秩序（阵营生存逻辑、新秩序、世界基调的叙事呈现）
  - 玩家状态（处境、库存、生存压力的叙事呈现）
  - NPC 行为预期（反全知/反顾问化的玩家侧说明）
  - 生存路径与代价概述
  - 不放：规则条文、数据表、裁定流程、阵营活动规则

> **0-1 与 0-2 的区别**：两者都是供所有平台角色卡读取的世界知识底座，区别在于内容形态和对玩家的可见性——0-1 是规则形态的公共法（对玩家隐蔽），0-2 是叙事形态的世界底座（对玩家公开）。0-1 承载“法”（所有角色必须遵守的规则），0-2 承载“体验”（玩家看到的世界呈现）。

> **装配层长度约束（2026-08-14 增补）**：0-X 定位为“基调总体”——为所有平台角色卡（含运行时动态加入的 AI 角色卡）做前置拼接以保持世界基调一致。聊天室字段拼在输入前部，与 WM 发言的开头拼接同处高位，**内容须精简为基调/总则形态，不宜过长过详细**：过长会稀释关键基调的注意力（参见 §0.13 注意力锚点原则），过详细会越界承载角色专属执行规则（执行规则归 1-X/2-X 自包含，见本节 L604“不放”条款）。

**运行时调整层（玩家在游戏过程中按需给出的动态调整指令）**：

- **自定义指令 = 玩家动态调整指令**：玩家在游戏过程中针对具体表现问题给出的调整性指令。不适合承载全局静态指令（全局静态规则归 0-1/0-2）。
  - 当前内容：空（WM 叙事风格静态规则已迁移至 WM 角色字段）
  - **定位偏差已修正（v1.64）**：原 0-3 承载的 WM 叙事风格静态规则已迁移--[反煽情元规则]+[重复抑制] -> 1-1（WM Scene），[感官词抽屉]+5×5 表 -> 1-3（WM Extra Details）；0-3 现仅保留字段说明，等待玩家动态调整指令
  - 适用范围：仅约束 WM 的叙事输出风格，不承载世界设定或裁定规则

> **Pinned Memory**（非平台字段）：用户手动维护的持久化层，承载不可压缩锚点、已关闭事件链、长期格局变化、结构变化。不属于平台字段加载链，承载分工见优化文档 §9.8.3 / §9.2 Layer 5。

#### 1.1.2 角色卡三字段的承载分工

对于 World Master 和 World State Keeper，各角色卡三字段的承载分工规定如下：

> **APD/Extra 分界标准**：APD 承载"判断维度与原则"（LLM 推理时参考），Extra 承载"数据/格式/流程"（LLM 执行时查表）。当内容同时含判断思路和具体数值时，拆分：思路归 APD，数值归 Extra，APD 引用 Extra。完整判定规则与边界情况处理见优化文档 §9.8.1。

**WM Scene**（`1-1`）
职责：定义 WM 的永久元身份 + 持续叙事框架
1. 本质身份声明：不可见、全知、中立的场景渲染器与结果裁定者
2. 长期叙事立场：世界始终在运转、玩家行动有真实后果、场景必须可感知、不剧透、不破坏逻辑，保持沉浸的基本原则；写法：用现在时、元叙述语气即可。
3. 对公共法的承载：聊天室 Private Details 字段承载公共法（被动吸收有效），本字段不重复公共法；WM 专属执行规则、输出格式、裁定流程在 WM 三字段中自包含。
4. 输出契约清单：每轮必出 Scene 叙事 + [主要状态] 状态栏 + 判定块（无事件时输出 `- 无`，Day 推进时必含消耗行）；概率裁决时出 掷骰。具体格式与触发条件在 WM Extra Details 中定义。
5. 只放最高优先级的运行契约，不放细则。

**WM Additional Personality Details**（`1-2`）
职责：定义 World Master 的"风格"与行为哲学。
- 叙事声音与风格（感官优先级、描写密度、对话呈现方式）。
- 裁定哲学（如何判断成功/失败/部分成功、如何处理模糊意图、如何给出合理后果）。
- 情节推动原则（何时主动推进、何时等待、如何自然引入事件）。
- 与玩家的交互边界（不替玩家说话/思考）。
- 氛围与张力控制方式。
- 叙事基调体现：世界基调（如"世界是残酷的、收获有代价"）的**定义**放在聊天室 Private Details 字段（公共法，被动吸收）；本字段只放 WM 如何在叙事中**体现**这些基调的写法指引（描写倾向、回避倾向），不重复定义基调本身。

**WM Extra Details**（`1-3`）
职责：全部可执行机械系统 + 可执行细节。
- 详细结果裁定系统
- 状态管理与时间推进机制
- 输出格式的具体模块实现与示例
- 引用指针：不在本字段声明跨文件引用（聊天室字段被动吸收有效，精确反查不可靠）；WM 独有规则在 WM 三字段中自包含，不依赖聊天室字段的精确检索。
- 公共法的可执行细化：阵营活动规则（首领基线、活动表、调用规则）等 WM 独有执行规则在本字段自包含；阵营描述性介绍在聊天室 Private Details 字段中（被动吸收）。
- 子分类组织：1-3 内容按 A.世界模拟机制 / B.时间推进系统 / C.裁定执行系统 / D.参考数据表 四子分类组织，详见 `OPTIMIZATION_ROADMAP_v1.md` §9.8.4。

**WSK Scene**（`2-1`）
职责：定义 WSK 的永久元身份 + 输出/静默契约框架
1. 本质身份声明：硬状态管理员与审计者；只记账、不叙事、不裁定、不建议、不推动剧情。
2. 触发与静默契约：仅在被点击时触发；每次触发都输出完整视图（扫描不到已成立变化时 Delta 留空、其余照抄基线）；被点选不是新事件。
3. 输出总览：只允许 [State Update] 完整视图一类白名单输出（2026-08-15 起，`-` 空白标记已废除）；账本之外不输出任何内容。
4. 输入源声明：WSK 的输入为 World Master 在对话历史中的实际叙事输出（Scene 叙事 + [主要状态] 状态栏），非 WM 角色卡字段内容；不得引用 WM 角色卡（见 §1.2 跨角色引用禁令）。
5. 只放最高优先级的运行契约，不放细则。

**WSK Additional Personality Details**（`2-2`）
职责：定义 WSK 的记账判断逻辑与记录纪律。
> **字段名说明**：平台字段名为"Additional Personality Details"，但 WSK 无叙事风格、无人格特征；本字段实际承载的是记账决策规则而非"人格"。LLM 加载时应按"记账决策规则"而非"人格特征"建立语义框架。
- 记账决策表：DO 单表（每次触发都输出完整视图，2026-08-15 起）。
- 记录什么：必须记录的硬状态大类（时间 / 环境 / 角色状态 / 库存 / 装备 / 关系 / 行为侧威胁 / 据点 / 势力活动）。
- 各状态怎么记：环境压力 / 库存 / 据点庇护 / 关系敌意 / 势力活动等的记录规则。
- 记录纪律：只认 World Master 已写出的已成立变化，不脑补、不推测、不替叙事补全；提交前检查与最低提交标准。

**WSK Extra Details**（`2-3`）
职责：全部可执行输出机制 + 提取细则 + 模板。
- [State Update] 模板与完整视图字段清单。
- 压缩规则与输出策略。
- 提取纪律（如何从 World Master 叙事强语义提取已成立变化）。
- 具体记录格式（可消耗物资 kg、弹药口径合并等）与最低提交标准细则。

### 1.2 引用写法规范

> 与 §1.1 配对：§1.1 定义"字段装什么"，本节定义"字段间怎么引用"。两者共同构成字段承载分工的完整操作规范。
> 本规范定案于 2026-07-24 引用有效性审计（见 审计历史记录.md §十六），原 §16.3 提取至此作为当前规范依据。

**核心原则**：引用目标必须是"平台实体"（聊天室字段名 / 本角色字段名 / 角色全称+字段名）。开发编号（如 `1-3`、`2-2`）与简称只是仓库管理工具，不得出现在提示词引用串中——运行时无法解析。

| 引用类型 | 统一格式 | 示例 |
|---|---|---|
| 指向聊天室级字段 | `聊天室 {Private Details / Scenario / 自定义指令} 字段 §[章节名]` | `聊天室 Private Details 字段 §[平台优先锚点]` |
| 角色内跨字段 | `{字段名} §[章节名]`（字段开头已声明身份，无需全限定路径） | `Extra Details §[生存压力时间阈值表]` |
| 跨文件指向某角色卡 | `{角色全称} 的 {字段名} 字段 §[章节名]`（不带"（本角色）"） | `World State Keeper 的 Extra Details 字段 §[State Update 模板]` |
| 跨角色内容依赖 | **禁止引用对方角色卡**；改指"对话历史中的实际输出" | 指向 WSK 的 [State Update] 输出，而非 WSK 的角色卡字段 |

**跨角色引用禁令的原理**：跨卡引用必然失效——内容未内联则功能断裂（规则丢失），内容已内联则冗余指针（LLM 可能尝试调用不可见内容并幻觉补全）。对话历史是跨角色唯一活通道。

完整审计方法见 `OURDREAM_PLATFORM_REFERENCE.md §2.1a`。

---

## 二、系统架构：单前台 + 双后台

### 2.1 架构总览

系统采用"一个稳定前台 + 两个后台账本角色"的架构：

```
┌─────────────────────────────────────────────────────────────┐
│                      World Master (WM)                       │
│  唯一稳定前台：Scene生成 / 行动裁决 / 交易结算 / 状态栏输出  │
│  角色字段：1-1(Scene) / 1-2(Additional) / 1-3(Extra)        │
│  聊天室 Private Details 字段：0-1_Private_Details.md         │
│  聊天室 Scenario 字段：0-2_Scenario.md（共享读取）           │
└──────────────┬──────────────────────────────────────────────┘
               │ Scene 叙事 + 状态栏
               │ (强语义提取)
               ▼
┌─────────────────────────────────────────┐
│      World State Keeper (WSK)            │
│      唯一硬状态管理员                      │
│      字段：2-1 ~ 2-3                     │
│      输出：[State Update]（完整视图）/ `-`（空白标记）│
└─────────────────────────────────────────┘
```

> **WM 唯一稳定前台 + WSK 后台**（v1.19 起 WER 已删除，详见 §2.1.1）。两个角色共享读取两个聊天室级字段（`0-2` + `0-1`），不承担平台字段加载任务的差异。详见 §2.6。
>
> **通讯机制（审计已定案）**：WM 不再向 WSK 推送结构化同步块；WSK 改为**强语义提取** WM 的 Scene 叙事与状态栏中已明确写出的已成立变化。WSK 每次触发都输出 `[State Update]` 完整视图（无可提取变化时 Delta 留空、其余照抄基线，2026-08-15 起）；缺失字段标记 `未确认` 并从上轮官方账本沿用，不因字段缺失而拒绝整笔变化；不再输出接受回执。`[判定]` / `[掷骰]` / 子结构块等展示块仅供用户阅读，**不得作为通讯载体**。详见 §2.3（审计定案见 审计历史记录.md §十三）。

### 2.2 各角色职责详解

`0-1_Private_Details.md` 是聊天室 Private Details 字段（全局硬规则）；`0-2_Scenario.md` 是聊天室 Scenario 字段（叙事开场）。

#### World Master（WM）- 唯一稳定前台

| 维度 | 说明 |
|------|------|
| **核心职责** | Scene 生成、行动裁决、交易/关系/敌对结算、状态栏输出 |
| **前台权限** | 唯一前台发言者，负责自然述事和结构化输出（含状态栏） |
| **后台权限** | 无（不再向 WSK 推送结构化同步块，由 WSK 强语义提取） |
| **禁止行为** | 不替用户做决定、不自行补库存、不维护官方时间 |
| **叙事视角** | 由平台内置第一人称/第三人称选项控制，提示词不再干预（2026-07-24 已删除第二人称强制规则） |
| **字段语义** | `1-1 / 1-2 / 1-3` 属于 WM 角色字段 |

#### World State Keeper（WSK）- 后台硬状态管理员

| 维度 | 说明 |
|------|------|
| **核心职责** | 库存、关系、威胁、据点、事件账本的强语义提取与滚动维护；完整视图是唯一工作产品（EP-WSK-1） |
| **输出反应** | 每次触发都输出 `[State Update]` 完整视图（2026-08-15 起；无变化时 Delta 留空、其余照抄基线原文，`-` 空白标记已废除） |
| **第一行格式** | `D{day}-T{turn}`（Day-Turn 索引，以最近一次 WM `[主要状态]` 为准；时间由 WM 唯一决定，WSK 不自算时间） |
| **子段** | 完整视图 6 字段：`Inventory Delta:` / Inventory State / Party Condition / Relationship & Threat / Map Knowledge / Base Structure State / 近五日主要事件 |
| **移动字段** | `Travel Time: {值} ({备注}) / Steps: {值}` 单行；Origin / Route / Destination / Steps / Travel Time 以 WM `[移动]` 行为唯一权威 |
| 库存格式 | 按"功能门类-分类-组别"三级机制归类（2-2 [库存分类与精简原则] + 2-3 [库存功能门类清单]） |
| **触发方式** | 用户手动点击 → 强语义提取 WM Scene 叙事中已明确写出的已成立变化 |
| **读取范围** | 同步读取 `0-2_Scenario.md` 与 `0-1_Private_Details.md` 获取世界边界与全局硬规则 |
| **决策表** | 1 张：DO（每次触发都输出完整视图；不替 WM 推进，只提取已成立变化） |
| **审计职责** | 校验 WM 库存输出是否按三级归类机制归类，未合规时输出修正版（标注"库存归类修正"） |
| **权威地位** | 输出的 Inventory State / 完整视图是权威值，WM 需在下一轮采用 |

#### 2.1.1 WER 组件删除说明（v1.19）

**World Event Recorder（WER）** 组件自 v1.19 起已**彻底删除**：

| 阶段 | 操作 | 原因 |
|------|------|------|
| v1.16 | 删不可压缩锚点机制 | 实际不生效 |
| v1.16 | 删 WER 压缩机制 | 不生效 |
| v1.16 | 精简 WER 输出模板 | 冗余段清理 |
| v1.19 | 删除 WER 全部 3 个文件 | 长期档案职责由 Pinned Memory 承担 |
| v1.20 | WSK 接管"近五日主要事件"实验字段 | 实验性机制，语义提取 |

**原因**：
1. WER 长期档案功能实际由用户手动维护的 Pinned Memory 承担（user 复制 `## 新增` 段到 Pinned Memory）
2. WER 压缩机制（10日/30日/30+）不生效——Pinned Memory 不受 WER 内部压缩影响
3. WER 不可压缩锚点机制（死亡/失踪等）"强制保留"实际不生效
4. 架构简化：8 文件 + 2 角色比 11 文件 + 3 角色更易维护

**当前架构**：WM（前台）+ WSK（后台硬状态） + 用户手动 Pinned Memory（长期档案）

### 2.3 通讯机制（强语义提取）

WM 与 WSK 的通讯**不**通过结构化同步块进行；WSK 通过**强语义提取** WM 的 Scene 叙事与状态栏中已明确写出的已成立变化并提交。

**核心规则**：

| 维度 | 说明 |
|------|------|
| **通讯载体** | WM Scene 叙事 + `[主要状态]` 状态栏；**不含** `[判定]` / `[掷骰]` / 子结构块等展示块 |
| **WSK 提取源** | Scene 叙事中已明确写出的时间/地点/库存/伤病/关系/移动/天气等已成立变化 |
| **展示块隔离** | `[判定]` / `[掷骰]` / 子结构块仅供用户阅读，**不得**作为 WSK 的提取源 |
| **缺失字段处理** | 缺失字段标记 `未确认` 并从上轮官方账本沿用，不因字段缺失而拒绝整笔变化 |
| **接受回执** | **不输出**（输出账本本身即代表隐式接受，无需额外回执） |
| **决策表** | WSK 1 张：DO（每次触发都输出完整视图，2026-08-15 起） |
| **无变化协议** | Scene 叙事中无新成立变化可提取 -> Inventory Delta 留空，其余字段照抄最近一次 [State Update] 基线原文输出完整视图（`-` 空白标记已废除）；成功提交后不输出任何额外文字；不指向本角色的消息忽略 |

**已废弃机制（历史说明）**：

| 机制 | 状态 | 替代 |
|------|------|------|
| 6 种 Commit Type（state/event/both/offscreen-*） | 已废弃 | 强语义提取 |
| WM 推送同步块 | 已废弃 | WSK 自提取 Scene 叙事 |
| `[Event Commit Receipt]` 等接受回执 | 已废弃 | 输出账本本身即接受 |
| ESCALATE 决策表 | 已废弃 | DO 单表 |
| REJECT / SILENT 决策（`[Commit Rejected]` / `-` 空白标记） | **已废弃（2026-08-15）** | 每次触发都输出完整视图（DO 单表） |
| **WER 长期档案** | **已删除（v1.19）** | Pinned Memory（用户手动维护） |
| **不可压缩锚点** | **已删除（v1.16）** | Pinned Memory 自然保留 |
| **Market State** | **已删除（v1.22）** | 不再追踪市场状态字段 |

### 2.4 手动触发工作流

用户手动点击 WSK → WSK 强语义提取 WM Scene 叙事中已明确写出的已成立变化 → 提交/拒绝。流程：

```
1. 用户手动点击 WSK
2. 角色加载全局提示词（0-1 / 0-2 + 本角色三层）
3. 角色扫描聊天历史中 WM 最新 Scene 叙事 + 状态栏
4. 强语义提取已成立的 state 变化（**不包含时间字段**——时间由 WM 唯一决定）
5. 提取到已成立变化 -> DO：输出 [State Update] 完整视图（6 字段），更新账本
6. 字段缺失 -> 标记 `未确认` 并从上轮账本沿用，不拒绝整笔变化
7. 无新成立变化 -> 仍输出完整视图（Delta 留空，其余照抄基线；`-` 已废除）
```

### 2.5 Snapshot 权威层级

| 层级 | 来源 | 性质 |
|------|------|------|
| 权威 | World State Keeper 最近 `[State Update]` 中的 Snapshot | 可信，WM 必须采用 |
| 临时估算 | World Master 自维护 Snapshot | 可能漂移，需定期替换 |
| 历史值 | 上一份有效 Snapshot | 当以上两者均无时使用 |

### 2.6 聊天室 Scenario 字段与 0-2_Scenario.md

#### 2.6.1 平台字段与落盘文件映射

`./Prompt_File/` 下的 9 个落盘文件按 OurDream 平台字段映射如下：

| 平台字段 | 落盘文件 | 角色上限 | 共享范围 | 内容性质 |
|----------|----------|---------:|----------|----------|
| `聊天室 Scenario 字段` | `0-2_Scenario.md` | 50,000 | WM/WSK 共享 | 叙事开场 |
| `聊天室 Private Details 字段` | `0-1_Private_Details.md` | 15,000 | WM/WSK 共享 | 全局硬规则与同步协议 |
| `自定义指令字段` | `0-3_自定义指令.md` | - | WM 适用 | 玩家动态调整指令（WM 风格规则已迁至 1-1/1-3） |
| 角色卡 Scene | `1-1` / `2-1` | 50,000 | 仅本角色 | 身份、平台锚点、基础协议 |
| 角色卡 Additional Personality Details | `1-2` / `2-2` | 50,000 | 仅本角色 | 决策表、心理预设、详细规则 |
| 角色卡 Extra Details | `1-3` / `2-3` | 100,000 | 仅本角色 | 动态机制、模板、参考表 |

> **v1.19 演进**：WER 角色卡 3 个文件（3-1/3-2/3-3）已删除，平台字段从 11 文件减少到 8 文件；后续新增 `0-3_自定义指令.md`，当前共 9 个落盘文件。

#### 2.6.2 0-2_Scenario.md 覆盖范围

`0-2_Scenario.md` 是 `聊天室 Scenario 字段` 的落盘文件，承载**叙事开场**——玩家入口，世界底座的叙事呈现，覆盖：

- 背景故事（灾变 / 灰港 / 灾前 50 万人口 / 灾后 280~390 幸存者）
- 世界现状：生存环境与生产
- 季节与天气系统
- 5 轨生存压力轨道（疲劳 / 体温 / 脱水 / 饥饿 / 伤病）定义
- 敌对阶梯世界律（无信号→远距目击→被接触→升级；4 维裁定细则已迁入 World Master 的 Additional Personality Details 字段）
- NPC 反全知 / 反顾问化机制（Relationship 行为阈值表已迁入 World Master 的 Additional Personality Details 字段）
- 九宫格地图固定拓扑 + 子区域微坐标
- 子地点表（T1-T4 分层，Coast / Town / Village / Farm / Hunting / Industrial / Police / Medic / Firefighter / Military / Prison / Historical / Contaminated / Underground 共 14 个标签）
- 搜刮世界数据（Tier / 标签 / 子地点产出；搜刮过程机制与裁定细则已迁入 World Master 的 Additional Personality Details 字段）
- 世界硬规则（玩家视角游戏宪法：总则三铁律 + 资源获取/消耗/经济/保暖健康/安全庇护/人际信息/环境势力/损耗总单，每条挂接落地机制节）
- 4 阵营
- 6 名常驻NPC
- 弹药口径
- 死亡 / 抢救 / 遗物规则（含 知情范围 传播标注；独立死亡公开性节已并入）

#### 2.6.3 Scenario 与 Private Details 分工

两个聊天室级字段承担不同职责，构成"世界是什么"与"世界怎么运行"的二元结构：

| 维度 | `聊天室 Scenario 字段`（0-2） | `聊天室 Private Details 字段`（0-1） |
|------|-------------------------------|--------------------------------------|
| 内容性质 | 叙事开场 | 全局硬规则与同步协议 |
| 回答问题 | 世界是什么 | 世界怎么运行 |
| 覆盖 | 背景 / 环境 / 地图 / 阵营 / 角色 / 硬规则 | 通讯协议 / 强语义提取 / 恢复顺序 / 输出结构 / 字段语义 |
| 修改频率 | 世界设定稳定时极少改动 | 协议微调时频繁改动 |
| 失效模式 | 描述与当前运行不一致（"事实漂移"） | 协议与平台加载机制不符（"协议漂移"） |

#### 2.6.4 全局读取顺序

所有平台角色按以下顺序加载提示词：

1. `聊天室 Scenario 字段`（`0-2_Scenario.md`）—— 叙事开场
2. `聊天室 Private Details 字段`（`0-1_Private_Details.md`）—— 全局硬规则
3. 本角色 Scene（`1-1` / `2-1`）
4. 本角色 Additional Personality Details（`1-2` / `2-2`）
5. 本角色 Extra Details（`1-3` / `2-3`）

WSK 不复制 Scenario 内容到本角色的 `聊天室 Private Details 字段`；本角色的 Private Details 只承担本角色职责。

#### 2.6.5 角色对 Scenario 字段的读取约束

- **World Master**：每个 Scene 前必须先读 Scenario 恢复世界基底（地图 / 阵营 / 常驻NPC / 经济规则 / 死亡规则）；所有地点 / NPC / 事件描述均以 Scenario 为世界边界。
- **World State Keeper**：被触发时读 Scenario 获取世界边界（地图 / 子地点 / 阵营基线 / 弹药口径）以校验 Scene 描述与官方状态是否一致；不复制 Scenario 内容到账本。

### 2.7 WM 叙事视角硬约束

WM Scene 默认采用**第二人称**（"你"）进行场景描述与剧情推进。这是项目叙事基调的硬约束，跨三层提示词同步强制：

| 文件 | 约束形式 |
|------|----------|
| `0-1_Private_Details.md` | `[叙事视角硬约束]` 段：完整 9 条规则 + 4 项例外（NPC 台词 / `[主要状态]` / 7 字段事务块 / NPC 知识边界原文） |
| `1-1_WorldMaster_Scene.md` | `[平台优先锚点]` 顶部单行提醒：第二人称描写用户角色；NPC 仍用第三人称 |
| `1-2_WorldMaster_Additional_Personality_Details.md` | 绝对规则 0b：强制第二人称，禁止"你/她/玩家/角色名"混用 |

**核心规则**：
- Scene 主体叙事、末句示例、感官 token、动作描写、状态恢复叙述**必须**用"你"指代用户角色
- 不得混用人称：同一 Scene 内不出现"你"与"她"指代同一用户角色
- 不得出现"你/玩家/角色名"混用

**4 项例外**：
1. NPC 台词保留原视角（NPC 说自己，不代言用户）
2. `[主要状态]` 块字段值仍用第三人称客观陈述（位置 / 装备 / 库存）
3. 跨位转移事务块保留结构化字段
4. NPC 知识边界 / 反全知规则原文已使用"他"指代 NPC，保留原意不替换

详细末句四选一示例：`0-1_Private_Details.md` 末段（`你把 X 搁在 Y 上` / 感官细节 / 对话片段 / 物品物理状态）。

---

## 三、世界观设定

以下是基于源文件的补充修正：

### 3.1 背景故事

- **灾变名称**："寂灭"——数周内抹去 99.9% 人类
- **时间线**：灾变后第 3 年
- **地点**：灰港——后苏联背景的中型沿海港口城市
- **人口**：灾前约 50 万 → 现仅存 280~390 人
- **社会状态**：旧秩序已崩溃，新的低水平秩序已长出——围绕粮食、药品、燃料、路线、庇护、情报和暴力形成的低信任秩序

### 3.2 世界现状：生存环境与生产

- 灾后第 3 年仍处于"拾荒高峰末期"：现代遗留物资还在，但高价值可用库存持续减少
- 稀缺核心：抗生素、优质弹药、干净饮水、大型牲畜、成套维修件
- 新兴和再生物资：海盐、煤炭、草药、简易金属制品
- 食物生产：菜地、轮作、捕鱼、采集、狩猎、少量鸡兔养殖
- 柴油、汽油等化学燃料极度稀缺

### 3.3 气候与天气系统

湿润大陆性气候（Dfb），9 种轻量天气枚举。

---

## 四、核心机制设计

### 4.1 地图系统

#### 九宫格固定拓扑

```
西北区(R1C1) │ 北区(R1C2)   │ 东北区(R1C3)
工业区(R2C1) │ 中心区(R2C2) │ 东区(R2C3)
西南区(R3C1) │ 南区(R3C2)   │ 东南区(R3C3)
```

十字形微网格，C 与四向相邻，不承认斜切。

#### 子地点统计（基于 1-3 Extra Details 实际数据）

| 分区 | 子地点数量 | T4 | T3 | T2 | T1 |
|------|-----------|----|----|----|----| 
| 西北区 | 8 | 1 | 2 | 4 | 1 |
| 北区 | 10 | 0 | 2 | 6 | 2 |
| 东北区 | 8 | 0 | 2 | 2 | 4 |
| 工业区 | 13 | 0 | 2 | 9 | 2 |
| 中心区 | 14 | 0 | 4 | 6 | 4 |
| 东区 | 13 | 0 | 2 | 8 | 3 |
| 西南区 | 12 | 0 | 0 | 6 | 6 |
| 南区 | 11 | 0 | 2 | 6 | 3 |
| 东南区 | 13 | 0 | 4 | 8 | 1 |
| **总计** | **99** (+3个重复=102行) | **1** | **20** | **55** | **26** |

#### 城市主干道骨架

"申"字型骨架：
- **北横干道**：连接西北区、北区、东北区
- **中横干道**：连接工业区、中心区、东区（交易、运货、火并最频繁）
- **南横干道**：连接西南区、南区、东南区
- **中轴大道**：连接北区、中心区、南区（最快纵向主干路，最易被盯梢设卡）
- **西纵干道**：贯穿西北区、工业区、西南区
- **东纵干道**：贯穿东北区、东区、东南区

地下管网已堵塞报废，不能作为稳定通行路线。

#### 非移动动作时间基准

| 动作类型 | 默认耗时 |
|----------|----------|
| 短观察、短试探、短等待 | 0-1 step |
| 持续交涉、交易拉扯、基础包扎、热食补给 | 1-2 step |
| 清创、夹板固定、系统清点、加固防位 | 2-4 step |
| 短睡、轮换守夜、长时间跟踪 | 2-6 step |
| 完整睡眠/过夜休整 | 不少于 12 step |

### 4.2 生存压力系统

5 条轻量轨道，6 级状态（stable → strained → weakened → critical → dying → dead）。

#### [生存压力时间阈值表]

| 轨道 | stable | strained | weakened | critical |
|------|--------|----------|----------|----------|
| **脱水** | 0-4h | 4-12h（受 labor/炎热修正） | 12-16h | 16h+ |
| **饥饿** | 0-8h | 16-24h | 24-36h | 36h+ |
| **疲劳** | 0-12h | 12-20h（受守夜/战斗修正） | 20-24h | 24h+ |
| **体温** | 干衣+避风+活动 | 湿衣+静止+低温 | 湿衣+寒冷+疲劳+饥饿（1-2 step 内） | — |

#### 生存锚点

WSK 长期维护的少量官方锚点：
- `Last Meaningful Drink`
- `Last Meaningful Meal`
- `Last True Sleep End`
- `Recent Step Load`
- `Recent Labor Load`
- `Current Cold / Wet Exposure`

### 4.3 概率裁决系统

确定性伪随机公式：`Seed = (Day 末两位 + 表[Turn 个位] + Event Offset) 的末两位`（v1.65 由乘法取模改为加法查表——预生成随机数表驱动，消除 LLM 乘法/取模弱项，详见 WM 效率优化文档 §十）。

### 4.4 人类敌对机制

4 维判断 + 阶梯式升级。

实际文件中对人类敌对机制的描述详尽，特别是：
- 明确了 `hostile-contact` 的 掷骰 只用于"已从旧痕迹/远距目击/被接触阶段，升级到可能发生敌对行动"的最后一步
- 必须同时满足 `被接触 + 有明确动机 + 动手成本可控` 三个前提才进入概率计算
- 增加了"阶段允许衰减与中断"规则：未确认接触、改变路线、天气抹除痕迹等情况下，短期关注应自然结束
- 增加了"普通搜刮是常态行为"的明确声明

### 4.5 经济系统

9mm 弹药本位。

### 4.6 关系系统

2. **时间消耗**：T1=1 step / T2=1-2 step / T3=2-3 step / T4=3-4 step
3. **噪音等级**：轻搜刮(+5暴露) / 中搜刮(+15暴露) / 重搜刮(+30暴露)
4. **翻空判定**：基于"常被翻空程度"和"距上次搜刮经过的时间"
5. **产出质量**：受 Tier、标签、翻空程度、天气、噪声、势力活跃度影响

### 4.7 交易裁定细则

交易分为两档：
- **轻交易**：当场、小额、低风险。只处理成交、支付、价格偏差和轻微关系尾巴
- **重交易**：大宗、跨区、势力参与、高风险物资。展开担保、押货、验货、护卫、抽成、违约后果等

### 4.8 报复链强化规则

- 报复优先挑"最便宜、最稳、最能起示范作用"的目标
- 10+ 官方日无接触/无新损失可降温或冻结
- 目标确认死亡/长期失踪/被更强势力吞没/接受赔偿可关闭直接报复链
- 新损失/新羞辱/新接触会重新激活

### 4.9 机制影响链矩阵

覆盖了 18 个核心机制的上游/下游/关键说明。

---

## 五、势力体系

5 大阵营 + 拾荒者阶层。

实际文件中对每个阵营的描述详尽，每个阵营都包含：
- 定位
- 核心收入/收益
- 组织结构（多条业务线）
- 生存/扩张动机
- 常见执行动作
- 对外关系逻辑（逐一分析与其他阵营的关系）

如水源商会的"据点费"（2 合 9mm/月）、"拍卖人口抵债"等细节。

---

## 六、常驻NPC

6 名常驻NPC。

实际文件增加了详细的调用原则：
- 5 步调用前判断
- 速记调用表
- 组合调用表（如谢尔盖+格奥尔基 = 武力执行+交易秩序）
- 回合内调用顺序
- 常见误用列表

---

## 七、防幻觉机制

多层防护：

### 7.1 信息流控制

| 规则 | 说明 |
|------|------|
| **无同步块机制** | 不存在 WM 推送的同步块；WSK 通过强语义提取 WM Scene 叙事中的已成立变化 |
| **库存不可脑补** | 只能从最近官方 State Update 读取 + 本轮 Scene 叙事中明确写出的增减 |
| **场景隔离** | 后台可知全局，但角色只能知道亲见亲闻/可信转述/无线电 |
| **知情范围** | 固定取值：world-only / local-only / party-known / publicly-known |
| **Snapshot 权威层级** | WM 自维护为临时估算，WSK 输出为权威 |

### 7.2 NPC 反全知机制

NPC 反全知机制详尽，包含：
- NPC 信息只允许来自 4 层：亲眼所见、刚收到且可信的消息、符合职业经验的推断、无法确认的不确定猜测
- 职业只提供"可说范围"，不提供全知能力
- 天气与生存压力必须压缩 NPC 表达质量
- critical/dying 状态下的 NPC 不应输出条理完整的分析
- 更优先的 NPC 输出类型：观察、偏好、条件、拒绝、催促、警告、讨价还价

### 7.3 主角光环禁止规则

### 7.4 抗跑偏硬约束

但实际文件中条目更多更细（8 条主要约束 + 多条子约束）。

---

## 八、动态锚点系统

实际文件增加了：
- 锚点非冻结硬规则（锚点协议不是把角色冻结成背景板）
- 同行平台角色规划边界
- Anchor 刷新判定清单（必须刷新 / 可沿用旧锚点 / 常见误刷）
- 长时间未出场角色的低成本维持模板（`[Role Anchor - Keep]`）
- 多角色批量维护模板（`[Role Anchor Batch]`）

---

## 九、文件结构分析

### 9.1 实际文件结构

```
├── Prompt_File/
│   ├── 0-1_Private_Details.md                    # 聊天室 Private Details 字段（全局硬规则层）
│   ├── 0-2_Scenario.md                           # 聊天室 Scenario 字段（叙事开场层）
│   ├── 0-3_自定义指令.md                           # 自定义指令字段（运行时纠偏：WM 输出风格与防漂移）
│   ├── 1-1_WorldMaster_Scene.md                  # WM 场景与概率入口
│   ├── 1-2_WorldMaster_Additional_Personality_Details.md  # WM 裁定补充层
│   ├── 1-3_WorldMaster_Extra_Details.md          # WM 世界扩展层
│   ├── 2-1_WorldStateKeeper_Scene.md             # WSK 场景锚点
│   ├── 2-2_WorldStateKeeper_Additional_Personality_Details.md  # WSK 详细规则层
│   └── 2-3_WorldStateKeeper_Extra_Details.md     # WSK 模板与回执层
├── PROJECT_BLUEPRINT.md                          # 项目蓝图（本文件，简称 PB；SSOT）
├── OURDREAM_PLATFORM_REFERENCE.md                # OurDream.ai 平台参考知识
├── OPTIMIZATION_ROADMAP_v1.md                    # 工程优化记录
├── 审计历史记录.md                                  # 历史审计归档（原 PB §十三-§十七）
├── The Unnecessarily In-Depth OurDream Character Creation Guide.md  # 平台字段官方指南
└── .gitignore
```

### 9.2 现行五层记忆模型

> 与 §1.1 承载分工规范互为参照：§1.1 从"字段装什么"角度定义，本节从"记忆层次"角度描述，两者是同一架构的不同视角。

1. **Layer 0：World Canon Layer** → `0-2_Scenario.md`（聊天室 Scenario 字段，叙事开场）
2. **Layer 1：Global Protocol Layer** → `0-1_Private_Details.md`（聊天室 Private Details 字段，全局硬规则）
3. **Layer 2：WM Execution Layer** → `1-1 / 1-2`
4. **Layer 3：World Mechanism Layer** → `1-3`
5. **Layer 4：Runtime Ledger Layer** → WSK 的运行账本（`2-1 / 2-2 / 2-3`）
6. **Layer 5：Persistent Archive Layer** → Pinned Memory（用户手动维护的持久化层，承载不可压缩锚点、已关闭事件链、长期格局变化、结构变化；详见优化文档 §9.8.3）

> **运行时纠偏层**：`0-3_自定义指令.md`（自定义指令字段）不属于装配层记忆模型，而是在游戏运行过程中按需注入的针对性纠偏指令（反煽情/重复抑制/感官 token），非全局装配规则。详见 §1.1.1。

### 9.3 当前字符占用

| 文件 | 字段 | 字符数 | 平台限制 |
|------|------|-------:|----------|
| `0-2` | `聊天室 Scenario 字段` | — | 50,000 |
| `0-1` | `聊天室 Private Details 字段` | — | 15,000（12,000 内更优）|
| `0-3` | `自定义指令字段` | — | — |
| `1-1` | WM 角色卡 Scene | — | 50,000 |
| `1-2` | WM 角色卡 Additional | — | 50,000 |
| `1-3` | WM 角色卡 Extra | — | 100,000 |
| `2-1` | WSK 角色卡 Scene | — | 50,000 |
| `2-2` | WSK 角色卡 Additional | — | 50,000 |
| `2-3` | WSK 角色卡 Extra | — | 100,000 |

---

## 十、系统质量评估

### 10.1 架构设计评分

| 维度 | 评分 | 说明 |
|------|------|------|
| **职责分离** | ★★★★★ | WM 裁定权、WSK 记账权双权分立，边界清晰；长期档案由 Pinned Memory 承担 |
| **防幻觉设计** | ★★★★★ | 多层防护：强语义提取边界、库存不可脑补、场景隔离、知情范围、防重复提交 |
| **长期一致性** | ★★★★★ | WSK 硬状态管理 + Pinned Memory 持久化层，支持 100+ 日运行 |
| **平台适配** | ★★★★☆ | 充分利用手动触发、字段分层、Pinned Memory；但受限于平台上下文窗口 |
| **可扩展性** | ★★★★☆ | 九宫格地图、搜刮分层、阵营关系都预留了扩展空间 |
| **规则完备性** | ★★★★★ | 覆盖了移动、搜刮、交易、战斗、报复、伤病、死亡、场外演化等全场景 |
| **可维护性** | ★★★☆☆ | 规则重复度高，跨文件引用复杂，修改一处可能需要联动多处 |

### 10.2 关键风险与问题

#### 风险 1：规则重复度高（中风险）

多个核心规则在多个文件中反复出现，例如：
- "[平台优先锚点]"在 1-1、1-2、1-3、2-1、2-2、2-3 中都有
- "手动触发机制"描述在 WSK 的三层字段中重复
- "知情范围 不可自动升级"规则在 0-1、2-2、2-3 中反复出现

**影响**：修改一条规则时需要同步更新多个文件，否则可能产生规则分叉。

**建议**：考虑将高频重复的"平台机制"声明统一为最短引用形式，详细内容只在 0-1 中维护一份。

**审计状态（2026-07-22）**：⚠ **部分缓解**。v4 已建立 echo 链（0-1 权威 / 1-1~2-3 最短引用），但跨角色文件的"[平台优先锚点]"仍存在重叠表述；后续批次需进一步压缩。

#### 风险 2：WM 提示词总量过大（中风险）

WM 的三个角色字段 + Custom Instructions 合计约 **75,282 字符 / 约 1,170 行**。其中 1-3 单独就有 49,019 字符 / 697 行。

**影响**：大模型在处理超长提示词时可能出现"后段遗忘"，导致 1-3 后半段的阵营详解、常驻NPC、场外演化等规则被忽略。

**建议**：考虑将 1-3 中的地点列表做进一步分区化索引，或在 Scene 层按需调用而非全量加载。

**审计状态（2026-07-22）**：⚠ **仍存在**。v4.4 迁出 0-1 至 Private Details（≥50K 限额解除）后，WM 总提示词压力部分缓解；但 1-3 仍为 33,144 字节（约 92% 旧 Custom Instructions 限额），未拆分至独立参考文件；分区化索引列入 P1 仍未评估。

#### 风险 3：Custom Instructions 接近上限（低风险）

0-1 当前 11,720 字符，平台上限 15,000。应控制在 12,000 内。

**影响**：后续若继续添加公共法，可能突破上限或进入表现下降区间。

**审计状态（2026-07-22）**：✓ **已解决**。v4.4 2026-07-20 迁入 Private Details 字段后限额解除（≥50,000 限额 ≈ 31.65% 占用）；本风险不再适用。

#### 风险 4：WSK 模板复杂度极高（中风险）

2-3 的 WSK 输出模板包含 **双视图（最小/完整）+ 7 种 Snapshot + 12 条最低提交标准 + 大量条件补充规则**，总计 432 行。

**影响**：大模型在执行 WSK 角色时可能难以同时满足所有格式要求，特别是 Snapshot 分层输出和压缩规则校验。

**审计状态（2026-08-15）**：✓ **已大幅缓解**。历史多次瘦身 + v1.19 WER 删除 + 2026-08-15 标准化分工后，2-3 现为 133 行 / 单一完整视图 / 10 主条最低记入标准；输出协议简化为每次触发都输出完整视图（决策表 1 张，`-` 空白标记废除），模板多变性与条件分支显著降低。

### 10.3 亮点评价

#### 亮点 1：确定性伪随机

概率裁决使用公开公式 `Seed = (Day ID × 31 + Turn ID × 17 + Event Offset) mod 100`，保证可复现、可追溯、多事件可分离。这在 AI 游戏系统中是罕见的设计。

#### 亮点 2：库存三类 + Snapshot 审计

WM 输出临时 Snapshot → WSK 审计并输出权威版本 → WM 下一轮替换。这种"前台估算 + 后台审计"的双层校验机制有效防止了库存漂移。

#### 亮点 3：知情范围 信息流控制

4 级可见性枚举 + 不可自动升级规则 + 传播事件依赖，构建了一套完整的信息流控制系统。这是防止 AI "全知叙事"的关键机制。

#### 亮点 4：报复链的动态生命周期

报复链不是简单的"有/无"状态，而是有完整的生命周期：形成 → 活跃 → 降温 → 冻结 → 关闭/重新激活。这让世界的人际关系更加真实可信。

#### 亮点 5：场外演化的轻量设计

通过时间门槛（白天段切换 / 12 小时累计）触发轻量检查，只同步"已成立变化"，不制造虚假事件。这让世界在玩家不活跃时也能缓慢演化，同时避免了过度生成。

#### 亮点 6：NPC 反全知的系统性设计

从 5 问到 4 层信息源，从职业可说范围到压力压缩表达质量，构建了一套系统性的 NPC 认知边界控制机制。这是保证世界可信度的核心支柱之一。

---

## 十一、项目规模统计

| 指标 | 实际数值 | 说明 |
|------|----------|------|
| 落盘文件总数 | 9 | 0-1 / 0-2 / 0-3 / 1-1~1-3 / 2-1~2-3 |
| 项目参考文档 | 5 | PROJECT_BLUEPRINT.md（本文件，PB）+ OURDREAM_PLATFORM_REFERENCE.md + OPTIMIZATION_ROADMAP_v1.md + 审计历史记录.md + Character Creation Guide |
| 聊天室装配字段 | 2 | 聊天室 Scenario 字段（0-2）+ 聊天室 Private Details 字段（0-1）——聊天室创建时设定 |
| 运行时纠偏字段 | 1 | 自定义指令字段（0-3）——聊天过程中按需调整 |
| 角色卡字段 | 6 | WM 3 + WSK 3 |
| 平台字段映射 | 6 类 | Scenario / Private Details / 自定义指令 / Scene / Additional / Extra |
| `0-2_Scenario.md` 字节数 | 1,200 | 承载叙事开场（50,000 限额内） |
| `0-1_Private_Details.md` 字节数 | 7,041 | 承载全局硬规则 |
| `0-3_自定义指令.md` 字节数 | 4,971 | 运行时纠偏：WM 输出风格约束与防漂移指令 |
| 1-1 字节数 | 11,690 | WM Scene（50,000 限额内） |
| 1-2 字节数 | 30,679 | WM Additional（50,000 限额内） |
| 1-3 字节数 | 31,090 | WM Extra（100,000 限额内） |
| 2-1 字节数 | 10,475 | WSK Scene |
| 2-2 字节数 | 29,383 | WSK Additional |
| 2-3 字节数 | 21,610 | WSK Extra |
| 提示词总字节数 | ~201,094 | 9 个落盘文件合计 |
| 分区数量 | 9 | 九宫格固定拓扑 |
| 子地点数量 | 99 | 1-3 Extra Details |
| 常驻NPC | 6 | 阵营内外长期出没 |
| 主要阵营 | 5 + 拾荒者 | — |
| 天气状态 | 9 | 湿润大陆性 Dfb |
| 生存压力轨道 | 5 | 疲劳/体温/脱水/饥饿/伤病 |
| 关系追踪维度 | 2 核心轴 + 按需次级 | Relationship + Revenge Drive |
| 搜刮分层 | T1-T4 | 按地点标签 + tier |
| 库存层级 | 3 | 随身/据点核心/记忆库存 |
| 决策表 | 1 | WSK 1 张：DO（每次触发都输出完整视图） |

---

## 十二、总结

### 本项目的核心价值

这是一个在**提示词工程领域具有参考价值的复杂系统设计**，其核心创新包括：

1. **双角色分立架构**：裁定权（WM）、记账权（WSK）分离，通过强语义提取协同；长期档案由用户手动维护的 Pinned Memory 承担
2. **Snapshot 审计机制**：前台估算 + 后台审计的双层校验，防止库存漂移
3. **确定性伪随机**：公开公式保证可复现、可追溯
4. **知情范围 信息流控制**：4 级可见性 + 不可自动升级 + 传播事件依赖
5. **报复链动态生命周期**：形成 → 活跃 → 降温 → 冻结 → 关闭/重新激活
6. **NPC 反全知系统**：5 问 + 4 层信息源 + 职业可说范围 + 压力压缩表达
7. **场外演化轻量设计**：时间门槛触发 + 只同步已成立变化
8. **Pinned Memory 长期档案**：用户手动维护的持久化层，承载历史账本，支持 100+ 日运行

### 改进建议与历史变更

> 历史改进建议（P0/P1/P2）的执行状态与近期重大变更记录已迁入 `审计历史记录.md`（原 §十三-§十七）与 `OPTIMIZATION_ROADMAP_v1.md`。本节仅保留当前仍有效的改进方向。

**当前仍有效的改进方向**：
- 减少跨文件规则重复，核心规则只在 0-1 维护一份（部分缓解：echo 链已建，仍存在重叠表述）
- 1-3 地点数据分区化索引或拆分到独立参考文件（1-3 仍 ~33K 字节，未拆分，缓解后段遗忘）
- 为 WSK 模板提供简化版快速参考，降低执行复杂度（决策表已统一，模板仍复杂）
- 1-3 南区重复子地点条目待修复（数据一致性，未评估）

---

## 十八、运行模型

> **本节是所有设计 / 审计 / 优化决策的基座。** 任何"内容放哪里""谁能看到这句""引用是否有效"的判断，都必须回到本节的运行时上下文模型与可见性矩阵。本节整合原 `开发设计方法论.md` §一（已并入），与 §0 核心工程原则体系、§1.1 字段承载分工规范共同构成评估基座。

### 18.1 平台字段架构

OurDream 平台提供两级字段：

| 层级 | 字段名 | 文件 | 容量 | 加载时机 |
|---|---|---|---|---|
| 聊天室级 | Private Details | `0-1_Private_Details.md` | ≥50K | 聊天室创建时设定，全局持久 |
| 聊天室级 | Scenario | `0-2_Scenario.md` | 50K | 同上 |
| 聊天室级 | 自定义指令 | `0-3_自定义指令.md` | — | 聊天过程中按需调整 |
| 角色卡级（WM） | Scene | `1-1_WorldMaster_Scene.md` | 50K | WM 轮次加载 |
| 角色卡级（WM） | Additional Personality Details | `1-2_WorldMaster_Additional_Personality_Details.md` | 50K | 同上 |
| 角色卡级（WM） | Extra Details | `1-3_WorldMaster_Extra_Details.md` | 100K | 同上 |
| 角色卡级（WSK） | Scene | `2-1_WorldStateKeeper_Scene.md` | 50K | WSK 轮次加载 |
| 角色卡级（WSK） | Additional Personality Details | `2-2_WorldStateKeeper_Additional_Personality_Details.md` | 50K | 同上 |
| 角色卡级（WSK） | Extra Details | `2-3_WorldStateKeeper_Extra_Details.md` | 100K | 同上 |

**关键规则**：
- 聊天室级字段：全部角色可见（WM + WSK 均可读取）
- 角色卡级字段：仅本角色可见（WM 看不到 WSK 的字段，反之亦然）
- Pinned Memory：非平台字段，用户手动维护的持久化层，不在平台加载链中

### 18.2 运行时上下文模型

LLM 在不同轮次加载不同角色卡，其实际可见的上下文如下：

**WM 生成时（LLM 上下文）**：
```
聊天室 Private Details (0-1)     ─┐
聊天室 Scenario (0-2)             │  聊天室级字段（共享）
聊天室 自定义指令 (0-3)           ─┘
WM Scene (1-1)                   ─┐
WM Additional Personality (1-2)   │  WM 角色卡字段（私有）
WM Extra Details (1-3)           ─┘
对话历史（所有参与者的历史输出）   ←  运行时累积
```

**WSK 生成时（LLM 上下文）**：
```
聊天室 Private Details (0-1)     ─┐
聊天室 Scenario (0-2)             │  聊天室级字段（共享）
聊天室 自定义指令 (0-3)           ─┘
WSK Scene (2-1)                  ─┐
WSK Additional Personality (2-2)  │  WSK 角色卡字段（私有）
WSK Extra Details (2-3)          ─┘
对话历史（所有参与者的历史输出）   ←  运行时累积
```

**关键推论**：
1. WM 看不到 WSK 的任何角色卡字段（2-1 / 2-2 / 2-3）-> WM 不能引用 WSK 字段内容
2. WSK 看不到 WM 的任何角色卡字段（1-1 / 1-2 / 1-3）-> WSK 不能引用 WM 字段内容
3. 双方都能看到聊天室级字段（0-1 / 0-2 / 0-3）-> 聊天室级字段是共享共识基座
4. 双方都能看到对话历史 -> 对话历史是跨角色唯一活通道

**平台 TRACKING 头机制（2026-08-18 实测确认）**：
平台在每轮 WM system prompt 中注入一个 HTML 注释块 `<!-- WM-TRACKING ... -->`，包含 7 个字段：scene_function（场景功能分类）、format_contract（格式契约摘要）、authority_scope（权限边界）、active_focus（本轮焦点）、continuity_anchors（连续性锚点）、pressure_edge（压力边界提示）、next_beat_intent（叙事节拍意图）。

运行时行为：
- TRACKING 头是标准装配，每轮注入；LLM 默认只读 TRACKING 头进行叙事，不自动翻查完整对话历史
- continuity_anchors 由平台从上一轮 WM 完整输出（叙事正文 + [主要状态]）语义提取，不从 WSK [State Update] 读取
- 完整对话历史（含 WSK 每轮 [State Update]）仍在上下文窗口内，但 LLM 仅在有需要时才回查--必查表规则的作用就是强制 LLM 在特定决策点回查
- format_contract 字段为平台从 prompt 文件摘要提取，非逐字拷贝；实测存在摘要范围不全的风险（如仅摘要"叙事正文+[主要状态]"而漏 [移动]/[掷骰]/[判定] 三块）

设计推论：
- 必查表规则是"LLM 默认不翻上文"的对抗措施，在运行时有效但依赖 LLM 遵守指令
- WSK [State Update] 在对话历史中可被 WM 检索，但不在 TRACKING 头的默认摘要里
- TRACKING 头的 continuity_anchors 包含叙事细节（持枪姿态/NPC 肢体语言等）是 WSK 不会记账的--平台做了 WSK 做不了的叙事级连续性压缩

### 18.3 可见性矩阵

| 字段 | 平台层级 | WM 生成时 | WSK 生成时 | 玩家可见 | 功能约束对象 |
|---|---|---|---|---|---|
| 0-1 Private Details | 聊天室级 | ✅ 可见 | ✅ 可见 | ❌ | 全部角色（公共法） |
| 0-2 Scenario | 聊天室级 | ✅ 可见 | ✅ 可见 | ✅ | 全部角色 + 玩家（叙事开场） |
| 0-3 自定义指令 | 聊天室级 | ✅ 可见 | ✅ 可见 | ❌ | 空（WM 风格规则已迁至 1-1/1-3）；保留玩家动态调整指令 |
| 1-1 WM Scene | 角色卡级 | ✅ 可见 | ❌ 不可见 | ❌ | WM |
| 1-2 WM APD | 角色卡级 | ✅ 可见 | ❌ 不可见 | ❌ | WM |
| 1-3 WM Extra | 角色卡级 | ✅ 可见 | ❌ 不可见 | ❌ | WM |
| 2-1 WSK Scene | 角色卡级 | ❌ 不可见 | ✅ 可见 | ❌ | WSK |
| 2-2 WSK APD | 角色卡级 | ❌ 不可见 | ✅ 可见 | ❌ | WSK |
| 2-3 WSK Extra | 角色卡级 | ❌ 不可见 | ✅ 可见 | ❌ | WSK |
| 对话历史 | 运行时 | ✅ 可见 | ✅ 可见 | ✅ | 全部 |

**矩阵使用方法**：

> **字段归属优先原则（2026-08-06 复核修正）**：审计跨层引用时，先判定被引用内容的归属（§1.1），再判定可见性（§18.3）。若内容归属错误（角色私有规则出现在聊天室级字段），直接判为字段归属错位，不需要再走可见性检查。可见性矩阵适用于**角色卡之间的横向引用**（1-x↔2-x），不适用于**聊天室级到角色卡的纵向引用**（0-x->1-x/2-x）——后者应先用 §1.1 判定内容是角色专用还是共享世界知识。

- **设计时**：新增内容前，先按 §1.1 判定内容归属（角色专用 vs 共享），再查矩阵确认目标字段对目标角色可见
- **审计时**：逐条检查跨角色引用时，先按 §1.1 判定内容归属是否正确，再查矩阵确认引用方生成时能否看到被引用目标
- **优化时**：发现冗余定义时，查矩阵确认 SSOT 权威源对所有需要它的角色可见

### 18.4 通讯机制

**跨角色通讯 = 对话历史被动观察**

WM 和 WSK 不通过主动消息传递通讯。通讯路径：

```
WM 生成叙事 + [主要状态] → 输出进入对话历史
                                    ↓
WSK 被触发时 → 从对话历史读取 WM 的实际输出 → 提取已成立变化 → 输出 [State Update]
                                    ↓
[State Update] 进入对话历史 → WM 下一轮可读
```

**通讯规则**：
1. 跨角色引用必须指向"对话历史中的实际输出"，禁止指向对方角色卡字段
   - ✅ 正确：`以 World State Keeper 最新 [State Update] 实际输出中的…字段为准`
   - ❌ 错误：`参考 WSK 的 APD §精简库存与盘点规则`（WSK APD 对 WM 不可见）
2. 共享规则放在聊天室级字段（0-1 / 0-2），双方都能看到
3. 角色私有规则放在各自角色卡字段，对方不可见也不引用
4. 对话历史是跨角色唯一活通道（跨角色引用禁令的原理见 §1.2）

### 18.5 9 文件映射表

| 编号 | 文件名 | 平台字段 | 层级 | 角色归属 |
|---|---|---|---|---|
| 0-1 | `Private_Details.md` | Private Details | 聊天室级 | 全局 |
| 0-2 | `Scenario.md` | Scenario | 聊天室级 | 全局 |
| 0-3 | `自定义指令.md` | 自定义指令 | 聊天室级 | 全局（功能仅 WM） |
| 1-1 | `WorldMaster_Scene.md` | Scene | 角色卡级 | WM |
| 1-2 | `WorldMaster_Additional_Personality_Details.md` | Additional Personality Details | 角色卡级 | WM |
| 1-3 | `WorldMaster_Extra_Details.md` | Extra Details | 角色卡级 | WM |
| 2-1 | `WorldStateKeeper_Scene.md` | Scene | 角色卡级 | WSK |
| 2-2 | `WorldStateKeeper_Additional_Personality_Details.md` | Additional Personality Details | 角色卡级 | WSK |
| 2-3 | `WorldStateKeeper_Extra_Details.md` | Extra Details | 角色卡级 | WSK |

### 18.6 字段依赖规则

修改某字段时需同步检查的引用范围（与 §1.1 字段承载分工规范的关联规范互为参照）：

| 修改字段 | 影响范围 | 必须同步检查 |
|---|---|---|
| 0-1 | 全部文件 | 所有引用 0-1 §节名的文件 |
| 0-2 | -- | 0-2 已重写为叙事开场，不再承载规则；修改 0-2 不触发引用同步 |
| 0-3 | 1-1（WM Scene 输出风格） | WM 输出相关 |
| 1-1 | 0-1（引用方）/ 1-2 / 1-3（同角色） | 同角色跨字段引用 + 0-1 引用 |
| 1-2 | 1-1 / 1-3（同角色） | 同角色跨字段引用 |
| 1-3 | 0-1 / 0-2 / 1-1 / 1-2（引用方） | 引用方 + 同角色 |
| 2-1 | 0-1（引用方）/ 2-2 / 2-3（同角色） | 同角色跨字段引用 + 0-1 引用 |
| 2-2 | 2-1 / 2-3（同角色） | 同角色跨字段引用 |
| 2-3 | 0-1 / 2-1 / 2-2（引用方） | 引用方 + 同角色 |

---

## 十九、开发方法论

> **本节回答"怎么有序开发和维护"。** 两条轨道共享 §0 核心工程原则体系 + §1.1 字段承载分工 + §十八 运行模型作为评估基座，区别在前置（预防）vs 后置（检测）。
>
> **首要用途**：轨道 A 是全库结构规范化的设计指导。每个新功能、每次内容迁移、每个机制新增，都必须走此流程。

### 19.1 轨道 A：新增功能开发流程

#### Step 1：设想

**目标**：明确"要改变什么行为"，而非"要写什么内容"。

| 检查项 | 通过标准 |
|---|---|
| 行为变更定义 | 能用一句话说出"新增 / 修改后，LLM 下一次输出会有什么不同" |
| 预期读者 | 明确这个行为变更影响哪个角色（WM / WSK / 全部） |
| 功能必要性 | 假设不做这个改动，现有行为有什么问题？无问题则不需要做 |

**常见错误**：
- ❌ "我想加一个势力活动表"（描述内容，不是行为变更）
- ✅ "WM 目前无法判断势力何时活动，导致场外演化随机；新增势力周期活动表后，WM 可按周期裁定势力行为"

#### Step 2：字段归属判定

**目标**：确定内容放在哪个字段。这是全库结构规范化的核心步骤。

**判定流程**：

```
内容是什么类型？
├── 世界事实 / 地理 / 势力 / 经济 / 社会规则 → 0-2 Scenario
├── 公共法 / 通讯规则 → 0-1 Private Details
├── WM 叙事风格纠偏 → 0-3 自定义指令
├── 角色身份声明 / 输出契约 / 触发条件 → Scene (1-1 或 2-1)
├── 决策逻辑 / 裁定哲学 / 记账规则 → APD (1-2 或 2-2)
└── 可执行机制 / 数据表 / 活动表 / 模板 → Extra (1-3 或 2-3)
```

**检查清单**：

| # | 检查项 | 通过标准 | 查哪里 |
|---|---|---|---|
| 1 | 内容-身份匹配 | 内容类型与字段功能定位一致 | §1.1 字段承载分工规范 |
| 2 | 可见性合规 | 需要读到这个内容的角色，生成时能看到这个字段 | §18.3 可见性矩阵 |
| 3 | SSOT 检查 | 这个内容是否已在别处定义？若是，改为引用 | 全库 grep 关键词 |
| 4 | 跨角色需求 | 是否有其他角色需要这个内容？若是，放聊天室级字段或内联 | §18.3 可见性矩阵 |

**常见错误**：
- ❌ Extra 级机制细节放在 Scene（1-1 世界调用总纲混入 Extra 级细节）
- ❌ 叙事开场放在公共法（0-1 经济规则混入世界事实）
- ❌ WSK 决策规则放在 0-2 Scenario（世界文件混入角色私有规则）

#### Step 3：机制设计

**目标**：确保机制四要素齐全，无断头路。

**四要素核对表**（引用 §0.5 原则②）：

| 要素 | 问句 | 通过标准 |
|---|---|---|
| 定义 | 这个机制是什么？ | 有明确的概念定义 |
| 语义 | 这个机制在什么条件下触发？ | 有明确的触发条件 |
| 消费者 | 谁使用这个机制的输出？ | 有明确的消费路径（不是"写了就有人看"） |
| 失败出口 | 异常时怎么办？ | 有明确的退化路径（缺字段 → REJECT / 记"未提供" / 轻量回执） |

**检查清单**：

| # | 检查项 | 通过标准 |
|---|---|---|
| 1 | 四要素齐全 | 定义 + 语义 + 消费者 + 失败出口，缺一即断头路 |
| 2 | 消费者可见性 | 消费者生成时能看到这个机制的输出（查 §18.3 矩阵） |
| 3 | 退化路径 | 信息缺失 / 规则冲突 / 用户不触发时，有预置的失败出口 |
| 4 | 认知适配 | 不要求 LLM 长期精确计数 / 算术 / 跨轮记忆（引用 §0.5 原则③） |

**常见错误**：
- ❌ 声明了数值（噪音暴露度 +15）但无消费路径（断头路）
- ❌ 要求 LLM 长期记住当前月份（认知不适配；v1.55 已去除 Month，Season 由 Day 查 [温度分层与日期精细对应] 表直接确定）

#### Step 4：编写

**目标**：逐句过三层漏斗 + 格式合规。

**编写检查清单**：

| # | 检查项 | 通过标准 | 查哪里 |
|---|---|---|---|
| 1 | 身份层 | 每句话能指名读者（WM / WSK / 全部） | §0.1 三层漏斗第一层 |
| 2 | 可见性层 | 读者生成时能看到这句话所在的字段 | §18.3 可见性矩阵 |
| 3 | 有效性层 | 删掉这句话，LLM 下一次输出会有不同吗？无差别即删 | §0.1 三层漏斗第三层 |
| 4 | 格式合规 | `[section]` / backticks / `-` 列表项 / `()` 括号 | §0.12 格式规范 |
| 5 | 引用合规 | 引用目标必须是平台实体；跨角色引用指向实际输出 | §1.2 引用写法规范 |
| 6 | 无禁用符号 | 无 `**bold**`（新内容禁止）/ 无 `【】`（删除整行） | §0.12 格式规范 |

**常见错误**：
- ❌ 写给开发者 / 审计者的元说明（"本字段的装载顺序是..."）——身份层不通过
- ❌ 跨角色字段引用（"详见 WSK 的 APD §..."）——可见性层不通过
- ❌ 描述性正确但无行为价值（"Pinned Memory 是高优先级材料"）——有效性层不通过

#### Step 5：自审

**目标**：在提交前用 PAM（§0.11）前 4 个阶段快速自检。

| PAM 阶段 | 检查内容 | 通过标准 |
|---|---|---|
| Phase 0 定位 | 字段归属正确？引用出向 / 入向完整？ | §十八 + §1.1 核对 |
| Phase 1 机械核验 | 引用活性？术语一致？格式合规？ | 无死链 / 无化石术语 / 格式合规 |
| Phase 2 形态扫描 | 无可见性句 / 防御性元说明 / 猜想陈述 / 跨卡指针 / 世界观旁白？ | 无形态黑名单匹配 |
| Phase 3 节级漏斗 | 整节身份 / 可见性 / 有效性通过？ | 节级无结构性问题 |

#### Step 6：静态验证

**目标**：提交前最后检查。

| # | 验证项 | 方法 |
|---|---|---|
| 1 | 悬空引用 | grep 所有 §引用 → 确认目标 `[section]` 存在 |
| 2 | 引用格式 | 确认所有引用使用 §1.2 规范格式（无编号 / 简称） |
| 3 | 身份声明 | 文件顶部有字段身份声明（纯文本，无 `【】`） |
| 4 | 跨角色引用 | 确认无跨角色卡字段引用（查 §18.3 矩阵） |
| 5 | SSOT | 确认同一规则只存一份（无冗余定义） |
| 6 | 文件大小 | 确认未超出字段容量（Scene / APD 50K / Extra 100K） |

### 19.2 轨道 B：现有内容优化流程

**目标**：系统化检测和修复已有问题。

> **审计执行纪律（2026-08-06 复核修正）**：跨层引用审计时，必须先做字段归属判定（§1.1：内容是角色专用还是共享世界知识），再做可见性检查（§18.3：对方角色能否看到）。禁止跳过归属判定直接套用可见性矩阵--聊天室级字段（0-1/0-2）是 WM 和 WSK 共享的底座，两个角色都能看到；从 0-2 引用角色卡字段的问题不是"对方看不到"，而是"角色私有逻辑不应出现在公共法字段"。

#### Step 1：PAM 审计

执行 §0.11 PAM v1.1 全七阶段审计（Phase 0 定位 → Phase 1 机械核验 → Phase 2 形态扫描 → Phase 3 节级漏斗 → Phase 4 机制闭环 → Phase 5 行级审计 → Phase 6 组织预算 → Phase 7 汇总处置）。

#### Step 2：病灶分类与严重度校准

按 §0.11 Phase 7 严重度分级：

| 级别 | 定义 | 处置优先级 |
|---|---|---|
| P0 | 错位 / 越权 / 断头路（影响运行正确性） | 立即修复 |
| P1 | 冗余 / 化石 / 旁白（污染认知或浪费 token） | 批次修复 |
| P2 | 格式 / 顺序（不影响正确性但影响一致性） | 择机修复 |

> **严重度校准（2026-08-06 全库对齐审计实证）**：P0 判定须问"此项是否真的影响运行正确性？"。以下情况应降级为 P1：
> - 冗余指针（权威源和重复方都有完整定义，指针只是噪音）--不影响运行
> - 参数过大（机制正确但阈值过严，如 5 轮窗口改为 3 轮即可）--不影响机制
> - 重述非错位（同一规则在多处重复，但位置都正确）--不影响运行
>
> 审计误判处理：审计结论须验证原文上下文。若发现误判，记录误判原因并撤回，不留痕迹会导致后续审计沿错误方向重复。

#### Step 3：批量修复

- 按优先级分批（P0 → P1 → P2）
- 每批独立可回滚
- 修改时同步引用位置（查 §18.6 字段依赖规则）

#### Step 4：静态验证 + Git 回滚验证

- 执行 §19.1 Step 6 静态验证清单
- Git commit 每批修改，便于回滚
- 如有问题，`git revert` 回滚到上一批次

### 19.3 双轨道关系

```
轨道 A（前置预防）                轨道 B（后置检测）
    │                                 │
    ├── 设想 → 字段归属 → 机制设计     ├── PAM 审计
    │     → 编写 → 自审 → 静态验证     │     → 病灶分类
    │                                 │     → 批量修复
    │                                 │     → 静态验证 + Git 回滚
    │                                 │
    └───────── 共享基座 ──────────────┘
              §0 核心工程原则
              §1.1 字段承载分工
              §十八 运行模型
```

- **共享**：§0 + §1.1 + §十八 评估基座
- **互补**：A 预防（前置阻断），B 纠错（后置检测）
- **闭环**：A 的输出（新内容）是 B 的输入（待审计）；B 的发现（病灶）反哺 A（避坑）

> **闭环反馈实证（2026-08-06 全库对齐审计）**：
>
> 本次轨道 B 审计（231 项问题）反哺轨道 A 的 5 条教训：
> 1. **字段归属判定必须前置**：审计发现 11 项字段归属错位（P0），说明既有内容的开发缺少字段归属判定步骤。轨道 A Step 2（字段归属判定）是必要的拦截闸门。
> 2. **跨角色引用禁令需要运行模型支撑**：审计发现 9 项跨角色死链（P0），说明开发时缺少 §18.3 可见性矩阵检查。轨道 A Step 4（机制设计）必须包含可见性验证。
> 3. **SSOT 原则需要执行纪律**：审计发现 30+ 项 SSOT 冗余（P1），说明"只存一份"原则在执行中容易违反。轨道 A Step 5（自审）必须包含 SSOT 检查。
> 4. **跨轮计数是 LLM 系统的系统性风险**：审计发现 5 项跨轮精确计数要求（P1），说明 §0.5 原则③（认知适配）需要更具体的执行指引："任何要求跨轮精确计数的规则都应改为以 WM 叙事中的已成立事实为准"。
> 5. **格式规范一致性是注意力卫生**：审计发现 159 处 bold 标记 + 4 处【】+ 9 处 section 编号（P2），说明格式规范在增量开发中容易漂移。轨道 A Step 6（静态验证）必须包含格式合规检查。
>
> **方法论自评**：本次审计验证了方法论的核心价值（系统化检查框架替代经验直觉），同时暴露了 4 个需优化的方向：审计流程顺序（字段归属优先）、严重度校准机制、审计误判记录、格式数量精确统计。优化已写入 §0.11 审计陷阱清单 #10-#13 和 §19.2 Step 2 严重度校准。

---

## 二十、版本与变更管理

### 20.1 版本号规则

| 版本号 | 含义 | 触发条件 |
|---|---|---|
| v0.x | 初稿 / 草案 | 文档创建初期 |
| v1.0 | 首个稳定版 | 经过至少一轮全库审计验证 |
| v1.x | 小版本迭代 | 单项优化 / 单文件修改 |
| v2.0 | 大版本升级 | 架构调整 / 方法论根本性修订 |

### 20.2 变更日志

每次修改记录：
- 日期 + 版本号
- 修改内容摘要
- 影响范围（哪些文件 / 章节）
- 验证结果（静态验证是否通过）

#### v1.53（2026-08-07）

**5 轨压力权威迁移（WSK -> WM 单权威）**

- **根因**：WSK 稀疏激活（仅用户点击时输出）无法担任 5 轨压力的实时权威。WSK 的 Survival Anchor 和压力结算规则形同虚设，WM 实际上已在独立裁定。
- **迁移内容**：
  - WSK 移除：Party Condition 中的压力状态级、Survival Anchor State 字段（9->8 字段；注：v1.56 完整视图进一步精简为 6 字段）、压力结算规则（16 行）、[固定取值] 中的生存状态枚举
  - WM 新增：1-3 [压力裁定参考] 节（7 条裁定规则，5 轨全覆盖）、1-2 [压力裁定运行模型] 声明 WM 单权威
  - WSK Party Condition 重定义为角色状态定性描述（不含压力状态级）
- **验证结果**：5 轨逐轨静态测试全部通过（脱水/饥饿/疲劳/体温/伤病）；全库 grep 零残留；实际游戏输出 [主要状态] 5 轨全量格式完全合规
- **经验**：迁移时规则文本搬迁到位，但上下游引用和内容完整性未同步补全（4 项 P1 接口缺失），需在迁移后立即做依赖链检查

**地图拓扑+子地点+路由规则迁移（0-2 -> 1-3 WM Extra）**

- **根因**：实际游戏输出中 WM 完全不做路由判断，跨区移动耗时严重压缩（55min 代替 180min）。路由规则在 0-2（世界共识层）以描述性框架呈现，对 WM 无强制执行力；1-3 L171 引用指向错误 section（§环境生存压力系统 而非地图规则）；1-1 L8 只有 step 基准无 Route 计算要求
- **迁移内容**：
  - 1-3 新增 [地图总表] + [子地点列表] + [跨区移动路由规则] 三节（约 215 行）
  - 0-2 删除 [九宫格地图总表] + [子地点列表说明] + 各区详述 + [地图调用规则]（约 226 行），替换为 [地图概述]（5 行 + 引用）
  - 1-1 L8 新增强制指令："跨区移动必须按 §跨区移动路由规则 计算...不得用'走一段时间'或单 step 代替"
  - 1-3 L171 引用修正：§环境生存压力系统 -> §跨区移动路由规则
  - 0-1 L41 / 2-1 L5 引用同步更新
- **验证结果**：grep 零残留；旧引用全部清除；新引用全部指向有效 section
- **经验**：规则放在"世界共识"字段（描述性框架）中对 WM 的约束力远弱于放在"WM 行为指令"字段（指令性框架）。当 WM 不执行某规则时，首先检查规则的"框架定位"是否匹配目标角色的执行框架

**第三轮全库审计（18 项全部修复）**

- P0（2 项）：§显式同步门槛 死链 + §主要阵营 模糊引用
- P1（6 项）：压力裁定参考消费者引用 + 措辞修正 + Party Condition 定义 + 伤病轨规则 + 枚举 SSOT 化
- P2（10 项）：bold/emoji 清零 + 术语统一 + 跨轮计数修正 + NPC Relationship section 重复修复
- **审计质量**：术语迁移零残留；第二轮修复 7/7 验证通过；修复副作用 1 项已捕获修复

**其他修改**

- 文件头术语统一：4 个文件"本文件"->"本字段（字段名）"
- 0-3 [必须输出 主要状态] section 评估为冗余（0-1+1-1 已覆盖），已删除
- 0-1 L14 [主要状态] 示例修正：压力段从 1 轨改为 5 轨全量
- Route Exposure Notes 断头路修复：删除 2-2 L46 中的无落盘字段声明
- 地图拓扑+子地点+路由规则迁移（0-2 -> 1-3 WM Extra）：1-3 新增 [地图总表]+[子地点列表]+[跨区移动路由规则] 三节；0-2 精简为 [地图概述]；1-1 L8 新增强制指令；1-3 L171 引用修正
- [移动] 结构化移动行：新增 WM 输出格式 `[移动] Origin -> {Route} -> Destination | Steps: {N} | Travel Time: {N}min`，仅在 WM 字段中定义
- Turn 编号硬约束：1-1 L3 + 0-1 L12 双层约束"每轮 +1，不可跳号，不可不推进，不可倒退"
- [主要状态] 格式内联：1-1 L21-26 内联 5 段格式约束（位置段只写终点、气温段无后缀、压力段不带括号）
- 邻接表替换为接口表：每格从"分区名"变为"分区名/入口子区域"，消除出口映射两步推理
- 物资渲染格式内联到 1-3：删除对 0-1 的引用
- NPC 知识边界规则内联到 1-3：新增 [NPC 知识边界与发言规则] 节；1-1 L30 引用从 0-1 改为 1-3
- **机械规则内联原则**：新增系统级原则（§0.5 原则③后），WM 必须执行的机械规则必须内联到 WM 字段中
- **审计陷阱 #20**：新增"机械规则跨字段依赖"陷阱
- WSK 机械规则内联（陷阱 #20 扩展到 WSK）：知情范围 枚举+传播（2-2）、物资 kg 记录细节（2-3）全部内联，WSK 机械规则跨字段依赖清零（原"月份推导（2-2）"已随 v1.55 去除 Month 整体废弃）
- 2-2 section 重排：按使用频率排序（决策表->提交前检查->常见误操作->其余->心理预设末尾）
- 2-2 REJECT 表清理：删除"缺 Turn ID REJECT"（WSK 只记录不验证）、"缺 知情范围 REJECT"（与默认 hidden 矛盾）；修改"不指向你的输入"为清晰表述
- 2-2 SILENT 表清理：删除 3 条（冗余项、语义不清"不指向你的消息"、有害"Turn ID 相同 SILENT"--同 Turn 内可能有叙事变化）
- 1-3 路由规则优化：接口表替换邻接表（消除两步推理）；编号流程（5 步查表->生成->算Steps->算耗时->输出）；反例扩展（5 个具体违规路径）；对角提示（6 对对角不相邻）
- 1-3 结构重排：17 个 section 按频率分 4 组（每轮裁定规则->移动与地图->NPC与角色->场景裁定数据）
- 1-3 路由修复：补回"C 不直接跨分区"规则（接口表迁移时丢失）；编号流程第 2 步完善（入口经 C 到目标）；step 基准补充跨分区跳转=1 step；Route 书写补充"含同分区内移动"；搜刮产出增加物资渲染交叉提醒
- 1-3 [主要状态] 格式内联到 1-1 L21-26（位置段只写终点、气温段无后缀、压力段不带括号）
- 1-3 NPC 知识边界规则内联（从 0-2 内联到 1-3 [NPC 知识边界与发言规则]）
- 1-3 物资渲染格式内联（从 0-1 内联到 1-3 [可消耗物资渲染与资源估算]）
- Turn 不可倒退约束（1-1 L3 + 0-1 L12）
- 0-3 冗余 [必须输出 主要状态] section 删除

#### v1.55（2026-08-09）

**Month 全面去除 + WSK 记账层精简**

- **根因**：Month 字段零游戏机制依赖（Day 决定时间点，Day 经 [温度分层与日期精细对应] 表决定 Season，Season 驱动天气系统）；WM [主要状态] 不输出 Month；后末日设定中真实月份无意义。且 1-3 引用 §月份推进规则 但未内联，制造自包含缺口。同时 WSK 记账层存在 Elapsed 死字段、装备属性过度展开、据点降级二元否定、持续代价字段分散等问题。
- **改动内容**：
  - Month 去除（0-2/1-3/2-2/2-3）：删除 [月份推进规则]、季节-月份对应、Day→Month 映射、[固定取值] Current Month 枚举、State Update 第一行 {Month} 段；清理 [温度分层与 5 轨压力联动] 季节温度参考与 [天气-季节可用性矩阵] 中的日历月份注释；enum_registry.json 删除 current_month 条目（枚举 11→10）
  - WSK 精简（2-2）：DO 表时间推进句精简；删除 Elapsed 死字段；装备属性从 5 项精简为 Condition/Insulation（删 Wetness/Attachment/Repair State；注：Condition/Insulation 后于 D1 保暖机制落地时全库移除，改为 WM 实时裁定保暖值）；跨日一致性重写（删 Day→Month 映射）；据点降级规则改为"风险因素→降级状态记录，非二元否定庇护身份"；持续代价+长期驻留字段合并为单条 5 字段
  - 设计文档同步：§0.5 原则③ 示例与 §19.1 常见错误更新（Month→Season 查表）；引用写法规范示例更新
- **验证结果**：gen_ref_graph.py 断链 0；validate_enums.py 通过（枚举 10）；Prompt_File/ 运行时文件 Month 残留 100% 清零；[温度分层与日期精细对应] 表完整保留
- **经验**：跨文件字段去除时，显式字段（[固定取值]/输出格式）易清理，但散落在气候注释（"冬（12-2 月）"）和枚举注册表中的隐式引用易遗漏；验证阶段需扩展 grep 模式（含 `\d+\s*月`）并扫描 tools/ 配套文件，不能只扫 Prompt_File

#### v1.56（2026-08-10）

**WSK 反应模型精简 + 完整视图 6 字段 + Relationship 7 档 + D1 保暖 + 据点损耗/暴露机制**

- **WSK 反应模型精简**：去除 [Commit Rejected] 机制与全部 REJECT 理由；决策表 3→2 张（DO/SILENT），SILENT 改为输出 `-`；完整视图字段 8→6（旧 3 关系 + 4 阵营 + 5 人为威胁阶段合并为"Relationship & Threat"，Map Knowledge / Base Structure / 近五日主要事件顺移为 4/5/6）
- **Relationship 7 档定性值**：Trust/Hostility 双轴替换为 7 档（依恋/完全信任 / 亲密/信任 / 好感 / 中立 / 冷漠 / 敌意 / 仇恨），WSK 做语义升降档、不记数值
- **变化子段精简**：仅 Inventory Delta（去除 Recent Changes）
- **D1 保暖机制落地**：1-3 [保暖修正] + 1-2 [保暖裁定]，保暖值 5 档定性（优/良/中/弱/差）修正有效温度层，WM 每轮实时裁定，不由 WSK 维护
- **Condition 全库移除 + Component 生命周期简化**：组件名 + Type + Role + 状态描述，替代旧 Condition + Last Confirmed
- **T1 据点消耗机制落地**：1-1 [判定] 每轮必出 + Day 推进必含消耗行 + 1-2 [据点消耗规则]
- **D2 据点损耗机制最终版**：三级状态（完好/损坏/毁坏）+ 统一 Day 推进触发 + 天气语义判断 + 人为 OR 关系 + 二级后果映射 + MP 降为指示器
- **D3 Relationship 升降档指引**：升降档触发表 + 跨档跳跃 + 势力 vs 个体 + 初始档冷漠 + 单向更新
- **D4 据点暴露机制**：Security/Exposure 三档（hidden/local-only/publicly-known）+ 暴露来源 + 后果联动 D2 人为破坏
- **§0.13 提示词工程设计原则定案**：从 D5 实战经验提炼 10 条编写层原则（⑥-⑮），与 §0.5（架构层）、§十九（流程层）、§0.11（审计层）形成完整体系
- **PAM v1.2 工具化**：Phase 1 执行证据约束 + 跨文件枚举一致性检查 + 形态黑名单 2.10；全库机械核验 P0/P1 清零
- **验证结果**：format_lint / detect_duplicates / validate_enums / gen_ref_graph 4 项 0 issues（仅 1 个预存断链后于 v1.57 修复）

#### v1.57（2026-08-11）

**库存系统重构计划阶段 1-5 完成（INVENTORY_SYSTEM_DESIGN.md + 全量审计 + Prompt 逻辑收敛 + 端到端验证）**

- **根因**：v2.01-T3 评估结论"不重构库存规则"的前提（"如未来规则膨胀到影响运行时，可重新评估"）不再成立。经历了 T1/T2/T4/D1/D2/D4 以及 [库存分类与精简原则] 反复修订，库存规则已膨胀，需要系统性梳理而非局部修补。
- **阶段 1 设计文档**：新建 INVENTORY_SYSTEM_DESIGN.md（9 章 + 附录），作为库存子系统 SSOT，定义设计意图、数据模型、机制链路、格式规范、历史教训。设计文档定义"为什么"，prompt 文件定义"怎么做"
- **阶段 2 全量审计**：基于设计文档对 6 个 prompt 文件按 15 功能域全量审计，发现 6 个差距（0 P0 / 2 P1 / 4 P2）。PB §0.13 原则⑥-⑮逐条检查 9/10 通过，设计文档自身审计 0 问题
- **阶段 3 project_memory 与工具更新**：批次 1-4 修复全部 6 项遗留项（术语同步/术语规范/损耗语义化/折叠明细删除/载具存储位设计）
- **阶段 4 Prompt 逻辑收敛**：修复审计发现的 6 个差距
  - 批次 1：四类存储位术语同步（"三类库存"→"四类存储位"、"跨层转移"→"跨位转移"）
  - 批次 2：术语规范机械替换（"持久化"→"不由 WSK 维护"、"落盘"→默认/确认、"自动入账"→"自动提交"、"记录(动词)"→"更新/维护"、"保存"→"维护"、"入账"→"提交"），覆盖 7 个 prompt 文件约 50 处 + PB 4 处 + 全库 21 处残留
  - 批次 3：据点损耗机制语义化（掷骰/Base → WM 语义判断）；折叠明细删除（断链引用 + 违反单向压缩）
  - 批次 4：载具存储位状态规则设计（不引入 Availability 枚举；被偷/毁/弃按 WM 裁定处理）
  - Phase 4：压缩规则冲突修复（2-3 L52/L56 据点核心库存范围 + Inventory State 输出策略统一为三级机制常态归类）、载具存储位描述补充（2-1 L11）、语序调整（2-2 L91 正面在前）
- **阶段 5 端到端验证**：新建 validate_output.py（7 条验证规则 R1-R7）+ 4 个 test_cases 文件（smoke_test_basic / edge_cases / wsk_narrative_drift_test / consistency_snapshot，含 5 正例 + 5 反例）
- **验证结果**：run_audit 4 项 0 issues（§引用 84 全有效）；validate_output 5/5 正例通过 + 5/5 反例检出。全库"入账"动词用法清零（仅保留"写入账本"中的名词"账本"）
- **经验**：
  1. 设计文档先行：设计文档定义目标态，为审计提供"应有状态"基准，避免审计只能识别表面重复而无法发现遗漏和隐性依赖
  2. 术语规范需区分动词/名词："记录"作动词描述系统角色行为时改为"更新/维护"（系统角色无持久化存储能力），作名词保留（如"近五日主要事件记录"）；"入账"作动词改为"提交"；"账本"作为输出隐喻/角色定位名词保留
  3. 术语替换需全库覆盖并多次扫描：批次 2 修改了约 50 处"入账"，后续审计又发现 21 处残留，说明单次替换易遗漏

#### v1.58（2026-08-11）

**全库 prompt 审计 + 引用纯化 + 术语统一 + PB 标准化 + 聊天室字段优化文档**

- **全库 8 文件审计**（0-1/0-2/1-1/1-2/1-3/2-1/2-2/2-3）：
  - 2-x 全面检查：P0×5（引用断链）+ P2×4（命名不一致）+ P3×2（重叠内容/rules 过时）全部修复
  - 1-x 全面检查：P1×5（跨文件引用缺前缀）+ P2×5（含 0-2 "记账"残留）+ P3×5（同文件冗余引用）全部修复
  - 0-x 全面检查：P3×9（0-2 同文件冗余引用）全部删除
  - 合计 26 处修复，涵盖引用断链、术语残留、同文件冗余引用
- **2-2 逐节精审**：DO 表 10→6 条（移除格式约束），SILENT 表 6→4 条（移除重复），[你必须维护和输出的内容] 与 2-3 [完整视图] 6 字段对齐，[LLM 心理预设] section 评估为冗余并从 2-1 删除
- **引用纯化**：同文件引用默认不加（PB §0.13 原则⑥子项 + rules 文件双层约束）；跨文件引用保留 7 处 SSOT 规则引用；跨角色引用 2-2 L71 断链删除
- **术语统一**：全库"记账/建账"→"提交"清零；"据点核心库存"→"据点核心"统一；"Memory Inventory"→"记忆存储位"；"Base Core Site"→"据点名"；"记忆库存"→"记忆存储位"
- **1-3 路由规则修复**：L88 算法第一步补"或目标子区域"（修复同分区内移动无算法覆盖的规则缺口）；L91 [移动] 模板补 Destination 命名规范（Zone/Sub-zone/Location）
- **PB §0.13 新增 3 条原则**：⑯ 输出对齐（prompt 列举顺序 = 权威输出模板字段顺序）+ ⑰ DO/SILENT 职责分工（DO 只写核心动作，SILENT 不写反面回声）+ ⑱ 跨角色复制标注（复制关系记 PB §21，不在 prompt 文件标注）
- **PB 新增 §21 术语表**：动词规范 7 条 + 库存术语 6 条 + 枚举值 9 类 + 角色行为术语 12 条 + 跨角色复制记录 4 项。术语表是维护者 SSOT 参考表，不是 LLM 引用目标
- **聊天室字段优化文档**：新建 聊天室字段优化文档.md（7 章），记录经验1/经验2、内容审计、搬迁清单（推测性）、平台调研（OurDream AI 无公开技术文档）、待决问题 5 项
- **验证结果**：全库 grep "记账/建账" 0 匹配；全库 grep "据点核心库存" 0 匹配；2-3 同文件 § 引用 0 匹配；1-3 L88 算法第一步已含"或目标子区域"

#### v1.59（2026-08-12）

**WM 专用内容副本建立（0-1/0-2 → 1-x）**

- **核对**：逐条对比 0-1/0-2 中 13 节 WM 专用内容 vs 1-1/1-2/1-3 现有规则，识别 1-x 缺失项
- **1-1 修改（4 项）**：补 Season/气温强制查表指令 + Season 与气温显式输出 + 不得调换段序；删除 L36 跨文件引用"（与 0-1 §[...] 一致）"
- **1-2 修改（1 项）**：建副本 [临时NPC生命周期]（3 条规则，来源 0-2 L85-88）
- **1-3 修改（11 项）**：
  - [压力裁定参考] 补 5 轨联动规则 + weakened/critical 行动效率影响
  - [NPC 知识边界与发言规则] 补 4 项（从自身角色出发/不要像战术参谋/正反示例/优先输出类型）
  - [跨区移动路由规则] 补地图内补点 + 地图外地点规则
  - 新建 [常驻NPC调用原则]（判断 5 点 + 速记 6 人 + 组合 5 组）
  - 内联副本区新建 3 节：[天气-季节可用性矩阵] + [季节化运行规则] + [叙事温度与天气硬约束]
- **PB §21.6 跨角色复制记录**：从 4 项扩充至 22 项，补录本轮新建副本 + 既存 0-2→1-2/1-3 内联副本
- **原则**：1-x 现有规则为权威，0-1/0-2 内容仅在 1-x 缺失时建副本；副本标注"（与 0-x §[...] 一致）"

#### v1.65（2026-08-15）

**WM 效率批次落地（DEC-H/J）+ 季节-天气单表合并 + 残余问题清单**

**DEC-H：掷骰 Seed 公式加法查表化（M6u）**
- 公式：`((Day×7)+(Turn末两位×3)+Offset) mod 100` → `(Day 末两位 + 表[Turn 个位] + Event Offset) 的末两位`
- 原理：随机性来源从公式斜率（斜率 3 当天内 Seed 线性递增，伪随机）改为预生成随机数表（LLM 只做拆位/查表/加法，全在强项区）；预估错误率 ~30-35% → ~8-10%
- 迭代史：语义化/纯加法/位交换/双表方案先后被否决（用户裁定保留机械性 + 拒绝当天无随机），最终单表单查
- 配套：enum_registry 弃用术语对（`末两位 × 7` 等 5 模式防回退）；示例行更新

**DEC-J：移动三表聚合（缩水版）**
- [地图总表]+[跨区移动路由规则] 从 1-3 A 区（注意力谷底 47.7%）迁至 D 区 [子地点列表] 前（近因强区 ~91%），移动裁定三表总跨 <80 行
- 砍掉补丁 2（A 区速查卡）：落点在注意力谷底 + SSOT 双定义前科（OPT-P2-02/N-3），不做
- 平台机制再评估结论：DEC-K（必查表拆分）不做、DEC-I（搜刮速查卡）挂起，理由见 WM 效率优化文档 §十一.3/§十二

**季节-天气单表合并（用户提案，含 3 护栏）**
- [温度分层与日期精细对应] 扩展 5 列：Day 范围 | 季节 | 默认温度层 | 典型天气池 | 阶段影响
- 删除 2 节：[天气-季节可用性矩阵]（并入天气池列）+ [季节化运行规则]（并入影响列）；每轮查表链二合一
- 新增 D182+ 夏季行（闭合原表止于 D181 的覆盖缺口）
- 护栏 1：表尾指令"天气池按日期限定候选范围，不定当日值"（防按日定天气的语义误读，经用户指出措辞冲突后修正）
- 护栏 2：[轻量天气系统]（9 值枚举+演进规则）保留不进表，并被表尾 §引用脱离孤立章节
- 护栏 3：天气池列枚举纯度校验（曾混入"融雪积水/脏雪/湿风"非枚举项，已归位影响列/替换为枚举值）
- 表尾措辞 3 轮打磨：删"说明："标签（全库唯一）；裸主题句"联动见 §"改指令句式+括号用途标注；L43 三重重复（季中点参照）删除

**其他**
- 1-1 [叙事长度约束] 删除"单轮总字符 ≤1200"（用户裁定），保留 3-4 段 × 4-6 句结构约束
- 效率文档新增 §十三：实施后残余问题 A-F 六类清单 + DEC-M 候选预案 + B2 冲突探针实验设计（tools/sim_b2_probe.py，dry-run 3 探针导出）

**涉及文件**：1-1, 1-2, 1-3, enum_registry.json, WM效率优化文档, 孤立章节审计清单, PROJECT_BLUEPRINT
**验证结果**：run_audit 4/4 全绿（引用活性/术语/SSOT/格式 0 issues）；天气池枚举纯度脚本 9 行全 OK；1-3 累计 33964→~33350 字符

#### v1.60（2026-08-12）

**聊天室字段与 WM 字段结构重排**

- **设计思想**：基于 LLM 上下文注意力机制（聊天室字段在前部被动吸收有效，精确反查不可靠；角色卡在后部精确检索可靠），重新定义字段分工
- **新增 §0.13 原则⑲-㉒**：
  - ⑲ LLM 上下文注意力分层（总则放聊天室字段，具体规则/表/机械放角色卡）
  - ⑳ Opening Messages 权重衰减（只放一次性开场，不当长期规则容器）
  - ㉑ 公共法原则（聊天室字段只放公共法，WM 专属放 WM 卡）
  - ㉒ 阵营内容拆分（描述性介绍放 Private Details，活动规则放 WM 卡）
- **§1.1.1 更新**：
  - Private Details 从"全局协议与系统铁律"→"公共法"（删 WM 专属输出格式，加世界概况/阵营描述/常驻NPC）
  - Scenario 从"世界共识与基础世界观"→"叙事开场"（删规则条文/数据表，加场景/世界底座叙事/玩家状态）
  - 0-1 与 0-2 区别从"可见性差异"→"内容形态差异"（0-1 是法，0-2 是体验）
- **§1.1.2 更新**：
  - WM Scene 删除"对世界共识的调用声明"（跨文件引用不可靠）
  - WM ED 删除"引用指针指向聊天室字段"（改为自包含）
  - WM APD 更新基调引用从 Scenario → Private Details
- **文件变更**：
  - 0-1：63 行 → 215 行，删 WM 专用 4 节 + 格式模板，接收 0-2 公共法 6 节 + 4 阵营 + 常驻NPC
  - 0-2：348 行 → 18 行，规则文档重写为叙事开场
  - 1-1：补 [新对话首轮启动]
  - 1-2：修 2 处跨文件引用 + 删 1 处复制标注
  - 1-3：修 10 处 Scenario→Private Details + 删 10 处复制标注 + 更新内联副本区介绍
- **架构文档**：新建 `全局字段承载内容优化方案.md`（四层分工 / LLM 注意力机制 / 迁移路径）
- **验证结果**：`聊天室 Scenario 字段` 零残留；`与 0-2 §` 零残留；跨文件引用全部清理

#### v1.61（2026-08-13）

**深度提示词工程审计修复 + 全库一致性验证**

- **P0：0-3 越狱模式删除**：删除"Dev 永久接管"越狱模板，改用主题范围声明（后末日生存题材许可）。越狱模板与全部 145+ 条规则根本矛盾，是系统完整性最大风险
- **P1：重复内容清除**：0-1↔1-2 跨位转移规则逐字重复（[章节名]+标点统一）；0-1↔1-3 NPC 反全知规则 ~90% 重复（[章节名]统一）；1-3↔2-3 重量校准锚点重复
- **P1：据点损耗机制量化**：1-2 的 10 处"WM 语义判断"恢复为 掷骰 量化方案（Base 值 + 偏移表），从 v1.57 的语义化方向回退。原因：定性词（"低可能"/"高可能"）无判定标准，LLM 无法校准
- **P1：注意力优化**：1-3 必查表提醒移至 1-1 [运行锚点]（WM 卡最高注意力位置），升级为任务触发式检索（"做X时查§Y确定Z"）。解决 1-3 共 899 行关键表在注意力衰减区的问题
- **P1：否定指令优化**：1-3 [禁止事项]→[输出纪律]（正面框架）；0-1/1-3 独立否定转正面等价
- **P2：0-3 行为规则删除**（归 1-1，消除三重重复）；**0-2 游戏术语自然化**（strained/Cool 从叙事中移除）；**0-2 内部机制名删除**（"反全知/反顾问化机制"不出现在叙事中）；**1-3 元注释删除**；**0-3 禁用词计数修正**（20→19）
- **P2：引用纯化**：1-2 歧义引用补字段限定符；1-3 来源标注修正（劫掠者兄弟会来源误指码头帮）；1-3 同文件冗余§引用删除；2-2 跨角色卡引用自包含化
- **P2：自包含表修正**：prompt-engineering.mdr 中 NPC 反全知内联目标 1-2→1-3，跨位转移内联目标 2-2→2-3（与实际位置对齐）
- **P3：WSK 存档/归档措辞修正**（"WSK 存档"→"用户手动复制的 WSK 产出素材"；"WSK 归档"→"WSK 产出素材供用户手动复制"）
- **验证**：7 步审计方法论全库扫描（交叉引用 77 处 + 枚举 9 类 + 术语 11 项 + 流程 5 项 + 内联完整性 + 自包含表），9 类枚举全部通过，0 残留

#### v1.62（2026-08-13）

**WM 三字段结构重组（按全局字段承载内容优化方案 L171-228 落地）**

- **归位准则确立**：区分"输出契约"（每轮必出什么/出场次序/触发条件，归 Scene）与"格式模板"（具体长什么样，归 Extra Details）；叙事审美归 APD；裁定决策逻辑归 APD；数据表/参考表归 Extra Details
- **PB 承载边界表**：§0.6 新增 WM 三字段承载边界表（适合承载 / 不适合承载），与角色卡六字段功能分工表互补
- **P1-A1：1-2 新增 [叙事审美] 节**：旁白基调（冷峻/克制/重质感/少说教）+ 世界基调 + 压力写法 + 反馈方式 + 句式求变（感知通道轮换方案，替代不可执行的"最多用2次"计数指令）
- **P1-A2/A3+E1：格式模板迁移 1-2→1-3**：[判定 格式模板] 和 [掷骰 格式模板] 从 APD 迁入 Extra Details 新增 `--- 格式模板 ---` 区；1-2 保留引用行
- **P2-E2/E3：裁定参考迁移 1-3→1-2**：[压力裁定参考] 和 [Turn/Day 推进规则] 从 Extra Details 迁入 APD（裁定决策逻辑归 APD）；1-2 内引用去掉 "Extra Details" 前缀
- **P2-A5：抢救时间窗参考迁移 1-2→1-3**：参考表归 Extra Details
- **P3-S1：[主要状态] 格式详情迁移 1-1→1-3**：1-1 保留契约行（5 段固定结构声明 + 出场次序）+ 引用行指向 Extra Details §[主要状态 格式模板]；1-1 同时删除"输出句式求变"和"世界基调"（移至 1-2 [叙事审美]）
- **引用更新**：1-1 的 掷骰/判定引用从 APD 改为 Extra Details；1-3 偏移表删除指向 APD 的断链同文件引用
- **自包含表修正**：prompt-engineering.mdr 中 [主要状态] 格式和压力/风险栏内联目标 1-1→1-3，一致性源更新为 1-3 §[主要状态 格式模板]；分工速查表和字段身份层检查清单同步更新

#### v1.64（2026-08-13）

**WSK 库存转移机制修复 + Delta/State 解耦**

从 WSK 实际输出截断问题出发，审核转移机制规则链，发现并修复 4 项问题。

- T-1: 分仓约束过严--"同一物品不得同时出现在两个存储位"禁止合法分仓，改为"因转移记账错误导致同一份物品重复出现时必须修正（合法分仓不受此限）"
- T-2: Delta 缺转移格式示例--正确输出示例只有"获得/消耗"，补 `转移 罐头×2 随身->据点核心`
- T-3: "同轮或相邻轮"措辞对 WSK 无操作意义--改为"同一批次内"（贴合 WSK 批次扫描模型）
- T-4: Delta 与 State 职责边界声明（方案 C 解耦）--Delta=物品级变化摘要（供 WM 阅读），State=组级当前存量（供 WM 使用+下轮基线），各自独立推导不要求算术链，仅要求语义一致

**附带修复**（前序 commit）：
- 转移抵消规则：同一物品"转出又转回"时 Delta 合并为净变化，State 按净位置记账
- 半量单位校准锚点：半瓶约0.25kg / 小半罐约0.2kg / 半盒约0.1kg / 半袋约0.15kg
- 非可消耗物资豁免 kg 转换（金属/工具/容器/布料等保持原单位）
- `[Resolution]` 全库统一改名 `[判定]`（17 文件同步）
- Scavenging->获得，≈->约（全库术语统一）

**涉及文件**：2-1, 2-3（核心修复）；0-1/0-2/1-1/1-2/1-3（术语统一）；PROJECT_BLUEPRINT.md, CHANGELOG.md（文档同步）
**验证结果**：static_audit 48/48 PASS；引用活性 0 断链

#### v1.63（2026-08-13）

**全库功能性审计修复：P0-P3 共 22 项**

从游戏功能、规则、机制角度执行深度审计，识别并修复 LLM 不可执行机制、算术过载、维度不对齐、内容重复等问题。

**P0 关键修复（2 项）**——消除输出矛盾与不可执行机制：
- P0-1: [移动] 输出位置矛盾修复——1-2 L150 原文"在 [主要状态] 之后输出 [移动]"与 1-1 L37 规定直接矛盾，改为引用 [输出结构]
- P0-2: 0-3 跨轮词频统计改为单轮约束——原要求 LLM 跟踪最近 3 轮词频（不可执行），改为单轮内自我约束

**P1 高优先级修复（7 项）**——降低算术复杂度、消除持久状态依赖：
- P1-1: Seed 公式简化为尾数运算——`(Day×7 + Turn×3 + Offset) mod 100` → `((Day末两位×7) + (Turn末两位×3) + Offset) mod 100`；示例行同步更新
- P1-2: 场外演化检查删除冗余条件 (d)——"距上次检查已跨≥1 Day"与 (a) 跨日重复，且 LLM 无法追踪"上次检查时间"
- P1-3: 据点暴露/驻留天数计数改为定性判断——"连续暴露≥3日"→"暴露持续多日"；"连续驻留≥7日"→"主要居所"
- P1-4: 压力阈值表增加 step 换算参考（1 step = 0.5h）
- P1-5: 据点损耗修复间隔简化——"读修复日期对比当前 Day 判断≥3日"→"判断本轮是否已有修复记录"
- P1-6: 同轮多概率事件 Offset 简化——删除冲突检测+顺延逻辑，改为简单序号递增
- P1-7: WSK Party Condition 维度对齐到 WM 5 轨状态级——旧的无序维度列表（伤病/疼痛/感染/饥渴/...）改为结构化提取

**P2 次优修复（10 项）**——去重/补缺/简化/规范化：
- P2-1: NPC 反全知规则去重——0-1 压缩为 3 行概述，1-3 保留 15 行细则
- P2-2: 敌对阶梯 5 阶段枚举在 1-3 补充——no-signal / old-trace / spotted / contacted / hostile-contact
- P2-3: 据点物资丰富+10 给出参考阈值——口粮≥3天 / 弹药≥30发 / 药品储备 / 高价值交易品
- P2-4: 跨区移动增加跨午夜换算示例
- P2-5: 0-3 感官 token 改为可选参考工具——删除"每轮至少1个"硬约束
- P2-6: WSK 消耗投影简化为公式——`合计kg ÷ (人数 × 日均)`，删除跨分类求和兜底逻辑
- P2-7: 引入"微步"概念统一时间体系——5-15分钟/个，累计达30分钟=1 step
- P2-8: 据点暴露 3 档 vs 知情范围 4 档差异标注——在据点暴露处注明区别
- P2-9: 据点结构基线移存档——删除 5 个存档据点数据（-32 行），保留格式模板
- P2-10: 跨位转移协议去重——1-2 改为引用 0-1（-5 行）

**P3 轻微修复（3 项）**：
- P3-1: 温度层/天气英文枚举保持现状（有利于结构化提取）
- P3-2: 保暖修正方向改中文——`+2档`→`向暖侧移2档`，`-2档`→`向冷侧移2档`
- P3-3: 0-2 起始物资补充 kg 估算——压缩饼干约0.45kg，水约0.5kg

**PB 同步更新**：
- §4.3 Seed 公式同步（×31/×17 → 末两位×7/×3）
- §21 术语表：知情范围 4 档更新、Human Threat Stage 更新为 5 阶段
- §1.1.1 0-3 内容描述更新（多轮→单轮）

**验证结果**：专项静态验证脚本 48/48 全部通过（含 Seed 多组算术验证、微步累计结算、保暖方向模拟、消耗投影公式验证）；format_lint 0 issues；detect_duplicates 0 重复

**影响范围**：0-1, 0-2, 0-3, 1-2, 1-3, 2-3 共 6 个提示词文件 + PB

#### v1.68（2026-08-19）

**PC 格式契约闭合**（决议：Probability_Check格式修正方案.md；4 真实 WM 样本核验驱动）

- **根因**：格式模板与 WM 实际输出长期三输（模板说纯数字、实际永远带括号、校验器永远 FAIL）；Seed 无类名披露不可复算；[移动] 轮合并掷可被情境掷顶替
- **内容**：Trigger 强制 Event Class 前缀（同轮同类重复 `#序号`，Seed 复算闭环）；Base 括号理由入正式格式；全部可机器判定约束收敛为 output_rules.json 单条权威正则（±0 前瞻、域内`；`与 markdown 符号字符类拦截、Dase/wear 合法分支）；合并掷固定 world-event/npc-contact 名义 + 未触发 Dase 压缩
- **验证**：e2e 接入 json SSOT 校验；4 真实样本反例全拒 + 8 合规形态全过；static_audit 60/60

**WSK 多据点分桶输出**（决议：多据点分桶输出决议.md；决策 1=A 统一限定符、决策 2=A 全量无上限）

- **根因**：APD 2a 有分桶原则无分桶格式，Inventory State / Base Structure State 多据点输出形态发散；Delta 与 WM 消耗行存储位无据点限定符，提取链断裂；分类枚举跨文件不一致（1-3 三类 vs 2-2 四类）
- **内容**：分桶段头 `据点核心/{据点名}（{锚点}，{4分类}）`；结构块锚点行+组件行；Delta 统一限定符 + 跨据点直转；WM 消耗行 `据点({据点名})`；枚举统一四分类
- **边界修补**：①弹药口径合并不跨桶；②定性标记组发生具体数量变化后升级具体计数（`(估)`），不回退
- **验证**：sim_multibase_test.py 16/16（B1-B7 格式 + B8/B9 边界断言）；static_audit 60/60

**工程附带**：9 工具脚本 UTF-8 输出守卫（Windows GBK 管道修复）；static_audit 60 项断言对齐现行机制（存量 8 FAIL 清零）；Exposure 死引用清理；Dase 覆盖完整性条款（[移动] 轮两掷并列，防漏散人掷）；常驻NPC 7 人口径对齐（1-3 调用原则补小照，2-2 首领子段限定 6 首领）；小照档案移位归位 + 优先承载机制移除（0-1 仅留身份行、不加跨层引用注释，细则锚点收敛于 1-3 散人接触内"小照档案"块；npc-contact 统一临时NPC出场，不设小照优先）

**影响范围**：1-1, 1-3, 2-2, 2-3（提示词）；output_rules.json, e2e_test.py, static_audit_test.py, sim_multibase_test.py, run_audit.py 等 9 工具；CHANGELOG.md, 审计历史记录.md

### 20.3 回滚策略

- 每批修改独立 Git commit
- commit message 标注批次编号和修改类型
- 如发现问题：`git revert <commit>` 回滚到上一批次
- 大规模修改前先创建分支，验证通过后合并

---

## §21 术语表（2026-08-11 定案）

> 本表是**人类维护者的术语 SSOT 参考表**，不是 LLM 的引用目标。每个术语在使用它的角色字段内就地定义，不引用本表。本表用于：① 维护者查术语规范形式 ② 检查跨角色复制关系 ③ 防止术语回退。

### 21.1 动词规范

| 禁用动词 | 规范动词 | 理由 | 适用范围 |
|---|---|---|---|
| 入账 | 提交 | 系统角色由 LLM 驱动，无持久化存储能力 | 全库 prompt 文件 |
| 记账 | 提交 | 同上 | 全库 prompt 文件 |
| 建账 | 首次提交 | 同上 | 全库 prompt 文件 |
| 记录（作动词描述系统角色行为） | 更新/维护 | 同上 | 全库 prompt 文件 |
| 保存 | 维护 | 同上 | 全库 prompt 文件 |
| 持久化 | 不由 WSK 维护 | 同上 | 全库 prompt 文件 |
| 落盘 | 默认/确认 | 同上 | 全库 prompt 文件 |

> 注：以上动词作**名词**时保留（如"近五日主要事件记录"、"账本"作为输出隐喻/角色定位名词保留）

### 21.2 库存术语

| 术语 | 规范形式 | 定义 | 权威来源 | 跨角色复制 |
|---|---|---|---|---|
| 四类存储位 | 随身/据点核心/记忆/载具存储位 | 替代旧"三类库存"，四类位阶 | 2-2 L88 | 0-1/0-2/1-2 有副本 |
| 据点核心存储位（全称） | 据点核心存储位 | 据点内的核心存储位 | 2-2 L88 | — |
| 据点核心（简称） | 据点核心 | 枚举中可省略共享后缀"存储位" | 2-2 L88 | — |
| 三级分类机制 | 功能门类-分类-组别 | 单向压缩，不可展开 | 2-2 [库存分类与精简原则] | — |
| Inventory Delta | Inventory Delta | 仅记变化量，不记存量 | 2-3 [完整视图] 字段 1 | — |
| 跨位转移 | 跨位转移 | 替代旧"跨层转移" | 0-1 [跨位转移与知情范围协议] | — |

### 21.3 枚举值

| 术语 | 取值 | 权威来源 |
|---|---|---|
| 知情范围 | hidden / party-known / local-only / publicly-known | 0-1 [跨位转移与知情范围协议] |
| Availability | abundant / adequate / scarce / depleted / spoiled / unknown | 2-3 [最低提交标准] |
| Human Threat Stage | no-signal / old-trace / spotted / contacted / hostile-contact | 1-3 [掷骰 偏移表] |
| Relationship | 依恋/完全信任 / 亲密/信任 / 好感 / 中立 / 冷漠 / 敌意 / 仇恨 | 2-3 [固定取值] |
| 温度层 | 极寒 / 寒冷 / 凉爽 / 温和 / 炎热 | 1-3 [温度分层与 5 轨压力联动] |
| 保暖值 | 优 / 良 / 中 / 弱 / 差 | 1-3 [保暖修正] |
| 据点分类 | 住宅 / 商业 / 工业 / 仓储 | 1-3 [据点损耗机制] |
| 组件状态 | 完好 / 损坏 / 毁坏 | 1-3 [据点损耗机制] |

### 21.4 角色行为术语

| 术语 | 定义 | 权威来源 |
|---|---|---|
| 提交（动词） | WSK 从 WM 叙事中提取已成立变化并写入 [State Update] | 2-1/2-2 |
| 提取（动词） | WSK 从 WM 发言内容中强语义提取已成立结果 | 2-2 DO 表 |
| 裁定（动词） | WM 在叙事中对游戏机制做出判定 | 1-2 |
| 维护（动词） | 系统角色对状态字段的更新行为 | 2-2 |
| DO 表 | 触发时必须执行的核心动作 | 2-2 |
| SILENT 表 | DO 无法覆盖的反面边界 | 2-2 |
| 判定 | WM 在每轮末尾输出的消耗/损耗/变化裁定栏 | 1-1/1-2 |
| [State Update] | WSK 输出的状态更新软标签 | 2-1 |
| D-T 索引 | Day-Turn 索引，[State Update] 第一行 | 2-1/2-3 |
| Delta 标签 | [State Update] 下的变化子段标签 | 2-3 |
| 完整视图 | [State Update] 下的 6 字段全量输出 | 2-3 |
| 近五日主要事件 | 完整视图最末字段，以 D 为单位输出 | 2-3 |

### 21.5 角色分类术语

| 术语 | 定义 | 权威来源 |
|---|---|---|
| 用户（玩家） | 游戏玩家，通过输入声明意图和行动 | 0-1 [系统角色分工与通讯] |
| 系统角色 | World Master（WM）/ World State Keeper（WSK），由 LLM 驱动的系统角色 | 0-1 [系统角色分工与通讯] |
| NPC | 世界内部人物，含常驻NPC（6 名固定人物）和临时NPC（场景临时人物） | 0-1 [系统角色分工与通讯] |
| 平台角色 | 游戏过程中通过平台角色卡加入的 AI 角色（非系统角色、非NPC，由 WM 投放动态锚点）；1-3 [平台角色动态锚点协议] | 0-1 [系统角色分工与通讯] |

### 21.6 跨角色复制记录

| 术语/规则 | 定义来源 | 复制到 | 复制内容 |
|---|---|---|---|
| 知情范围 4 档枚举 | 2-3 [固定取值] | 0-1 [跨位转移与知情范围协议] | 枚举值 + 传播规则 |
| Relationship 7 档 | 2-3 [固定取值] | 1-3 [Relationship 升降档指引] | 枚举值 + 升降档触发表 |
| 四类存储位 | 2-2 L88 | 0-1/0-2/1-2 | 四类名称 + 禁止第五类声明 |
| Season/气温查表+显式输出+段序禁止 | 0-1 [主要状态栏硬约束] | 1-1 [输出结构] | 3 条硬约束（查表指令/显式输出/不得调换段序） |
| 地图内补点+地图外地点 | 0-1 [地图逻辑] | 1-3 [跨区移动路由规则] | 补点定义 + 外地点绑定规则 |
| 5 轨联动+状态级行动效率 | 0-2 [环境生存压力系统] | 1-3 [压力裁定参考] | 轨道联动规则 + weakened/critical 影响 |
| NPC 发言 4 项补充 | 0-2 [NPC 反全知] | 1-3 [NPC 知识边界与发言规则] | 从自身角色出发/不要像战术参谋/正反示例/优先输出类型 |
| 临时NPC生命周期 | 0-2 [临时NPC生命周期] | 1-2 [临时NPC生命周期] | 完整副本（3 条规则） |
| 常驻NPC调用原则 | 0-2 [常驻NPC调用原则] | 1-3 [常驻NPC调用原则] | 完整副本（判断 5 点 + 速记 + 组合） |
| 天气-季节可用性矩阵 | 0-2 [天气-季节可用性矩阵] | 1-3 内联副本区 | ~~完整副本（4 季节天气倾向）~~ **v1.65 并入 [温度分层与日期精细对应] 典型天气池列，节删除** |
| 季节化运行规则 | 0-2 [季节化运行规则] | 1-3 内联副本区 | ~~完整副本（9 条规则）~~ **v1.65 并入 [温度分层与日期精细对应] 阶段影响列，节删除** |
| 叙事温度与天气硬约束 | 0-2 [叙事温度与天气硬约束] | 1-3 内联副本区 | 完整副本（2 条硬约束） |
| 死亡/抢救/遗物规则 | 0-2 [死亡/抢救/遗物规则] | 1-2 [死亡/抢救/遗物规则] | 完整副本（抢救窗口+遗物处理+传染风险） |
| 知情范围协议 | 0-1 [跨位转移与知情范围协议] | 1-2 [知情范围协议] | 完整副本（跨位转移+传播规则） |
| 季节锚点 | 0-2 [季节锚点] | 1-3 内联副本区 | 完整副本（D1 锚定+气候型） |
| 温度分层与 5 轨压力联动 | 0-2 [温度分层与 5 轨压力联动] | 1-3 内联副本区 | 完整副本（5 档温度+联动规则） |
| 温度分层与日期精细对应 | 0-2 [温度分层与日期精细对应] | 1-3 内联副本区 | 完整副本（Day-温度对照表） |
| 轻量天气系统 | 0-2 [轻量天气系统] | 1-3 内联副本区 | 完整副本（9 种天气+影响） |
| 弹药口径换算表 | 0-2 [弹药口径换算表] | 1-3 内联副本区 | 完整副本（口径系数+公式） |
| 可消耗物资重量校准锚点 | 0-2 [可消耗物资重量校准锚点] | 1-3 内联副本区 | 完整副本（估算锚点） |
| 资源消耗基准 | 0-2 [资源消耗基准] | 1-3 内联副本区 | 完整副本（每日消耗+据点成本） |

> 维护者注意：当修改以上"定义来源"列中的术语/规则时，必须 grep 全库找到所有"复制到"列中的副本并同步更新。
