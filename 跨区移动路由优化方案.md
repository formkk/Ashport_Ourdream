# 跨区移动路由 + Turn 编号优化方案

> **创建日期**：2026-08-07
> **来源**：real_output.md 实际游戏输出评估
> **状态**：待讨论

---

## 一、问题来源

基于 real_output.md 实际游戏输出的评估，发现以下关键问题：

### 问题 1（P0）：跨区移动无路由计算，耗时严重压缩

**实际输出**：用户从北区/S（高地公寓）移动到工业区/N（化工厂质检小楼），WM 给出耗时 55 分钟（12:15 -> 13:10）。

**应有结果**：
- 北区与工业区不相邻（北区 S=中心区，工业区 N=西北区），需经中心区或西北区中转
- 合法 Route 约 6 step = 180 分钟（3 小时）
- WM 未写出 Route、未计算 step、未输出 5 要素（Origin/Destination/Route/Steps/Travel Time）

**已执行的修复**：
- 路由规则从 0-2（世界共识层）迁移到 1-3（WM Extra Details），新增 [跨区移动路由规则] 节
- 1-1 L8 新增强制指令："跨区移动必须按 §跨区移动路由规则 计算...不得用'走一段时间'或单 step 代替"
- 1-3 L171 引用从 §环境生存压力系统 修正为 §跨区移动路由规则
- 0-2 精简为 [地图概述] + 引用

**待验证**：迁移后是否生效？需要新开游戏测试。

**待讨论的补充方案**：
- 是否需要在 1-2 [执行顺序补充] 中增加一步"若本轮涉及移动，先按 §跨区移动路由规则 计算 Route 和 step"？
- 是否需要在 1-1 [叙事长度约束] 中说明"Route 可简写为子区域序列，不占叙事字符配额"？（解决叙事长度与 Route 书写的张力）
- 是否需要在 [主要状态] 中增加 Route 行？（当前 5 要素只要求写在叙事正文中，但叙事正文有 1200 字符限制）

### 问题 2（P0）：Turn 编号未推进

**实际输出**：三轮输出 Turn 全程为 T188（06:45 / 12:15 / 13:10），未按规则 +1。

**规则要求**：1-3 L171"Turn 编号每轮 +1（与时间独立）"、"每轮 +1 不可跳号"。

**根因分析**：
- 1-3 L171 的规则在 [Turn / Day 推进规则] 节中，但 WM 可能未将其识别为每轮必须执行的硬约束
- 1-1 L20 只说了"[主要状态] 每轮必出"，没有说"Turn 每轮必 +1"
- 1-2 L61"若本轮已裁定明确耗时，必须在 [主要状态] 中推进官方时间（Day/Turn/时间）" -- 这条在 [执行顺序补充] 第 6 步，但可能被 WM 忽略

**修复方向**：
- 方案 A：在 1-1 [运行锚点] 中增加一条硬约束"Turn 编号每轮 +1，不可跳号，不可不推进"（与"[主要状态] 每轮必出"同级）
- 方案 B：在 1-2 [执行顺序补充] 第 6 步强化措辞，从"必须"改为"每轮强制推进 Turn +1，无例外"
- 方案 C：在 0-1 [主要状态栏硬约束] 中增加"Turn 编号每轮 +1"（与"每轮必出"同级）

### 问题 3（P2，仅观察不修复）：叙事字符严重超标

**实际输出**：搜刮轮叙事约 2500+ 字符，远超 1200 字符限制。

**规则要求**：1-1 L39"单轮叙事总字符 ≤1200"。

**处置**：仅观察，不修复。根因是 LLM 输出长度控制能力有限，且搜刮场景天然需要较长叙事。后续可考虑放宽限制或调整叙事结构，但不在本轮优化范围内。

---

## 二、已执行修复清单

| 修复 | 文件 | 内容 | 状态 |
|------|------|------|------|
| 路由规则迁移 | 1-3 | 新增 [地图总表] + [子地点列表] + [跨区移动路由规则] | 已执行 |
| 0-2 精简 | 0-2 | 删除地图详述，替换为 [地图概述] + 引用 | 已执行 |
| 1-1 L8 强制指令 | 1-1 | "跨区移动必须按 §跨区移动路由规则 计算...不得用'走一段时间'或单 step 代替" | 已执行 |
| 1-3 L171 引用修正 | 1-3 | §环境生存压力系统 -> §跨区移动路由规则 | 已执行 |
| 0-1 L41 引用更新 | 0-1 | 指向 WM Extra §地图总表 等 | 已执行 |
| 2-1 L5 引用更新 | 2-1 | 移除"地图/子地点/Tier/标签" | 已执行 |
| 0-1 L14 示例修正 | 0-1 | 压力段从 1 轨改为 5 轨全量 | 已执行 |

---

## 三、待讨论问题

### 3.1 Turn 编号未推进的修复方案

**问题**：三轮输出 Turn 全程 T188 未 +1。

**讨论选项**：

| 方案 | 位置 | 内容 | 优点 | 缺点 |
|------|------|------|------|------|
| A | 1-1 [运行锚点] | 增加"Turn 编号每轮 +1，不可跳号，不可不推进" | 与"[主要状态] 每轮必出"同级，最强约束 | 1-1 已较长 |
| B | 1-2 [执行顺序] 第 6 步 | 强化措辞为"每轮强制推进 Turn +1，无例外" | 在执行流程中，顺序明确 | 可能被 WM 跳过执行顺序 |
| C | 0-1 [主要状态栏硬约束] | 增加"Turn 编号每轮 +1" | 与格式约束同位 | 0-1 是全局协议，Turn 是 WM 专用概念 |
| D | A+B 双保险 | 1-1 声明 + 1-2 执行 | 多层强化 | 冗余 |

### 3.2 Route 书写与叙事长度约束的张力

**问题**：完整 Route（如 10 个子区域节点）在叙事正文中展开会占用大量字符，与 1200 字符限制冲突。

**讨论选项**：

| 方案 | 内容 | 优点 | 缺点 |
|------|------|------|------|
| A | Route 写在 [主要状态] 之后作为独立结构块 | 不占叙事字符配额 | 改变输出结构 |
| B | Route 简写为子区域序列（如"北区/S->北区/C->北区/W->西北区/E->..."） | 压缩字符 | 可读性降低 |
| C | 叙事正文中只写起点和终点，Route 写在 [主要状态] 的位置段之后 | 最小叙事开销 | 5 要素分散在两处 |
| D | 放宽叙事长度限制至 2000 字符 | 解决根因 | 可能导致输出过长 |

### 3.3 迁移后路由规则是否生效的验证计划

**验证方法**：新开游戏，执行一次跨区移动，检查：
1. WM 是否写出 Route（以子区域为粒度）
2. WM 是否计算 step 数
3. WM 是否结算正确耗时（step × 30min + 修正）
4. WM 是否在 [主要状态] 中推进 Turn +1 和正确时间戳
5. WM 是否在叙事中包含 5 要素

---

## 四、优先级

| 优先级 | 问题 | 处置 |
|--------|------|------|
| P0 | Turn 编号未推进 | 需讨论修复方案（§3.1） |
| P0 | 路由规则迁移后是否生效 | 需新开游戏验证（§3.3） |
| P1 | Route 与叙事长度张力 | 需讨论解决方案（§3.2） |
| P2 | 叙事字符超标 | 仅观察，不修复 |
