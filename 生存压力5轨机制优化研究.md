# 生存压力 5 轨机制优化研究

> **定位**：本文件是生存压力 5 轨机制的专题研究文档，记录现状分析、优化起因、优化目的及讨论结论。作为后续修改决策的基础，不直接承担平台字段。
>
> **创建日期**：2026-08-05
> **关联文档**：`PROJECT_ANALYSIS.md`（§0.1 字段身份 / §4.2 生存压力系统）/ `OPTIMIZATION_ROADMAP_v1.md`（§八 字段身份审计）/ `Prompt_File/*`

---

## 一、优化起因

### 1.1 问题发现

在对项目进行字段身份审计（`OPTIMIZATION_ROADMAP_v1.md §八`）的过程中，发现生存压力 5 轨机制的维护链路存在结构性脆弱点：

1. **WSK 稀疏输出与 WM 主要维护者身份不匹配**：WSK 每 1-2 游戏日才由用户手动点击输出一次，但 5 轨压力需要每轮（每个 Turn）跟踪演化。当前机制下，WSK 是"硬状态管理员"，但 5 轨的实际日常维护完全依赖 WM 的增量演化。

2. **WM 漏输 [主要状态] 导致状态丢失**：WM 偶尔会漏输 [主要状态]。原规则下压力段"仅列非 stable 轨道，全部 stable 时写 `-`"，如果漏输一轮，下一轮 WM 无法从上一轮的压力段恢复完整 5 轨状态--因为 stable 轨道没有被显式记录。

3. **增量演化的锚点表述不精确**：原规则表述为"以自己上一轮 [主要状态] 的压力状态为锚点"，但 WM 漏输时不存在"上一轮"，应回溯到"对话历史中上一次出现的"。

### 1.2 已执行的修改

针对上述问题，已执行两项修改：

| 修改 | 文件 | 内容 |
|------|------|------|
| 压力段全量输出 | `0-1_Private_Details.md` L19 | 从"仅列非 stable"改为"5 轨全部列出（包括 stable）" |
| 锚点回溯规则 | `1-2_WorldMaster_Additional_Personality_Details.md` L14 | "上一轮"改为"对话历史中上一次出现的"，应对漏输 |

### 1.3 引发的深层问题

上述修改解决了 WM 侧的锚点完整性问题，但引出了一个需要深入讨论的问题：

> **在 WM 全量输出 5 轨后，WSK 是否还需要记录和输出 5 轨？**

以及更广泛的问题：

> **5 轨压力在 WM 和 WSK 中的职责分工应该如何重新界定？**

---

## 二、优化目的

### 2.1 核心目标

确保生存压力 5 轨在长期运行中：
1. **每轮可追踪**：WM 每轮输出完整 5 轨状态，不依赖外部校准
2. **抗漏输**：WM 漏输一轮后，下一轮可从最近一次 [主要状态] 完整恢复
3. **抗漂移**：增量演化的累积漂移有纠偏机制
4. **抗窗口丢失**：长 session 上下文窗口超出后有兜底恢复源
5. **Token 高效**：在满足以上条件的前提下最小化 Token 开销

### 2.2 约束条件

- 不能破坏现有的"WM 唯一前台 + WSK 后台校准"架构
- 不能增加 WSK 的触发频率（WSK 仍然是稀疏触发的后台角色）
- 不能引入需要 LLM 长期精确计数/算术/跨轮记忆的规则（原则③ 认知适配）
- 不能破坏 WSK 的"审计者"角色定位

---

## 三、现状分析

### 3.1 机制全貌

5 轨压力在项目中连接了 **5 层、约 35 个机制点**，分布在全部 9 个提示词文件中：

#### 第 1 层：核心定义（0-2 Scenario，不依赖任何角色）

| 机制 | 位置 | 作用 |
|------|------|------|
| 5 轨名称与状态级 | 0-2 L30-32 | 疲劳/体温/脱水/饥饿/伤病 × stable~dead |
| step 结算规则 | 0-2 L33 | 1 step = 30 分钟，环境->行动->状态->决定 |
| 5 轨联动规则 | 0-2 L40 | 湿身->失温->疲劳->失误；感染->脱水；饥饿->恢复慢 |
| 恢复比恶化慢 | 0-2 L41 | 短暂停留只止跌，真正恢复需安全/干燥/保暖/饮水/热量/休息 |
| weakened/critical 行为影响 | 0-2 L42-43 | weakened 影响行动效率；critical 显著影响，可进入 dying |
| 温度分层联动 | 0-2 L493-505 | Mild~Heat 各档对 5 轨的推动规则 |
| 天气驱动体温 | 0-2 L484-489 | 各天气类型对体温的推动 |
| NPC 表达压缩 | 0-2 L301-302 | 寒冷/湿身/脱水/失血/疲劳压缩 NPC 表达；critical/dying 碎片化 |
| 死亡窗口 | 0-2 L53 | 脱水致死 1-5 天，属死亡/抢救规则 |

#### 第 2 层：WM 裁定与输出（1-1/1-2/1-3/0-3，不依赖 WSK）

| 机制 | 位置 | 作用 | 修改状态 |
|------|------|------|---------|
| [主要状态] 压力段输出 | 0-1 L19 + 1-2 L15 | 5 轨全量列出 | ✅ 已改为全量 |
| 增量演化模型 | 1-2 L14 | 以上一次 [主要状态] 压力段为锚点 | ✅ 已改为"上一次出现的" |
| 速率校准参考 | 1-3 L116-158 | 时间阈值表（脱水/饥饿/疲劳/体温/伤病） | 未改 |
| 伤病->状态级映射 | 1-3 L146-158 | 擦伤 stable ~ 贯穿伤 critical 起步 | 未改 |
| 感官 token 抽屉 | 0-3 L26-41 | 5×5 状态级对应感官 token | 未改 |
| 压力影响行动裁定 | 1-2 L92 | 伤病恶化链条 | 未改 |
| 敌对暴露度含伤病 | 1-2 L22 | 伤病是暴露度来源 | 未改 |
| 战斗趁伤病 | 1-2 L75 | 趁伤病/夜间接近更常见 | 未改 |
| 成功后追算代价 | 1-2 L164 | 伤病/噪音/暴露是行动后果 | 未改 |

#### 第 3 层：WSK 记录与校准（2-1/2-2/2-3，WSK 自身维护）

| 机制 | 位置 | 作用 | 是否可简化？ |
|------|------|------|------------|
| Party Condition 字段 | 2-3 L43 | 完整视图第 2 字段，5 轨压力落盘载体 | 待讨论 |
| Survival Anchor State | 2-3 L48 + 2-2 L61/L75 | 完整视图第 7 字段，Last Meaningful Drink/Meal/Sleep End 等锚点 | 待讨论 |
| WSK 压力计算规则 | 2-2 L62-68 | 距锚点时长 + 修正项计算各轨 | 待讨论 |
| 锚点同步 | 2-2 L63 | WM 裁定有效饮水/进食/睡眠后，WSK 同步更新锚点 | 待讨论 |
| 首次建账必出 | 2-2 L215 | Party Condition 含 5 轨是首次建账强制项 | 待讨论 |
| Active Concerns [生存] | 2-3 L53 | WSK 汇总 5 轨压力为生存关注点 | 待讨论 |
| 消耗投影消费 | 2-3 L82 | 消耗投影的队伍人数从 Party Condition 获取 | 待讨论 |
| 伤病细分类型 | 2-2 L125-135 | 伤病不仅是状态级，还有具体伤情类型 | 待讨论 |

#### 第 4 层：WM 消费 WSK 校准（1-2/1-3，部分依赖 WSK）

| 机制 | 位置 | 作用 | 依赖性质 |
|------|------|------|---------|
| 周期校准 | 1-2 L17 | WSK 锚点纠正 WM 增量演化漂移 | 周期性（非每轮） |
| 常驻角色状态 | 1-3 L183 | 常驻角色存活/伤病以 WSK Relationship 字段为准 | 非玩家 5 轨 |

#### 第 5 层：NPC/世界交互（0-2，不依赖任何角色）

| 机制 | 位置 | 作用 |
|------|------|------|
| NPC 职业表达范围 | 0-2 L293 | 医护谈伤势/体温/感染 |
| NPC 带立场意见 | 0-2 L297 | 应体现伤病/旧仇/压力 |
| 保暖与健康 | 0-2 L24 | 衣物/取暖/庇护是体温安全三支柱 |
| 庇护分级 | 0-2 L25 | 临时庇护所/据点缓解体温与暴露压力 |

### 3.2 当前数据流

```
WM 每轮裁定
  ├─ 读取：对话历史中上一次出现的 [主要状态] 压力段（5 轨全量）
  ├─ 增量演化：依据本轮变化（step/饮水/进食/睡眠/冷湿暴露/战斗）
  ├─ 输出：本轮 [主要状态] 压力段（5 轨全量，含 stable）
  └─ 不依赖 WSK 实时数据

WSK 点击时校准（1-2 游戏日/次）
  ├─ 提取：WM Scene 叙事中已成立变化
  ├─ 维护：Survival Anchor（Last Meaningful Drink/Meal/Sleep End 等）
  ├─ 输出：Party Condition（5 轨全量）+ Survival Anchor State
  └─ WM 下一轮读取 WSK 输出做周期校准
```

### 3.3 WSK 不可替代的职责

即使 WM 全量输出 5 轨，WSK 仍有 3 个不可替代的职责：

| 职责 | 依赖链路 | 移除后果 |
|------|---------|---------|
| **周期校准** | WSK 的 Survival Anchor 纠正 WM 增量演化漂移 | WM 漂移持续累积，无法纠正 |
| **上下文窗口兜底** | 长 session 后 WM [主要状态] 被挤出窗口，WSK State Update 是唯一恢复源 | 状态完全丢失 |
| **审计职责** | WSK 校验 WM 的压力输出是否合理 | 无人审计 WM 的压力判断 |

---

## 四、待讨论问题

> **状态更新（2026-08-07）**：§4.1-4.5 的全部待讨论问题已在本轮迁移中定案，结论见 §七 迁移记录。

### 4.1 WSK 的 5 轨记录是否可以简化？

**结论（2026-08-07）**：**移除**。WSK 不再记录 5 轨压力状态级。[主要状态] 已有结构化输出（`脱水 strained`），WSK 记录是纯冗余。

### 4.2 Survival Anchor 的维护权归属

**结论（2026-08-07）**：**移除 Survival Anchor State 字段**。迁移后 WSK 不裁定、不结算、不纠正压力状态，锚点无实际用途。WM 靠 [主要状态] 历史自锚定（回溯最近一次出现的压力段），不依赖锚点时间戳。

### 4.3 压力段全量输出的 Token 代价是否可接受？

**结论（2026-08-07）**：**接受**。每轮 +35 字符的代价远小于状态丢失的风险。全量输出是 WM 自锚定机制的基础，不可压缩。

### 4.4 WSK 压力计算规则（2-2 L62-68）是否仍然需要？

**结论（2026-08-07）**：**移除**。WSK 的压力计算规则（距锚点时长 + 修正项）已迁移到 WM 的 [压力裁定参考] 节（1-3），WSK 不再做压力计算。

### 4.5 伤病细分类型（2-2 L125-135）的归属

**结论（2026-08-07）**：**维持现状**。WSK 记录伤病细分类型用于账本记录，WM 使用伤情->状态级映射用于裁定。两者用途不同，WSK 侧保留记录规范，WM 侧保留裁定映射。

---

## 五、讨论记录

> 本节记录讨论过程中的决策和结论，按日期排列。

### 2026-08-05 初步分析

1. **压力段全量输出已执行**（0-1 L19 + 1-2 L14/L15）
2. **WSK 仍然需要记录 5 轨**，但角色从"主要记录者"转变为"校准者+兜底者"
3. **5 轨压力连接约 35 个机制点**，分布在全部 9 个文件中，不可轻率改动
4. **待讨论问题已列出**（§4.1-4.5），需逐一研究后定案

### 2026-08-07 迁移定案

1. **§4.1-4.5 全部定案**：WSK 移除 5 轨压力记录 + Survival Anchor + 压力计算规则；WM 成为唯一权威
2. **迁移方案经设计开发方法论审计**（一致性/完整性/最小变更/无冗余/可验证五维度全部通过）
3. **迁移执行**：4 个文件、36 项变更（详见 §七）
4. **迁移后全库审计**（第三轮）：18 项发现全部修复（详见 §八）

---

## 六、修改追踪

> 本节记录与本优化相关的所有已执行修改。

| 日期 | 文件 | 位置 | 修改内容 | 状态 |
|------|------|------|---------|------|
| 2026-08-05 | `0-1_Private_Details.md` L19 | 压力格式 | "仅列非 stable" -> "5 轨全量列出（含 stable）" | 已执行 |
| 2026-08-05 | `1-2_WorldMaster_Additional_Personality_Details.md` L14 | 压力裁定运行模型 | "上一轮" -> "对话历史中上一次出现的" | 已执行 |
| 2026-08-05 | `1-2_WorldMaster_Additional_Personality_Details.md` L15 | 压力裁定运行模型 | 新增全量输出说明 | 已执行 |

---

## 七、迁移记录（2026-08-07）

### 7.1 迁移目标

将 5 轨生存压力的权威从"WM 实时裁定 + WSK 周期校准"的双系统架构，迁移为"WM 单权威"架构。WSK 全面退出压力裁定、记录和纠正。

### 7.2 迁移根因

WSK 稀疏激活（仅用户点击时输出）意味着它无法担任 5 轨压力的实时权威。当前架构存在"空心权威"问题：

| 设计意图 | 实际运行 |
|---------|---------|
| WSK 维护锚点并周期性纠正 WM 漂移 | WSK 只在用户点击时才能记录，间隔可能数十轮 |
| WSK 是压力结算权威（2-2 L60-75） | WM 每轮已经在独立裁定并输出 [主要状态] |
| WSK 锚点是"再校准"源（1-2 L17） | WM 靠自己的 [主要状态] 历史自锚定，WSK 校准形同虚设 |

### 7.3 迁移方案

**移除项**：
- WSK 的 Party Condition 中的 5 轨压力状态级记录
- WSK 的 Survival Anchor State 完整视图字段（#7）
- WSK 的压力结算规则（2-2 原 L60-75，共 16 行）
- WSK 的 [固定取值] 中的"生存状态"枚举
- WSK 的 5 轨压力格式行

**迁移项**：
- 2-2 原 L64-70 的 7 条裁定规则迁移到 1-3 新增 [压力裁定参考] 节

**保留项**：
- Party Condition 字段本身（内容改为角色状态定性描述，不含压力状态级）
- Active Concerns [生存] 类别（数据源改为从 Inventory 存量推算 + WM 叙事提取）
- 完整视图从 9 字段变为 8 字段

### 7.4 迁移执行清单（36 项变更，4 个文件）

#### 文件 1-2（WM Additional Personality Details）

| 行 | 操作 | 内容 |
|----|------|------|
| L13 | 改 | 章节名"每轮增量演化 + 周期校准" -> "WM 单权威 + 增量演化" |
| L14 | 新增 | "WM 是 5 轨生存压力的唯一权威源；[主要状态] 压力段为实时权威状态。WSK 不裁定、不结算、不纠正压力状态。" |
| L17 | 改 | 删除"WSK 锚点是周期性再校准锚点..."，补充"各轨裁定参考规则见 §压力裁定参考" |

#### 文件 1-3（WM Extra Details）

| 行 | 操作 | 内容 |
|----|------|------|
| L119 | 改 | 删除"World State Keeper 压力结算以官方锚点与固定状态级为准"，改为"WM 以 [主要状态] 压力段为唯一权威源" |
| L162-168 | 新增 | [压力裁定参考] 节：7 条裁定规则（step 作用/脱水/饥饿/疲劳/体温/伤病/恢复判定） |

#### 文件 2-2（WSK Additional Personality Details）

| 行 | 操作 | 内容 |
|----|------|------|
| L42 | 改 | 删除"以及 5 条生存压力轨道的状态级" |
| L51 | 改 | 章节名"[环境与压力记录规则]" -> "[环境记录规则]" |
| L60-70 | 删 | 删除 13 行压力结算规则（已迁移到 1-3） |
| L71-73 | 改编号 | HTS 规则重编号 12/13/14 -> 2/3/4 |
| L74-75 | 删 | 删除"当前状态级未跨阈值..."和"Survival Anchor 在完整视图 9 字段中..." |
| L215 | 改 | 删除"（含 5 轨压力状态级）" |

#### 文件 2-3（WSK Extra Details）

| 行 | 操作 | 内容 |
|----|------|------|
| L10/L17/L19/L29/L41/L98/L118/L124 | 改 | 全局"9 字段" -> "8 字段"（8 处） |
| L18 | 改 | 引用"§[环境与压力记录规则]" -> "§[环境记录规则]" |
| L23 | 删 | 删除"5 轨压力用自然语言 + 状态级..."行 |
| L48 | 删 | 删除"7. Survival Anchor State" |
| L49 | 改编号 | "8. Base Structure State" -> "7. Base Structure State" |
| L53 | 改 | 删除"（5 轨压力）" |
| L103 | 删 | 删除"生存状态: stable / strained / weakened / critical / dying / dead" |

### 7.5 迁移后现状

#### 5 轨压力权威架构（迁移后）

```
WM 每轮裁定（唯一权威）
  ├─ 读取：对话历史中上一次出现的 [主要状态] 压力段（5 轨全量）
  ├─ 增量演化：依据本轮变化（step/饮水/进食/睡眠/冷湿暴露/战斗）
  ├─ 裁定参考：§压力裁定参考（7 条规则）+ §生存压力时间阈值表（速率校准）
  ├─ 输出：本轮 [主要状态] 压力段（5 轨全量，含 stable）
  └─ 不依赖 WSK 任何压力数据

WSK（不再参与压力）
  ├─ 不裁定、不结算、不纠正压力状态
  ├─ 不记录 5 轨压力状态级
  ├─ 不维护 Survival Anchor
  └─ Party Condition 仅含角色状态定性描述（伤病/疼痛/感染等）
```

#### WSK 完整视图字段（迁移后，8 字段）

| 编号 | 字段 | 变化 |
|------|------|------|
| 1 | Inventory State | 不变 |
| 2 | Party Condition | 内容改为角色状态定性描述（不含压力状态级） |
| 3 | Relationship | 不变 |
| 4 | Faction | 不变 |
| 5 | Human Threat Stage | 不变 |
| 6 | Map Knowledge | 不变 |
| ~~7~~ | ~~Survival Anchor State~~ | **已移除** |
| 7（原 8） | Base Structure State | 编号 8 -> 7 |
| - | 近五日主要事件 | 不变 |

---

## 八、迁移后全库审计（第三轮，2026-08-07）

### 8.1 审计概况

迁移完成后对全库 9 个文件执行 PAM v1.1 全流程审计（Phase 0-7），发现 18 项问题（P0=2 / P1=6 / P2=10），全部已修复。

### 8.2 迁移引入的问题（4 项 P1）

迁移本身成功（术语零残留、字段编号正确），但引入了 4 项接口缺失类问题：

| 问题 | 根因 | 修复 |
|------|------|------|
| [压力裁定参考] 无显式消费者引用 | 运行模型节未指向新增节 | 1-2 L17 补充 §压力裁定参考 引用 |
| "距 Last Meaningful..."措辞与增量演化矛盾 | 裁定参考沿用旧表述 | 改为增量演化表述 |
| Party Condition 内容定义空缺 | 移除压力后未定义剩余内容 | 2-3 L42 补充内容定义 |
| 裁定参考缺伤病轨规则 | 5 轨中仅 4 轨有裁定规则 | 补充伤病轨规则，5 轨全覆盖 |

### 8.3 预存问题（2 项 P0，非迁移引入）

| 问题 | 修复 |
|------|------|
| `§显式同步门槛` 死链（1-3 L216/L390） | 删 `§` 前缀，改为行内引用 |
| `§主要阵营` 模糊引用（9 处） | 删 `§` 前缀，改为自然语言引用 |

### 8.4 格式清理（10 项 P2）

| 类别 | 修复前 | 修复后 |
|------|--------|--------|
| bold 标记 | 2 处 | 0 处 |
| emoji（✅/❌/✓） | 22 处 | 0 处 |
| section 名术语不一致 | "5 轨道" vs "5 轨压力" | 统一为"5 轨压力" |
| 跨轮精确计数 | "连续 15 轮" | "长期" |
| NPC Trust section 名重复 | 1-2 + 1-3 双声明 | 1-2 改为行内引用 |

### 8.5 审计质量指标

| 指标 | 结果 |
|------|------|
| 术语迁移残留 | 0 |
| 第二轮修复验证 | 7/7 通过 |
| 迁移副作用 | 1 项（P2-7 引起的"下文"引用失效，已修复） |
| 最终 grep 验证 | 全部零残留 |

详细审计报告见 `全库对齐审计报告_第三轮_20260807.md`。
