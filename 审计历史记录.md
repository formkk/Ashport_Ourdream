# Ash Harbor - 审计历史记录

> **文档定位**：本文件归档 `PROJECT_BLUEPRINT.md`（项目蓝图，简称 PB）的历史审计记录（原 §十三-§十七），仅供历史追溯，不作为当前规范依据。当前规范以 `PROJECT_BLUEPRINT.md` 为准。
>
> 章节号保留原编号（§十三-§十七），以便跨文档追溯；PB 内相关引用已更新为指向本文件。
>
> **归档日期**：2026-08-06 | **来源**：从 PROJECT_ANALYSIS.md（现 PROJECT_BLUEPRINT.md）§十三-§十七 迁入

---
## 十三、架构审计结果（2026-07-22）

> **历史快照声明**：本节为 2026-07-22 的审计快照，基于当时的 11 文件 + 3 角色（WM/WSK/WER）架构。WER 已于 v1.19 删除（详见 §2.1.1），本节中涉及 WER 的内容仅作历史记录，当前架构以 §一、§二 为准。`0-3_自定义指令.md` 为审计后新增，未纳入本节。

### 13.1 审计范围

- 11 个落盘文件（`Prompt_File/` 下 0-1 / 0-2 / 1-1~1-3 / 2-1~2-3 / 3-1~3-3；其中 3-1~3-3 已于 v1.19 删除）
- 2 个聊天室级字段（`聊天室 Scenario 字段` + `聊天室 Private Details 字段`）
- 3 个系统角色（WM / WSK / WER；其中 WER 已于 v1.19 删除）的职责分工与功能能效性

### 13.2 核心审计结论

| 维度 | 审计结论 | 状态 |
|------|----------|------|
| 通讯机制 | WM 不推送同步块；WSK/WER 强语义提取 Scene 叙事中已明确写出的已成立变化 | **已定案** |
| 接受回执 | 不输出（账本/Ledger 本身即隐式接受） | **已移除** |
| 拒绝回执 | WSK `[Commit Rejected]` / WER `[Event Commit Rejected]` 字段不完整时返回 | **保留** |
| 展示块隔离 | `[Resolution]` / `[Probability Check]` / 子结构块仅供用户阅读 | **已隔离** |
| 决策表 | WSK / WER 各 3 张：DO / REJECT / SILENT | **已统一** |
| 沉默协议 | 无新成立变化可提取时静默；成功提交后无额外文字 | **已统一** |
| Pinned Memory | 持久化层由人类管理员维护（不属 AI 定义范围） | **不** |
| 平台字段映射 | 5 类（Scenario / Private Details / Scene / Additional / Extra） | **无变化** |

### 13.3 顶层架构图

```
                        ┌──────────────────────────────────┐
                        │  聊天室 Private Details 字段      │
                        │  (0-1_Private_Details.md)         │
                        │  全局硬规则 + 通讯协议 + 输出格式 │
                        └──────────────────────────────────┘
                                       ▲ 共享读取
                        ┌──────────────────────────────────┐
                        │  聊天室 Scenario 字段             │
                        │  (0-2_Scenario.md)                │
                        │  静态世界共识（背景/地图/阵营/硬规则）│
                        └──────────────────────────────────┘
                                       ▲ 共享读取
   ┌──────────────────┐    ┌─────────────────────┐    ┌──────────────────┐
   │   WM 角色卡      │    │   WSK 角色卡        │    │   WER 角色卡     │
   │   1-1 Scene      │    │   2-1 Scene         │    │   3-1 Scene      │
   │   1-2 Additional │    │   2-2 Additional    │    │   3-2 Additional │
   │   1-3 Extra      │    │   2-3 Extra         │    │   3-3 Extra      │
   └────────┬─────────┘    └──────────┬──────────┘    └─────────┬────────┘
            │                         │                          │
            │ 唯一前台                  │ 后台账本                 │ 后台档案
            │ Scene + 状态栏            │ [State Update]          │ [WER 历史]
            │                         │                          │
            │      强语义提取          │ 强语义提取                │
            └─────────────────────────┴──────────────────────────┘
                                          ▲
                                          │
                                    Pinned Memory
                                  (人类管理员持久化)
```

### 13.4 三角色职责矩阵

| 维度 | WM | WSK | WER |
|------|----|----|----|
| **前台/后台** | 唯一前台 | 后台 | 后台 |
| **核心职责** | Scene + 状态栏 | 硬状态账本 | 长期历史 |
| **触发方式** | 持续运行 | 用户手动点击 | 用户手动点击 |
| **写入场景** | 每个用户回合 | 提取到变化时 | 提取到事件时 |
| **输出标签** | 无（Scene） | `[State Update]` | `[WER 历史]` |
| **拒绝回执** | 不适用 | `[Commit Rejected]` | `[Event Commit Rejected]` |
| **接受回执** | 不适用 | 不输出 | 不输出 |
| **决策表** | 内部规则 | DO/REJECT/SILENT | DO/REJECT/SILENT |
| **展示块处理** | 生成 | **不解析** | **不解析** |
| **持久化** | 平台上下文 | WSK 自维护账本 | 用户拷贝到 Pinned Memory |
| **读取 Scenario** | ✓（每 Scene） | ✓（边界校验） | ✓（重大事件判定） |
| **读取 Private Details** | ✓ | ✓ | ✓ |
| **5 层记忆位置** | Layer 2-3 | Layer 4（账本） | Layer 4（Ledger） |

### 13.5 聊天室字段分工

| 维度 | 聊天室 Scenario 字段（0-2） | 聊天室 Private Details 字段（0-1） |
|------|----------------------------|----------------------------------|
| 字符限额 | 50,000 | ≥50,000（实测） |
| 内容性质 | 静态世界共识 | 全局硬规则 + 通讯协议 |
| 回答问题 | 世界是什么 | 世界怎么运行 |
| 共享范围 | WM/WSK/WER + 玩家 + NPC | WM/WSK/WER |
| 修改频率 | 世界设定稳定时极少改动 | 协议微调时频繁改动 |
| 主要覆盖 | 背景 / 环境 / 季节 / 天气 / 压力 / 敌对 / NPC / 地图 / 子地点 / 搜刮 / 硬规则 / 4 阵营 / 弹药口径 / 死亡公开性 | 通讯机制 / 输出格式 / 决策表 / 字段语义 / 状态栏硬约束 / 视角硬约束 / 不可越界清单 |
| 失效模式 | 描述与当前运行不一致 | 协议与平台加载不符 |

### 13.6 角色卡文件分工（9 个）

| 角色 | Scene (50K) | Additional (50K) | Extra (100K) |
|------|-------------|------------------|--------------|
| **WM (1-x)** | 平台锚点 + 视角硬约束 + 状态栏 + 概率块位置 | 决策心理预设 + 5 轨压力时间阈值 + 敌对 4 维 + 关系 3 轴 + 报复链 + 伤病恶化 + 交易 2 档 + 搜刮细则 | 动态机制（势力周期活动表 / 锚点协议 / 据点结构基线 / 角色维持 / 死亡公开性） |
| **WSK (2-x)** | 平台锚点 + 输出格式（`[State Update]` 三模式） + 决策表 + 不可越界 + 7 字段事务块 | 详细规则（10 大类必记 + 势力活动追踪 + 环境压力记录 + 库存 3 层 + 据点 4 分类 + 8 条结构 + 提交前检查） | 输出模板（默认视图 / 完整视图 / 极简回执 / 拒绝回执 / 压缩规则 / Availability 6 档 / External Location / Inventory Transaction） |
| **WER (3-x)** | 平台锚点 + 沉默协议 + 3 决策表 + 输出段名（必出 2 段 + 条件性 3 段）+ 触发频率 | 决策心理 + 重大事件清单 + 压缩策略 + 不可压缩锚点 + 结构变化 5 字段 + 据点首次建立 5 字段 | 压缩规则 + 输出模板（必出段 + 条件性段）+ 死亡公开性处理 |

### 13.7 功能 / 机制能效性总览

| 机制 | 设计文件 | 触发条件 | 生效情况 |
|------|----------|----------|----------|
| Scene 强语义提取 | 0-1 + 1-1 + 2-1 + 3-1 | 用户手动点击 WSK/WER | ✓ 生效 |
| 状态栏（6 字段） | 0-1 + 1-1 + 1-2 | 每个 Scene 必出 | ✓ 生效 |
| `[State Update]` 三模式 | 2-1 + 2-2 + 2-3 | WSK 输出 | ✓ 生效 |
| `[WER 历史]` 必出 2 段 + 条件性 3 段 | 3-1 + 3-2 + 3-3 | WER 输出 | ✓ 生效 |
| 拒绝回执 | 2-1 / 3-1 | 字段不完整 | ✓ 生效 |
| DO/REJECT/SILENT 决策表 | 2-2 + 3-2 | 每次点击 | ✓ 生效 |
| 7 字段事务块 | 0-1 + 1-1 + 2-1 | 跨层转移 | ✓ 生效 |
| 5 轨压力轨道 | 0-2 + 1-2 + 2-2 | 每次结算 | ✓ 生效 |
| 6 势力周期活动 | 1-3 + 2-2 | 白天段切换 / 12h 累计 | ✓ 生效 |
| Faction Exposure Tracker | 1-3 + 2-2 | 劫掠者伪装 | ✓ 生效 |
| 据点结构 8 条 | 1-3 + 2-2 + 2-3 | 首次建 / Delta | ✓ 生效 |
| 库存 3 层 | 2-2 + 2-3 | 每次入账 | ✓ 生效 |
| Knowledge Scope 4 档 | 0-1 + 各角色 | 每次入账 | ✓ 生效 |
| Death Publicity 4 档 | 0-2 + 1-3 + 2-2 | 死亡事件 | ✓ 生效 |
| NPC 反全知 | 0-2 + 1-1 | NPC 每次发言 | ✓ 生效 |
| NPC 锚点协议 | 1-3 | 长时间未出场 | ✓ 生效 |
| 第二人称叙事 | 0-1 + 1-1 + 1-2 | 每个 Scene | ✓ 生效 |
| Pinned Memory | 平台外（人类） | 用户手动 | ✓ 生效（不属 AI） |

### 13.8 关键问题清单（22 个；第一轮 14 个 + 第二轮 8 个）

#### P0 阻塞（2 个）

| # | 文件 | 行 | 问题 | 修复方向 |
|---|------|----|----- |----------|
| P0-1 | `2-1_WorldStateKeeper_Scene.md` | L80 | "不复制 World Master 的 Extra Details 表格"（弹药口径实际在 0-2） | 改为"不复制聊天室 Scenario 字段 §弹药口径换算表" |
| P0-2 | `2-1_WorldStateKeeper_Scene.md` | L87 | "见 World Master 的 Extra Details"（Death Publicity 实际在 0-2） | 改为"见聊天室 Scenario 字段 §死亡公开性 4 档枚举" |

#### P1 重要（11 个；第一轮 5 + 第二轮 6）

| # | 文件 | 行 | 问题 | 修复方向 |
|---|------|----|----- |----------|
| P1-1 | `1-3_WorldMaster_Extra_Details.md` | L176 | "每周期（白天段 6h）"与 L34 定义 14h 矛盾 | 改为"每周期（白天段 14h）" |
| P1-2 | `1-3_WorldMaster_Extra_Details.md` | L5 / L196 | §十二 身份暴露风险段名编号 | 统一为 `[身份暴露风险]`（去掉 § 前缀） |
| P1-3 | `1-1_WorldMaster_Scene.md` | L119 | 引用 `§Inventory Snapshot 压缩规则` 段名不存在 | 改为 `WSK 的 Additional Personality Details §精简库存与盘点规则` |
| P1-4 | `1-3_WorldMaster_Extra_Details.md` | L17-18 | "平台角色和世界角色都会因脱水...死亡"（术语错） | 删除"平台角色和"或统一为"系统角色" |
| P1-5 | `0-2_Scenario.md` L3 + 1-3 + 2-2 | 多处 | "4 阵营" vs "6 势力" 冲突 | 统一为"6 势力"（4 阵营 + 拾荒者 + 劫掠者兄弟会） |
| **P1-6** | `1-3_WorldMaster_Extra_Details.md` L168 + `2-3_WorldStateKeeper_Extra_Details.md` L29 | — | "Base Structure Snapshot 每次必须输出完整基线" 与 2-3 L29 例子 "物资不变(见上一轮完整账本)" 矛盾 | 1-3 改为"首次建账 + Base Structure Delta 时输出完整基线；日常用 Snapshot 简表" |
| **P1-7** | `0-2_Scenario.md` L60 + `2-2_WorldStateKeeper_Additional_Personality_Details.md` L83 | — | 温度层术语不一致：0-2 = `Mild / Cool / Cold / Bitter Cold / Heat`；2-2 = `Bitter Cold / Cold / Cool / Mild / Warm` | 统一为 `Mild / Cool / Cold / Bitter Cold / Heat`（2-2 移除 Warm） |
| **P1-8** | `0-1_Private_Details.md` L121 + `2-3_WorldStateKeeper_Extra_Details.md` L20 | — | WSK 提取最小集含 `Base Structure`，但 2-3 L20 子段定义无 `Base Structure Snapshot` | 2-3 L20 子段补 `Base Structure Snapshot:` |
| **P1-9** | `3-3_WorldEventRecorder_Extra_Details.md` L99-101 + `3-1_WorldEventRecorder_Scene.md` L114 | — | WER 死亡公开性读取源矛盾：3-3 说"从 WM Scene 读"；3-1 说"可读 WSK State" | 统一为"WER 优先从 WSK State Update 读，缺失时按 hidden 处理" |
| **P1-10** | `2-1_WorldStateKeeper_Scene.md` | 缺 | WSK 缺"上次发言位置"维护规则（WER 3-1 L34-39 有明确维护） | 在 2-1 沉默协议段补"上次发言位置 = 我最后一次 `[State Update]` 输出位置" |
| **P1-11** | `0-1_Private_Details.md` L21 + `1-1_WorldMaster_Scene.md` L27 | — | "WM 发给 WSK 的同步除 `Location Change` 外…" 是过时的"同步块"残留术语 | 改为"WM 在 Scene 叙事中体现移动时…" |

#### P2 一般（9 个；第一轮 7 + 第二轮 2）

| # | 文件 | 行 | 问题 | 修复方向 |
|---|------|----|----- |----------|
| P2-1 | `2-2_WorldStateKeeper_Additional_Personality_Details.md` | L115 | "§十二 身份暴露风险 5 级响应"段名不存在 | 改引用名为 `[身份暴露风险]`（与 P1-2 同步） |
| P2-2 | `1-1_WorldMaster_Scene.md` | L66 | 概率块输出位置 | 确认是否仍需保留（无同步块通讯后） |
| P2-3 | `1-1_WorldMaster_Scene.md` | L82-103 | 反煽情元规则 + 5 轮内高频词禁用 | 需 L1 检查法去污染 |
| P2-4 | `2-3_WorldStateKeeper_Extra_Details.md` | L82-85 | `[State Update]` 通用压缩规则 | 确认段名与本文件 §2.2 一致 |
| P2-5 | `0-1_Private_Details.md` | L132 | 重大事件清单 | 与 WER 3-2 重大事件清单去重 |
| P2-6 | `0-1_Private_Details.md` | L134 | 5 字段结构变化 | 与据点首次建立 5 字段统一表述 |
| P2-7 | `0-1_Private_Details.md` | L91-103 | 状态栏 6 字段固定 | 与 WM 状态栏 1-1 引用统一 |
| **P2-8** | `1-1_WorldMaster_Scene.md` L106-110 | — | Scene ≤800 字符 + ≤3 段 与 0-1 L69 "2-3 段自然述事" 紧贴边界 | 维持现状；触发率受 LLM 输出影响 |
| **P2-9** | `0-1_Private_Details.md` L43 | — | LLM 上下文窗口限制下长 Session 后 WSK 输出可能不在上下文 | 不可控；标记为已知风险 |

### 13.9 修复优先级建议

| 阶段 | 任务 | 涉及问题 | 影响 |
|------|------|----------|------|
| **批次 1（P0）** | 修复 2-1 L80 / L87 引用源 | P0-1, P0-2 | 立即可执行；防止 WSK 因引用死链而误判 |
| **批次 2A（P1 术语统一）** | 平台角色/系统角色 + 4 阵营/6 势力 + 温度层 Heat/Warm + 旧同步块术语 | P1-4, P1-5, P1-7, P1-11 | 跨文件术语一致；防止 LLM 解释分裂 |
| **批次 2B（P1 数字/段名修正）** | 白天段 6h/14h + §十二 段名 + 引用段名 | P1-1, P1-2, P1-3 | 段名 / 数字一致；防止引用死链 |
| **批次 2C（P1 字段/语义补全）** | 据点结构输出策略 + WSK 子段补全 + WER 读取源 + 上次发言位置 | P1-6, P1-8, P1-9, P1-10 | 字段与语义对齐；防止行为分裂 |
| **批次 3（P2）** | 段名细节 + 风险标注 | P2-1 ~ P2-9 | 长期可维护性 |

### 13.10 审计原则声明

本次审计遵循以下原则：

1. **冲突以审计结果为准**：本节内容与项目其它文档冲突时，以本节为准
2. **L1 检查法**：每行必须为可执行指令或定义；无元数据污染；无审计者口吻
3. **SSOT 原则**：本文件是项目设计及说明的唯一文档；跨文件引用通过本文件 §X.Y 定位
4. **机制优先于文档**：若文档描述与已确立机制冲突，机制优先
5. **已废弃机制保留历史记录**：所有已废弃机制在 §2.3 列出，便于回溯

### 13.11 修复执行记录（2026-07-22 完成）

| 批次 | 内容 | commit | 项数 | 文件 | 风险 | 状态 |
|------|------|--------|------|------|------|------|
| **1** | P0 引用源（2-1 L80/L87） | `045e57d` | 2 | 1 | 无 | ✓ 已完成 |
| **2A** | P1 术语统一 | `e583e13` | 4 | 4 | 低 | ✓ 已完成 |
| **2B** | P1 数字/段名 | `6240226` | 3 | 2 | 低 | ✓ 已完成 |
| **2C** | P1 字段/语义 | `2abafe5` | 4 | 4 | 中 | ✓ 已完成 |
| **3** | P2 段名清理 | `1522e57` | 1（实质）+ 8 个非问题 / 风险标注 | 1 | 低 | ✓ 已完成 |

**总计**：**14 项实质修改 / 13 个文件改动 / 5 个 commit**，全部已推送至 `main`。
**P2 非问题清单**（执行验证后发现无需修改）：
- P2-2（1-1 L66 概率块位置）：保留，[Probability Check] 仍由 WM 输出供用户阅读
- P2-3（1-1 L82-103 反煽情元规则）：保留，元规则层不需 L1 重构
- P2-4（2-3 L82-85 压缩规则）：保留，与 §2.2 一致
- P2-5（0-1 L132 重大事件清单）：无重复，3-2 L70 仅引用
- P2-6（0-1 L134 结构变化 5 字段）：已显式标注"与据点首次建立 5 字段不同"，无须统一
- P2-7（0-1 L91-103 状态栏 6 字段）：0-1 为权威源，1-1/1-2 已正确引用
- P2-8（1-1 L106-110 Scene ≤800 字符）：硬约束已正确，受 LLM 实际输出影响
- P2-9（0-1 L43 上下文窗口）：平台机制，提示词层不可控

#### 修复后跨文件一致性复核

| 维度 | 一致性 |
|------|--------|
| 弹药口径引用 | 2-1 L80 → 0-2（一致） |
| 死亡公开性引用 | 2-1 L87 → 0-2（一致） |
| 6 势力 vs 4 阵营 | 0-2 / 0-1 / 1-3 / 1-2（一致为 6 势力） |
| 温度层 5 档 | 0-2 / 2-2（一致为 Mild/Cool/Cold/Bitter Cold/Heat） |
| §十二 段名 | 1-3 L5 / 2-2 L115（一致为 [身份暴露风险]） |
| 白天段时间 | 1-3 L34 (14h) / 1-3 L176 (14h)（一致） |
| 系统角色术语 | 1-3 L17-18（一致，无平台角色残留） |
| WSK 旧同步块术语 | 0-1 L21（一致，Scene 叙事提取） |
| WSK 子段 Base Structure | 0-1 L121 / 2-3 L20（一致，补全） |
| WER 死亡公开性读取源 | 3-3 L99-101（WSK 优先；WM Scene 回退）（一致） |
| WSK 上次发言位置 | 2-1 L37（与 WER 3-1 L34-39 对齐） |

**审计结论**：22 个问题中 14 个已实质修复，8 个为非问题或已知风险。**全部机制现已生效（✓）**。

#### 不在本次修复范围

- `output.md`（untracked 模板文件）
- `OURDREAM_PLATFORM_REFERENCE.md`（平台参考）

#### 暂不修复（标记 P2-9）

- LLM 上下文窗口限制：平台机制，不在提示词层可控
- WM Scene ≤800 字符：硬约束已正确，影响仅在 LLM 实际输出

---

## 十四、近期重要版本演进（v1.21-v1.26）

本节记录 v1.21-v1.26 期间的关键机制变更。详细变更历史见 `OPTIMIZATION_ROADMAP_v1.md`。

### 14.1 时间机制重构（v1.21）

**变更**：确立 WM 为唯一时间源。

| 维度 | 原状态 | v1.21+ |
|------|--------|--------|
| 时间字段决定者 | WSK 强语义提取 + REJECT 冲突 | WM 唯一决定 |
| WSK 时间验证 | WSK 验证 + REJECT | WSK 不验证 / 不推算 / 不判定冲突 |
| Official Day 冲突 | REJECT 回执 | 删除机制，信任 WM |
| 月份推算 | WSK 自动推算 | WM 决定，WSK 记录 |
| Current Season/Temperature | WSK 推算 | WM 决定，WSK 信任 |

**影响**：消除 WSK 验证与 WM 决策的冲突，避免日期漂移。

### 14.2 Day 推进 5 种触发（v1.22）

1. 用户明确指令（如"第二天"）
2. 夜间/日落后睡眠醒来
3. WM 自然叙事跨日（日落→日出）
4. 连续行动超 16-18 小时提示疲劳
5. WM 语义判断构成跨日

### 14.3 Season/气温强制查表（v1.22）

WM 输出 [主要状态] 时必须按当前 Day 查 `0-2 §温度分层与日期精细对应` 表确定 Season/气温，允许 ±1 档浮动（需叙事依据）。

### 14.4 WSK 全量状态快照（v1.22）

WSK 每次被点击时，输出变化子段后**必须继续输出当前所记录的全部状态和库存**（Inventory Snapshot / Party Condition / Relationship / Base Structure / 近五日主要事件），供 WM 读取。确保 WM 每轮可独立获取完整世界状态。

### 14.5 Market State 机制删除（v1.22）

WSK 不再追踪/记录/输出市场状态字段。保留：地名（老市场/批发市场）、传播机制（市场传闻）、交易规则（等价 9mm）。

### 14.6 移动硬约束（v1.23 + v1.25 强化）

**v1.23 时间准确性**：
- 1 step = 30 分钟基准，不得压缩
- 跨子区域移动必须显式写 Route + 总 step + 总耗时
- 反例：T178/10:36 跨 3 子区域 → 11:12（+36）违规；正确 12:06（+90）

**v1.25 地理准确性**：
- 跨子区域移动必须严格按 `0-2 §分区邻接` 拓扑书写
- 不得凭印象虚构路径或跳过相邻区
- 涉及据点/势力/资源点时必须经过该点所在子区域
- 例：去码头调度楼（东南区/C）必须经过东南区 N → C

### 14.7 [主要状态]必出规则冗余精简（v1.24）

同一规则原本在 4 个文件重复，导致 LLM 注意力疲劳。v1.24 变体 A：保留 0-1（主权威定义）+ 1-1（WM Scene 必读），删除 1-2 L26 0c. 段。从 4 文件 → 2 文件。

### 14.8 Turn 编号规则补强（v1.26）

在 v1.22 "Turn 编号每轮 +1（与时间独立）" 基础上补强 3 条硬约束：

- 每日从 T1 开始计数（Day 推进时 Turn 重置为 T1）
- 每轮 +1 不可跳号（不可从 T5 直接到 T7）
- 单轮只 +1 Turn（无论本轮内容多复杂）

### 14.9 实验性字段：近五日主要事件（v1.20）

WSK 新增子段：`近五日主要事件:`（按 D 升序，1500 字符总量，150-500 字符/条）。LLM 语义提取该日主要事件（人员/据点/阵营/关系/区域 5 类）。天气/经济变动/路线变动已放弃。

### 14.10 WSK 弹药口径合并（v1.5，v1.68 修订）

弹药库存按口径（弹药规格）合并记录（如 `9mm×67发`），禁止按武器类别分组。例外：交易/经济场景可临时折算为"等价 9mm 单位"。
**v1.68 修订**：合并范围限定为"同一存储位（桶）内"（随身/各据点桶分别记录，不跨桶合并）；"等价 9mm 折算"例外已在后续版本删除（2-3 §[弹药口径格式硬约束] 现行禁止统一换算）。

---

## 十五、近期版本演进（v1.27-v1.32）

本节记录 v1.27-v1.32 期间的关键机制变更。v1.21-v1.26 变更见 §14，完整历史见 `OPTIMIZATION_ROADMAP_v1.md`。

### 15.1 WSK 输出视图重组（v1.27）

**变更**：废弃 SNAPSHOT 模式概念，改为每次点击输出 = 变化子段（仅 Inventory Delta / Recent Changes）+ 完整视图。无需区分默认/完整视图模式，简化心智模型。

### 15.2 WSK 输出结构去重（v1.28）

**变更**：v1.27 存在 4 处字段重复（Inventory Snapshot / 近五日主要事件 / Base Structure 等），遗漏 3 个关键字段（Human Contact Status / Scavenging Status Snapshot / Survival Anchor Snapshot）。v1.28：变化子段仅保留 2 个（Inventory Delta / Recent Changes），完整视图扩展为 13 字段全集，消除全部重复与遗漏。

### 15.3 WSK 新增 Map Knowledge（v1.29）

**变更**：完整视图新增 Map Knowledge 字段——记录玩家已探索子区域清单 + 已知资源点。近五日主要事件调整至完整视图最末（便于 WM 优先读状态后回看历史）。

### 15.4 删除 Contamination 字段（v1.30）

**变更**：完整视图从 13 字段精简至 11 字段。删除 Contamination（污染）字段——前期低频，污染事件可通过 Recent Changes 描述。涉及文件：0-1 / 2-1 / 2-2 / 2-3。

### 15.5 Inventory Snapshot 输出策略调整（v1.31）

**变更**：从旧规则"超过 10 项必压缩为 ×1组"转为"分类 + 明细"——按功能组归类（武器/弹药/医疗/工具/食物等），但保留每件物品具体信息（口径/数量/状态）。仅普通物资（绳索、容器等）仍可压缩为"具备×1组"。

### 15.6 可消耗物资双轨制计量规范（v1.32）

**变更**：
- **WM 渲染**：采用"容器描述 + 估算重量（≈kg）"双轨格式（如"一小袋大米，约 5kg"）
- **WSK 记录**：提取净重值，**按 kg 统一记录**（"大米×5kg"），剥离容器描述
- **LLM 实时估算优先**：不依赖预设换算表；校准锚点仅作常识参考（非强制）
- **涉及文件**：0-2（主规范 + 校准锚点）/ 1-1（WM 渲染硬约束）/ 2-1（WSK 记录硬约束）/ 2-3（kg 合并规则）

### 15.7 WSK 输出增强：消耗投影 + Active Concerns（v1.33）

**变更**：
- **消耗投影**：Inventory Snapshot 末尾新增资源剩余天数估算（`消耗估算（N人）：食物≈X天 / 饮水≈Y天 / 燃料≈Z天`），WSK 根据队伍人数 + kg 库存自动估算，供 WM 快速判断生存紧迫度
- **Active Concerns**：变化子段之后新增 3-5 项紧急关注点摘要（`[生存]/[人际]/[环境]/[据点]` 四分类），WSK 扫描全部字段自动汇总当前最需 WM 优先处理的事项
- **涉及文件**：0-1（输出格式描述）/ 2-1（输出结构）/ 2-3（模板 + 消耗投影规则 + Active Concerns 定义）

---

### 15.8 [主要状态] 格式精简为 5 段结构（v1.37）

**变更**：
- [主要状态] 由原 8 字段+字段名前缀格式精简为 5 段结构：`D{Day}-T{Turn} {时间} | {位置} | {Season}-{天气}-{气温} | {压力} | {风险}`
- 去除字段名前缀（不写 `Day=` / `位置=` 等），8 项信息按段序隐含；信息完整性不变
- **涉及文件**：0-1（状态栏硬约束 + 标准输出模板）/ 1-1（状态栏硬约束精简版）

### 15.9 v4.3 批次四项机制优化（v1.38）

**变更**：
- **NPC Trust 行为阈值映射**：0-2 新增 Trust 阈值表（0-20 敌对 / 20-40 警惕 / 40-60 中立 / 60-80 友善 / 80-100 信任），定义交易/信息/帮助基调；1-2 关系规则引用。解决 Trust 数值"死数据"问题
- **5 轨压力格式化**：[主要状态] 压力段强制 `{轨道名} {状态级}` 格式，仅列非 stable 轨道，与 WSK Party Condition 对齐
- **消耗估算 WM 驱动化**：消耗投影改为 WM 叙事估算优先（标注 WM判断），WSK 基准值兜底（标注 基准估算）；禁止"富余"等描述词。解决 WSK 公式估算脱离生活经验问题
- **Active Concerns 分类约束**：4 分类边界定义 + 禁止自创类别（如 `[威胁]`）+ 判断示例
- **涉及文件**：0-2 / 1-2 / 0-1 / 2-3 / 1-1

### 15.10 [主要状态] 不输出根因修复（v1.39）

**问题**：v1.37 后 WM 频繁不输出 [主要状态]。

**根因**：v1.37 精简格式时只更新了 0-1 `[状态栏硬约束]`（L106），漏改同文件 `[标准输出模板]`（L139），造成**文件内双重定义矛盾**（新 5 段格式 vs 旧 8 字段格式）。LLM 面对冲突格式产生不确定性，进而回避输出。

**为何全面检查未捕获**（三叠加因素）：
1. 检查锚定修改者心智模型——编辑时漏掉的位置，检查时同样漏掉
2. 双重定义藏在不同名章节（"状态栏硬约束" vs "标准输出模板"），按概念名扫描时"模板"章节被跳过
3. 旧格式用全合法字段名（无禁用旧术语），术语级 grep 全绿——问题在结构而非术语

**修复**：L139 改为纯引用（"以 [状态栏硬约束] 为唯一权威定义，本段不复述格式字符串"），消除双重定义。

### 15.11 基于字段可见性的 SSOT 架构重构（v1.40）

**核心原则（二维判定）**：
- **维度一·可见性**：WM 看不到 WSK 角色卡（2-x），WSK 看不到 WM 角色卡（1-x）；聊天室级字段（0-1/0-2）所有角色共享。跨角色共用概念的权威源**必须**放聊天室级，否则引用是死链。
- **维度二·字段角色**：行为规则 → Scene/Additional；格式模板/表格 → Extra Details；决策表 → Additional。

**权威源布局**：

| 概念 | 权威源 | 依据 |
|------|--------|------|
| [主要状态] 格式 | 0-1（聊天室） | WM 产出 + WSK 提取，跨角色 |
| 5 轨压力 | 0-2（聊天室） | WM/WSK 共用世界机制 |
| WSK 完整视图 11 字段 + 第一行格式 | 2-3（WSK Extra） | WSK 专用格式模板 |
| WSK kg 记录格式 | 2-3（WSK Extra） | WSK 专用格式 |
| DO/REJECT/SILENT 决策表 | 2-2（WSK Additional） | WSK 决策逻辑 |

**实施的 SSOT 化**：
- WSK 11 字段列表：2-3 `[完整视图]` 为唯一权威，2-3 内 L19/L21/L122 + 2-1 改引用
- 5 轨压力状态级：0-2 为权威，0-1/2-3 引用（顺带修复 0-1 漏 `dead` 状态级漂移）
- WSK kg 规则：格式权威归 2-3，提取纪律归 2-1
- 0-1 L149（WM 对 WSK 输出知情）：去精确字段名留结构概览（WM 读实际输出即可，不引用 2-3）

**经验沉淀**：冗余定义本身就是风险源；SSOT 治本优于"保持一致"；改格式前后用字面字符串 grep 枚举所有出现位置（对抗锚定盲区）。全面位置审计确认：项目整体布局健康，无共享概念被困单角色字段。

> **结论修正（2026-07-24）**：本节"无跨角色死链引用"的结论已过期——后续审计发现 6 处跨卡冗余指针（详见 §16.2），其内容均已就地内联故未造成功能失效，但属于会误导 LLM 的死指针。教训：引用审计必须按"引用方角色生成时能否看到被引用内容"逐条验证，而非仅看布局。

---

## 十六、引用有效性审计与引用写法统一（2026-07-24）

### 16.1 背景：装配视角下的引用失效问题

提示词开发时以文件编号（0-1/0-2/1-x/2-x）组织，但装配到平台后：
- **平台没有这些文件名**——聊天室只有 Private Details / Scenario 字段，角色卡只有 Scene / Additional Personality Details / Extra Details 字段；写 `0-2 §月份推进规则` 的引用在运行时无法被解析
- **平台只认角色全称**（World Master / World State Keeper），引用串里的 WM/WSK 简称无法被识别
- **角色卡互不可见**：World Master 生成时看不到 World State Keeper 的字段，反之亦然；跨角色共享只能靠对话历史中的实际输出

### 16.2 审计发现（全库 8 文件逐条扫描）

**① 编号/简称引用 21+ 处**（全部改写）：

| 旧写法 | 新写法 | 分布 |
|---|---|---|
| `0-2 §月份推进规则 / 环境生存压力系统 / 季节锚点 / 温度分层与日期精细对应 / 分区邻接 / 可消耗物资渲染规范 / 世界硬规则 / NPC Trust 行为阈值表 / 非移动动作时间基准` | `聊天室 Scenario 字段 §…` | 0-1 / 1-1 / 2-1 / 2-2 / 2-3 |
| `1-1 §Probability Check` / `1-2 §生存压力时间阈值表` / `1-3 §13 据点结构基线` | `World Master 的 Scene / Additional Personality Details / Extra Details 字段 §…` | 0-2 |
| `WSK Extra Details §弹药口径格式硬约束 / 压缩规则` / `WM Extra Details §演化触发流程` | `World State Keeper / World Master 的 Extra Details 字段 §…` | 0-1 / 0-2 |

**② 跨卡死指针 6 处**（引用方运行时不可读，全部消除）：

| 位置 | 原引用 | 处理 |
|---|---|---|
| 1-1 工具组归类自检 | 「参考 WSK 的 APD §精简库存与盘点规则」（唯一指令式跨卡引用，风险最高） | 改为"与最新 [State Update] 实际输出保持分组一致" |
| 2-2 Faction Name | 「官方名以 1-3 §势力周期活动表 为准」 | 本地已枚举 → "以上 6 个官方名即权威枚举" |
| 2-2 Suspicion Level | 「升级判定以 WM Extra Details [身份暴露风险] 为权威」 | 标准内联（单一异常→L1 / 多指标→L2 / 持续3+周期→L3） |
| 2-2 硬下限追踪 | 「（1-3 §硬下限）」 | 删指针（执行规则本就内联） |
| 2-3 场外演化 ×2 | 「按 WM Extra Details §势力活动 Scene 显式叙事规约」 | "以 World Master 已在 Scene 叙事中显式写出为准" |
| 1-3 周期检查数据源 | 「WSK 的 APD 中 Faction Activity Calendar 字段」 | "World State Keeper 最新 [State Update] 实际输出中的…字段" |

**③ 角色内引用说法混乱 28 处**（统一为单一格式）：`本角色 Scene §X` / `本文件 Extra Details` / 裸 `Scene §X` / `以 Extra Details 为准` 等变体 → 统一为 `{角色全称}（本角色）的 {字段名} 字段 §章节`。

### 16.3 引用写法规范（已提取至 §1.2）

> 本节内容已提取至 **§1.2 引用写法规范**，作为当前规范依据。本节保留为 2026-07-24 审计历史记录。
>
> 提取时的增强：§1.2 在原表格基础上新增了"示例"列和"自定义指令"字段覆盖，并补充了跨角色引用禁令的原理说明。

### 16.4 经验与教训

1. **引用目标必须是"平台实体"**：开发编号与简称只是仓库管理工具，一旦写进提示词引用串就是运行时无法解析的死引用；引用只有三种合法形态——聊天室字段名 / 本角色字段名 / 角色全称+字段名。
2. **跨卡引用必然失效，但失效方式分两种**：内容未内联 = 功能断裂（规则丢失）；内容已内联 = 冗余指针（LLM 可能尝试调用不可见内容并幻觉补全，浪费 token 且引入风险）。指令式引用（"参考 X"）比说明式引用（"权威在 X"）危险得多。
3. **对话历史是跨角色唯一活通道**：任何"A 角色需要 B 角色的规则/数据"的需求，正确解法要么把规则内联到 A 可见处，要么指向 B 的实际输出（[State Update] / [主要状态]）。
4. **引用修复会滚雪球**：每次 SearchReplace 的 diff 都会暴露同模式的漏网引用（本轮 3 次补漏），修复后必须用模式级 grep 终扫（编号模式 + 简称进引用串模式 + 旧变体模式）。
5. **审计结论有保质期**：v1.40 的"无跨角色死链"结论在后续多轮编辑后失效（新引入的引用未过可见性检查）。引用审计应作为每次大批量编辑后的固定工序，而非一次性结论。

---

## 十七、聊天室基础字段层审计：语义分工与放置错位（2026-07-24）

> **历史快照声明**：本节为 2026-07-24 审计快照，仅覆盖 0-1 和 0-2 两个装配字段。当前字段承载分工已扩展（含 0-3 运行时纠偏字段），完整定义以 **§1.1.1** 为准。本节保留作为审计历史记录，不作为当前规范依据。

### 17.1 背景：两个聊天室字段的语义分工

0-1（Private Details）与 0-2（Scenario）**可见性完全相同**（全角色共享），区别在语义语境——角色按什么框架理解读到的内容：

| | 0-2 Scenario | 0-1 Private Details |
|---|---|---|
| 定位 | **世界共识**："这个世界是什么" | **系统协议**："这套机器怎么运转" |
| 承载 | 背景/气候/地图/势力/压力/敌对/经济/死亡 | 读取顺序/出场序列/后台通讯/提取规约/输出格式 |
| 判定问句 | 这是世界本身的事实/规律吗？ | 这是角色分工/输出/通讯的规定吗？ |

注意："压力系统""敌对阶梯"也是机制，但属**世界规律**（玩家也需感知）归 0-2；"谁记账、规则在哪、不得越界"是**系统分工**归 0-1。

### 17.2 审计发现与修复（7 项放置错位 + 1 项示例瑕疵）

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| 1 | 0-2 文件头三行 | 系统分工目录（动态机制→WM ED / 决策规则→WSK APD / 心理预设→Scene）混在世界共识文件；其中"角色决策规则（经济/关系/库存）见 WSK APD"与 WM 每轮亲裁的领域重叠，可能诱发 WM 越权谦让或幻觉补全 | 整块迁入 0-1 §平台优先锚点；WSK 决策规则条目加**显式排他声明**（仅该角色适用且仅其可读，其他角色无需关注、不得引用或复现） |
| 2 | 0-2 [Scene 温度与天气硬约束] | "WSK 在 State Update 第一行记录 Weather…"——WSK 记账行为规格放在世界文件，且 0-1/2-3 已有权威定义（三重冗余） | 删除 |
| 3 | 0-1 [地图逻辑] | 九宫格坐标/邻接/step/Route 与 0-2 [地图调用规则] 双重定义（v1.39 同类病灶） | SSOT 化为纯引用；保留 0-2 没有的补点/地图外字段规则 |
| 4 | 0-1 [NPC 发言边界] | 与 0-2 [NPC 反全知/反顾问化机制] 重复摘要 | 改纯引用 |
| 5 | 0-1 [经济规则] | 世界共识（实物计价/硬通货）放进系统协议文件 | 迁入 0-2 §世界硬规则 |
| 6 | 0-1 L4 | "WSK 自己的聊天室 Private Details 字段"——Private Details 是聊天室级共享字段，不属于 WSK，概念混淆 | 改写为"WSK 的角色卡字段（其他角色不可见）" |
| 7 | 0-1 §后台提取规约 | "每日一次 WSK 累计前一日收获"——WSK 是被动触发，"每日一次"非事实 | 改为"触发时累计扫描窗口内未入账收获" |
| 8 | 2-1 第一行示例 | `/party-known` 缺空格，与格式定义不符 | 补空格 |

**WM/WSK 机制有效性结论**：除上述放置问题外，两个基础字段内的机制链路全部闭环——WM 每轮必出链（读账本→叙事→[主要状态]→结构块）、WSK 稀疏触发兼容（扫描窗口覆盖多日缺口 + carry-forward + 轻量回执 + 长期未触发兜底）均验证通过。

### 17.3 经验与教训

1. **可见性相同 ≠ 语义语境相同**：聊天室字段虽全角色共享，内容仍须按"世界共识 vs 系统协议"分家。把 A 角色的职责规则放进共享字段，等于让主叙事者 WM 每轮暴露在其他角色的规则噪音下——尤其当指针主题（经济/关系/库存）与 WM 自身裁定领域重叠时，会诱发越权谦让或幻觉补全。
2. **对自卡内容的导航指针价值≈0，代价真实**：WSK 不需要 0-2 里的指针来找自己角色卡里已有的规则（自卡永远在其上下文中）；这类指针收益近零，却每轮暴露给 WM。跨角色指向的目录行应默认怀疑，除非能证明目标消费者真实受益。
3. **摘要式重复 = 双重定义时间炸弹**：0-1 对 0-2 内容的"简要复述"（地图拓扑、NPC 边界）与 v1.39 [主要状态] 事故同病灶——改一处漏一处。修法统一为 SSOT 化：保留独有部分 + 重复部分改纯引用。
4. **聊天室字段内容三步判定法**（本次审计的操作方法）：逐段问——这段在描述"世界是什么"（留 0-2）/"系统如何运转"（留 0-1）/"某角色如何行为"（应在该角色卡）？第三类出现在聊天室字段即为放置错位。
5. **干扰类问题的修复不是简单删除，而是"迁移 + 显式排他"**：信息本身可能有保留价值（人类读者/目标角色需要），正确做法是移到语义正确的家，并在原位留下排除声明（"其他角色无需关注、不得尝试引用或复现"），主动压制 LLM 对不可见内容的脑补冲动。

### 17.4 文件头化石问题（2026-07-24 补记）

**发现**：0-1 开头 `[World Master 规则分级与索引]` 及"World Master 读取顺序"——聊天室级共享字段被框定为 WM 专属，且 5 处文件引用的"§全局读取协议"章节实际不存在（软死链）。

**根因**：
1. **头部化石**：项目从 WM 单角色卡起家，多轮架构演进中正文不断搬迁重组，文件头从未重写——头部是全库修改频率最低、过时风险最高的部位
2. **审计缝隙**：头部既非引用也非正文规则，是"元描述"，落在引用审计与放置错位审计两种维度的夹缝中
3. **WM 中心惯性**：早期架构以 World Master 为唯一主角，旧框架语言残留

**修复**：标题改 `[聊天室 Private Details 字段定位与全局读取协议]`；读取顺序改角色通用版（"任一系统角色生成时…"）；5 处引用从此有真实权威定义。

**新增审计项——文件头自我描述审计**（纳入开盘前检查清单）：
- 每个文件的标题/定位声明必须与其实际装配位置（聊天室级 vs 角色卡）与读者范围一致
- 文件头中声称的章节/概念必须实际存在于该文件（防软死链）
- 同一文件头内不得出现互相矛盾的自我描述

**提示词本体论补充**：字段装配到平台后，字段内不存在"仅供开发者看的标注"——每一个字符都是 LLM 消费的提示词。括号标注（如"静态世界资料""全局硬规则"）是有弱功能的提示词（为模型提供语义框架与导航），不是开发注释；开发性注释只能留在仓库文档（如本文件），不得进入装配字段。

### 17.5 提示词越权指挥平台动作问题（2026-07-24）

**发现**：多处提示词以"先读 X 再读 Y""必须先读取 Pinned Memory"等句式指挥平台机制。逐条剖析：

| 句式 | 问题定性 |
|---|---|
| 「任一系统角色生成时，先读 Scenario 再读本字段再读本角色卡三段」 | **虚构时序**：平台在角色生成前已把聊天室字段与角色卡全部注入上下文，模型是一次看到全部，不存在"先读后读"动作；且角色卡装载是平台机制，不需提示词设定 |
| 「WM 每次进入新 Scene 前必须先读取 Pinned Memory」 | **指挥无权动作**：Pinned Memory 的注入/检索机制归平台管（仅知优先级高），模型无法"主动去读"；此句真正有效的内核是后半句的反幻觉约束（事件记录只定义"过去"，不得反推硬状态） |
| 「每轮先读最新官方状态，再读最近正式记录」 | **伪时序包裹的真内核**：真实意图是证据权威优先级（State Update = 现在 > 历史段 = 过去），应改写为优先级声明 |

**确立新原则——提示词不得指挥平台动作**：装载、注入、读取顺序、检索机制都是平台职责；提示词只能定义**解释方式、权威优先级、使用纪律**。凡"先读/再读/必须读取"式句子，若主语动作实际由平台完成，即为对抗性虚构，应改写或删除。判定边界：若"读"的对象是对话历史中的可见内容（模型确实能选择性处理），如 1-3 世界角色锚点解读优先级，则属合法的解释纪律，保留。

**修复**：0-1 全局读取协议改写为"信息源清单 + 权威优先级"并显式声明"无需也不存在主动读取动作"；Pinned Memory 条款改为"机制以平台为准，提示词不指挥其装载"+ 保留反幻觉内核；全库 6 处"读取顺序"措辞统一改为"信息源与权威优先级"（0-1/1-1/1-2/1-3/2-1/2-2）；0-1 Layer 4 恢复条款改为优先级表述。

**经验**：提示词写"谁在什么时候做什么"时，先问一句"这个动作的执行权在模型还是在平台？"——执行权在平台的，只能写"以平台为准"+ 解释约束；执行权在模型的（解读、裁定、输出），才能写行为指令。
