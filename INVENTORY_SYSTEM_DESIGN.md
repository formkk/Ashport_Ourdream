# 库存系统设计文档（INVENTORY_SYSTEM_DESIGN）

> **文档定位**：库存子系统 SSOT（Single Source of Truth），设计原则层文档。
> **承载范围**：库存系统的设计意图、数据模型、机制链路、格式规范、历史教训。机械规则仍由 prompt 字段承载，本文档不直接作为 prompt 输入。
> **用途**：阶段 2 审计的依据；prompt 维护与重构的基准；新增库存相关功能的设计参考。
> **信息源**：PROJECT_BLUEPRINT.md（PB）、v2_discussion.md、审计历史记录.md、Prompt_File/ 下 6 个文件、project_memory.md。
> **术语状态**：本文档为目标设计态，部分术语已更新（位置分类更名为"四类存储位"、转移机制更名为"跨位转移"等）。prompt 文件的同步修改将在后续进行，文中引用的 prompt 章节名可能与当前版本不一致。

---

## 一、游戏背景与库存系统定位

### 1.1 游戏背景

灰港（Ashport）是面向 Ourdream.ai 平台的硬核后末日生存群聊游戏。设定为灾变后第3年，地点为后苏联背景的中型沿海港口城市。游戏基调为：**低资源、低信息、高风险、强后果、慢积累、反爽文**。玩家每次行动都有代价，物资稀缺驱动玩家行为（搜刮、采集与渔猎、制作生产、交易、探索、社交、冲突、据点维护）。

### 1.2 库存系统在游戏中的定位

库存系统是生存压力的核心载体，承担以下功能：

1. **生存压力的物化**：食物/饮水/燃料的存量与消耗，直接驱动饥渴、疲劳、体温等压力轨
2. **世界在动的证据**：消耗、损耗、转移、冲突、被掠夺等变化，让玩家感受到世界持续运转
3. **玩家可读的世界状态快照**：库存账本是玩家了解"我有什么、世界发生了什么"的核心窗口
4. **决策依据**：库存存量影响玩家下一步行动（采集与渔猎、制作生产、交易、探索、社交、冲突、据点加固）

### 1.3 库存系统的核心目标

| 目标 | 说明 |
|------|------|
| 准确性 | WSK作为库存唯一权威帐本角色，只更新 WM 已裁定的已成立变化，不脑补、不推测 |
| 可读性 | 防止清单无限膨胀，保持玩家可读 |
| 完整性 | 6 字段平铺每次必出，不省略以避免反例 |
| 紧凑性 | LLM 生成账本时输出紧凑，节省垂直空间 |
| 可验证 | 格式与规则可被工具化检查 |

---

## 二、设计原则

### 2.1 LLM 认知约束

LLM 不做精确计数、算术、跨轮记忆。库存系统设计必须顺应此约束：

- **根据 WM 裁定更新存量**：WSK 根据 WM 在 `[判定]` 中裁定的消耗量更新库存存量
- **基于已知值比较触发**：消耗/损耗的触发基于两个已知值的比较（如当前轮 Day vs 上一轮 Day），不依赖跨轮计数
- **依赖显式锚点**：硬状态只认最新正式 `[State Update]`，历史记忆只认 Pinned Memory

**承载位置**：此约束是全局性的，体现在 2-1 身份、1-1 运行锚点中。

### 2.2 角色边界原则

| 角色 | 职责 | 禁止 |
|------|------|------|
| WM | 消耗裁定、损耗裁定、叙事渲染、资源估算 | 不记账（库存唯一官方记录角色是 WSK） |
| WSK | 提取、归类、审计、记账 | 不裁定、不自行扣减、不脑补、不自判物品重要性 |

**设计依据**：WM 拥有叙事上下文，适合做语义裁定；WSK 作为硬状态管理员，只做结构化提取与更新，避免越界。

### 2.3 显式锚点原则

- **硬状态只认最新正式 `[State Update]`**：当前硬状态以最近一次正式 `[State Update]` 为准
- **历史记忆只认 Pinned Memory**：由用户手动复制并不断补充更新的"近五日主要事件"段
- **跨角色通讯 = 对话历史被动观察**：WM 与 WSK 不互相调用，WSK 通过读取 WM 在对话历史中的输出提取变化

**设计依据**：Ourdream 平台角色卡字段互不可见，跨角色引用必然失效（审计历史教训）。

### 2.4 节省垂直空间取向

> 此取向不写入 prompt 执行 rule（目标性指令"尽量节省空间"会与"标签必出/不得省略"硬规则冲突，LLM 会拿它当省略许可，重新引入"省略成为反例"）。通过具体机械规则体现。

WSK 生成 `[State Update]` 账本时，输出应紧凑，节省垂直空间。

**机械规则**：
- 软标签 + 第一行同行
- 标签必出，无变化时内容留空
- 变化子段仅 Inventory Delta
- 6 字段平铺不嵌套标题
- 压缩规则

**设计依据**：project_memory 已记录此取向及不写入 prompt 的理由。

### 2.5 规则内联分层标准

LLM 整体全看（并行 attention）非层层查阅，注意力分布：Scene（靠前+强指令）高 > Additional（中段）中 > Extra（靠后+长表格）低。

| 层级 | 承载字段 | 规则类型 | 内容示例 |
|------|---------|---------|---------|
| Scene | 2-1 / 1-1 | 每次输出都用到的规则 | 身份、输出结构、输出内容、心理预设 |
| Additional | 2-2 / 1-2 | 按需触发的判定/边界/流程规则 | 库存分类原则、四类存储位定义、消耗规则、损耗机制 |
| Extra | 2-3 / 1-3 | 枚举/表格/格式模板 | 完整视图、压缩规则、口径格式、固定取值 |

**设计禁忌**：
- 禁止"内联一切到 Scene"——会稀释关键规则注意力，与节省垂直空间取向冲突
- SSOT 声明（"以 X 字段为权威"）对 LLM 无效，不写入 prompt

### 2.6 防止反例原则

- **标签必出，无变化时内容留空**：不得省略，省略会成为反例
- **6 字段每次必出**：Inventory State / Party Condition / Relationship & Threat / Map Knowledge / Base Structure State / 近五日主要事件
- **变化子段内容按需填写**：无变化时内容留空，但标签必出

**设计依据**：project_memory 已记录"无变化时留空"规则的建立过程。

### 2.7 语言一致性原则（长期）

> 此原则指导全库优化方向，不直接作为 prompt 内容。

现有的英文定义、软标签、枚举值、字段名等，在不影响有效性的情况下应在优化过程中逐步改成中文，以保持语言一致性。

**实现方式**：
- 这不是一次性全部改完，而是在每次相关优化时渐进推进
- 评估每个英文项时需确认改动不影响运行时有效性（如工具正则匹配、LLM 注意力锚点等）
- 涉及工具验证规则中硬编码的英文标签时，需同步更新工具

**涉及的英文项（示例）**：
- 软标签：`[State Update]`、`Inventory Delta:`、`Inventory State`、`Party Condition`、`Relationship & Threat`、`Map Knowledge`、`Base Structure State`
- 枚举值：`confirmed-intact`、`uncertain`、`likely-moved`、`hidden`、`local-only`、`publicly-known` 等
- 字段名：`Availability`、`Filter Remaining` 等

**设计依据**：project_memory 已记录此长期原则。

### 2.8 术语规范原则（长期）

> 此原则指导全库优化方向，不直接作为 prompt 内容。

描述系统角色（WM/WSK）对数据和事件的处理行为时，使用"更新"、"维护"等词，不使用"记录"、"保存"等词。

**理由**：系统角色由 LLM 根据上下文和提示词约束进行输出，没有持久化存储能力。"记录"、"保存"暗示系统有存储功能，与实际机制不符。"更新"、"维护"更准确描述 LLM 的行为——每次输出都是基于上一轮状态的"更新"。

**实现方式**：
- 这不是一次性全部改完，而是在每次相关优化时渐进推进
- "记录"作为名词使用时可保留（如"近五日主要事件记录"）
- "记录"作为动词描述系统角色行为时应改为"更新"或"维护"
- "保存"改为"更新"或"维护"

**设计依据**：project_memory 已记录此长期原则。

### 2.9 术语 SSOT 原则

每个术语只有一个权威定义。标签、字段名、枚举值在全局唯一。

**要求**：
- 同一概念只用一个术语（禁止"据点库存"与"据点核心存储位"并存）
- 同一术语只有一个含义（禁止同一术语在不同场景有不同含义）
- 新增术语时必须在设计文档中定义
- 标签/字段名/枚举值在所有文件中保持一致

**审核检查**：跨文件术语扫描，识别同义异名和异义同名。

**设计依据**：提示词工程成熟经验。术语不一致会导致 LLM 注意力分散和输出格式混乱。

### 2.10 单一职责原则

每个字段/规则只承担一个职责，避免字段含义重叠。

**示例**：
- Inventory Delta 只含变化量，不含存量（存量在 Inventory State）
- Base Structure State 只含结构节点状态，不含库存（库存在 Inventory State）
- Party Condition 只含角色状态，不含物资（物资在 Inventory State）

**审核检查**：字段语义重叠扫描——检查两个字段是否描述同一类信息。

**设计依据**：提示词工程成熟经验。字段含义重叠会导致 LLM 在多个字段中重复输出同类信息，浪费输出空间并增加不一致风险。

### 2.11 正反示例原则

每条输出格式规则配正反示例。反例特别重要——LLM 容易从反例中学习错误模式。

**要求**：
- 正例展示正确格式
- 反例展示常见错误（标注错误原因）
- 反例不得使用可能被 LLM 模仿的完整错误输出——用文字描述错误类型，或用文字标注正反（如"正例：…"/"反例：…"）

**设计依据**：提示词工程成熟经验。反例是 LLM 学习格式的重要锚点，但完整错误输出可能被模仿。

### 2.12 负面指令最小化原则

优先用正面指令（"做 Y"），最小化负面指令（"不要做 X"）。

**理由**：负面指令的注意力锚点是被禁止的行为本身，LLM 可能模仿被禁止的行为。

**例外**：安全关键场景保留负面指令：
- "不得脑补未确认信息"
- "不得自行扣减库存"
- "不得以任何其他字符开头"

**审核检查**：负面指令扫描——评估每条负面指令是否可以转化为正面指令；保留的安全关键负面指令是否有充分理由。

**设计依据**：提示词工程成熟经验。负面指令在非安全关键场景的效果差于正面指令。

---

## 三、角色职责边界

### 3.1 WM 的库存相关职责

| 职责 | 说明 | 承载位置 |
|------|------|---------|
| 消耗裁定 | Day 推进时在 `[判定]` 写消耗行（据点/野外/断粮） | 1-1 §[输出结构]、1-2 §[据点消耗规则] |
| 损耗裁定 | Day 推进时检查天气/人为/使用损耗，叙事写出故障 | 1-2 §[据点损耗机制] |
| 渲染 | 可消耗物资用"容器描述 + ≈kg"双轨格式 | 1-3 §[可消耗物资渲染与资源估算] |
| 资源估算 | 叙事中估算关键资源剩余天数 | 1-3 §[可消耗物资渲染与资源估算] |
| 不记账 | 库存唯一官方记录角色是 WSK | 1-1 §[运行锚点] |

### 3.2 WSK 的库存相关职责

| 职责 | 说明 | 承载位置 |
|------|------|---------|
| 提取 | 从 WM Scene 叙事 + `[判定]` 强语义提取已成立变化 | 2-1 §[输出结构]、2-2 DO 决策表 |
| 归类 | 按功能组归类，生存关键物资保留具体名+数量 | 2-2 §[库存分类与精简原则] |
| 审计 | 用户点击触发时执行输出策略校验 | 2-2 §[库存分类与精简原则] 6b、2-3 §[压缩规则] |
| 记账 | 维护四类存储位账本 | 2-1 §[身份]、2-2 §[四类存储位定义] |
| 记忆存储位裁定 | 记忆存储位的 Availability 降级、建立、更新由 WSK 裁定（WM 不裁定记忆存储位状态） | 2-2 §[记忆存储位规则] |
| 不越界 | 不裁定 WM 域事务（消耗/损耗/关系等）、不自行扣减、不脑补 | 2-1 §[LLM 心理预设]、2-2 SILENT 决策表 |

### 3.3 不越界清单

WSK 在库存系统中的禁止行为：

1. **不进行交易换算**：仅更新 WM 裁定的实际数量（project_memory 硬约束）
2. **不自判物品重要性**：物品重要性裁定属 WM 域，WSK 按机械规则归类（project_memory 硬约束）
3. **实时裁定值不由 WSK 维护**：保暖值、体温压力等由 WM 实时裁定（D1 教训）
4. **静态属性不由 WSK 维护**：Insulation、Condition 等（T2/T4 教训）
5. **不推测、不补写、不推断 WM 域事务**：对消耗/损耗/关系等 WM 域事务只提取 WM 明确写出的已成立变化；记忆存储位状态是 WSK 裁定域，不在此列
6. **不脑补未确认信息**：信息不足时写 `未知/未确认/待复核`
7. **不把用户口头描述当已成立事实**：必须从 WM 叙事提取

---

## 四、数据模型

### 4.1 四类存储位

> "存储位"是物资存放位置的分类（位置维度），区别于 §4.2 的"物资分类体系"（物资维度）。四类存储位为并列关系，无层级之分。

| 存储位 | 定义 | 特征 |
|------|------|------|
| 随身存储位 | 当前在玩家/队伍身上或当前随行容器里可立即动用的关键物资 | 可立即动用 |
| 据点核心存储位 | 已明确放入某个具体据点核心储备、且默认可被该据点维护与长期生存调用的正式库存 | 按据点分桶，需 Base Core Site |
| 记忆存储位 | 见过、清点过、故意放置过、或已确认存在于"非随身、非据点核心、非载具"位置的物资 | 位置化记忆，不等于可立即动用 |
| 载具存储位 | 存放在载具（车辆等）中的物资 | 移动性，可被偷/毁/弃，需绑定载具标识 |

**关键约束**：
- 多个正式据点时，据点核心存储位必须按据点分桶维护
- 记忆存储位必须附位置、最后确认时间与可用性说明
- 记忆存储位不是稳定现货，需保留不确定性
- 载具存储位需绑定载具标识；载具被偷/毁/弃时，物资按 WM 裁定处理（转移/丢失/降级为记忆存储位）

**承载位置**：2-2 §[四类存储位定义]。

### 4.2 物资分类体系

库存分类采用"功能门类-分类-组别"三级机制。组别级做数量合并实现折叠。

**三级层级定义**：
- **第一级·功能门类**：物资的大类（如工具器材、弹药、食品）
- **第二级·分类**：功能门类下的子分类（如修理工具、子弹、罐头）
- **第三级·组别**：合并折叠层，同功能同类型的物品在此级合并数量（如扳手、7.62、肉类罐头）

随身存储位、据点核心存储位、载具存储位共用三级机制，统一分类逻辑。记忆存储位不使用三级机制，使用独立结构（Location / Last Confirmed / Availability / Items）维护位置化记忆。具体功能门类-分类清单、组别级记录规则、弹药口径合并、同义工具去重等机械规则见 2-2 §[库存分类与精简原则] 6a/6c、2-3 §[弹药口径格式硬约束]。

**承载位置**：2-2 §[库存分类与精简原则] 6a/6c、2-3 §[弹药口径格式硬约束]。

### 4.3 库存分类与精简原则

> 此设计意图**不写入 prompt 执行 rule**（"保留相关/折叠无关"是目标性指令，WSK 自判相关性会越界——物品重要性裁定属 WM 域）。通过机械规则落地。

**设计意图**：防止 WSK 库存清单无限膨胀，保留生存/游戏相关物品，折叠无关杂物，保持玩家可读性。

**精简原则**（设计层）：

| 原则 | 设计意图 |
|------|---------|
| 生存关键物资保留具体名 | 武器/弹药/药品/饮水/主食/燃料等生存关键物资，在组别级保留具体名+数量，不进一步合并——确保玩家可见、决策可用 |
| 普通杂项在组别级合并 | 默认按三级归类，组别级合并数量实现折叠——防止清单膨胀 |
| 归类是单向压缩 | 不可展开——避免"展开/折回/内部保留"等暗示可逆的表述引入认知混乱 |
| 单独条目需显式信号 | 仅当 WM 叙事显式状态/交易焦点/用户要求完整盘点时，在组别级下列具体物品——这是"单独记录"不是"展开" |

**设计禁忌**：
- 禁止引入"任务瓶颈/决策结果/唯一工具"等 WSK 无任务概念的越界表述
- 归类是单向压缩，禁止引入"展开/折回/内部保留"等暗示可逆的表述

**承载位置**：2-2 §[库存分类与精简原则]（机械规则：功能门类-分类清单、组别级记录规则、同义工具去重、弹药口径合并、审计职责）。

### 4.4 记忆存储位 Availability

记忆存储位维护 Availability（可用性）字段，取值反映物资当前位置的可取性状态。Availability 是 WSK 裁定域（见 §5.5）。

**设计意图**：记忆存储位不是稳定现货，需保留不确定性——防止玩家把"见过"当成"现在还有"。

**降级原则**（设计层）：出现失守/封锁/公开冲突/他人活动/搜刮迹象/受潮污染/时距过长等干扰条件时，Availability 应降级；缺少依据时优先降为 `uncertain`；不得因无人再次确认而无限期保留 `confirmed-intact`。

**承载位置**：2-2 §[记忆存储位规则]（Availability 取值枚举与降级规则）、2-3 §[固定取值]（枚举列表）。

### 4.5 物资状态属性

**不由 WSK 维护的属性**（历史教训）：
- Insulation（保暖值）——静态属性（T2 教训）
- Condition（物品状态枚举）——枚举化生命周期不可靠（T4 教训）
- 保暖值——实时裁定（D1 教训）

**由 WSK 维护的属性**：
- 污染区相关物资：Filter Remaining 或防护状态
- 伤病记录：仅保留伤情分类，不推断治疗需求、不推演机制效果

**承载位置**：2-2 §[物资状态规则]。

---

## 五、机制清单

每个机制说明：触发 → 裁定 → 更新的完整链路。

### 5.1 消耗机制（T1）

**触发**：Day 推进（WM 读 `[主要状态]` Day 值对比上轮，不等 = 推进）。这是简单值比较，非跨轮计数。

**裁定**（WM 侧）：Day 推进时，WM 必须在 `[判定]` 中写一行消耗摘要。消耗行格式（据点过夜/野外过夜/两者并存/断粮场景）与食物/燃料优先级见 1-2 §[据点消耗规则]。

**增强（软指引）**：WM 在 Scene 叙事中涉及做饭/取暖时，应参照上一轮 WSK `[State Update]` 中的 Inventory State，按优先级选择消耗物。此为软指引，漏了不影响主机制兜底。

**更新**（WSK 侧）：WSK 从 `[判定]` 消耗行提取，更新 Inventory Delta + Inventory State。WSK 不做计算，不自行扣减——只提取 WM 裁定的消耗量。

**承载位置**：1-1 §[输出结构] [判定]、1-2 §[据点消耗规则]、2-1 Inventory Delta、2-2 DO 决策表。

### 5.2 损耗机制（D2）

**触发**：Day 推进（与消耗机制共用触发点）。

**裁定**（WM 侧）：Day 推进时一次性检查三项损耗（天气损耗/人为破坏/使用损耗）。WM 在 Scene 叙事中写出故障（不是 `[判定]` 结构化行，是自然叙事）。三类损耗的触发条件、概率量化机制（掷骰 + Base，v1.61 从 v1.57 语义化方向回退：定性词无判定标准，LLM 无法校准）与组件选择见 1-2 §[据点损耗机制]；人为破坏与暴露信号的联动见 §5.4。

**组件状态生命周期**（设计决策：三级自然语言状态模型，T4 教训）：
- 完好 → 损坏（损耗事件触发）
- 损坏 → 毁坏（再次损耗且未修补）
- 损坏 → 完好（玩家修补）
- 毁坏 → 完好（玩家重建）
- 完好 → 完好+加固（玩家加固，更耐损耗）

全程用自然语言状态描述，不用枚举、不用数值——此为 T4 教训的设计决策（见 §9.2）。

**更新**（WSK 侧）：WSK 从叙事提取，更新 Base Structure State 组件状态描述。

**历史注记**：v1.67 起 Maintenance Pressure 及 Rest/Shelter Availability、Heat/Dryness、Occupancy/Residency Load、Supply/Sanitation Strain 等据点状态字段全部删除--均为无触发依赖的纯记录字段，其信息由组件状态描述（自然语言）+ Scene 叙事承载。

**承载位置**：1-2 §[据点损耗机制]、2-1 Base Structure State、2-2 §[据点与庇护记录规则]。

### 5.3 转移机制

**跨位转移**：物资在四类存储位之间移动时，需同时更新源位减、目标位加两侧，防止只增不减或只减不增。据点核心存储位转移需 Base Core Site。

格式与结算规则（含估算标注、重复结算处理等）见 2-2 DO 决策表、2-3 §[压缩规则] 跨位转移结算。

**承载位置**：2-2 DO 决策表、2-3 §[压缩规则] 跨位转移结算。

### 5.4 据点暴露机制（D4）

**暴露来源**：夜间火光/照明、反复走同一路线、与他人交易、被跟踪回据点、枪声/施工噪音、前住户/访客。

**联动**：D2 人为破坏由 [主要状态] 风险栏的据点暴露信号驱动掷骰（Base 按信号强度 40/55，v1.67 简化：从"暴露档位 + Relationship 查组合表"改为信号驱动，消除 Relationship 归属歧义与中间档未覆盖问题）。暴露是否仍为活跃威胁由 WM 每轮风险栏语义判断（写入风险栏 = 活跃），不依赖独立档位字段。

**历史注记**：v1.67 起 Security/Exposure 三档字段（hidden/local-only/publicly-known）已删除--信号驱动后无机制触发依赖，纯记录职能不足以维持字段存在。暴露的长期信息由叙事 + 近五日主要事件承载。

**承载位置**：1-2 §[据点损耗机制] 人为破坏部分。

### 5.5 记忆存储位降级

**定位**：记忆存储位由 WSK 更新及维护，功能和机制上只能由 WSK 裁定。记忆存储位的 Availability 降级、建立、更新均由 WSK 裁定，WM 不裁定记忆存储位状态。

**触发与裁定**（WSK 裁定域）：自上次 `Last Confirmed` 之后出现失守/封锁/公开冲突/他人活动/搜刮迹象/受潮污染/时距过长等已成立变化时，WSK 裁定 Availability 降级。这是 WSK 的裁定域，不是 WM 裁定。具体触发条件、降级路径与取值见 2-2 §[记忆存储位规则]。

**承载位置**：2-2 §[记忆存储位规则]。

### 5.6 同伴库存维护

**设计意图**：WSK 可维护 User 同行伴侣的可见库存（如双人同行场景），不限于单一 User 角色的硬状态。

**职责边界**：仅维护同行伴侣的随身可见硬状态与同据点期间的资产变化；不维护伴侣的个人剧情、心理、恋爱等次级状态（属 WM/Relationship Status 职责）。同行伴侣 ≠ 常驻世界 NPC / 阵营角色 / 路人 NPC。

进入条件、标注方式、禁止行为等机械规则见 2-2 §[同伴库存维护]。

**承载位置**：2-2 §[同伴库存维护]。

---

## 六、输出格式规范

### 6.1 完整视图出场次序

完整视图的出场次序（设计决策：分离顺序声明与细节约束，参考 1-1 模式）：

`[State Update] D{day}-T{turn}` -> Inventory Delta -> Inventory State -> Party Condition -> Relationship & Threat -> Map Knowledge -> Base Structure State -> 近五日主要事件

**设计约束**：
- 软标签 + 第一行同行（`[State Update] D{day}-T{turn}` 在同一行）
- 标签后不换行，不得换序
- 6 字段平铺不嵌套标题
- 无可提取变化时整体输出 `-`（此为整体输出标记，区别于字段级"无变化时内容留空"）

**承载位置**：2-1 §[输出结构]、2-3 §[完整视图]。

### 6.2 Inventory Delta 格式

- 标签必出，无变化时内容留空
- 变化量写法：`获得/消耗/丢失/转移 + 数量 + 单位`（转移变化量追加 `源位->目标位`，见 2-2 DO 决策表）
- 是本轮库存变化的唯一结构化记录

**承载位置**：2-1 §[输出内容]、2-2 DO 决策表。

### 6.3 Inventory State 格式

**设计要点**：
- 按"功能门类-分类-组别"三级归类（分类体系见 §4.2，精简原则见 §4.3）
- 可消耗物资（食物/饮水/燃料等）按 kg 计量；药品按剂型（如针剂×12）；弹药按发
- 弹药按口径在组别级合并（口径即组别）
- 随身存储位、据点核心存储位、载具存储位共用三级机制
- 记忆存储位在 Inventory State 之后独立段，不进入 Inventory State 字段；载具存储位在 Inventory State 中按载具分桶维护

**承载位置**：2-3 §[完整视图]、2-3 §[压缩规则]、2-2 §[库存分类与精简原则] 6a。

### 6.4 压缩规则与审计输出

**压缩设计**：三级机制本身即压缩机制（组别级合并数量实现折叠）。同义工具去重与高频/低频单件处理等机械规则见 2-2 §[库存分类与精简原则] 6c。

**审计输出**：用户点击触发时，WSK 执行库存归类审计，确保三级机制正确落地（归类正确性、压缩有效性、关键物资完整性）。审计发现归类错误时，WSK 应输出修正版本。审计项与输出格式见 2-2 §[库存分类与精简原则] 6b、2-3 §[压缩规则]。

**承载位置**：2-2 §[库存分类与精简原则] 6b/6c、2-3 §[压缩规则]。

---

## 七、规则内联策略

基于 LLM 注意力分布（§2.5），库存相关规则的承载分工：

### 7.1 Scene 层（2-1 / 1-1）

**判断标准**：每次输出都用到的规则。

| 规则 | 内容 |
|------|------|
| 身份 | WSK 是硬状态管理员，维护四类存储位 |
| 输出结构 | 完整视图出场次序 |
| 输出内容 | 各字段定义（Inventory Delta、Inventory State 等） |
| LLM 心理预设 | 压制凑齐字段、自动提交、过度展开、礼貌自介、叙事漂移 |

### 7.2 Additional 层（2-2 / 1-2）

**判断标准**：按需触发的判定/边界/流程规则。

| 规则 | 内容 |
|------|------|
| 库存分类与精简原则 | 分类体系、归类规则、单独条目触发条件、审计职责、同义工具去重 |
| 四类存储位定义 | 随身/据点核心/记忆/载具存储位的定义与约束 |
| 记忆存储位规则 | Availability 取值与降级规则 |
| 同伴库存维护 | 进入条件、标注方式、职责范围 |
| 物资状态规则 | 污染区物资、Filter Remaining |
| 据点消耗规则（1-2） | Day 推进触发、消耗行格式、食物/燃料优先级 |
| 据点损耗机制（1-2） | 三类损耗、组件状态生命周期、节奏控制 |
| 据点与庇护记录规则 | Base Category、组件状态描述 |

### 7.3 Extra 层（2-3 / 1-3）

**判断标准**：枚举/表格/格式模板。

| 规则 | 内容 |
|------|------|
| 完整视图 | 各字段的具体格式示例 |
| 压缩规则 | 审计输出格式、跨位转移结算 |
| 弹药口径格式硬约束 | 口径列表、合并记录规则 |
| 固定取值 | Availability 枚举、Relationship 7 档、Human Threat Stage |
| 可消耗物资渲染（1-3） | "容器描述 + ≈kg"双轨格式、资源剩余天数估算 |
| 弹药口径换算表（1-3） | 仅供交易场景，不适用于库存字段 |

---

## 八、验证策略

### 8.1 工具化检查点

| 工具 | 检查内容 | 覆盖库存项 |
|------|---------|-----------|
| validate_output.py | 输出格式验证 | [State Update] header、Inventory Delta 标签、6 字段出场 |
| validate_enums.py | 枚举一致性 | Availability、Relationship、Human Threat Stage |
| detect_duplicates.py | 重复检测 | 跨文件规则重复（如跨位转移规则） |
| gen_ref_graph.py | 引用活性 | §引用断链、孤立节检测 |
| format_lint.py | 格式 lint | 标签格式、引用格式 |
| detect_negative_redundancy.py | 冗余负面表述扫描 | 正面表述已足够约束时仍附加负面表述（PB §0.13 原则⑩） |

### 8.2 一致性检查

- **跨文件术语一致**：`据点核心存储位`（不写 `Inventory - Base Core`）
- **跨文件规则不冲突**：同一规则在多处出现时措辞一致
- **引用链路活性**：§引用可解析，无断链
- **枚举一致**：Availability、Relationship 等枚举跨文件一致
- **负面指令最小化**：正面表述已足够约束时不附加负面表述；同一条规则中正面在前（PB §0.13 原则⑩）

### 8.3 运行时验证

- **测试用例验证**：用 `test_cases/` 验证 WSK 输出
  - `smoke_test_basic.md`：基础输出格式
  - `edge_cases.md`：边界场景
  - `wsk_narrative_drift_test.md`：叙事漂移防护
  - `consistency_snapshot.md`：一致性快照
- **机制链路完整性**：
  - 消耗机制：1-1 [判定] → 1-2 消耗行 → 2-1 Inventory Delta 提取 → 2-3 格式
  - 损耗机制：1-2 损耗叙事 -> 2-1 Base Structure State 提取

---

## 九、历史教训与设计禁忌

### 9.1 静态属性不由 WSK 维护（T2 Insulation 教训）

**背景**：v1.55 保留了 Insulation 在 WSK，但 WSK 定位 = "更新会变化的状态"，Insulation 是衣物静态属性（一件冬衣保暖值不变），不由 WSK 维护。

**决议**：从 WSK 移除。

**设计禁忌**：静态属性不由 WSK 维护。物品的固有属性（保暖值、材质、基础规格）不由 WSK 维护，只更新会变化的状态（数量、位置、可用性）。

**依据**：v2_discussion.md T2、project_memory。

### 9.2 枚举化生命周期不可靠（T4 Condition 教训）

**背景**：Condition 枚举（Pristine/Worn/Damaged/Badly Damaged/Ruined）原用于物品状态和据点结构节点状态。评估发现：物品 Condition 降级联动难以落地；据点结构 Condition 使用物品枚举套用于结构不合适；Component 生命周期（前 Condition → 后 Condition 跨轮追踪）LLM 不可靠执行。

**决议**：全库删除 Condition，简化 Component 生命周期为"组件名 + Type + Role + 状态描述（自然语言）"。

**设计禁忌**：不使用枚举化生命周期追踪。组件状态用自然语言描述（完好/损坏/毁坏 + 修复/加固信息），不做跨轮枚举降级。

**依据**：v2_discussion.md T4、审计历史记录.md。

### 9.3 实时裁定值不由 WSK 维护（D1 保暖值教训）

**背景**：D1 重新设计保暖机制。保暖值由 WM 根据玩家穿着以语义裁定（优/良/中/弱/差 5 档），实时变化，不是持久状态。

**决议**：保暖值不由 WSK 维护。WSK 在 Party Condition 中更新体温定性状态，保暖机制驱动体温变化由 WM 裁定。

**设计禁忌**：实时裁定值不由 WSK 维护。由 WM 根据当前条件实时裁定的值（保暖值、体温压力推进等）不由 WSK 维护，WSK 只更新这些裁定产生的结果（如体温定性状态）。

**依据**：v2_discussion.md D1、project_memory。

### 9.4 跨角色卡引用必然失效

**背景**：Ourdream 平台角色卡字段互不可见。WM 与 WSK 的字段不能互相引用。

**设计禁忌**：库存规则的引用逻辑必须构建在共享字段（对话历史）中，不得跨角色卡引用。WSK 通过读取 WM 在对话历史中的输出提取变化，而非调用 WM 字段。

**依据**：审计历史记录.md、PB §十八运行模型。

### 9.5 格式定义双重矛盾导致输出崩溃（v1.37 教训）

**背景**：v1.37 出现"[主要状态] 不输出"问题，根因是格式定义双重矛盾。

**设计禁忌**：格式定义必须统一、无歧义。同一字段的格式规则不得在不同文件中给出冲突定义。若多处定义，必须明确主权威，且其他处只做引用不重复定义。

**依据**：审计历史记录.md v1.37。

### 9.6 大规模条目物理移动风险高（T3 教训）

**背景**：v2.01-T3 评估库存规则梳理时，结论为"不大重构。当前结构已通过三轮验证，运行时合规。大规模条目移动风险高，收益是结构整洁而非运行时改善"。

**设计禁忌**：重构以逻辑收敛为主，物理位置调整为辅。修改范围严格限制在审计识别的问题，不做额外"顺手优化"。

**依据**：v2_discussion.md T3。

### 9.7 目标性指令与硬规则冲突

**背景**："WSK 输出尽量节省垂直空间"作为目标性指令写入 prompt 时，会与"标签必出/不得省略"硬规则冲突——LLM 会拿"节省空间"当省略许可，重新引入"省略成为反例"。

**设计禁忌**：见 §2.4（目标性指令不写入 prompt，通过机械规则体现）。

**依据**：project_memory "WSK 输出以节省垂直空间为设计取向"、"库存分类与精简设计意图"。

### 9.8 WSK 不自判物品重要性

**背景**：库存分类与精简原则的"保留生存相关、折叠无关杂物"是设计意图。若写入 prompt 作为执行 rule，WSK 会自判物品相关性——但物品重要性裁定属 WM 域，WSK 越界。

**设计禁忌**：见 §4.3（WSK 不自判物品重要性，通过三级分类机械规则落地）。

**依据**：project_memory "库存分类与精简设计意图"。

### 9.9 归类是单向压缩

**背景**：库存分类的折叠机制通过"归类记录"实现（如"工具器材-修理工具-扳手×5"）。这种折叠是单向压缩，无法再次展开。

**设计禁忌**：见 §4.3（归类是单向压缩，不可展开）。

**依据**：project_memory "库存分类与精简设计意图"。

---

## 附录：设计文档与 prompt 的承载关系

| 设计文档章节 | 对应 prompt 承载位置 |
|-------------|---------------------|
| 一、游戏背景与库存系统定位 | 0-1/0-2 世界知识底座（运行时不保证加载）；1-1/2-1 身份 |
| 二、设计原则 | 体现在 1-1/2-1 运行锚点、身份、心理预设中 |
| 三、角色职责边界 | 1-1 §[运行锚点]、2-1 §[身份]、2-2 DO/SILENT 决策表 |
| 四、数据模型 | 2-2 §[四类存储位定义]/§[库存分类与精简原则]/§[记忆存储位规则]/§[物资状态规则]、2-3 §[固定取值] |
| 五、机制清单 | 1-2 §[据点消耗规则]/§[据点损耗机制]、2-2 §[据点与庇护记录规则]/§[同伴库存维护]、2-3 §[压缩规则] |
| 六、输出格式规范 | 2-1 §[输出结构]/§[输出内容]、2-3 §[完整视图]/§[压缩规则] |
| 七、规则内联策略 | 指导 prompt 字段分层，不直接作为 prompt 内容 |
| 八、验证策略 | tools/ 下脚本、test_cases/ 下测试用例 |
| 九、历史教训与设计禁忌 | project_memory 防回退约束 |

---

## 文档维护

- **版本**：v1.0（2026-08-11 初稿）
- **维护原则**：设计文档只承载设计原则层，机械规则仍由 prompt 字段承载。修改 prompt 中的库存规则时，应同步检查本文档是否需要更新。
- **更新触发**：新增库存机制、修改设计原则、新增历史教训时更新本文档。
