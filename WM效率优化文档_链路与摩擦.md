# WM 链路与机械逻辑摩擦效率优化文档

> **文档用途**：在字数优化（EFF 批次）触及天花板后，从"链路结构"和"机械逻辑摩擦"角度评估 WM 响应效率，提出优化方向供讨论与验证。
>
> **状态**：已收尾（终版）。经历三轮演进：原文档 → seed 评估审计（修订 1，3 处勘误 + 5 采纳）→ **平台加载机制再评估（修订 2，用户质疑触发）**。最终落地：DEC-H M6u（加法查表 Seed）+ DEC-J 缩水版（三表聚合）已实施；DEC-K 不做、DEC-I 挂起。全过程讨论与结论见 §十/§十一/§十二。
>
> **基座**：PB §1.1 字段承载分工 / §0.13 提示词工程原则 / glm-WM总体优化文档.md §八 EFF 批次

---

## 一、评估背景

字数优化（EFF-01/A/B/C）已实施完成，实测节省 -1662 字。但 WM 响应慢的根本原因不是字数，而是：

1. **跨字段跳转过多**（搜刮 5 次、移动 5 次、概率 4 次）
2. **算术与数值计算反 LLM 能力特性**（Probability Check）
3. **多枚举并行查表**（敌对 4 维、保暖+温度层）

LLM 每次跨字段跳转需：中断当前推理 → 定位 section → 加载上下文 → 返回原位继续。这是响应慢的核心结构性原因。

---

## 二、链路过长问题

### 2.1 搜刮裁定链路（最严重）

```
玩家声明搜刮
↓ 1-2 [必查表] 触发："查 §[搜刮过程机制]"
↓ 1-2 [搜刮裁定思路] 读取裁定流程（8 步）
↓ 1-3 [搜刮过程机制] 查 Tier/噪音/产出（跨字段跳转 1）
↓ 1-3 [子地点列表] 查具体点位 Tier+tag（跨字段跳转 2，D 区末尾，距 C 区 300+ 行）
↓ 1-3 [可消耗物资渲染与资源估算] 查渲染格式（跨字段跳转 3）
↓ 1-3 [区域-势力-事件-概率统一表] 查超规格概率（跨字段跳转 4，若触发）
↓ 1-2 [压力裁定参考] 更新压力（跨字段跳转 5）
```

**问题**：一次搜刮裁定涉及 **5 次跨字段跳转**（1-2 ↔ 1-3 来回）。

**摩擦点**：
- 1-2 内有 **10 处** "见 Extra Details §[搜刮过程机制]" 引用（[搜刮裁定思路] 内 8 处 + 敌对规则 L123 + 压力校准 L38；外部评估核验修正，原估 8 处偏低 25%），每处都是一次"中断-跳转-返回"
- 1-3 [子地点列表] 在 D 区末尾（L758+），距 [搜刮过程机制] C 区（L419+）有 300+ 行间隔

### 2.2 概率裁决链路

```
触发概率事件
↓ 1-2 [必查表]："查 §[Probability Check 偏移表]"
↓ 1-3 [Probability Check 偏移表] 查 Event Offset（跳转 1）
↓ 1-3 [Probability Check 格式模板] 查 Seed 计算公式（同文件二次跳转）
↓ 执行算术：(Day×7 + Turn末两位×3 + Offset) mod 100
↓ 1-3 [人类敌对风险机制] 查 Base Probability 区间（跳转 3，若敌对事件）
↓ 1-2 [敌对势力动态规则] 查 4 维评分（跳转 4）
```

**问题**：4 次跳转 + 1 次算术 + 多维评分映射。

### 2.3 移动裁定链路

```
玩家声明移动
↓ 1-2 [必查表]："查 §[地图总表] 和 §[跨区移动路由规则]"
↓ 1-3 [地图总表] 查起点/终点分区（跳转 1）
↓ 1-3 [跨区移动路由规则] 查 Route 计算（跳转 2，同文件）
↓ 1-3 [子地点列表] 查具体点位（跳转 3，D 区末尾）
↓ 1-2 [Turn/Day 推进规则] 计算 step 累计（跳转 4，回 1-2）
↓ 1-2 [压力裁定参考] 更新压力（跳转 5）
```

**问题**：5 次跳转，且 [地图总表]-[路由规则]-[子地点列表] 三者分散在 1-3 的 A 区和 D 区，物理距离远。

---

## 三、机械逻辑摩擦

### 3.1 Probability Check 算术（最高摩擦）

```python
Seed = ((Day × 7) + (Turn末两位 × 3) + Event Offset) mod 100
```

**摩擦分析**：
- LLM 不擅长乘法 + 取模（估算 ~30% 错误率）
- "Turn末两位"需先从 Turn ID 提取末两位（如 T500 -> 00），是字符串操作
- 多变量代入 + 算术 + 取模 + 大小比较，5 步串行计算
- OPT-P2-03 加了语义校验，但校验本身又增加一步推理

**本质问题**：用算术做"随机性模拟"是反 LLM 能力特性的。LLM 擅长语义判断，不擅长精确计算。

### 3.2 敌对 4 维评分叠加

```
暴露维度（Exposure）+ 资源价值维度（Value）+ 敌意维度（Hostility）+ 成本维度（Cost）
-> 单维度 ±10 修正
-> 总和限制 0-100
-> 映射到 Base Probability 区间
```

**摩擦分析**：
- 4 维独立评分，每维需语义判断
- ±10 修正需数值累加
- 区间映射是非线性对应（如 20-40 vs 40-70 vs 60-90）
- 4 次语义判断 + 1 次数值累加 + 1 次区间映射 = 6 步

### 3.3 压力 5 轨增量演化的"锚点回溯"

```
读上一次 [主要状态] 压力段（不一定是上一轮，需回溯对话历史定位锚点）
↓ 逐轨判断：本轮已成立变化 + 修正项
↓ 5 轨分别演化（脱水/饥饿/疲劳/体温/伤病）
↓ 5 轨联动修正（湿身->失温->疲劳->失误...）
↓ 输出 5 轨完整状态
```

**摩擦分析**：
- "不一定是上一轮，回溯到最近一次" → 需先搜索对话历史定位锚点
- 5 轨 ×（起点 + 修正 + 联动）= 至少 15 步推理
- 联动规则是隐式因果链（湿身→失温→疲劳→失误），需链式推导

### 3.4 Day 推进检测的间接判断

```
读本轮 [主要状态] Day 值
↓ 对比上轮 Day 值
↓ 不等 = 推进
↓ 触发 [据点消耗规则] 的 T1 消耗行（勘误：[据点消耗规则] 实际在 1-2 L65，距 [Turn/Day 推进规则] L51 仅 14 行，同文件近距 N 级，无跨文件二次跳转成本）
```

**摩擦分析**：本身简单，但"对比上轮"需回溯对话历史。触发后还需查 [据点消耗规则] 的具体消耗项，二次跳转。

### 3.5 保暖裁定的"有效温度层移动"

```
当前温度层（查 §[温度分层]）
↓ 保暖档位（查 §[保暖修正] 5 档）
↓ 档位 -> 有效温度层移动（+2/+1/0/-1/-2 档）
↓ 环境修正（风/湿/涉水/夜间/静止）
↓ 最终有效温度层 -> 压力推进速率
```

**摩擦分析**：5 步串行，且"温度层移动"是相对量计算（向暖侧/冷侧移 N 档），需先知道当前层再移动。相对量计算比绝对值判断更耗认知。

---

## 四、结构性低效

### 4.1 [必查表] 的"任务触发式检索"认知负担

当前 [必查表] 8 项，每项格式："X 时：查 §[Y]"。

**问题**：LLM 需先判断当前任务类型（是搜刮？移动？概率？），再匹配 [必查表] 对应项，再跳转。这个"判断-匹配-跳转"三步本身是开销。

**对比**：如果相关规则直接内联在裁定 section 内（如 [搜刮裁定思路] 内直接包含 Tier 表），则省去跳转。但这违反三层分工原则（1-2 不放数据表）。**这是分工效率 vs 内联效率的固有矛盾**。

### 4.2 1-3 四子分类的物理分散

A/B/C/D 四子分类虽然逻辑清晰，但：
- [地图总表]（A 区）+ [跨区移动路由规则]（A 区）+ [子地点列表]（D 区）分散在 1-3 两端
- [搜刮过程机制]（C 区）+ [子地点列表]（D 区）+ [可消耗物资渲染]（C 区）也分散

**问题**：同一裁定链路需要的数据表分散在不同子区，物理距离远。LLM 在 1-3 内部跳转也有成本。

### 4.3 多层枚举的"查表-匹配"模式

WM 大量使用枚举查表：
- 敌对阶梯 5 档（no-signal/old-trace/spotted/contacted/hostile-contact）
- 保暖 5 档（优/良/中/弱/差）
- 温度层多档
- 据点暴露 3 档（hidden/local-only/publicly-known）
- 知情范围 4 档（hidden/party-known/local-only/publicly-known）
- 压力 5 轨 × 5 档（stable/strained/weakened/critical/dying）

**问题**：每次裁定需"判断当前档位 → 查修正规则 → 应用"。多个枚举并行时（如保暖+温度层+暴露），认知负载叠加。

---

## 五、优化方向建议（按收益/风险排序）

### 方向 1：搜刮裁定链路内联化（最高收益，中风险）

**方案**：在 1-2 [搜刮裁定思路] 末尾追加一个"快速参考块"，内联 Tier 耗时/噪音/Tier 典型产出的简表（非完整数据），避免每次跳转 1-3。

**收益**：搜刮裁定从 5 次跳转降至 2 次（仅超规格概率 + 渲染格式需跳转）。

**风险**：违反三层分工（1-2 不放数据表）。但可视为"裁定速查卡"而非完整数据表，类似 1-1 的运行锚点模式。

**待讨论**：
- "速查卡"与"数据表"的边界如何界定？
- 是否适用于其他高频链路（移动、概率）？

### 方向 2：Probability Check 算术简化（高收益，需 DEC）

**方案 A**：将 Seed 公式从 `(Day×7 + Turn末两位×3 + Offset) mod 100` 简化为 `Day + Turn + Offset` 的末两位（去除乘法）。

**方案 B**：用语义判断替代算术——"WM 根据本轮氛围语义判断是否触发（高紧张度场景更易触发），用 Day+Turn 作为倾向性参考而非计算公式"。

**收益**：消除算术摩擦，匹配 LLM 能力特性。

**风险**：
- 方案 A 降低随机性质量
- 方案 B 放弃机械随机性，可能引入 WM 偏好

**待 DEC**：是否允许放弃/简化算术随机性机制？

### 方向 3：1-3 同链路数据表物理聚合（中收益，低风险）

**方案**：将 [子地点列表] 从 D 区末尾迁回 A 区，紧邻 [地图总表] 和 [跨区移动路由规则]。或反向，将 [地图总表]+[路由规则] 迁到 D 区与 [子地点列表] 聚合。

**收益**：移动裁定和搜刮点位的跳转距离缩短。

**风险**：违反 OPT-P2-04 的 A 区连续性优化（子地点列表曾因打断 A 区被迁出）。需权衡"A 区机制连续性"vs"D 区查表聚合"。

**待讨论**：A 区连续性 vs D 区聚合，哪个对 LLM 注意力更友好？

### 方向 4：敌对 4 维评分简化（中收益，中风险）

**方案**：将 4 维独立评分（暴露/价值/敌意/成本）简化为 2 维复合评分（威胁度 = 暴露+敌意；收益度 = 价值-成本），减少评分步数。

**风险**：丢失维度独立性，可能影响裁定精度。

**待讨论**：4 维评分的实际使用频率是否高到值得简化？

### 方向 5：压力 5 轨的"锚点内联"（低收益，低风险）

**方案**：在 1-2 [压力裁定参考] 末尾内联一个"5 轨速查矩阵"（行=5 轨，列=修正项），避免逐轨分别查规则。

**收益**：5 轨演化的认知负载从"15 步串行"降至"1 次矩阵查表"。

**风险**：低，但收益相对有限（压力裁定本身是 WM 核心能力）。

---

## 六、待 DEC 项

> **修订 1**：新增 DEC-K（必查表拆分）；各 DEC 的建议裁定方向与实施细节见 §九.5。

| 编号 | 内容 | 影响范围 | 决议状态 |
|------|------|---------|---------|
| DEC-H | Probability Check 算术是否允许简化/语义化 | 1-3 [PC 格式模板] + 1-2 [必查表] | ✅ **已裁定 M6u（加法查表版，用户定制）**：`Seed = (Day末两位 + 表[Turn个位] + Offset) 的末两位`，拒绝语义化。见 §十 |
| DEC-I | 三层分工原则是否允许"裁定速查卡"例外（方向 1） | PB §1.1.2 + 1-2 多个裁定 section | ⏸️ **挂起（再评估降级）**：SSOT 双定义风险，待运行时证据（见 §十一.3） |
| DEC-J | A 区连续性 vs D 区聚合的权衡（方向 3） | 1-3 A 区/D 区布局 | ✅ **已裁定缩水版并实施**（核心迁移 + 补丁 1；砍补丁 2，见 §十一） |
| DEC-K | [必查表] 是否允许拆分"默认项 + 触发嵌入" | 1-2 [必查表] + 6 个裁定 section 首句 | ❌ **不做（再评估收回）**：if 扫描节省可忽略，前部合规清单价值更高（见 §十一.3） |

---

## 七、待验证项

> 以下假设需通过运行时数据验证，当前仅为静态分析推测。

1. **跨字段跳转的实际耗时占比**：5 次跳转 vs 算术计算 vs 叙事生成，哪个是响应慢的主因？需运行时 token 消耗数据。
2. **Probability Check 算术错误率的真实值**：seed 估算 ~30%，但无真实运行数据。若实际 <10%，则方向 2 优先级降低。
3. **[必查表] 触发命中率**：LLM 是否每次都按 [必查表] 执行？若 LLM 常忽略 [必查表] 直接内联推理，则"跳转开销"假设不成立。
4. **5 轨演化的实际步数**：LLM 是否真的逐轨串行推理，还是并行处理？若并行，则"15 步"估算偏高。
5. **方向 1"速查卡"的注意力稀释风险**：内联速查卡会增加 1-2 字数，可能稀释其他裁定规则的注意力。需权衡"跳转减少"vs"注意力稀释"。

---

## 八、核心判断

**字数优化已触及天花板，进一步效率提升需从链路结构入手**。但链路优化涉及三层分工原则和 DEC 决议，风险较高。

**建议优先级**：
1. 方向 1（搜刮内联速查卡）：收益最高，风险可控（用"速查卡"绕过分工原则）
2. 方向 2（Probability Check 简化）：需 DEC-H 裁定，但能消除最大机械摩擦
3. 方向 3（数据表聚合）：低风险但需权衡 A 区连续性
4. 方向 4/5：收益相对较低

**关键前置条件**：建议先进行 3-5 轮运行时验证（见 §七），用真实数据确认静态分析的假设是否成立，再决定是否实施链路优化。避免基于错误假设做结构性改动。

---

## 九、外部评估（seed-2.1-turbo）独立审计与方案修订

> **来源**：《seed-WM效率优化建议.md》（修订 1，2026-08-14）。
>
> **审计方式**：对其全部事实声明逐项 grep/read 核验，采纳项与质疑项分列，独立形成修订结论。

### 9.1 事实核验结果（全部成立）

| seed 声明 | 核验方式 | 结果 |
|-----------|---------|------|
| [人类敌对风险机制] 在 1-2 L107（非 1-3） | grep 1-2 | ✅ 成立，本档 §二.2 已勘误 |
| [据点消耗规则] 在 1-2 L65（非 1-3） | grep 1-2 | ✅ 成立，本档 §三.4 已勘误 |
| 1-2 内 §[搜刮过程机制] 引用 10 处（非 8 处） | grep count | ✅ 成立，本档 §二.1 已勘误 |
| 1-1 L32 存在未声明缩写 `APD §[据点消耗规则]` | grep 1-1 | ✅ 成立，列入 P3 修复 |
| 1-3 关键行号（地图 L83/路由 L132/搜刮 L417/PC 模板 L575/保暖 L675/子地点 L758） | grep -n | ✅ 全部一致 |
| 移动链路 [地图总表]→[子地点列表] 跨 +675 行（79% 文件高度），为全系统最远单次跳转 | 行号差计算 | ✅ 成立 |

### 9.2 采纳项（5 项，经独立评估同意）

**采纳 1：X/F/N 三级跳转分级框架**

比我原"5 次跳转"的计数方式精确：
- **X 级**（跨文件 1-2↔1-3）：等效 600-1000 字被动阅读负载
- **F 级**（同文件 ≥200 行）：等效 200-400 字
- **N 级**（同文件 <200 行或 1-2 内任意）：等效 <50 字

搜刮链路真实成本 ≈ 2X + 2F + 3N ≈ 1750-2950 字等效阅读量（约 1-3 全文的 5-8%），不是简单计数问题。

**采纳 2：触发频率权重（重要洞察，我原文档遗漏）**

- **每轮必做**：5 轨压力 + 保暖裁定（单次 10-12 步 × 频率 100%）
- **玩家触发**：搜刮/移动/概率（单次重但频率 ~25-40%）

修正后的累计负载排序：**每轮必做项 > PC 算术 > X/F 级跳转**。我原文档只看单次成本，低估了"每轮必做"的累计贡献。

**采纳 3：摩擦量级重估（3 项降级）**

| 摩擦点 | 原估 | 重估 | 理由 |
|--------|------|------|------|
| 敌对 4 维评分 | 6 步串行 | 2-3 步 | LLM 是并行语义直觉映射，非逐项加减；降为 P4 不做 |
| 5 轨锚点定位 | O(n) 回溯 | O(1) | [主要状态] 是每轮末尾固定结构块，读上一条即可 |
| 5 轨演化步数 | 15 步 | 10-12 步 | LLM 半并行处理 5 轨整体状态 |

**采纳 4：DEC-J 方案 C（反向聚合），替代我原方向 3 方案 A**

我原方案 A（子地点迁回 A 区）会**回退 OPT-P2-04 的 A 区连续性成果**，且子地点列表本身也是纯查表数据，迁回 A 区不享受被动吸收红利——seed 的反驳成立。

方案 C 核心论证我独立复核后认可：
- **5 维度验证地图/路由是"纯 §引用触发型查表"**：全项目仅 2 处 §引用触发（必查表移动项 + L152 移动裁定嵌入），无任何"被动吸收做环境渲染"的调用；30 个子区域精确邻接矩阵（60+ 条拓扑）不可能凭印象记住，必须显式查表——位置靠前的被动吸收便利几乎零价值
- **与 A 区真被动吸收型对比**：温度分层（±1 档浮动即可）/势力首领（粗粒度印象）允许近似；地图拓扑和 step 计算是硬规则不允许错
- **方案 C 内容**：[地图总表]+[跨区移动路由规则] 迁 D 区与 [子地点列表] 聚合（三表总跨 <70 行，F→N 降级）；A 区机制链完整保留；补丁 1（2 处引用加"三表聚合于本字段末端"位置 hint）；补丁 2（A 区腾出 ~200 行空窗内联温度-压力分类速查卡，变现被动吸收红利）

**采纳 5：DEC-K 必查表拆分（8→2+6）**

8 项中仅 2 项每轮必做（温度查表/Day 消耗格式），6 项玩家触发。拆分后每轮省 5-6 次"判断不匹配"的 if 扫描注意力开销。嵌入句保留完整 `Extra Details §[xxx]` 锚点前缀。

### 9.3 质疑项（3 项，采纳但降级为假设）

| seed 论据 | 我的质疑 | 处置 |
|----------|---------|------|
| 召回率"测试结论"（位置 0-20%→99-100%，20-80%→97-98% 等） | **无来源引用，数字伪精确**。定性趋势（首因/近因高、中部低）符合注意力机制常识，但具体百分比不可当作实测值 | 采纳定性结论，数字标注为"假设性估计"，待运行时验证 |
| 拼装位置计算用 1-2=19750 字符 | **旧快照**。EFF-B 后 1-2=14524，1-3=33833。1-3 起始位置实际更靠前（约 33% 而非 37%），方案 C 的召回损失估算略偏悲观 | 定性结论不变，数字待校准 |
| 净收益量化（每轮 -209 token，提速 4.2%） | 全链路为估算推导，无实测支撑 | 标注为估算，列入 §七运行时验证 |

**澄清（无冲突）**：seed 撤回其"引用路径进一步简化"建议（DEC-L），理由是字段锚点名（Extra Details 等）不可省略——**与已实施的 EFF-C 无冲突**：EFF-C 保留字段锚点名，只删了角色前缀"World Master（本角色）的"。seed 核验"38 处已用最短合法锚点"即 EFF-C 后状态，双方结论一致。

### 9.4 修订后优先级（3 批次 + P3）

> **⚠️ 本表已被修订 2（平台加载机制再评估）整体取代**，保留仅作演进记录。终局状态见 §十一.3 与 §十二：
> - 批次 1a（DEC-J）→ 缩水版已实施（砍补丁 2）
> - 批次 1b（DEC-K）→ **不做**（收回）
> - 批次 2（DEC-H）→ 已实施，但方案从 seed 的"语义化 B+"改为用户裁定的"机械 M6u"
> - 批次 3（DEC-I）→ **挂起**（降级）
> - P3（1-1 L32 APD 扩写）→ 仍有效待做

| 优先级 | 优化项 | 收益指数 | 回退风险 | DEC 依赖 | 说明 |
|--------|--------|---------|---------|---------|------|
| **P0 批次 1a** | DEC-J 方案 C：移动三表反向聚合 + 补丁 1（位置 hint）+ 补丁 2（A 区速查卡变现） | 9.5/10 | 0（不碰 A 区机制链） | DEC-J | 移动 F 级跳跃↓90%（F→N）；补丁 2 每轮变现保暖/压力加速 |
| **P0 批次 1b** | DEC-K 必查表拆分：8→2 默认 + 6 嵌入裁定 section 首句 | 7.8/10 | 0 | DEC-K | 每轮省 5-6 次 if 扫描；与 1a 无互斥可并行 |
| **P1 批次 2** | DEC-H 方案 B+：Seed 简化为 `(Day末两位 + Turn末两位 + Offset) 末两位`，大小关系为默认倾向锚点，语义极端允许偏离需叙事自洽 | 8.5/10 | 中（放弃绝对机械随机性） | DEC-H | 消除最大单点推理密度瓶颈；PC 耗时↓40-50% |
| **P2 批次 3** | DEC-I 搜刮速查卡：仅分类直觉（Tier 定义/噪音 3 档语义/标签 6 类产出感），0 精确数值，末尾显式"精确数值以 1-3 为唯一权威" | 6.5/10 | 低中 | DEC-I | 搜刮减少 1-2 次 X 级跳转 |
| **P3 格式** | 1-1 L32 `APD` 扩写为 `Additional Personality Details` | 1.5/10 | 0 | 无 | 消除未声明缩写，与 L13 用词一致 |
| P4 不做 | 敌对 4 维评分合并 | 3.0/10 | 高 | - | §9.2 采纳 3 证明实际 2-3 步，收益低风险高 |

### 9.5 修订后 DEC 清单

| 编号 | 内容 | 建议裁定方向 |
|------|------|------------|
| **DEC-H** | PC 算术语义化 | **方案 B+**：去乘法保留加法锚点；保留 7 字段结构化行；删除原算术语义校验句（简化后无精确算术，安全网不再需要）；同步在 run_audit 弃用术语枚举追加 `Day×7`/`×3`/`mod 100` |
| **DEC-I** | 速查卡边界 | **通过，边界 = 分类直觉 + 0 精确数值 + 显式冲突解决句**（"精确数值以 1-3 §[xx] 为唯一权威"）。速查卡定位为决策辅助，不属"承载数据定义"，不违反 PB §1.1.2 三层分工 |
| **DEC-J** | 移动三表聚合 | **方案 C + 2 补丁**（反向聚合；明确否定方案 A 子地点迁回 A 区 = 回退 OPT-P2-04）；同步更新 1-3 L1 字段声明加执行链路近邻化说明 |
| **DEC-K** | 必查表拆分 | **通过**：2 默认项保留 + 6 触发项嵌入各自裁定 section 首句；嵌入句保留完整字段锚点前缀 |
| ~~DEC-L~~ | ~~引用路径简化~~ | seed 自行撤回；与 EFF-C 无冲突，无需行动 |

### 9.6 修订后待验证项（合并 §七）

原 §七 5 项全部保留，追加 2 项：

6. **DEC-J 召回损失对冲效果**：实施后采样 30 次移动裁定，统计 Route 正确率（Steps 算错/子区域漏写/邻接关系错）。错误率 >5% 则补丁 1 不足，需在 D 区开头加显式注释"本 D 区尾部 = 移动裁定三表聚合块"
7. **每轮必做项 vs 触发项的实测耗时占比**：对 20 轮 WM 输出采样"推理耗时/总字符/PC 次数/移动搜刮轮次标记"，验证 §9.2 采纳 2 的累计负载排序是否成立

### 9.7 审计结论

1. **seed 评估整体质量高**：6 项事实声明全部核验成立；3 处纠正了我原文档的事实错误；"触发频率权重"和"§触发型 vs 被动吸收型"两个分析维度是我原文档的真实盲区
2. **需保留的批判性态度**：召回率百分比和 token 量化数字无实测来源，只能作为方向性参考；拼装位置计算基于旧字符快照
3. **架构层面共识**（seed §九.4 与我独立判断一致）：不做大范围结构性冒险，保留三层分工 SSOT 框架，用"物理就近 + 热内联分类直觉 + 显式索引 hint"做针对性补丁
4. **实施前置**：批次 1a/1b 零回退风险可先行；批次 2/3 建议在 DEC 裁定 + 批次 1 验证后实施

---

## 十、DEC-H 裁定与实施记录（M6u 加法查表版）

> **裁定时间**：2026-08-14。**用户否决语义化（方案 B+），经 M4→M5→M6→M6s→M6u 迭代裁定最终方案**。

### 10.1 方案迭代史（关键否决点）

| 方案 | 公式 | 否决/采纳理由 |
|------|------|--------------|
| 原公式 | `((Day×7)+(Turn末两位×3)+Offset) mod 100` | 3 层反 LLM：字符串截取+多位乘法+取模歧义，综合错误率 ~30-35% |
| B+ 语义化 | Seed 作倾向锚点 + 语义判断 | **用户否决**：不想用语义 |
| M3 强制竖式 | 原公式 + CoT 展开 | 每次多 30-50 token，耗时反升，违背效率目标 |
| M4 加法化 | `(Day末两位+Turn末两位+Offset)末两位` | **用户否决**：斜率 1 → 当天内 Seed 等差递增，长连续块，等于没有随机 |
| M5 位交换 | Turn 末两位十位个位交换后相加 | 10 轮内有交替但模式仍规律 |
| M6 双表查表 | `Day末两位 + 表A[个位] + 表B[十位] + Offset` | 随机性最优但占 2 张表 |
| M6s 单表双查 | 同一表查个位+十位 | 十位当天内近似常数，双查冗余 |
| **M6u（采纳）** | `(Day末两位 + 表[Turn个位] + Offset) 的末两位` | 单表单查；预生成随机数驱动当天内无规律跳变 |

**M6u 核心原理**：随机性来源 = 预生成随机数表（经典随机数表原理），非公式斜率。当天内 Turn 个位逐轮 +1 → 逐次取表中**无规律的下一个预随机值** → Seed 当天内跳变无周期性。

**已知特性（用户知情接受）**：表 10 项 → Seed 序列周期 10，当天内 T2/T12/T22 同值。缓解：每次 Threshold 由 Base+Modifiers 构成（暴露/天气/行为不同 → Threshold 不同），Seed 重复 ≠ Result 重复。

### 10.2 实施内容（已完成）

1. [1-3 L579](file:///d:/Mycode/AshHarbor_OD_Trae/Ashport_Ourdream/Prompt_File/1-3_WorldMaster_Extra_Details.md#L579)（[Probability Check 格式模板]）：
   - 公式替换为 `Seed = (Day 末两位 + 表[Turn 个位] + Event Offset) 的末两位`
   - 新增查表行：`0->07；1->13；2->29；3->41；4->53；5->67；6->71；7->83；8->97；9->05`（前导 0 按个位数值用）
   - 保留 OPT-P2-03 算术语义校验句（加法也可能错，安全网继续有效）
   - 保留七字段结构化行（Base/Modifiers/Final/Seed/Threshold/Result/Outcome）
2. [1-3 L674] 示例行更新：`D83-T502 → 表[2]=29 → (83+29+37) 的末两位 = 149 -> 49`
3. [enum_registry.json](file:///d:/Mycode/AshHarbor_OD_Trae/Ashport_Ourdream/tools/enum_registry.json) term_pairs 追加弃用术语对：`末两位 × 7` / `末两位 × 3` / `mod 100` / `× 7) +` / `× 3) +`（精确匹配防回退，避免误伤 `罐头×3` 类数量表示）
4. **验证**：run_audit 4/4 全绿（7 term pairs 扫描 0 issues = 全项目旧公式无残留）

### 10.3 效果评估

| 维度 | 原公式 | M6u |
|------|--------|-----|
| LLM 操作 | 字符串截取 + 2 次多位乘法 + 3 项加法 + mod | 拆 1 位 + 查 1 次表 + 3 项加法 + 取末两位 |
| 乘法错误（~20%） | 有 | **归零** |
| mod 歧义（~10%） | 有 | 归零（"取末两位"与查表行为同构） |
| 预估综合错误率 | ~30-35% | **~8-10%**（只剩加法进位） |
| 当天内随机性 | 斜率 3 线性（伪） | 预随机表驱动（无规律跳变） |
| 确定性 | 100% | 100% |
| 新增行数 | - | +2 行（公式行 + 查表行） |

---

## 十一、DEC-J 缩水版裁定与实施记录

> **裁定时间**：2026-08-14。**基于平台加载机制再评估**（用户质疑触发），对 seed 方案 C 砍掉补丁 2，仅实施核心迁移 + 补丁 1。

### 11.1 再评估的两个关键修正（相对 seed 方案）

**修正 1：迁移方向是"谷底→强区"，比 seed 声称的更安全**

平台装配实算（0-x + 1-1 + 1-2 ≈ 24252 字符在前，1-3 起始于全提示词 ~42%）：
- [地图总表] 原位置（A 区 L83）≈ 全提示词 **47.7%——U 型注意力谷底**
- 新位置（D 区尾部，[子地点列表] 前）≈ **~91%——近因强区，紧邻对话历史**

seed 声称"召回 98%→92% 损失"的曲线方向与已发表 lost-in-the-middle 研究相反且无来源。按 U 型曲线，本次迁移**大概率是改善而非损失**。

**修正 2：砍掉补丁 2（A 区速查卡）的两个理由**

1. seed 称 A 区空窗是"首因红利延伸区"——**错误**。装配提示词的首因区是 0-1/1-1（0-19%），A 区空窗在 ~47%（注意力谷底），被动吸收恰恰最弱
2. 温度-压力分类速查卡 = 与 1-3 权威表**双定义结构**。本项目已有两次 SSOT 事故前科（OPT-P2-02 同名 section、N-3 药品漏改），不应主动再造一个

### 11.2 实施内容（已完成）

| 子项 | 内容 | 状态 |
|------|------|------|
| 核心迁移 | [地图总表]（47 行）+ [跨区移动路由规则]（30 行）从 A 区 L83/L129 迁至 D 区 [子地点列表] 前（L679/L727），三表聚合总跨 <80 行（N 级） | ✅ |
| 补丁 1 | [1-2 L25](file:///d:/Mycode/AshHarbor_OD_Trae/Ashport_Ourdream/Prompt_File/1-2_WorldMaster_Additional_Personality_Details.md#L25)（必查表移动项）+ [L151](file:///d:/Mycode/AshHarbor_OD_Trae/Ashport_Ourdream/Prompt_File/1-2_WorldMaster_Additional_Personality_Details.md#L151)（2a 移动裁定）追加"（与 §[xx] 三表聚合于本字段末端）"位置 hint | ✅ |
| L1 声明 | 1-3 L1 字段声明追加"注意：为降低移动裁定链路检索成本，[地图总表] 与 [跨区移动路由规则] 与 [子地点列表] 按执行链路聚合于 D 区尾部" | ✅ |
| ~~补丁 2~~ | ~~A 区空窗插温度-压力速查卡~~ | ❌ **砍掉**（见 11.1 修正 2） |

**迁移后结构**：
- A 区：[季节锚点] → [温度分层×2] → [天气矩阵] → [势力首领] → …（机制链连续，无查表数据打断）
- D 区尾部：[感官词锚点] → [地图总表] → [跨区移动路由规则] → [子地点列表]（移动裁定三表聚合）

**验证**：run_audit 4/4 全绿（引用活性 0 断链——§锚点名未变，与位置无关；SSOT/术语/格式 0 issues）。1-3 字符 33887→33964（+77，来自 L1 声明 + 补丁空行）。

### 11.3 与 DEC-K / DEC-I 的裁定状态（同轮再评估）

| DEC | 结论 | 理由 |
|-----|------|------|
| DEC-K（必查表拆分） | **不做（收回此前采纳）** | [必查表] 8 行 ≈ 240 字符，占 30K token 输入 0.8%，if 扫描节省可忽略；它位于装配 ~17.5% 前部强注意力区，是紧凑合规清单，拆掉损失大于收益 |
| DEC-I（搜刮速查卡） | **不做/挂起（降级）** | 收益基于被高估的跳转成本隐喻；Tier/噪音/标签第二定义点 = SSOT 双定义风险（有 N-3 前科）。待运行时证据（搜刮裁定档位错误频发）再考虑 |

---

## 十二、效率优化批次总收尾（修订 2：平台加载机制再评估）

> **触发**：用户对"调整后的收益/功能损失风险"提出担心，要求以平台加载机制重新全面评估 DEC 各项。
>
> **方法修正**：本轮先审计了成本模型本身——此前分析（含我原文档与 seed 评估）大量借用"人类/计算机导航隐喻"（跳转/滚动/中断-返回），需还原为 LLM 真实处理模型后再评估。

### 12.1 平台加载机制的四个本质特征（成本模型地基）

1. **无导航成本**：WM 发言时装配为一段连续文本（0-1→…→1-3→对话历史），LLM 用**并行注意力**处理。"跳转 5 次"是隐喻；真实成本只有**检索是否命中**（注意力是否落到目标 section）+ **局部连贯性**（相邻文本权重更高）。
2. **U 型注意力曲线**（lost in the middle，有实证文献）：首尾强、中部弱。seed 给出的"中部 97-98%、80-95% 降至 92%"曲线**方向与已发表研究相反且无来源**，已降级为不可信。
3. **装配位置实算**（0-x+1-1+1-2 ≈ 24252 字符在前，1-3 起始于 ~42%）：A 区 ≈ 42-63% = **注意力谷底**；D 区尾部 ≈ ~91-94% = **近因强区**（紧邻对话历史）。
4. **响应延迟构成**：输出侧（正文 + 五段结构 ≈ 每轮 300-700 输出 token 的逐 token 生成）很可能是大头；输入侧压缩 1-2K 字对总延迟贡献有限——**EFF 字数批次与链路批次的提速预期都应据此下调**。

### 12.2 DEC 终局裁定与理由

| DEC | 终局 | 一句话理由 |
|-----|------|-----------|
| **DEC-H（PC 算术）** | ✅ 已实施 **M6u** | 唯一有实证支撑的成本模型（LLM 乘法/取模弱项）；方案经 M4→M5→M6→M6s→M6u 五轮迭代，用户否决语义化与"当天无随机"缺陷，最终以预生成随机数表实现机械随机 |
| **DEC-J（三表聚合）** | ✅ 已实施**缩水版** | 迁移方向恰是谷底→近因强区（比 seed 声称的更安全）；砍补丁 2（速查卡落点在谷底 + SSOT 双定义前科） |
| **DEC-K（必查表拆分）** | ❌ 不做 | 240 字符清单在 30K token 中 if 扫描开销可忽略；作为前部（~17.5%）紧凑合规清单的价值更高 |
| **DEC-I（搜刮速查卡）** | ⏸️ 挂起 | 收益基于隐喻成本模型；双定义风险有前科；待运行时证据 |

### 12.3 方法论沉淀（三轮演进的核心教训）

1. **隐喻成本模型的陷阱**：早期分析（我原文档与 seed 评估均如此）用"跳转/导航/中断"框架量化收益，得出 token 级精确数字——看似严谨实则虚。修订 2 用"并行注意力 + U 型曲线 + 输出侧延迟"三个可核实机制替换后，多项结论反转。
2. **数字伪精确的鉴别标准**：无来源的召回率百分比（99%/97%/92%）、无实测的 token 节省（-209/轮）、旧快照的位置计算（1-2=19750 字符）——凡不可核验的量化一律降级为方向性参考。
3. **功能安全的三重底线**（用户担心的直接回应）：语义零改动（纯位置/格式）、run_audit 机械验证（4/4 全绿）、git 单 revert 可回滚——满足此三条的改动最坏情况是"偶发可观测的质量波动"（如 Route 偶错），非系统性功能损坏。
4. **双定义结构零容忍**：本项目 SSOT 事故两次前科（OPT-P2-02 同名 section、N-3 药品漏改）。任何"速查卡/摘要/内联副本"方案默认视为双定义温床，除非有压倒性运行时证据。

### 12.4 遗留与下一步

| 项 | 内容 | 触发条件 |
|----|------|---------|
| P3 格式修复 | 1-1 L32 `APD` 扩写为 `Additional Personality Details` | 随时（零风险，5 分钟） |
| 运行时验证 §七 1-5 + §9.6 6-7 | 七项假设待真实数据 | 平台实际运行 3-5 轮后采样 |
| **输出侧延迟观测**（新增，最高优先） | 记录 WM 每轮响应时间 vs 输出长度；若强相关则输出侧是主因 | 下一轮平台测试时同步做 |
| DEC-I 复议 | 搜刮速查卡 | 运行时证据显示搜刮裁定档位错误频发时 |
| WSK 侧优化批次 | 2-2 内联跨位转移 + "WSK 不维护容器重量"规则迁移 | WM 运行验证稳定后立项 |
