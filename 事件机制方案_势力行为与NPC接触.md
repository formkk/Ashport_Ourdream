# 事件机制方案（v2：势力行为档案 + 散人 NPC 接触）

> 创建日期：2026-08-16（v1 阶梯统一方案已于同日实施为 CHANGELOG v1.65；本方案为 v2 范式升级）
> 状态：**方案定稿**（全部 8 项 DEC 已裁定；经两轮自审 + seed 第三方评估吸收，待实施）
> 演进：v1（阶梯统一，已落地）→ 讨论中发现范式问题 → v2（Event 机制，本方案）

---

## 一、范式诊断：为什么 v1 仍是中间形态

### 1.1 用户洞察（本方案的出发点）

> "敌对机制，实际上是个事件，是这个世界在动的表现，也是 LLM CHATROOM RPG 最吸引人最核心的功能。所以这根本是个 Event。各势力对玩家的行为，出于势力本身的定位和行为规律，应该不同。"

| 维度 | v1（玩家视角威胁检测） | v2（世界视角势力行为） |
|---|---|---|
| 核心问题 | 对我有没有危险 | 他们在过自己的日子，我撞进了他们的活动 |
| 产出体验 | 遭遇感 | 世界感 |
| 概率角色 | 每轮判"威胁是否升级" | 只判"撞见"；撞见后走行为分支 |
| 势力差异 | 修正项数字 | 反应菜单内容 |

### 1.2 v1 遗留的三个痛点（v2 全部解掉）

1. **概率计算污染上下文**：掷骰收缩到只剩"撞见"一掷（统一表现成）；反应分支是确定性映射，不需要掷骰。Probability Check 使用频率大降。
2. **LLM 漂移**：菜单分支预定义，WM 只做模式匹配（玩家弱不弱/有没有货/在不在禁区），不做自由推理。防漂移效果与"查表"等价但保留语义。
3. **三重门槛检查负担**（被接触+动机+成本可控）：内化为菜单分支条件（如"玩家武装强 → 绕开"分支就是"成本不可控所以不动手"的预制答案）。护栏从检查清单变成结构本身。

---

## 二、v2 架构：三层复用，只补一块

| 层 | 载体（现状） | v2 角色 | 改动量 |
|---|---|---|---|
| 撞见层 | 1-3 [区域-势力-事件-概率统一表]（13 行，已有） | 势力在某时某地的活动 + 撞见概率 | **零改动**（现成） |
| 行为层 | **[势力行为档案]（新增）** | 撞见后按势力定位的反应菜单 | **新建**（唯一缺口） |
| 记账层 | 1-3/2-3 [敌对阶梯] 9 档（v1.65 已统一） | 动作词汇表：叙事信号 + WSK 记账语言 | **瘦身**（删概率分组表） |

### 2.1 撞见层：触发流程（已定稿）

**触发源分两档，全部复用现成硬触发，零新增算术状态：**

| 档 | 触发条件 | 机制依据 | 频率 |
|---|---|---|---|
| **基础档（本方案实施）** | ① [Move] 成立（跨 Zone/Sub-zone）当轮掷；② Day 推进（读 [主要状态] Day 值 != 上轮）掷一次 | [Move] 每轮必出 + 1-2 L67 Day 推进检测（相等比较，零算术，据点消耗共用此触发点） | 每次移动 1 次 + 每日约 1 次 |
| **加密档（预留，不实施）** | + Turn 个位 ∈ {0,3,6,9} 且本轮无 [Move] | 同 Probability Check Seed 查表先例（读个位+集合匹配，无除法） | 每 3 轮 1 次 |

**完整触发流（基础档）：**

```
1. [Move] 成立（跨 Zone/Sub-zone）或 Day 推进
2. 选行收窄（恒定 2 掷）：① WM 按叙事合理性从当前区域行中选 1 行势力行（时间/地点/近期剧情贴合度——WM 本来就要做的叙事判断，零额外成本）；② 散人行固定掷 npc-contact（全图行，无需选行）
3. 对选中势力行掷 world-event + 散人行掷 npc-contact；每轮掷骰次数恒定 2，禁止对同区多行各掷
4. 掷中敌对行（L2/L3 涉敌对）-> 必查表核对最近 [State Update] Inventory State -> 匹配 [势力行为档案] 分支；L1 行仅氛围渲染，不入行为档案、不入 9 档
5. 掷中 NPC 接触行 -> 按接触类型表（§2.4）选择接触类型 -> 走接触菜单
6. 分支动作叙事化（一轮一档）-> WSK 提取档位/关系/临时NPC 入账
7. 每轮至多 1 个敌对遭遇（多势力同轮撞见留给 WM 叙事裁量）；NPC 接触可与势力事件同轮并存
8. 未触发 -> 输出压缩记号（Dase，见 §2.5），可选 signs 级氛围渲染（不入账，不构成档位信号）；同区域同类事件上轮已触发时，本轮该行默认跳过
9. 同轮多触发源（换区 + Day 推进叠加）合并为一次掷，以换区侧区域行为准
```

**T3 跨角色依赖（已定）**：WM 判行为分支前核对最近 [State Update] Inventory State（玩家有无货/武装可见性）——复用 1-1 L7 据点描写前核对 Base Structure State 的同款机制，加入必查表。
**核对深度（粗匹配，防算术漂移）**：只做档位化粗判断——① 武器条目数量与火力类型（长枪/短枪 ≥2 = 武装强）；② 弹药条目合计发数档位（≥30 = 有战斗力）；③ 食品/饮水条目是否满载感（条目 8+ 或含 2kg+ 大物资 = 显眼有货）。不要求精确合计 kg，不做算术运算。

### 2.2 行为层：势力行为档案（核心新增）

每势力一条档案，四个字段。

**菜单语法规范（R1，硬约束）**：菜单只写 NPC 侧动作序列；玩家反应节点一律用占位符 `（玩家反应）` 标注，NPC 后续动作以"视玩家反应"分支形式挂在占位符之后——WM 不得替玩家选择反应分支，只按玩家实际输入推进。禁止在菜单中出现预设玩家决定（如"拒绝则 robbed"裸写形式）。

**R2（匹配失败退化）**：玩家状态同时不匹配所有分支时，按保守原则走该势力菜单中风险最低的分支（弱势力默认绕开；强势力默认远距观察后不接触）；禁止为匹配分支脑补玩家未在叙事或 Inventory State 体现的状态。

**R3（词汇双轨）**：菜单箭头两侧必须是 9 档合法档名（none/signs/observed/followed/probed/blocked/robbed/violent/lethal）或明确的 NPC 动作短语（堵路索要/盘查证件/绕开观察/领地驱赶）；不得写含玩家结论的词（玩家结果结论只进 [判定] 段）。

**分支条件合法锚点（显式读取，禁自由回忆）**：
- 玩家状态（弱小/武装强/有货）：最近 [State Update] Inventory State（粗匹配，见 §2.1 核对深度）
- 报复链活跃：最近 [State Update] Relationship & Threat 条目明确写"报复中/追踪中"，或近五日主要事件明确出现报复链成立字样；5 日前旧事件或模糊对话回忆不构成依据
- 跨轮衔接（上轮推进到哪）：唯一合法锚点 = 最近 [State Update] Relationship & Threat 的 HTS 档位（上轮 followed → 本轮按分支推进 blocked；上轮 robbed/violent → 本事件收尾或开新事件，不复用同分支）；不依赖 WM 对上文的模糊回忆

```
[势力行为档案]（示例：劫掠者兄弟会）

活动圈：全图流窜；偏好夜间干道/空置建筑/据点周边
注意信号：火光/枪声/照明 +15~20；显眼搬运 +10~15；血迹拖痕 +10
反应菜单（按玩家状态选分支，一轮只推进一档；箭头两侧均为 NPC 动作，玩家节点用占位符）：
  A 玩家弱小 + 有货     -> followed（尾随评估，保持距离观察）-> 下轮 blocked（堵路索要）-> （玩家反应）-> 视反应：交出财物 = robbed 收尾 / 逃跑或反抗 = violent 升级（烈度用叙事信号对比：玩家可见武器数/火力 vs 对方人数/可见武器，占优方压制入叙事；无法判断默认降一档）
  B 玩家武装强/人多      -> observed 后绕开，不接触（本轮即收束，无后续节点）
  C 玩家近其赃物点/营地  -> 直接 blocked（领地防御，跳过试探）-> （玩家反应）-> 视反应：撤离 = 收束 / 停留 = violent（默认比 A 分支高一档，优先堵路/推搡/驱赶叙事；对方先动手且致命反击明确才升 lethal）
  D 报复链活跃           -> 跳过全部试探，直接 violent/lethal 倾向（具体档位按报复链烈度）
免接触条件：白天人流密集公共区 / 附近有势力巡逻（满足则本分支整轮跳过，输出 signs 级氛围）
```

六势力差异化预览（数据源 = 0-1 阵营章节定性转译）：

| 势力 | 默认分支特征 | 菜单基调 |
|---|---|---|
| 劫掠者兄弟会 | 掠夺优先 | 试探→索要→动手（如上例） |
| 水源商会巡逻 | 秩序优先 | probed（盘查）→ 依配合度收尾；反抗才 blocked |
| 码头帮 | 地盘敏感 | 领地内 probed→blocked；领地外 observed 绕开 |
| 煤矿队 | 护产优先 | 近煤路/煤车才 C 类领地反应；否则互不接触 |
| 东北农场 | 防御保守 | 运粮线武装护卫；遭遇玩家默认 observed 互绕 |
| 拾荒者 | 回避优先 | A：绕开；仅 5 人以上武装队可能 blocked |

### 2.5 Dase 记号表（未触发掷骰的压缩输出）

**定位**：例行掷骰未触发时用压缩记号替代完整概率块——下轮可读（WM 自锚定）、上下文省（~130 字符 → 6 字符）。触发侧仍输出完整块（算式外化防漂移 + 玩家可验证，已裁定保留）。

**枚举定义**：

| 记号 | 含义 | 对应掷骰 |
|---|---|---|
| `Dase-1` | world-event 撞见掷未触发 | 触发流第 3 步 ① |
| `Dase-2` | npc-contact 接触掷未触发 | 触发流第 3 步 ② |
| `Dase-3` | world-event 触发（势力事件成立，走行为档案，细节见完整块与叙事） | 触发流第 3 步 ① |
| `Dase-4` | npc-contact 触发（接触成立，类型见叙事） | 触发流第 3 步 ② |
| `Dase-5` | （预留）加密档停留掷未触发 | 触发流加密档，暂不使用 |

**输出规则**：
- 位置：[Probability Check] 块内，与触发的完整块共存。示例：`[Probability Check] Dase-1；Dase-2`（双未触发）；`[Probability Check] Dase-2` + 完整 world-event 块（一触一未）
- 每记号恒定 1 行不展开；触发与未触发不同记号混排用 `；` 分隔
- 记号仅用于"撞见层"例行掷（world-event / npc-contact）；其他概率类（获得超规格、第三方目击、交易变数、据点天气损耗）**不适用**——它们本就低频，维持完整块
- WM 读档自锚定：上轮 `Dase-1` = 该区域势力行掷过未中；配合触发流第 8 步（同区域同类上轮已触发默认跳过）

**命名风格（已定）**：数字编号族（`Dase-N`）+ 表内一行定义。理由：① 与项目既有标签风格一致（[Move]/[判定] 皆短码）；② 语义后缀族（`Dase-hit`/`Dase-miss-w`）更长且中英混排；③ 扩码只加表行不动旧码。枚举表为唯一权威定义处，prompt 落地于 1-3 [Probability Check 格式模板] 新增小节，1-1 L34 补一句"例行撞见掷未触发时用 Dase 记号压缩输出"。

**signs 级氛围硬护栏（DEC-V2-4 选 B 的附加条件，双向闭环）**：
- **WM 侧三类定义**（写入 DEC-V2-4 落地文本）：signs 级氛围仅限 ① 未见其人先闻其声（远处狗叫/玻璃碎裂/脚步）；② 纯环境痕迹（烟头/被踩过的脚印/丢弃包装/涂鸦）；③ 远距离不可辨识身影（看不清脸/标志/武器/意图）。出现可辨认身份/阵营/动作意图/目光接触即不再是 signs 级，必须升级 HTS=observed 并走行为档案。
- **WSK 侧提取禁令**（2-3 [提取纪律] 新增 item 13——这是 WSK 侧唯一改动）：`13. signs 级氛围（未见其人先闻其声/纯环境痕迹/远距不可辨识身影三类）= 明确不入账信号，不产生 HTS/Relationship/临时NPC 任何条目，提取时完全忽略。`

### 2.4 散人 NPC 接触事件（新增，末日人际层）

**定位**：非势力散人（幸存者/流民/独行者）对玩家的主动接触——世界在动的另一面。也是**临时NPC 的概率化出场渠道**（现 1-2 [临时NPC生命周期] 依赖 WM 自由叙事出场，本表补上触发机制）。

**撞见行（统一表扩展 2 行）**：

| 区域 | 势力 | 事件 | Base | 时段偏移 | 相关性 |
|---|---|---|---|---|---|
| 全图（荒废区/干道为主） | 散人NPC | 求助/问询接触 | 20 | 白天+5 / 夜间−5 | L2 |
| 全图（Storm/夜间/寒期加权） | 散人NPC | 庇护求助 | 15 | 夜间+10 | L2-L3 |

Event Class = `npc-contact`（偏移建议 61，与现有 0/37/71/53/19 不冲突）。

**接触类型表（掷中后 WM 按叙事上下文选择类型，不二次掷骰）**：

| 类型 | 场景倾向 | NPC 行为模式 | 玩家反应分支 |
|---|---|---|---|
| 问消息/打听 | 路口/补给点/市场 | 谨慎接近，交换信息 | 交换 / 敷衍 / 驱赶 |
| 找人 | 任意 | 描述特征求确认 | 告知 / 否认 / 收酬帮找 |
| 医疗求助 | 玩家有可见医疗行为 | 恳求，提出交换物 | 救助 / 拒绝 / 有偿 |
| 物资求助 | 恶劣天气/饥荒期 | 讨要或以物易物 | 给予 / 交换 / 拒绝 |
| 庇护求助 | 夜间/Storm/失温风险 | 请求过夜/借火 | 收留 / 拒绝 / 附条件收留 |
| 警示/示好 | 玩家刚脱离危险区 | 主动提醒危险/势力动向 | 免费 / 换取报酬 |

**陷阱变体（真伪由 WM 叙事裁定，不掷骰）**：任一求助类型可为伪装（诱饵/踩点）。注意信号齐全时警惕：该区域劫掠者活跃、玩家显眼、报复链活跃、来人过度殷勤。陷阱揭穿走 [势力行为档案] 劫掠者分支记账（probed->blocked->robbed）。

**记账联动（全部走现有机制，零新增）**：
- 接触 NPC 具名 -> 1-2 [临时NPC生命周期]（名字/身份锚点/记忆一致性）
- 帮助 -> Relationship 上升（2-2 [关系记录规则]）；求助 NPC 知晓玩家部分信息 -> 知情范围 party-known 起
- 拒绝 -> 无账或轻微下降；陷阱 -> 9 档 + 报复链照常
- 交换物/酬劳 -> [判定] 获得/消耗行 -> WSK Delta 照常提取

**失败出口（吸收自第三方评估）**：
- **未具名接触**：接触结束仍未具名（无姓名/无稳定身份锚点）-> 不生成 Relationship 条目、不入 [临时NPC生命周期]；知情范围按 party-known 起（当事人已知），视为一次性角色；交换物/酬劳/救助等非关系类账目照常提取
- **陷阱揭穿但身份不明**：对方逃离或无法确认归属 -> 仅 HTS 升对应档（probed/blocked/violent 依叙事信号）+ 知情范围 local-only；不生成 Relationship、不触发报复链、不强归属势力（除非叙事有明确证据：劫掠者标志/口号/专属武装）

**多人同行强度调整（结构化，不做计数）**：队伍 ≥2 人时——① 医疗/庇护求助触发倾向显著下降；② 陷阱倾向默认为伪概率更低（对方不敢对多人设套）；③ 分支推进强度默认向下 1 档（散人更谨慎，敌对更倾向绕开或低烈度）。

### 2.3 记账层：[敌对阶梯] 瘦身

- **保留**：9 档定义（叙事信号描述，与 WSK 2-3 1:1 同名）——它是菜单动作的词汇表
- **删除**：概率触发分组表、4 维映射行、hostile-contact 触发前提行（职责全部转移给行为档案 + 撞见层）
- 分支推进产生的档位变化由 WSK 照常从叙事提取

---

## 三、待裁定决策点

| DEC | 议题 | 选项 | 裁定 |
|---|---|---|---|
| ~~DEC-V2-3~~ | ~~停留重掷节奏~~ | **已裁定：基础档（换区 + Day 推进）**；加密档预留不实施 | 见 §2.1 |
| ~~DEC-V2-1~~ | ~~菜单分支粒度~~ | **已裁定：每势力 3-5 分支**——太多则 WM 匹配负担回归；旧仇/剧情等极端情形留 WM 叙事裁量，菜单只管默认行为 | 2026-08-16 用户确认 |
| ~~DEC-V2-2~~ | ~~分支内推进节奏~~ | **已裁定：一轮一档**——给玩家反应窗口，与 9 档记账及 HTS 衔接锚点自然对齐 | 2026-08-16 用户确认 |
| ~~DEC-V2-4~~ | ~~未触发侧输出~~ | **已裁定：B + 硬护栏**——signs 三类定义（WM 侧）+ WSK 提取禁令 item 13（见 §2.5），双向闭环 | 2026-08-16 用户确认 |
| ~~DEC-V2-5~~ | ~~npc-contact 触发频率校准~~ | **已裁定：Base 20**——常见但不滥；庇护求助 15 + 夜间+10 已单独校准 | 2026-08-16 用户确认 |
| ~~DEC-V2-6~~ | ~~接触类型选择方式~~ | **已裁定：WM 按叙事上下文选**——避免掷骰嵌套；类型与场景强相关，掷骰错配 | 2026-08-16 用户确认 |
| ~~DEC-V2-7~~ | ~~陷阱变体真伪判定~~ | **已裁定：WM 叙事裁定**——真伪是叙事质量非概率问题；注意信号清单已给判断依据 | 2026-08-16 用户确认 |
| ~~DEC-V2-8~~ | ~~Dase 记号命名风格~~ | **已裁定：数字编号族（Dase-N）+ 枚举表唯一权威定义** | 见 §2.5 |

**全部 8 项 DEC 已裁定完毕。方案定稿，可进入实施（按 §四 Commit 0-5 顺序展开 diff）。**

---

## 四、实施清单（待 DEC 裁定后展开为 diff）

| 文件 | 改动 |
|---|---|
| 1-3 | 新增 [势力行为档案]（6 势力 × 4 字段 + R1/R2/R3 语法 + 分支锚点规则 + 反向归属声明）+ [散人NPC接触事件]（接触类型表 + 陷阱变体 + 失败出口 + 多人调整 + 记账联动）；[敌对阶梯] 删概率分组表瘦身回 9 档定义；统一表 +2 行（散人接触）；偏移表 +`npc-contact: 61`；[Probability Check 格式模板] + Dase 枚举小节 |
| 1-2 | [人类敌对风险机制] 四维"主要来自"清单压缩为行为档案参考；概率裁决补充 item 2/3 删（门槛已内化）；必查表改指行为档案 + "敌对遭遇/NPC接触判分支前核对最近 [State Update] Inventory State（粗匹配）" |
| 1-1 | L34 hostile-contact 门槛句简化 + Dase 记号高层锚点一句 |
| 0-1 | 阵营章节头注：公共法侧归属声明 + 冲突解决句（运行时以 1-3 菜单为准，提交级修复以 0-1 定性为源） |
| 2-3 | [提取纪律] + item 13（signs 级不入账禁令）——**WSK 侧唯一改动** |
| 文档层 | .mdr 1-3 职责补"势力行为档案 / NPC接触 / Dase"；CHANGELOG v1.66 |

**实施顺序（5 个独立 commit，支持单点回滚）**：
- **Commit 0（前置校验）**：1-2 必查表 3 条锚点规则（核对深度/报复链来源/HTS 衔接）+ 2-3 item 13；3 轮静态测试通过后才推进
- **Commit 1（核心功能）**：1-3 新增全部 section（行为档案 + 接触事件 + Dase + 统一表/偏移表扩展）；5 分支菜单命中静态冒烟
- **Commit 2（阶梯瘦身）**：独立 commit——若 WSK 记账漂移只需 revert 此层，核心功能不受影响
- **Commit 3（1-2 改动）** / **Commit 4（1-1 改动）** / **Commit 5（0-1 头注 + 文档层）**

删除清单（表/菜单接管的部分）：
- 1-2 概率裁决补充 item 2（反简化护栏句）、item 3（三重门槛+4维映射）
- 1-3 [敌对阶梯] 分组表 + 映射行 + 前提行
- 1-1 L34 门槛句

**保留不动**：9 档定义（WSK 记账词表）、护栏三条（不立刻开火/致命暴力条件/搜刮常态——管叙事质量不管概率）、偏移表 `hostile-contact: 0`、统一表。

---

## 五、风险与验证

1. **行为档案双拷贝风险（双向归属声明护栏）**：0-1 阵营章节（定性，NPC 卡世界知识底座）与 1-3 行为档案（结构化，WM 执行裁定）是同一实体的两个职责视图——单向声明只能防运行时回读，防不了提交级漂移。护栏为**双向声明**：① 0-1 头注"运行时以 1-3 菜单执行为准，方向级分歧提交级修复以 0-1 定性为源同步对齐"；② 1-3 [势力行为档案] 头注"菜单定性来源为 0-1 阵营章节，方向级不符以 0-1 为唯一权威源修正"。
2. **静态模拟验证点（5 个场景）**：① 劫掠者 A 分支三轮衔接（followed→blocked→视玩家反应），验证 HTS 衔接锚点 + WSK 记账逐轮对齐；② 散人未具名求助 + 玩家给物资，验证不升 Relationship；③ 陷阱揭穿但对方逃离无标志，验证不触发报复链；④ L1 命中不入门（运煤队路过只氛围，HTS 记 none）；⑤ 玩家主动挑衅打破菜单（菜单是默认不是牢笼，9 档照记）。
3. **回滚**：v1.65 与 v2 分 commit；v2 内部再按 §四 Commit 0-5 拆分，任一层出问题单 revert。

---

## 六、第三方评估吸收记录（seed-事件机制方案评估.md，2026-08-16）

**总评**：91/100，7 DEC 建议 6 项全同意 + DEC-V2-4 加护栏后同意。独立核验后：**5 类补充采纳 4 类全量、1 类简化后采纳；另发现评估自身 1 处事实错误已纠正**。

| 评估建议 | 核验结果 | 处置 | 落点 |
|---|---|---|---|
| R2 匹配失败退化出口 | 成立——原方案无默认分支 | ✅ 采纳 | §2.2 |
| R3 词汇双轨（菜单词限 9 档名/NPC 动作短语） | 成立——防菜单词汇漂移 | ✅ 采纳（**纠正其 9 档清单错误**：评估写 `harassed` 档——实际不存在，正确为 signs；已按 [固定取值] 修正） | §2.2 |
| 散人未具名/陷阱身份不明 2 条失败出口 | 成立——记账边界确实悬空 | ✅ 采纳 | §2.4 |
| A/C 分支"信号对比法"替代自由判分 | 成立——最后 2 处 WM 自由判分 | ✅ 采纳 | §2.2 示例 |
| 3 条锚点来源（核对深度/报复链/HTS 衔接） | **最有价值的发现**——HTS 衔接锚点填补跨轮推进的真实空白 | ✅ 采纳 | §2.1/§2.2 |
| DEC-V2-4 signs 双向护栏（WM 三类定义 + WSK item 13） | 成立——T3 双向闭环经典漏项 | ✅ 采纳（**改变"WSK 零改动"结论**：2-3 需 +item 13） | §2.5/DEC 表/§四 |
| 双向 SSOT 归属声明 | 成立——单向声明确实不充分 | ✅ 采纳（声明文本简化，评估原文过冗长） | §五/§四 |
| 多人同行调整 | 部分成立——概率数值调整（Base 1/2、-10~-15）与项目固定值修正风格冲突且引入算术 | ⚠️ 简化采纳——只保留结构化三条（触发倾向降/伪陷阱倾向降/强度降一档），不采纳数值修正 | §2.4 |
| 5-commit 实施顺序 + Step 0 前置校验 | 成立——回滚粒度合理 | ✅ 采纳 | §四 |
| 精确评分 91/98 | 沿用既有原则：无运行时数据支撑的精确分数仅作方向参考，不作决策依据 | ➖ 记录不采信 | — |

**评估准确率**：10 项建议中 8 项全量采纳、1 项简化、1 项记录；1 处事实错误（9 档清单）被纠正。整体质量高于 seed 前两次评估（WSK 优化 60% / 遗留问题 60%）——本份约 85-90% 有效，机制闭环四要素框架和 HTS 衔接锚点是实打实的增量。
